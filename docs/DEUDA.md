# Deuda técnica — registro único

> Estado: vivo · Última actualización: 2026-08-24 (auditoría del desglose de dinero, `specs/desglose-dinero-cliente.md`) ·
> **Reconciliado contra el CÓDIGO fila por fila el 2026-08-14** (no contra el tracker: se comprobó
> cada afirmación con el árbol actual, no con lo que una fase dijo que iba a hacer) ·
> Se invalida si: una fase retira un ítem sin actualizar su fila.

Vista de conjunto con severidad; el detalle vive en el doc citado (aquí no se duplica).
«Sin plan» = ninguna fase del tracker la retira hoy: candidatas a backlog o a decisión.

> ⚠️ **Lo que enseñó la reconciliación del 2026-08-14, para quien mantenga este documento.** Tres
> filas llevaban RETIRADAS desde Fase 1 y Fase 2 sin tachar —la jurisdicción legal ya es un ajuste de
> BD, `app/Support` **ya no existe** y el wordmark de los correos sale de `business.name`—, y cinco
> cifras habían envejecido en la dirección contraria a la que uno supondría: `Purchase` **creció** de
> 1.859 a 1.904 líneas mientras Fase 4 lo vacía, y la suite pasó de 213 a 270 ficheros. **Una fila que
> nombra un fichero o una cifra hay que abrirla, no creerla**: la ruta de la «cuarta copia de la
> aritmética» apuntaba a una carpeta en la que ese fichero ya no está.

## Alta

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **El `pre-push` cae por CARGA en los tests que dependen del RELOJ o del ENTORNO** (2026-09-07 → 2026-09-08, `#444`, `#445`; ⚠️ **el tercer caso de esta familia, `PackStayExtensionTest`, resultó NO serlo: era un testigo mal pasado y está CERRADO en `#447`** — *no todo rojo bajo carga es un problema de carga*) | El gate rechazó un push con `AuthSessionTest::test_spraying_many_accounts_from_one_ip_is_rate_limited`: **esperaba 429 y recibió 401**. ▶ **Lo medido**: pasa AISLADO, tarda **7,44 s** incluso con la máquina libre —dispara muchos intentos de login contra un limitador con ventana— y la pasada en la que falló declaró **`Time: 06:18:37`** de tiempo acumulado (las verdes declaran ~1:47). El reintento con la máquina quieta salió **verde y empujó**. ⚠️ Es la MISMA familia que el aviso de `ESTADO.md` sobre el timeout del renderizador SSR: no es un defecto del producto, es que **un test que mide una ventana de tiempo mide otra cosa cuando el reloj se estira**. ▶ **SEGUNDO CASO, 2026-09-08 (`#445`)**: `QrLogoTest::test_the_real_rasterizer_of_this_machine_turns_an_svg_into_a_256px_png` — «Failed asserting that null is of type string». Es el único caso del fichero que depende de la MÁQUINA: en local el único eslabón es `rsvg-convert` por `proc_open` **con tope de 2,0 s**. ⚠️ **Medido, y el tope NO está reproducido**: en reposo tarda **34–125 ms**; con la suite completa encima, 40 muestras dieron **peor 571 ms** y ninguna por encima del tope. Es el candidato, no la causa. ▶ Lo que sí se cerró es que **llegara mudo**: dos de las seis salidas a `null` no avisaban de nada y ahora avisan, y el caso dice qué eslabón cedió y cuánto tardó. | En el `pre-push`, que gatea con la suite: un rojo así cuesta una vuelta entera y —lo caro— **enseña a reintentar sin mirar**. ⚠️ Y el producto no está en duda: el limitador se verifica además con su propio recorrido. | Hacer que ese caso no dependa del reloj de pared —congelar el tiempo con `travel()` alrededor de la ráfaga, que es lo que hacen los casos del plazo de `#444`— o marcarlo como lento y sacarlo del gate. ⚠️ Lo primero es mejor: sacarlo del gate deja el limitador sin red donde importa. ▶ Para el del QR la salida NO es congelar el reloj (mide un proceso REAL, que es su razón de ser): es **subir el tope o darle margen bajo carga**, y eso solo se decide con la próxima aparición ya diagnosticada. |
| **El guardado del post-form encadena DOS órdenes de bloqueo CRUZADOS, y la forma está reproducida** (2026-09-07, `#444`, revisión adversarial de `specs/invitados-en-post-form.md`) | Los dos los dispara la MISMA petición, en este orden: `MixedPartySurcharge::reconcile()` bloquea `order_items` como primera sentencia y llega a `orders` por el `recordEdit` anidado; después `PostFormAddons::reconcile()` bloquea **`orders` primero** por decisión explícita (D11 de `#413`, que eligió ese orden tras reproducir OTRO interbloqueo). ▶ **La forma se interbloquea y está medida sobre InnoDB**: el control negativo que ya existe —`postform:verify-concurrency --scenario=cross --control`, que hace exactamente `order_items → orders` contra `orders → order_items` sobre el mismo pedido— da **4 interbloqueos de 8 procesos**; sin él, **0 de 8**. | ⚠️ **Estrecho, y por eso no se ha visto en producción**: `MixedPartySurcharge` solo alcanza `orders` **cuando el suplemento se mueve de verdad** (`recordEdit` lanza con delta 0), y es un reconciliador —en dos guardados seguidos, el segundo no escribe—. La ventana es «una petición cuyo suplemento cambia» contra «otra que toca extras», sobre la MISMA reserva y a la vez. El síntoma sería un `SQLSTATE[40001]` en el guardado de un cliente. ▶ **No lo agrava `#444`**: el ajuste de invitados usa `slots → order_items` (obligado por `AFORO-01`) y **no añade ninguna arista** al grafo — el único ciclo sigue siendo el par preexistente. | Alinear `MixedPartySurcharge` con la regla del subsistema (`orders` → `order_items` → hijas, el docblock de `PostFormAddons` la declara) tomando el pedido como primera sentencia. ⚠️ **Es dinero y está en el `CRITICAL_RE`**, así que va con su propio escenario en `postform:verify-concurrency` —el par REAL, no la forma— visto FALLAR antes de darlo por bueno. No se coló en `#444` a propósito: cambiar el orden de un escritor de dinero merece su tanda. |
| **El panel NO puede editar el teléfono (ni nada) de un cliente** (2026-09-06, `#440`) | Medido: `UserResource` registra solo `ListUsers` y `ViewUser` —**no existe `EditUser`**— y las cinco acciones de `ViewUser` son rotar carné, prueba de waiver, gestionar roles, enviar reset de contraseña y anonimizar. **Ninguna edita un dato de contacto.** Así que hoy la ÚNICA vía para que una cuenta gane teléfono es que su dueño compre por la web y `CheckoutDuties` se lo pida. Local: **5 de 25 cuentas sin teléfono, las 5 con correo** (el perfil que produce el alta con Google). | Muerde el día de la visita: la hoja de sala (`ReservationSlip:356`) y el resumen del día (`DailyReservationsSummary:137`) imprimen el teléfono, y con una cuenta sin él el monitor **no tiene a quién llamar**. Un operador que lo tenga apuntado en un papel no puede escribirlo en ninguna parte. | ⚠️ **La atenúa `#440`** —el pedido manual pasa a pedirlo al elegir un cliente sin él—, pero **no la cierra**: eso solo cubre al cliente que está comprando ahora mismo. Cerrarla es decidir si el panel edita datos de contacto de un cliente, y **es del owner**: hoy el panel es deliberadamente de solo lectura sobre la ficha del cliente (`RGPD`: cuantas menos manos escriban PII, mejor), así que abrirlo es una decisión, no un olvido. |
| **`customer_visits` dejó de crecer** (2026-08-28, `#234`) | `[DECIDIDO owner]` la tarjeta de «Visita» se retiró de la pantalla de puerta hasta que exista JumpPoints. Era el **único** sitio desde el que se registraba una visita, así que desde esa fecha no se escribe ninguna. La maquinaria sigue entera y probada (`registerVisit()`, idempotente por día, con operador y auditoría; `GateVisitsTest`): falta el botón. | Hoy no muerde: nada consume las visitas todavía. Morderá el día que JumpPoints arranque y descubra que **no hay histórico** entre esta fecha y la de reposición. | El diseño de ejecución de JumpPoints, que tiene que decidir dónde y cómo vuelve: `specs/lealtad-jumppoints.md` **§9.bis**, con las tres preguntas que deja planteadas. |
| **El calendario y las tablas del panel no son táctiles** (2026-08-28, `#232`) | Medido en iPad: el calendario tiene **8 reservas pintadas a 36 px de alto** y **5 botones a 32–36** (el mínimo táctil es 44); la tabla de «Pedidos» **se sale 97 px** en vertical y **163** en horizontal, así que hay que arrastrar de lado para ver columnas. El CSS del panel no tiene ninguna regla entre 640 y 1280 px. | Hoy no muerde: `[DECIDIDO owner, 2026-08-28]` el resto del panel se usa en ORDENADOR y la puerta tiene tablet propia. Mordería el día que alguien atienda con la tablet. | Una tanda propia. ▶ **La palanca ya existe y no se usa en NINGUNA tabla**: los componentes `Split`/`Stack` de `Filament\Tables\Columns\Layout` apilan una fila como tarjeta por debajo de un punto de ruptura. |
| **Los `$navigationSort` de las 19 pantallas escondidas ya no ordenan nada** (2026-08-28, `#223`) | Al salir del menú lateral, su orden lo manda `AdminSettingsHub::areas()`. Las 19 conservan un `protected static ?int $navigationSort` y algunas, además, un comentario que cita números de orden que ya no existen (`FaqResource`: «detrás de Atracciones (91), antes de Normas (93)», cuando vale 20). | No rompe nada: son propiedades muertas. Muerde al LEER — el siguiente que toque una de esas pantallas creerá que ese número decide algo. | Retirarlas en la misma pasada que la tanda del vocabulario (`specs/panel-navegacion.md` §6·U2), que ya vuelve a tocar los 19 ficheros. |
| **Los 14 hallazgos MENORES de la revisión de `#217`** (2026-08-28) | Una revisión adversarial de seis lentes confirmó 23 hallazgos sobre el pulido; **los 9 de conducta o seguridad se arreglaron** (`#217` addendum) y quedan estos, todos con su reproducción en el informe: **(a)** la caché del icono del QR no comprueba el valor de retorno de `Storage::put()` —el disco está declarado `throw => false`— así que un disco no escribible **re-rasteriza en cada correo** y su aviso es código muerto; **(b)** `QrLogo::png()` puede lanzar si `Log`/`Cache` fallan dentro de `warnOnce()`, contra su propio docblock, y dejaría sin correo a un pedido YA COBRADO; **(c)** el paquete de iconos de una instalación son SEIS ficheros y el QR solo mira el `.svg`: con un paquete PNG-only se estampa el icono del PRODUCTO; **(d)** el aviso dice «no hay rasterizador» aunque lo haya y lo roto sea el SVG del cliente; **(e)** `stores/{reservations,orders,purchases}` no se invalidan al cambiar de titular (el carné sí, ya arreglado); **(f)** la sección de menores de la ficha y las 7 claves de «Renovar QR» **no existen en `lang/zh_CN`** —y desde `#236` son SEIS claves más, las de la relación con el titular, que caen en el mismo agujero— (salen en crudo) y la puerta en chino mezcla 会员卡 con 二维码; **(g)** `lang/es/admin.php` (ajustes) aún dice «carné»; **(h)** dos comentarios citan un permiso `users.view` que no existe; **(i)** la columna de exención de la ficha no enseña la versión que su spec promete; **(j)** tres guardas menores que miden de menos (`data-gate-today` casa con su variante vacía; el «segundo extremo» del marco del QR lo satisface el propio bloque nuevo; la prosa de la guarda de tipografía de puerta describe algo que no hace). | Ninguno rompe hoy: (a)-(d) degradan en silencio en instalaciones concretas, (f) solo afecta al panel en chino —que el owner decidió no mantener (2026-08-25)—, y (j) son guardas que pasarían con el código roto | Sesión de repaso, o cada uno cuando se toque su fichero. Los de guardas (j) son de una línea cada uno |
| ~~**La PRIMERA PANTALLA de la portada no ofrece comprar ni navegar**~~ **CERRADA 2026-08-28** (`DECISIONES #227`) — abierta a sabiendas en `#226` y cerrada el mismo día | El hero se vació `[DECIDIDO owner]` —«por ahora sin texto y sin botones»— **y el armazón sigue naciendo OCULTO bajo él** (`#216`). Medido en navegador a 1280/768/390: en `scroll 0` la portada sirve el vídeo, la tira de marca y **nada más** — ni logotipo, ni menú, ni botón de comprar. ⚠️ `#216` le devolvió los dos botones al hero **exactamente por esto**: el mockup puede ocultar su cabecera porque su hero ofrece la acción. Al vaciar el hero sin tocar la otra mitad, el acoplamiento se rompe por el lado que quedaba. | La portada es la puerta de entrada del negocio. Un visitante que llega y no hace scroll no tiene ningún sitio donde comprar ni forma de navegar. Es el mismo agujero que `#211` cerró por otra puerta | ✅ **hecha** (`#227`): el owner eligió una TERCERA salida, mejor que las dos planteadas — la primera pantalla recupera **el mismo CTA del armazón**, abajo a la derecha, y al bajar **se apaga exactamente cuando el de la cabecera se enciende**, atado al mismo token (`--nav-reveal-live`). No aparece un segundo botón: aparece el mismo, arriba. En móvil no hace falta relevo —el CTA ya vive abajo— y lo que se arregló fue que la barra flotante **esperaba al hero**, así que la primera pantalla de móvil también estaba sin compra. Medido: a mitad del cruce el del hero está a 0,38 y el del armazón a 0,52, así que **nunca hay un fotograma sin CTA**. ▶ Guardado por `HomePageTest::test_the_hero_offers_the_purchase_because_the_frame_is_hidden_under_it`, que **asevera el estado decidido**: se pone rojo en cuanto alguien devuelva el botón al hero o haga visible el armazón, y obliga a cerrar esta ficha en vez de dejarla abierta para siempre. ⚠️ **No puede subir a producción sin resolverse.** |
| ~~Dependencias con avisos de seguridad~~ **RETIRADA 2026-08-13** (`DECISIONES #22`) | `composer audit` (2026-08-13): **26 avisos en 5 paquetes** — `guzzlehttp/guzzle` 9 (1 alta), `league/commonmark` 6 (4 altas, DoS y bypass de filtro de enlaces), `dompdf/dompdf` 6, `guzzlehttp/psr7` 4, `laravel/framework` 1. Todos son transitivos. | La app hace peticiones HTTP salientes (Redsys, Turnstile), renderiza Markdown y genera PDFs: las tres superficies afectadas están EN USO | **Un `composer update` normal los resuelve**: verificado con `--dry-run` que sube commonmark 2.8.2→2.10.0 (los avisos son `<2.9.0`), guzzle 7.10.4→7.15.3, psr7 2.10.1→2.13.0, dompdf 3.1.5→3.1.6 y framework 13.11.2→13.25.0 — **sin cambiar ninguna restricción de `composer.json` ni añadir nada**. ✅ **hecha**: `composer audit` y `npm audit` → 0; suite 2186 verde, Pint sin churn, verificadores sobre MySQL y PDF real generado |
| ~~Avisos npm en el árbol de build~~ **RETIRADA 2026-08-13** (`DECISIONES #25`) | `npm audit` daba **5 avisos — 2 críticas, 3 altas** (`shell-quote` vía `concurrently`, `vite`, `postcss`, `nanoid`), aparecidos DESPUÉS del saneado de `#22` y sin que `package.json`/el lock cambiaran | Cadena de construcción de assets; no el bundle servido a usuarios | ✅ **hecha**: `npm audit fix` sin `--force` → 0 avisos, `package.json` intacto (solo el lock); verificado con `npm run build`, suite 2224 verde y los assets servidos uno a uno por HTTP |
| **`docs-check.sh` no valida CONTENIDO, solo estructura y citas** | La auditoría del 2026-08-19 (`DECISIONES #103`) midió **794 afirmaciones** de la doc contra el código y encontró **52 desfases** con el gate **en verde** — incluidos uno de DINERO (`DEPOSITO` describía la guarda de reembolso más débil de lo que es), una invariante contradicha (`REDSYS` §14 mandaba deshacer `PAY-15`) y tres símbolos FANTASMA que nunca se portaron del origen. Ninguno se detecta leyendo: los siete graves salieron de EJECUTAR la afirmación | Todo agente arranca leyendo doc; un desfase de dinero o de invariante induce a escribir código incorrecto **y el gate lo bendice** | ⚠️ **La ficha sigue ABIERTA** —las 794 afirmaciones de `#103` siguen sin instrumento—, pero su **candidato acotado está CERRADO** (`#159`, 2026-08-25): `docs-check` gana el **check 8**, que exige que toda clase `*Test` citada en `INVARIANTES.md` exista en `tests/`. ❗ **Lo que lo hacía imposible hasta hoy no era la falta de ganas: una mención HISTÓRICA y una cita VIVA se escriben igual.** Los separa una convención nueva —el TACHADO, `~~ClaseTest~~`— documentada en `CONVENCIONES §4`. Verificado por mutación en sus DOS mitades. ⚠️ **Y la medida de partida de `#121` estaba caducada**: al re-medirla hoy había **CERO citas muertas** —`PAY-04` y `SEC-06` ya se re-apuntaron el 22 y el 23— y el primer instrumento que dio «cuatro» contaba menciones históricas y partía mal las filas con `\|` escapado. ▶ Historia: **candidato medido** en `DECISIONES #121`, 2026-08-22. Un `grep` de dos líneas encontró **dos citas muertas en `PAY-04`** —el reintento de pago— y una llevaba **un día** ahí sin que nadie la viera: el gate valida rutas de fichero, no nombres de clase sueltos. La cobertura estaba intacta, pero así es como una invariante se queda sin red sin hacer ruido |
| ~~**No hay mecanismo para configurar SECRETOS de BD**~~ **RETIRADA 2026-08-20** (`DECISIONES #109`) | Se leen solo de `settings` y el panel **no los edita a propósito** (docblock de `Settings.php`: «NUNCA editables aquí»). Medido el 2026-08-19: la única vía es escribir la fila a mano por `tinker`/SQL en el servidor. `ENTORNOS.md` §3 decía «van por el panel» y era falso (`#107`) | **Cada instalación de cliente lo necesita** —sin claves el anti-bot se autodesactiva en silencio— y es un paso a mano, no probado y fácil de olvidar | ✅ **hecha**: `app:set-setting`, idempotente, con guardas para las tres claves que cuestan dinero o corrompen la numeración, enmascarado de secretos (se ejecuta por SSH y su salida acaba en el log del despliegue), 13 casos y 5 mutaciones muertas |
| **`PAY-08` sin verificar de punta a punta: falta un `0900` REAL de Redsys** | La integración del reembolso REST **está verificada** contra el sandbox con las credenciales del owner (comercio `263100000`, terminal 45): firma `HMAC_SHA512_V2` aceptada por el banco, y el control con clave errónea sale en rojo desde `#127(b)`. Lo que NO se ha probado es un `Ds_Response=0900` de verdad, que exige una autorización previa en el sandbox | Es el único hueco que queda del reembolso REST. Todo lo demás de la cadena —conectividad, credenciales, firma, formato y parseo— está medido | **Pendiente del OWNER**: un pago de prueba con tarjeta en el sandbox desde el navegador (staging), y después `redsys:verify-sandbox --gateway-order=…`. ⚠️ Una medición anterior dio el sandbox por INALCANZABLE y era un error: se probó el puerto 443 y Redsys sirve el suyo en el **25443** |
| ~~**El DESGLOSE de dinero que ve el cliente descuadra**~~ **CERRADA 2026-08-24** (`DECISIONES #127`) | Lo que había: la API publicaba **2 de las 6** dimensiones, faltaba «Pagado en el parque» y la columna dejaba de sumar en cuanto la reserva se disfrutaba. Y debajo, **cuatro defectos de DOMINIO** que dos auditorías anteriores no llegaron a construir —un pedido cancelado que no anunciaba lo que se debía devolver, un reembolso total que dejaba el valor intacto, un pedido nunca pagado que declaraba dinero cobrado en puerta, y un reembolso total sin atribuir a ninguna reserva— más un **agujero de ingresos**: mover la fecha no re-tarificaba, así que comprar el día barato y pedir el cambio al sábado salía gratis | — | ✅ **Tandas A y B ejecutadas.** Medido al cerrar: **0 de 23** gestiones del panel dejan la columna ilegible (eran 18) y el eje del valor **cierra en 50 de 50** pedidos por HTTP real. Guardado por `PAY-16`, `PAY-17`, `PAY-18` y `LedgerSingleSourceTest`. ▶ **Queda la tanda C** (pantalla propia de «Mis pedidos»), que no toca dinero |
| God-class `ViewOrder` | `app/Filament/Resources/Orders/Pages/ViewOrder.php`. ⚠️ **La cifra de esta fila estaba CADUCADA**: re-medido el 2026-08-26 son **5.280 líneas**, no 5.029 — creció **+251 en doce días**, porque cada tanda de dinero (`#146`, `#149`, `#150`) entra por aquí. 98 métodos · **8 invocaciones reales** a dinero/aforo (⚠️ la fila decía «25 llamadas»: el grep contaba comentarios y definiciones, spec §8.6) · `executeItemEdit` sola tiene **~472 líneas** | Toda operación de dinero/aforo del panel roza INVARIANTES §1/§2. ⚠️ **Y el panel tiene su PROPIA disponibilidad**: cero `Booking\Contracts\*`, `Slot::query()` a mano — misma familia que la «cuarta copia de la aritmética de cesta». ⚠️ Y una SEGUNDA duplicación: `lockZoneDaySlots` re-implementa a mano la receta anti-sobreventa de `AFORO-01` (spec §8.9) | 🟦 **EN EJECUCIÓN, a UN paso del cierre**: spec revisada (`#167`), corregida (`#168`), ✅ del owner y **cinco tandas EMPUJADAS el 2026-08-26** (`#170` paso 0 · `#172` calendario→trait · `#173` presentación→trait · `#176` la oferta de re-programación al DOMINIO · `#177` el instrumento `panel-edit`, visto fallar). **`ViewOrder` está en 2.355 líneas y 50 métodos — cifra FINAL, medida** (de 5.280 y 98; 3.907 al empezar la 4b — `#184` A, `#185` B, `#186` C0, `#187` C, `#188` D+E, `#189` F+G): ya no contiene NINGUNA orquestación de dinero ni de aforo — todo vive en `app/Domain/Booking/` (`OrderItemEditor` · `ItemEditPricing` · `OrderItemEventDataWriter` · `OrderItemCanceller` · `OrderItemRefunder` · `ZoneDaySlotLock`, con `ItemActionOutcome` e `ItemRefundRequest` como contratos). Lo que queda es composición Filament y traducción de outcomes, y `[DECIDIDO owner]` (`#181`) no se parte. **El desmontaje, de agente, TERMINÓ el 2026-08-27; sigue 🟦 solo por la pasada de NAVEGADOR del owner (spec §6·5).** ❗ **Queda SOLO la extracción 4b** (mover la orquestación del dinero — **EN EJECUCIÓN desde el 2026-08-26 por la noche**, plan refinado en la spec §9.6, `#182`); `[DECIDIDO owner]` (`#181`): con la 4b el desmontaje TERMINA y la cifra final se declara aquí MEDIDA |
| ~~God-class `Purchase` + puente Alpine~~ **RETIRADA 2026-08-21** (`DECISIONES #111`, `#112`) | Eran **1.904 líneas** en `app/Livewire/Tickets/` (re-medido 2026-08-14; 1.859 el 13, 2.049 antes) más su blade de 713 y el puente `Alpine.store('purchase')`↔`$wire.step` sincronizado a mano. ⚠️ **Creció mientras Fase 4 lo sustituía** —cada paso transcrito añadía su espejo en el motor viejo— y esa es la lección: un sustituto a medias es DOS implementaciones, no una y media | Todo el checkout web | ✅ **hecha** en 4.7·2b·3: borrados el componente, su vista, el placeholder y el flag `SidebarSettings`; el cajón SPA queda como motor ÚNICO. La condición no fue el contador de `PurchaseRetirementTest` sino «todas las entradas clasificadas» (`#98`) más los cuatro caminos de navegador verificados en staging (`#100`→`#110`). ⚠️ Queda **`Sidebar.vue`**, el segundo objeto-dios, con su propia fila más abajo |
| ~~Sin morphMap~~ | **RETIRADA en Fase 2** (2026-08-12): `enforceMorphMap` con alias para los 30 modelos + migración de datos + barrido de `::class` en columnas morph + `MorphMapTest` (verificado también en MySQL dev: 0 FQCN restantes) | — | ✅ hecha |
| ~~Datos de negocio del cliente origen~~ **RETIRADA en Fase 1** (2026-08-12), verificado contra el código el 2026-08-14 | La jurisdicción **ya no está quemada**: es el ajuste `legal.jurisdiction` en BD (hoy `[PENDIENTE: partido judicial]`) que `LegalIdentity` interpola con el token `:jurisdiction`, y `ProductionSeeder` es la instalación **en frío neutra** —el seed real de SaltoPark vive aparte, en `LandingContentSeeder` (`DECISIONES #12.c`)— | — | ✅ hecha |
| ~~**El alta SUELTA no termina con el anti-bot activo: el widget de Turnstile NUNCA se monta en `/registro`**~~ **RETIRADA 2026-08-26** (`DECISIONES #171`) | Medido en headless el 2026-08-26 (`DECISIONES #169`, spec del waiver §9.10): en `/registro` el cajón nace abierto y `RegisterForm` se monta a los **756 ms**; `GET /config` —que trae la `turnstile_site_key`— responde a los **794 ms**. `RegisterForm.vue:97-104` llama a `mountTurnstile(captchaEl, {sitekey})` **solo en `onMounted`**, con la clave aún vacía → apaño inerte. Cuando la clave llega (`PurchaseSection.vue:221` → `authStore.setSignupSiteKey`), el `v-if="turnstileSiteKey"` pinta el contenedor —está en el DOM, **vacío**— y nadie monta el widget: ni script de Cloudflare, ni iframe, ni token. El envío va con `turnstile_token: ''` y el servidor responde **422 «No hemos podido verificar que no eres un robot»**, dos veces de dos. ⚠️ En local las claves de prueba se pusieron el 26/08 a las 14:48; el guion V17 del 23/08 corrió **sin** anti-bot y por eso nunca se vio. El paso 5 del embudo NO lo sufre (monta el formulario mucho después de `/config`) | **Toda instalación con claves de Turnstile** —staging las tiene— desde `#122` (2026-08-23): nadie puede crear cuenta desde `/registro` ni desde «Crear cuenta» del nav si llega antes que `/config`. Un fallo que ninguna guarda ve: el diff de árbol no monta el widget y la suite nunca siembra las claves | ✅ **hecha** (`#171`, 2026-08-26 noche, carril A): la decisión vive en el MÓDULO, no en el componente — `mountTurnstile` acepta **funciones** para el nodo y la clave, las re-lee en cada sondeo, **no inyecta el script de Cloudflare hasta tener clave y nodo delante** (con el anti-bot apagado la clave no llega nunca y no se carga un tercero), y se rinde en silencio a los 60 s. `RegisterForm` solo cambia dos líneas (pasa `() => captchaEl.value` y `() => props.turnstileSiteKey`; sigue en 37 de 40). **5 casos nuevos en `turnstile.test.js` (18), 4 mutaciones, las 4 muerden**; chunk +0,13 KiB (225,85 / 226). **Verificado en headless con Turnstile ENCENDIDO**: script inyectado a 1.323 ms (tras `/config` a 1.178), token a 3,1 s, `POST /auth/register → 201`, correo en Mailpit |
| ~~**El alta MANUAL del panel registra una firma «declarada por el operador» que el operador NO declara**~~ **RETIRADA 2026-08-26** (`#178`: casilla «Le he enseñado al cliente la exención vigente y declara que la acepta» con el texto a la vista en el modal del alta manual; sin marcarla NO hay firma — `CustomerRegistrar::register(…, waiverDeclared:)`; 4 casos, 2 mutaciones que muerden) | Revisión adversarial del waiver (`#169`, spec **§10.1**, confirmado 2/2): `CreateManualOrderPage::customerStep()` tiene **una sola casilla** (`privacy_informed`, la de privacidad; `grep -i waiver` → 0) y `CustomerRegistrar::register()` firma en modo interno con versión publicada y operador con sesión — sin enseñar el texto ni pedir nada. El PDF imprime «el operador X declara que la persona aceptó el texto en persona» (§8.4 `[DECIDIDO owner]`): le atribuye un acto que no hizo | Cualquier alta presencial en una instalación en modo `interno` con versión publicada (hoy ninguna). El día que haya que enseñar el PDF, el operador no podrá sostener la declaración — es §4.5 («fingir lo contrario es peor que decirlo») aplicado al operador | ✅ **`[DECIDIDO owner, 2026-08-26]` (spec §7·4): casilla del waiver en el alta manual, con el texto vigente a la vista; sin marcarla, NO hay firma.** Código pequeño (un campo en `CreateManualOrderPage`, la condición en `CustomerRegistrar`, su test) — carril A, detrás del anti-bot |
| ~~❗❗ **La cesta del PROPIO titular se PURGA cuando el cajón nace abierto** (`/entradas`, `/mi-cuenta`, `/login`, `/registro`, `/recuperar-contrasena`)~~ **RETIRADA 2026-08-27 noche** (`#202`, U0: el dueño se siembra en `index.js` desde `boot.userId` ANTES de montar; guarda estructural `SidebarMountTest::test_the_engine_seeds_the_cart_owner_from_the_boot_before_mounting` con DOS mutaciones vistas morder —sin siembra, y siembra después de montar—; caso en `stores/cart.test.js`; **la sonda re-corrida: 10/10, M1/M1bis conservadas**; A5 del guion corregido) | **Medido en headless el 2026-08-27 por la noche** (`specs/menores-a-cargo.md` §9.9.6, 10 medidas): con sesión y una cesta guardada con `owner = 457` (la que escribe `save()` tras identificarse), `/entradas` la **purga 2/2** —almacén borrado, 0 líneas, el cajón abre en el catálogo—; la misma cesta abriendo el cajón desde la home **se conserva 4/4**; una cesta sin dueño se conserva y con logout se purga (correcto). Causa, leída y confirmada: `props.userId` llega del HTML (`layout.blade.php` → `index.js` → `PurchaseSection.vue`), está declarada **y no la lee nadie** —la siembra se perdió al mover la identidad al store—; el dueño solo lo fija `GET /me`, que dispara `open()`, y el arranque «nace abierto» llama a `bootSpaEngine()` sin pasar por `open()`, así que `restoreCart()` corre con `owner = null` y `decideOwnership(457, null)` = `purge` | **Todo titular identificado que añade al carrito y después navega a una de esas cinco rutas** —«Mi cuenta» del pie es la más probable— pierde la cesta en silencio. Ninguna guarda lo ve: el diff de árbol no arranca el motor y la suite no tiene navegador. ⚠️ Y el guion A5·2 de `VERIFICACION-E2E-CAJON.md` espera una purga que la tabla de `decideOwnership()` no produce (cesta SIN dueño + otra cuenta → conservar): doc caducada | ✅ **`[DECIDIDO owner, 2026-08-27]` (`#202`·3): se arregla AHORA como unidad 0 de la tanda 4 de menores** (carril A): sembrar el dueño desde el HTML antes de restaurar —la línea que el docblock de `PurchaseSection` promete—, un caso JS, la sonda re-corrida (10/10) y A5·2 corregido |

## Media

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **La sección «Visítanos» pierde el APARCAMIENTO, y es la SEGUNDA vez que ese dato se retira por lo mismo** (2026-09-10, `#487`; `[DECIDIDO owner]`) | El artboard escribe «Se aparca en la calle, delante, y es gratis» y la web publicada dice «gratis 2 h en el recinto, después 1 €/h». **No hay campo en el panel** para ese dato, y `#297` ya lo había decidido una vez —«Parking gratis 2h» estaba en el código y no en el panel— con `#307` retirándolo. | Quien llega en coche no sabe dónde aparcar, y es una de las tres preguntas del mostrador. ⚠️ Y el conflicto entre las dos versiones **sigue sin resolver**: es el punto 10 de la lista que el canvas le debe al owner. | Del owner, y son dos: **(a)** un ajuste de texto libre junto a la dirección —vacío no se pinta, que es la conducta correcta para una instalación sin aparcamiento propio—; **(b)** decidir primero cuál de las dos versiones es verdad y entonces ponerlo. ⚠️ Lo que NO vale es escribirlo en el `lang/` del producto: sería la tercera vez. |
| **«Los festivos, como el finde» no se publica: el producto no puede afirmarlo** (2026-09-10, `#487`; `[DECIDIDO owner]`) | Es una regla de negocio del parque —qué horario rigen los festivos— y **no tiene campo**. La sección publica las **fechas especiales** que el panel tenga cargadas y nada más; medido en esta instalación: **cero**, así que hoy no sale ni el aviso de la que viene ni el pliegue con la lista. | El visitante que planifica en diciembre no sabe si el 25 abren. ⚠️ Y la regla del sistema dice que *«una regla no avisa de su excepción»*: sin la frase general **ni** las fechas cargadas, la sección no contesta la pregunta de ninguna de las dos maneras. | Del owner, y la barata es la segunda: **(a)** un ajuste de texto libre bajo la tabla; **(b)** **cargar las fechas especiales en el panel** —el mecanismo ya está entero y la sección las pinta sola, con aviso de la próxima y pliegue del resto—. La (b) no toca código. |
| **El botón FANTASMA de la web se contornea en TINTA y el canvas lo quiere en el color de INTERACCIÓN** (2026-09-10, `#485`; ⏸️ **APLAZADA por el owner en `#486`**: *«si es la fase 4 vale, lo dejamos por ahora»*) | El artboard de la sección 05 dibuja sus dos salidas en **Azul Muro** (`#0A5C93`, que es `--interactive` sobre papel): el enlace a normas y el botón fantasma. El enlace ya lo lee —los enlaces usan `--interactive` desde `#436`—, pero **`.btn--ghost` declara `color: var(--fg)` y `border: 1px solid var(--line-strong)`**, o sea tinta. | Todos los botones fantasma de la web pública **y del cajón**: `.btn--ghost` es una variante de la familia única (`#321`), así que cambiarla desde una tanda de la portada movería píxeles en pantallas de otra fase — exactamente lo que `#479` evitó con `/precios`. ⚠️ **No falla nada**: se ve, y solo se nota comparando con el artboard. | ⏸️ **Aplazada, y con las cuatro cifras ya medidas** (`#486`): el sistema declara ese botón **16/800, borde 1,5 px de `#0A5C93` y relleno azul al pasar**; el nuestro es **14/600, borde 1 px de tinta al 22 % y relleno de tinta**. ⚠️⚠️ **Y el ejemplo que la hoja de componentes usa para él es literalmente «Cómo llegar»**, que en nuestra web es ese mismo `.btn--ghost`: no es un botón de una sección, es EL botón secundario del sistema. ▶ Cuando se retome: **(a)** toda la web de una vez —45 usos, 11 ficheros del cajón—; **(b)** solo lo público y el cajón en su fase, declarando que durante un tiempo no coinciden. ⚠️ La talla (16/800) arrastra además la ficha de `#479` y **hay que medir el reflujo**: los botones crecen. |
| **`.btn` no declara `justify-content`, así que cualquier botón con ancho deja su rótulo a la IZQUIERDA** (2026-09-10, `#485`) | La familia es `inline-flex` con `align-items: center` **y nada más**. Mientras el ancho lo manda el contenido no se nota; en cuanto un botón recibe `width: 100%` (o lo estira una rejilla), el rótulo se queda pegado al borde izquierdo. **Medido: las SEIS reglas del repo que dan ancho a un `.btn` declaran ya `justify-content: center` a mano** —`.rate-card__btn`, `.price__cta`, `.account__danger-btn`, `.account__delete-btn`, `.purchase__cta`, `.orders__retry-btn`—, o sea que la convención existe y **no está escrita en ningún sitio**. | El séptimo que lo olvide. Le pasó a `.before__cta` en esta misma tanda y **lo vio la captura de móvil, no la suite**: ninguna guarda de este repo mira la alineación de un botón. | Lo coherente es **declarar `justify-content: center` en la base de `.btn`**: para un botón de ancho automático no hay espacio libre y no mueve un píxel, y para los seis actuales dice lo que ya dicen. ⚠️ **No se hizo aquí y el motivo es el alcance**: un `.btn` estirado por una rejilla —los hay en el cajón, Fase 4— sí cambiaría de alineación, así que pide su medición de reflujo. |
| **La portada ya no ofrece la TARJETA DE INVITACIÓN, y su rama se quedó sin llamante** (2026-09-10, `#483`) | `#231` puso en la portada un enlace sutil —«¿Te gustaría crear tu invitación personalizada?»— que llevaba a `/cumpleanos#tarjeta-invitacion`. Vivía en la rama `@unless ($showInvite)` de `events-section`, y **la portada era su único llamante**: `/cumpleanos` siempre pasa `showInvite=true`. Al rehacerse la sección 04 desde el canvas, la portada dejó de usar ese componente y la rama se quedó **sin nadie que la pinte**. | Desde la portada ya no se descubre la tarjeta de invitación: hay que llegar a `/cumpleanos` y bajar. ⚠️ La rama sigue en el código y **parece viva**, que es justo la deuda que este repo persigue (las 33 reglas muertas del armazón). `TouchTargetTest` perdió su entrada `bd-invite-cta` con su motivo escrito. | Del owner: **(a)** la invitación entra en la sección 04 de la portada —el canvas no la dibuja ahí—; **(b)** se queda solo en su página y la rama se retira al rehacer `/cumpleanos` en la **Fase 3**, que es cuando toca tocar ese componente. ⚠️ No se borra ahora a propósito: `showInvite` tiene `false` por defecto, así que retirar la rama cambiaría el contrato del componente desde una tanda de la portada. |
| **La misma regla para escribir una DURACIÓN existe dos veces en la landing** (2026-09-10, `#483`) | `WritesLandingValues::duracion()` la escribe para la sección de cumpleaños («2 h», «45 min») y `HeroStatus::opensLabel()` tiene su propia copia, línea a línea, para el chip del hero («Abrimos en 2 h»). ⚠️ **No se pudo unificar y el motivo es el grafo de módulos**: el trait vive en `Booking\Concerns` y `HeroStatus` en `Content`, que solo puede mirar a `Booking\Contracts` — lo impone `ModuleBoundariesTest`. | Dos escrituras de la misma regla **a un palmo en la misma página**: el día que una cambie —«2h» sin espacio, «120 min», el idioma— la otra no se entera. Es exactamente lo que el trait existe para evitar, y su propio docblock lo dice de los importes y las estaturas. | Tres salidas: **(a)** el formateo de duración sube a `Platform`, que todos pueden ver —es donde ya vive `Money`—; **(b)** `Content` recibe la duración ya escrita desde su llamante; **(c)** se declara la divergencia y se acepta. ⚠️ La (a) es la coherente: una duración es un valor de presentación como el dinero, no lógica de Booking. |
| **El panel puede vincular un COMPLEMENTO a una atracción y ya no lo publica nadie** (2026-09-10, `#482`) | `#228` dio a cada atracción un `ticket_type_id` opcional: si el complemento era comprable en su zona, la tarjeta del carrusel enseñaba **precio + CTA «Comprar»**. El carrusel se retiró con la sección 03 y `/atracciones` **no vende** (su artboard no dibuja CTA, y la sección declara «cero naranja»). ⚠️ **`#302` había decidido conservarlo a propósito** —*«es una venta, no una descripción; retirarlo cerraría un camino de compra sin que nadie lo hubiera pedido»*— con la cifra «1 de 23, la Tirolina». **Medido hoy: 0 de 23**, o sea que esa cifra caducó y no se cierra ningún camino vivo. | El operador sigue viendo el selector «Complemento vinculado» en la ficha de atracción del panel y **elegirlo no hace nada**: es la familia de «una opción que se puede elegir y no se puede leer» (`#475`). `LandingComplementResolver` y `Attraction::complementIsPurchasable()` quedan sin consumidor. | Del owner, y son tres salidas: **(a)** devolver precio y CTA a la ficha de `/atracciones` —diverge del artboard, que la dibuja sin comprar—; **(b)** retirar el campo del panel y con él el mecanismo entero; **(c)** dejarlo dormido a sabiendas. ⚠️ Mientras tanto el dominio está intacto: reactivarlo es una vista, no una migración. |
| **Los dibujos `zone-<slug>` del kit se quedan SIN NINGUNA PANTALLA** (2026-09-10, `#482`) | `hueco-ilustracion.md` (`#286`) declaró la gramática `zone-<slug>` para que una instalación pueda poner el dibujo de cada zona, y `#302` le dio pantalla en la **pestaña del selector** de la portada. Esa pestaña se fue con el carrusel, y la de `/atracciones` es la del sistema (`.tabset`), que el artboard dibuja **solo con texto**. Medido tras la retirada: la portada pide **cero** `client-kit.svg#zone-…`. | Una instalación que dibuje sus zonas en el kit no las ve en ninguna parte. ⚠️ **No falla ni avisa**: `<x-site.ilu>` no emite nada cuando la clave no tiene consumidor, que es el modo de fallo elegido (invisible, nunca caja vacía). ⚠️ Y con él se retiraron los dos casos de `IllustrationHoleRenderTest` que lo vigilaban — la nota está escrita en su sitio. | Del owner: **(a)** el dibujo entra en la pestaña de `/atracciones` (diverge del artboard, que la quiere de texto); **(b)** entra en la tarjeta de zona de la sección 01, que hoy lleva foto; **(c)** la gramática `zone-<slug>` se retira del producto. El mecanismo (`IllustrationKit`) no se toca en ninguna de las tres. |
| **El sitio ya no sabe abrir el cajón POSICIONADO EN UNA ZONA** (2026-09-10, `#482`) | La intención `openWith({ type: 'zone', slug })` la declaraba **una sola superficie**: el CTA de la tarjeta de atracción del carrusel. Medido tras retirarlo: `type: 'zone'` tiene **cero** consumidores en `resources/views`; la única intención viva es `packs` (servicios y la banda de cumpleaños). ⚠️⚠️ **Y hay un segundo hallazgo al lado**: el botón de la tarjeta de tarifa dice «Comprar 1 hora en **Jump**» y llama a `$store.purchase.open()` **sin intención**, así que el cajón abre sin la zona que el rótulo acaba de prometer (viene de `#479`, no de esta tanda). | Quien pulsa una tarifa de Kids entra en el cajón sin Kids elegido y tiene que volver a decirlo. El store sigue soportando el intento, así que es un camino que existe y nadie usa. | Pasar la intención en el CTA de `rate-rail.blade.php` (`openWith({ type: 'zone', slug })`), que es donde hoy el rótulo ya nombra la zona. Lo vigila el CENSO de `SidebarSeamTest`, que se pondrá rojo al añadir la superficie y obligará a declararla. |
| **La tarjeta de zona enlaza a un ancla que NO EXISTE en `/precios`** (2026-09-09, `#481`; el defecto es de `#478`) | `#478` puso la tarjeta entera de la sección «Para quién» como enlace a `/precios#zona-<slug>` — dos enlaces, uno por zona, verificados en el HTML de la portada. **Medido sobre la página servida: `/precios` emite CERO `id="zona-…"`.** Sus paneles de zona existen (`ticket-prices.blade.php`, con pestañas y estado `priceZone` en Alpine) pero no llevan ese `id`, así que el navegador no encuentra el ancla y deja al visitante al principio de la página. | El cliente pulsa «Zona Kids» y aterriza en la primera pestaña, que puede ser la otra. **No falla, no avisa y ninguna guarda lo mira**: es la misma familia que el `#gallery` de `#309` —un ancla sin destino lleva a la cabecera y parece que el enlace «no hace nada»—. | Dos mitades, y la segunda no es opcional: emitir `id="zona-<slug>"` en cada panel de `/precios` **y** que `priceZone` arranque leyendo el hash, o el ancla llevará al sitio y la pestaña seguirá siendo la primera. ⚠️ `/precios` es página de la **Fase 3** y su artboard (`Precios Pagina PJP`) declara que **no lleva pestaña de zona**, así que la salida definitiva puede ser retirar el ancla del enlace en vez de darle destino: se decide al rehacer la página. |
| ✅ ~~**A la sección de NORMAS ya no se llega navegando**~~ **CERRADA el 2026-09-10 por `#485`**, y por la segunda de sus dos salidas: la sección 05 «Antes de venir» lleva a `/normas` con «Ver todas las normas». ⚠️ Y con ella **la sección de normas de la portada desaparece entera** (`[DECIDIDO owner]`: las cuatro normas asomadas se van), así que el ancla `#rules` ya no existe — lo vigila `BeforeVisitSectionTest`, que asevera el ANCLA y no el `<section>` (la primera versión decía `'<section id="rules"'` y **una mutación que devolvía el ancla en un `<span>` pasó en verde**). Se conserva el diagnóstico: | El CTA «Conoce las reglas para venir» de Tarifas era el **único** enlace a `#rules` de toda la web —medido: el menú ofrece `#zones`, `#rides` e `#info`, y ninguno lleva a normas—, y el rediseño de la sección 02 desde el canvas lo retira (`[DECIDIDO owner]`: allí no hay más salida que comprar). El ancla se queda y la sección existe, pero **solo se alcanza con la URL**. | Un visitante que quiera saber qué traer no encuentra las normas desde la portada. ⚠️ Y **no lo ve ninguna guarda de las que había**: un ancla sin enlace no falla, no avisa y no rompe nada. Lo vigila un caso INVERTIDO en `RulesSectionTest` que se pondrá rojo el día que alguien la vuelva a enlazar. | **Dos salidas, y es decisión del owner**: entra en el menú como destino de sección, o **espera a la sección 05 «Antes de venir»**, que en el canvas es donde vive lo que hay que traer y hacer. |
| **77 reglas piden a la fuente de rótulo un peso y una anchura que NO tiene** (2026-09-09, `#479`) | Censo con `python3` sobre las tres hojas: **77 reglas** declaran `font-weight` (700/800) o `font-stretch: 75%` junto a `--font-display`. En esta instalación ese hueco lo rellena **Bungee, que trae UNA sola cara** —verificado en navegador: `[...document.fonts]` devuelve tres entradas y las tres son peso **400**—, así que el motor **falsifica** negrita y condensada. | Todo texto de rótulo del producto: cajón, panel, correos, hoja de sala y las páginas públicas que aún no se han rehecho. Es la misma trampa que `#478` pagó dentro de la tarjeta de zona y `#479` en la cabecera de sección. ⚠️ **No falla nada**: se ve, y solo se nota comparando con el dibujo. | `#479` arregló **las cinco cabeceras de sección**. Las **72 restantes** alcanzan al cajón (Fase 4) y al panel, así que van en tanda propia. ⚠️ Y **no es tan simple como borrar el peso**: `--font-display` es un HUECO, y una instalación con una fuente variable sí puede querer 800. La salida es decidir si el peso del rótulo pasa a ser un token del paquete. |
| **La familia única de botón está a 14/600 contra el 16/800 del sistema** (2026-09-09, `#479`) | El canvas fija el nivel **Botón** en 16 px con peso 800 y su regla dura dice «nunca menos de 16»; `.btn` declara `font-weight: 600; font-size: var(--fs-14)`. Por eso `--fs-button` sigue en la lista `SIN_ESTRENAR` de `SidebarTokenBudgetTest`. | Todos los botones de la web pública **y del cajón**: medido, `class="btn` aparece en **18 ficheros Blade y 22 de Vue**. Subirlo desde una tanda de la portada cambiaría el embudo de compra antes de su fase. | La tanda del botón, cuando la Fase 4 toque el cajón — o una decisión del owner para hacerlo antes en lo público. El trinquete de `SIN_ESTRENAR` se pondrá rojo el día que alguien estrene el nivel sin sacarlo de la lista. |
| **El rojo transitorio del pre-push, POR FIN CAPTURADO: es un `ProcessTimedOutException` del renderizador SSR** (2026-09-02; cierra el lazo que `DECISIONES #97` dejó abierto el 2026-08-16 y que `DECISIONES #164` no pudo explicar) | `SidebarDomContractTest` lanza `new Process(['node', $bundle])` con `setTimeout(60)` por cada caso de árbol. Bajo carga, ese `node` **se pasa de los 60 s** y el caso sale como ERROR, no como fallo de aserción. **Capturado el 2026-09-02 a las 19:58** en el log que el propio hook preserva (`/tmp/tmp.2FJRVMsSb0`, copia en `/tmp/claude-1000/prepush-timeout-2026-09-02.log`): **3 errores, los tres el mismo timeout**, en un pase que tardó **5:21 cuando lo normal son 1:50** — o sea con la máquina cargada. Reproducido de hecho tres veces esa tarde, siempre en el primer pase tras un `git rebase` o con otra suite corriendo a la vez; **tres pases seguidos con la máquina libre salieron verdes**. | El `pre-push` cae y bloquea un push que estaba sano. Y morderá más cuanto más crezca la suite: cada caso de árbol paga su propio arranque de Node, y `--parallel` multiplica los procesos. ⚠️ **No es un defecto del producto**: es contención de CPU en el instrumento. | ✅ **(a) HECHA el 2026-09-02: el tope pasa de 60 a 300 s**, con la medición dentro (un render normal tarda **~140 ms**, así que 60 s ya eran 430× de margen y aun así no bastaban; con 300 son ~2.000× y la guarda sigue cazando un proceso colgado). ⚠️ **Eso quita el síntoma, no el coste**, y quedan las dos de fondo: **(b)** que el renderizador sea un proceso **reutilizado** en vez de uno por caso (hoy son ~38 arranques de Node en un solo fichero); **(c)** marcar el fichero como no paralelizable. ▶ **La evidencia ya no se pierde**: la trampa que `#97` motivó y que `#164` dejó armada FUNCIONÓ — el hook nombró el fichero y por eso hay diagnóstico. *Un rojo transitorio sin nombre no se puede arreglar; con nombre, se arregla en diez minutos.* |
| **Justificante · las dos mitades del enlace CADUCAN distinto** (2026-09-02, `#406`, revisión adversarial) | El enlace que se reparte se firma con la caducidad de la LÍNEA (`OrderItem::guardianAuthorizationSignedUrl()` → `OrderItem::guestFormLinkExpiresAt()`: su franja + 14 d) y el `formAction` que la propia pantalla sirve, con la del PEDIDO (`GuardianAuthorizationController:130` → `AuthorizableReservationsReader:78` → `Order::guestFormLinkExpiresAt()`: la ÚLTIMA franja + 14 d). Como `max(order.slot) >= item.slot`, la del POST vive igual o más. **Medido** sobre el pedido 366: enlace 17/09, `formAction` 21/09. Y como GET y POST comparten URI y la firma de Laravel **no cubre el método**, ese `formAction` es también un enlace GET válido (`curl` → 200, con control a 403). | **Solo LECTURA, y está medido**: la ventana extra empieza siempre ≥14 días después del fin de esa visita, así que el dominio rechaza bajo el lock y la vista no pinta el formulario — **cero autorizaciones creadas y cero PII de menores escrita**. Lo que se gana de más es ver un resguardo ya bloqueado (producto, día, y nombre y teléfono del responsable) entre 0 y 4 días. 3 pedidos afectados hoy. | Firmar el `formAction` con `$reservation->guestFormLinkExpiresAt()` — la MISMA fuente que el enlace. ⚠️ **Y corregir TRES textos que afirman lo contrario del código**: el docblock de `OrderItem:562-564` («la caducidad sigue saliendo del PEDIDO»), su gemelo en `AuthorizableReservationsReader:76-77` y `RGPD-03` de `INVARIANTES.md`, que cita un método que ya no vive en `Order`. |
| **Justificante · `assignedDependents()` dice que acota a la cantidad y NO acota: la hoja de sala imprime plazas NEGATIVAS** (2026-09-02, `#406`) | El docblock de `GuardianPlaces::assignedDependents()` afirma que se acota «igual que `DependentAssigner::forOrderItems()`» — y aquél sí lo hace (`array_slice($list, 0, quantity)`), mientras éste es un `count()` pelado. Los dos consumidores directos divergen: el panel usa `max(0, quantity − assigned)` y **la hoja impresa NO** (`ReservationSlipController:97`). **Reproducido en transacción revertida**: línea de cantidad 1 con 3 menores a cargo asignados → la hoja calcula **−2** y el PDF imprime esa cifra en el aviso de desbordamiento. | Alcanzable: bajar la cantidad desde el panel **no poda** las asignaciones (solo las borra un `sync()` explícito), que es justo lo que `forOrderItems()` compensa al leer. El operador ve un número imposible en la hoja que se lleva a la sala. | Que el concepto «plazas de invitados de esta reserva» viva en UN sitio: un `capacityFor(AuthorizableReservation)` en `GuardianPlaces` con el `max(0, …)` dentro, y que la hoja y el panel lo llamen. ⚠️ **Parchear el sitio de la llamada dejaría la afirmación falsa en pie** para el siguiente que la lea. |
| **Justificante · el endpoint de la cuenta hace ~7 consultas por reserva, contra un docblock que promete DOS** (2026-09-02, `#406`) | `GuardianRoster:80-82` dice «DOS consultas, sean uno o cien justificantes… la ficha del pedido no puede permitirse una consulta por fila». Su llamador lo deshace: `OrderGuestMinorsController` llama `countFor()` por ítem, `forResponsible()` por reserva (2), `freeIn()` **dos veces** por reserva (2 `count()` cada una) y un `OrderItem::whereKey()->first()` más. El único presupuesto de consultas del subsistema (`GuestMinorSurfacesTest:216-240`) mide **la puerta**, no este endpoint. | Hoy no muerde (pedidos con pocas reservas marcadas), pero crece con el nº de reservas y es exactamente lo que el docblock existe para impedir. | Un `freeIn()` calculado una vez por reserva y un presupuesto de consultas para este endpoint, como el que ya vigila la puerta. |
| **Justificante · el docblock de `sendGuardianLinkAction` miente en sus DOS mitades** (2026-09-02, `#406`) | `ViewOrder:1058` afirma «No es una acción nueva de envío: **reutiliza `dispatchResend()`**, y con ella el permiso, el rastro y la revalidación de `canResend()`». `grep dispatchResend` da tres apariciones: su definición, su ÚNICA llamada (desde la acción hermana) y esa cita. `sendGuardianLinkAction()` inlinea `notify()` + `AuditLogger` y no la llama. | **NO es hueco de autorización — verificado**: la página ya exige `orders.view` (`OrderResource::canView`), que es justo lo que comprueba la acción hermana, así que el cinturón que falta es redundante. Lo que sí se pierde es la revalidación que el texto promete. | Llamar de verdad a `dispatchResend()`, o reescribir el docblock para que describa lo que hace. ⚠️ Lo segundo es barato y lo primero es lo correcto: la cita existe porque alguien quiso esa garantía. |
| **Justificante · el tope de plazas lo imponen DOS escritores sin lock compartido** (2026-09-02, `#406`, señalado por la crítica de completitud como refutación DÉBIL) | `GuardianPlaces::takenIn()` suma menores a cargo asignados + justificantes firmados, pero `DependentAssigner:286` compara solo `count(conservados) + count(pedidos) > quantity` **sin mirar `guardian_authorizations`**, y **no toma el lock del responsable** que sí toma `GuardianAuthorizationSigner:75`. | Línea de 20 con 5 padres ya firmados: el operador puede asignar 20 menores a cargo → `freeIn = 0` → los **15 padres que ya tienen el enlace en el WhatsApp** reciben «no caben», y el responsable ve **desaparecer el enlace de su cuenta sin explicación** (`OrderGuestMinorsController:95` pone `link: null`). El aviso de desbordamiento lo ve el OPERADOR, no ellos. Y sin lock compartido, «firmar mientras se asigna» no está serializado. | Decisión de producto antes que de código: ¿quién gana el cupo, el que asigna o el que firma? Con la respuesta, o el asignador mira las autorizaciones y toma el mismo lock, o el cupo se separa en dos. |
| **Waiver · `subject_name` es `varchar(120)` y el firmador corta a 255: `1406` en MySQL, VERDE en SQLite** (2026-09-01, encontrado por la revisión adversarial de `specs/waiver-por-reserva.md`, §4.3.1) | `WaiverSigner` guarda el nombre del menor con `mb_substr($dependent->fullName(), 0, 255)` y un comentario que dice, con esas palabras, *«se corta a lo que admite la columna»*. **La columna es `varchar(120)`** (migración `2026_08_27_200000`). Medido con un menor de 120 + 120: `LEN fullName=241` → `SQLSTATE[22001]: 1406 Data too long for column 'subject_name'`. | **Vivo hoy** para menores a cargo, no de una feature futura: un titular que declare un menor con nombre y apellidos largos revienta al firmar. ⚠️ **Y la suite NO lo ve**: en SQLite se guardan los 241 caracteres sin rechistar — *un test verde no dice nada sobre un límite de columna que solo existe en MySQL*. | La migración de la T1 de `specs/waiver-por-reserva.md`, que sube la columna a **255** (que es lo que el corte ya suponía). Si esa tanda no se hace, hay que arreglarlo igual: el defecto es del waiver de menores, no del justificante. |
| **Waiver · un `ON DELETE SET NULL` dentro del hash rompe la verificación de una firma que nadie tocó** (2026-09-01, encontrado al diseñar `specs/waiver-por-reserva.md` §10, reproducido de forma independiente por su revisión adversarial) | `waiver_signatures.declared_by_user_id` es `nullOnDelete` **y está dentro de `HASHED_FIELDS`** en las tres versiones canónicas. Medido con sonda en transacción revertida: firma declarada en mostrador → `verifyHash()` **`true`**; se borra la fila del operador → `declared_by_user_id` pasa a `NULL` → `verifyHash()` **`false`**. La fila probatoria no se ha tocado. | Estrecho pero real: la única vía que borra usuarios es `PurgeCustomerData` (la limpieza de go-live), que borra las firmas **de los usuarios purgados** y después los usuarios — así que muerde en la firma de un usuario **conservado** declarada por un operador **no conservado**. Y muerde **en silencio**: la firma sigue ahí, solo deja de verificar. `waiver:verify-chain` la marcaría, pero nadie lo corre después de un go-live. | Dos salidas, y son del owner: **(a)** sacar `declared_by_user_id` del hash (obliga a una versión canónica nueva y deja el campo como dato no probatorio) o **(b)** pasar la FK a `RESTRICT` (la purga tendría que resolver el orden, como ya hace con `user_id`). ▶ La **regla de diseño** que sale de aquí ya está aplicada en la spec nueva: *ninguna columna con `ON DELETE SET NULL` entra en un hash que se verifica* — por eso el justificante por reserva **no** guarda `signer_user_id`. |
| ~~**Cumple mixto · un cambio de TRAMO sigue moviendo lo escrito, en las DOS direcciones**~~ **RETIRADA el 2026-08-31** (`#288`, la T1: el SELLO de condiciones en `order_items.age_family_seal` — `AgeFamilySealTest` reproduce las dos direcciones y el primer cargo, y los tres quedan en 0 de movimiento) (2026-08-29, `#272`; **RE-MEDIDO y ampliado el 2026-08-30**) | ⚠️ **La primera redacción de esta ficha era irreproducible**: decía «Kids pasa a 1–12 → 40,00 €», y eso se midió con el precio de Kids forzado POR ENCIMA del de Jump; con los precios reales (11,00 / 15,00 €) da **0,00 €**, porque la diferencia hacia un pack más barato tiene suelo en 0. ▶ Lo medido de verdad, en transacción revertida: **(a)** si el cambio empuja a los invitados hacia el pack más CARO, aparece un cargo que nadie comunicó (**40,00 €** de la nada); **(b)** ❗ y al revés, un tramo ENSANCHADO **destruye un cargo ya escrito** — 40,00 € comunicados → el parque decide que a partir de 3 años se va a Jump → el cliente edita un **nombre** → **0,00 €**. La abstención de `#268` no lo ve porque el veredicto sigue **completo**, y el RECIBO de `#270` congeló el **precio**, no a **quién** se le aplica | Al cliente en la dirección (a) —recibe un cargo que nadie decidió para él— y al parque en la (b) —pierde uno comunicado—. Solo aparece si se mueven TRAMOS, cosa de una vez al año, pero entonces toca a TODAS las fiestas vivas a la vez. ⚠️ La dirección (b) es **benigna para el cliente**: hoy es el único camino que corrige un cargo hacia abajo en fiestas ya vendidas | Que la reserva recuerde **con qué tramos se hizo** (no solo con qué precio): columna nueva + migración + relleno. Cierra las dos direcciones y también el gemelo de la etiqueta. ✅ **DECIDIDO el 2026-08-31 (`#284` D1/D2): se cierran LAS DOS direcciones con el SELLO EN LA RESERVA** —cada reserva guarda al nacer los tramos y precios de su familia—, y sin histórico de precios: el precio viejo solo existe dentro de las reservas que lo llevan. ▶ Pendiente de CONSTRUIR (tanda T1 de §18.5), no de decidir. ⚠️ Y cierra de paso un hueco MAYOR que este, medido después: el PRIMER cargo se calcula hoy con el catálogo del día en que el cliente rellena el formulario, no con el del día en que compró (§18.4·A). ▶ **MITIGADA el 2026-08-30 (`#283`, §17.8)**: el catálogo ya **avisa antes de guardar** a cuántas fiestas vendidas afecta el cambio y por cuánto dinero, así que deja de poder pillar a nadie desprevenido. El agujero sigue abierto —solo que ahora se entra en él a sabiendas— |
| **Cumple mixto · la API no explica «una edad sin producto»** (2026-08-31, `#289`) | `GuestForm.status` queda `pending` con todas las fichas rellenas cuando una edad declarada no tiene producto en las condiciones de la reserva (`specs/cumple-mixto.md` §22.5), y la app no recibe ni el caso (`below`/`above`/`gap`) ni el texto del parque; la web sí los enseña. `[owner]`: la API queda fuera del alcance de mixtos (§12.7) | El cliente de la app móvil ve «pendiente» sin saber qué le falta ni a quién llamar | Cuando la API entre en alcance: `age_issues[]` en `GuestForm` (índice de ficha + caso + texto ya compuesto) y el mismo `frozen_missing_ages`; sin cambiar la forma de `status` |
| ~~**Cumple mixto · la etiqueta puede contradecir al cargo, ahora de forma PERMANENTE**~~ **RETIRADA el 2026-08-31** (`#288`: etiqueta y cargo salen del MISMO sello; `AgeFamilySealTest::test_the_label_follows_the_seal_not_the_catalogue`) (2026-08-29, `#272`) |
| **Cumple mixto · la API no explica «una edad sin producto»** (2026-08-31, `#289`) | Con la T2, una ficha con todas sus columnas pero con una edad que ningún régimen sellado cubre deja el formulario en `pending` (D6). La web se lo explica al cliente con el texto del caso y el teléfono; la API sirve el mismo `status: pending` **sin el caso ni el texto** — `GuestForm` no tiene campo para ello y `[owner]` la API queda fuera de mixtos (spec §12.7). Anotado en la descripción del contrato (`openapi/v1.yaml`) | El cliente de la app ve «pendiente» con todo relleno y no sabe qué le falta ni a quién llamar | Cuando la API entre en alcance: `age_issues[]` (índice · caso · texto del parque) en `GuestForm`, y `GuestFormResource` lo compone desde `guestRegimes()` + `MixedPartySettings::noProductText()` — la misma fuente que la web | Medido: con 40,00 € escritos y los tramos devueltos a su sitio, la hoja de sala imprime «Cumpleaños Jump» **sin** la etiqueta MIXTA, la pastilla de cada niño dice que está en su propio pack, y el mostrador cobra «por 8 invitados que corresponden a Cumpleaños Kids» | En el mostrador: tres superficies del mismo pedido dicen cosas distintas delante del cliente. Antes duraba hasta el siguiente guardado (que borraba el cargo, que era peor); ahora el cargo sobrevive | ✅ **DECIDIDO 2026-08-31** (`#284` D1): la misma copia del caso espejo. Con los tramos sellados en la reserva, etiqueta y cargo salen del mismo sitio. Pendiente de construir, tanda T1 |
| ~~**Desglose · el «online original» se reconstruye con la SEÑAL VIVA del catálogo → «Pendiente de devolución» FANTASMA**~~ **RETIRADA el 2026-09-01** (`#306`, la T1 del libro: `itemOriginalOnlineCents` sale del HECHO —`fila − Σ deltas − reparto deposit_split`— y ya no consulta `depositCents()`; medido sobre `T4-PRB01` tras migrar la BD local: la card dice **0,00 € pendiente y 10,00 € cobrados al nacer**, donde antes decía 20,00 € y 30,00 €. `EditMovementTest` y `OrderAdjustmentsBecomeMovementsMigrationTest` lo fijan) (2026-09-01, `#298` adenda 4; cazado por el owner sobre `T4-PRB01`) | Medido: `Order::itemOriginalOnlineCents()` reconstruye «lo que se pagó online al principio» como `depositCents(valor original)` con la configuración **VIVA** del producto. `T4-PRB01` nació con señal de 10,00 € y el producto hoy declara señal fija de 30,00 → tras cualquier edición de cantidad, la card dice «Pendiente de devolución −20,00 €» sobre dinero que **nunca se cobró**, mientras el eje del PEDIDO dice 0 — **divergencia pedido-vs-card en vivo**, la clase exacta que el cruce B4 existe para cazar. Camino de producción sin sonda: vender un pack con señal X, **cambiar la señal** del pack en el catálogo, y editar la cantidad de un pedido viejo | El operador que se fíe de la card devuelve dinero que el parque nunca cobró. Solo muerde si se cambia la señal de un pack con pedidos vendidos y luego se edita alguno | Es **el hermano de la señal** del caso espejo que el SELLO cerró para los tramos (`PAY-19`): la señal no está sellada en la reserva. ▶ Boceto verificado sobre los datos: la fila `deposit_remainder` del NACIMIENTO ya registra la verdad (60 − 50 = 10) — la reconstrucción puede leer el HECHO en vez de la config. Mini-tanda propia con diseño fino: `Order.php` es territorio `PAY-10/16/17` (`ItemPriceChangeReconstructionTest` + `DepositRefundCoherenceTest` de red) |
| **Cumple mixto · no hay forma de LISTAR las reservas con desfase** (2026-08-29, `#272`) | Medido: **cero** columnas y **cero** filtros con `MixedParty` en `app/Filament/**`. El desfase entre lo escrito y lo derivado solo se ve abriendo la ficha del pedido, de una en una | El día que el parque suba precios con N fiestas vivas: N fichas a mano para saber cuáles quedaron descolgadas. En la instalación local hoy son 7 reservas vivas de esa familia | Una columna o un filtro en `OrderResource`, o un contador en «Hoy». No es urgente hasta el primer cambio de tarifas con fiestas vendidas |
| ~~**Cumple mixto · tras anonimizar, el importe queda quieto también hacia abajo**~~ **CERRADA POR CONSECUENCIA 2026-08-31** (`#284` D8: no se podrá anonimizar con una reserva EN VIGOR, así que toda reserva anonimizada estará finalizada — y una finalizada no se reconcilia). ✅ **La puerta está CONSTRUIDA desde la T5** (`#298`, misma noche): las tres vías bloqueadas con explicación (`RGPD-01`), verificado por mutación en servicio y panel y visto bloquear en navegador · el texto original: | `RGPD-01` pone `guest_data` a `null`, así que el veredicto queda incompleto para siempre y `derivationGoverns()` prohíbe encoger. Bajar los invitados de 10 a 8 sigue cobrando por 10 | Al cliente anonimizado que reduce su fiesta: pagaría de más en el parque. No hay caso conocido todavía | Es el precio de no perder la deuda al ejercer el derecho al olvido: se cambió dinero MAL por dinero QUIETO. Cerrarlo exige guardar el nº de invitados afectados en el propio cargo y dejar que la CANTIDAD siga bajando |
| **¿Descuentos manuales en el parque?** (2026-08-29, `#271`) | Medido al preguntarse si el operador debía poder perdonar el suplemento: **en todo el sistema no existe ningún descuento manual** — ni cupón, ni cortesía, ni precio editable, para nada | Hoy no muerde: el parque cobra menos en caja y ya. Mordería el día que se quiera dejar rastro de un gesto comercial | `[PENDIENTE: owner]`: es una capacidad de producto propia, mucho mayor que este suplemento — sería raro poder regalar 6,00 € y no un pack de 200,00 € |
| **Cumple mixto · el operador no puede COBRAR un suplemento que el sistema no generó** (2026-08-29, `#271`) | El niño que cumple años entre la reserva y la fiesta: declaró 6, llega con 7. Hay diferencia que cobrar y no hay herramienta — el producto portador no es vendible y no aparece en la lista de complementos | Es la dirección que **pierde** dinero el parque, y es la contraria a la que se ha estado arreglando | `[PENDIENTE: owner]`. Nota: cambiar la edad declarada NO vale como atajo — dejaría un dato falso guardado y el rastro diría que lo cambió el cliente |
| ~~Carné QR · sin zona «Mi carné» en el cajón ni rotación desde el PANEL~~ **CERRADA 2026-08-28** (`#212`, carril A, `specs/identidad-qr-puerta.md` §9.6: `GET /me/card/png` —los mismos bytes que el correo— + zona `card` del cajón con «Descargar» y «Renovar» + acción «Rotar carné QR» en `ViewUser`; headless 14/14, techos subidos por feature) | `specs/identidad-qr-puerta.md` §9.4·6: el carné existe, viaja en el correo y por `GET /me/card`, y el titular lo rota por `POST /me/card/rotate` — pero **ninguna pantalla lo pinta ni lo rota**: el cajón SPA no tiene zona «Mi carné» y la ficha de usuario del panel no tiene la acción «Rotar carné» (el admin solo revoca con `revokeAllAccess()`, que además cierra sesiones y tokens). §4.5 prevé las dos | Un cliente que pierde el correo no tiene dónde ver su QR; un operador que quiere invalidar SOLO el carné no puede sin echar al cliente de su cuenta | **Dos piezas pequeñas cuando el owner las pida**: (1) zona del cajón (Vue, `resources/js/sidebar/**` — carril C hoy) que pinte el token como QR (`qrcode` en el navegador o un endpoint PNG) con «Renovar»; (2) `Action` «Rotar carné» en `ViewUser` que llame a `CustomerCards::rotate()`. Las dos sobre API/servicio ya probados |
| **Carné QR · el PNG va ADJUNTO, no incrustado (CID)** (Fase 6 · A, `#208`) | `OrderConfirmation` es un `MailMessage` de líneas y adjunta `carne-qr.png` (§9.4·5); incrustarlo *inline* exige un Mailable con plantilla propia. §4.10 lo deja como supuesto NO medido: hay que abrirlo en Gmail, Outlook y un cliente de móvil | Si el adjunto no se ve a la primera, el cliente llega sin QR y la puerta busca por email (funciona, pero es el plan B) | **Pendiente del OJO del owner** (§6). Si pide verlo en el cuerpo: plantilla propia con `$message->embedData()` — una tarde |
| Suite ciega a carreras InnoDB | SQLite `:memory:`; doble-cobro/sobreventa solo en comandos on-demand; el pre-push exige el flag `VERIFY_CONC=1`, no ejecuta los comandos | Un cambio de locks pasa verde y sobrevende en producción | Sin plan (mitigada por la guarda del hook; desde Fase 3 · paso 0 `CriticalPathGateTest` verifica además que esa guarda sigue cubriendo lo que debe). ⚠️ La mitigación es **parcial**: ver la fila de abajo |
| ~~**`purchase:verify-oversell` solo verifica ENTRADAS: el aforo de PACKS nunca se ha probado bajo concurrencia**~~ **RETIRADA el 2026-08-25** (`DECISIONES #147`) | Ya no es cierto: el comando tiene **tres escenarios** —`entry`, `pack` (cupo de FIESTAS) y `pack-guests` (cupo de INVITADOS)— y los tres se ejecutaron sobre **MySQL real** con 8 y 16 procesos concurrentes. **Los dos topes del pack aguantan**: 1 compra creada y N−1 `sold_out` en todas las pasadas. ⚠️ **Y el verde está respaldado por su CONTROL NEGATIVO**: retirando el `lockForUpdate()` de `OrderCreator::lockSlots`, el mismo instrumento cazó **8 fiestas donde cabía 1** y **48 invitados donde caben 10** | — | ✅ hecha, y **los tres huecos que dejó también** (`#148`): son **cinco** escenarios, con `pack-prep` (tramo multi-franja y montaje/limpieza **ACTIVOS**) y `mixed` (los dos pools a la vez), los dos con su control negativo. ❗ **Y `mixed` destapó que los dos aforos NO son independientes**: una fiesta de 8 invitados deja una franja de 10 plazas en 2. Lo fija `PackConsumesEntrySeatsTest`; **si debe ser así es decisión del owner** |
| **Cinco secuencias transversales del embudo, todavía en el componente** | `addToCart` (27 líneas, toca 5 dominios), `confirmReservation` (26), `retryPayment` (25), `poll` (17) y `showLineProblems` (17). Son las transiciones del embudo: piden a la API, mueven el paso y tocan varios stores a la vez. `sections/PurchaseSection.vue` se queda en **438 líneas** por ellas | Nada, hoy. `CE-6` mide sobre todo las llamadas a la API, y ya son **2** (el montaje) | **Sin plan, y a propósito.** Medido el 2026-08-22: las cinco viven **íntegramente en el embudo** —cesta, desenlace, catálogo, fecha, hora—, así que el ÁREA DE CLIENTE no las rozará y **su coste no crece con el tiempo**: es deuda SIN intereses. Sacarlas dentro de un año cuesta lo mismo que hoy. ⚠️ Y `addToCart` toca **cinco** stores: en un módulo plano serían cinco dependencias inyectadas, donde la ceremonia empieza a competir con el beneficio (`DECISIONES #119(e)`) |
| **El techo del bundle tiene margen sin usar** | Subido de 160 a **168 KiB** el 2026-08-22 con su medición (ocho stores ≈ 8,5 KB de fuente → +3,9 KiB minificados, ~0,5 KiB por store), dejando sitio para lo que faltaba de la reorganización | `SidebarBundleBudgetTest`: un techo con holgura sobrante deja de apretar, que es lo que un presupuesto no debe hacer | **Al cerrar la reorganización**: volver a medir el chunk y BAJAR el número a lo que de verdad ocupe. Está escrito en el propio test |
| **La mitad de ZONA de los enlaces profundos** | `catalog.js::toItem()` **descarta el campo `zone`** que la API sí publica (`GET /catalog/products` lo trae con `id`/`slug`/`name`), y `CatalogStep.vue` no lo menciona. El motor retirado hacía scroll a la ZONA concreta (`catalog-open-entries-zone`); la SPA aterriza en la sección «Entradas» y para ahí. Se devuelve marcado `exact: false` en vez de fingir paridad (`intent.js`, `DECISIONES #117`) | El CTA «Comprar» de una tarjeta de zona en la home. **No falla, hace menos**: el cajón abre y muestra las entradas, pero el cliente tiene que buscar la suya | **Sin plan — es una DECISIÓN, no una tarea.** Devolver la zona al modelo cambia el árbol que pinta `CatalogStep.vue`, y eso **toca el manifiesto de árbol CONGELADO**. ⚠️ Y hoy **no se puede verificar en staging**: su seed no tiene productos `type=entry`, así que la sección «Entradas» ni se pinta |
| ✅ ~~**El bloque de cuenta del cajón es Livewire, no Vue**~~ — **RETIRADA el 2026-08-23** | El bloque lo pinta ahora `sidebar/account/AccountPanel.vue`, teletransportado al hueco que el layout emite; la clase Livewire y su Blade **ya no existen** y la página sirve **0 atributos `wire:`**. `specs/account-context-vue.md`, `DECISIONES #123`. ⚠️ **Lo que NO cambió, y conviene no leerlo de más**: Livewire sigue instalado y sigue trayendo Alpine, así que `@livewireScripts` **no se puede retirar** — lo que cambia es que ahora es la fuente ÚNICA, y su guarda (`SidebarMountTest::test_livewire_scripts_are_still_served`) **por fin discrimina**: medido, retirarla la pone roja, y hasta el 2026-08-23 no lo hacía |
| El ámbito del presupuesto de tokens se define RASCANDO una plantilla | **Re-medido con la lógica real del test el 2026-08-15** (`DECISIONES #74`), corrigiendo una lectura anterior: `SidebarTokenBudgetTest` deriva el ámbito CSS del cajón de las clases que aparecen en `purchase.blade.php`. Al añadir las fuentes Vue el ámbito pasa de **1.073 a 1.307 declaraciones** y el presupuesto de 75%→70% y de 3→4 colores crudos. **Pero lo que entra NO es CSS del cajón mal tokenizado**: son 60 declaraciones del bloque `auth__*`/`form__*`/`check`/`pwd-input` —los formularios de login y alta, compartidos con el modal de auth y con `/mi-cuenta`— más `eyebrow`, `icon` y `tk`, que son del sitio. **Del cajón solo había UNA**, el velo `.jj-loading`, ya tokenizada | Rascar una plantilla mete en el «ámbito del cajón» toda clase compartida que su marcado use. Con Vue eso crece, porque los formularios de auth viven DENTRO del cajón | **Antes de 4.7·2b·3, y es una DECISIÓN, no una tarea**: definir el ámbito por las familias propias del cajón (`purchase__`, `cart__`, `bk-`, `cal__`, `wiz__`, `qtybox`, `catalog-acc`, `jj-`, `sidecart`…) en vez de por lo que diga una plantilla — así el presupuesto deja de depender del motor. Tokenizar el bloque de formularios del sitio es **Fase 5** («theming como paquete coherente»), no la retirada |
| Suite no auditada contra la FECHA | **Medido el 2026-08-15** (`DECISIONES #64`): al cruzar medianoche UTC aparecieron **3 fallos que nadie había causado** —los 2 casos del calendario de `SidebarDomContractTest`, cuya foto congelada caduca **cada día**, y `CatalogTest`, que falla **sábados y domingos** porque su fixture declara una tarifa `weekdays: [0,6]`—. Los tres se arreglaron congelando el reloj. **Lo que NADIE ha hecho es barrer el resto**: ningún mecanismo impide que otro test solo pase ciertos días | El gate de `pre-push`: un rojo que no viene del cambio hace perder una sesión entera —y, peor, enseña a regenerar fixtures sin mirar, que es como se pierde un contrato congelado en silencio— | ⚠️ **La ficha sigue ABIERTA, pero por un motivo distinto al de antes** (`#162`, 2026-08-26). El barrido **se ha hecho**: `Tests\TestCase` lee `TEST_CLOCK` y `scripts/audit-clock.sh` corre la suite entera en diez fronteras, con las fechas **relativas a hoy** para que la lista no caduque. Encontró **DOS** defectos reales y los dos están arreglados: una ventana de temporada escrita a mano que **iba a tumbar el gate el 2026-09-01** —seis días después de medirla— y la **aritmética de meses de PHP, que desborda** (`2026-08-31 +3 meses = 2026-12-01`; `2026-12-31 +2m` y `+3m` caen los dos en marzo). Detalle en `TESTING.md` §2.septies. ▶ **Lo que queda abierto es el DISPARO**: no hay CI que lo ejecute, así que depende de que alguien corra el script al cerrar una tanda con fixtures de calendario. Medido: meterlo en el `pre-push` multiplicaría por diez el tiempo de cada push. ⚠️ **Y un hueco declarado**: el reloj no llega a `tests/Unit/` (extienden `PHPUnit\Framework\TestCase` por `CONVENCIONES §3.ter`). Medido: 4 ficheros, ninguno con calendario. ▶ **Y `#164` cerró la caza de `#97`**: el reloj **queda EXONERADO** —10 fronteras congeladas, 4 cruces de medianoche a mitad de pase con el modo nuevo `TEST_CLOCK_START`, y la suite entera en 2 órdenes aleatorios: todo verde—, así que la explicación que la doc le daba no sobrevive a la medición. **La causa sigue sin nombre**, pero el `pre-push` ya **anuncia la ruta de su log al caer** (antes sobrevivía y nadie lo decía), así que la próxima vez se responderá con datos |
| `$guarded = []` masivo | **24 de 30 modelos sin `$fillable`** y **27 declaran `$guarded = []`** (re-medido 2026-08-14; la fila decía 22). `User` sigue sin ninguno de los dos | Mass assignment en código nuevo; `preventSilentlyDiscardingAttributes` solo en no-prod | Sin plan |
| Branding residual | **RETIRADO en Fase 1** (2026-08-12): quedan solo 6 líneas intencionales (regla 7, guard-bash, cron de ejemplo neutro, y el `assertDontSee('jumpingjump.com')` que actúa de GUARD) + fixtures `JJ-…` autoconsistentes en tests (cosmético, sin valor de marca) y códigos forenses en comentarios | — | ✅ hecha |
| ~~`app/Support` cajón desastre~~ **RETIRADA en Fase 2** (2026-08-12), verificado el 2026-08-14 | **La carpeta `app/Support` ya no existe**: sus 57 ficheros se repartieron entre los cinco módulos de `app/Domain/` con frontera ejecutable | — | ✅ hecha |
| Composer global `'*'` | `View::composer('*')` vigente, memoizado por request (sin memo: ~1.900 queries/GET) | El rendimiento de toda la web pende de un memo; acopla vistas al provider | Fase 5 |
| **2FA de admin inexistente** | Ninguna implementación; solo un comentario «queda para sus sub-fases» en la página Settings | Panel con PII de menores + reembolsos protegido solo por contraseña | **Sin plan — decisión de owner** |
| Hueco test: alcance del lock zona/día | `lockZoneDaySlots` sin assert de ALCANCE (SQLite no reproduce la carrera) | Fase 2 podría estrechar el lock sin que nada falle → sobrellenado | Sin plan (INVARIANTES AFORO-05 ⚠️) |
| Hueco test: no-legibilidad de secretos | Solo se asevera la escritura; nada asevera que el form no CARGUE `redsys_secret_key` | Una regresión pintaría el secreto en el HTML del panel | Sin plan (INVARIANTES SEC-11 ⚠️) |
| Una sola factory | `database/factories/` = solo `UserFactory` para 30 modelos y **270 ficheros de test** (re-medido 2026-08-14; eran 213). Fixtures a mano: **1.252 `::create(`** (eran ~1.100) | Cada test nuevo escribe su propio fixture; helpers `admin()`/`staff()` duplicados | Sin plan (ver `TESTING.md` §datos) |
| Vocabulario de sector en código | `PuertaSettings` vigente y `waiver` en **14 ficheros** de `app/` y `routes/`; zonas/cumpleaños en clases, rutas y BD (verificado 2026-08-14) | Multi-sector exige renombrar clases y valores morph | ⚠️ **Fase 1 y Fase 2 están CERRADAS y no lo retiraron**, así que la fila decía una fase que ya pasó: hoy es **sin plan**. Fase 1 decidió el criterio (`DECISIONES #12.a`: español ahora) y Fase 2 movió los ficheros sin renombrar el vocabulario |
| «Precio desde» no filtra tarifas activas | El mínimo sale de TODOS los `prices` del producto (`CatalogReader::fromPriceCents`, y así era ya en `Purchase`). `RateResolver` solo elige entre tarifas `is_active`, así que un precio colgado de una tarifa desactivada se anuncia pero no se puede comprar | Una instalación que desactive una tarifa barata sin borrar sus precios anunciaría un «desde» inalcanzable — en web y en API por igual | **Sin plan — decisión de owner** (toca lo que se le anuncia al cliente; no se cambió al extraer el read-model, Fase 3 · paso 1b) |
| Cacheabilidad del catálogo público sin decidir | `GET /api/v1/catalog/*` responde con el `Cache-Control: no-cache, private` por defecto de Symfony; ni CDN ni proxy pueden cachearlo | La superficie pública de más tráfico previsible va sin caché HTTP; hacerlo cacheable exige decidir el `Vary` del idioma | Sin plan (Fase 3 · paso 1b lo dejó anotado, no resuelto) |
| Cuarta copia de la aritmética de cesta en el panel | ⚠️ **La ruta de esta fila estaba mal**: es `app/Filament/Pages/CreateManualOrderPage.php`, no `Resources/Orders/Pages/` (corregido 2026-08-14). `CreateManualOrderPage::estimateLineCents()` + `cartOnlineDueCents()` + `cartHasDeposit()` reproducen las reglas de precio, complementos y señal que desde Fase 3 · paso 4a viven en `Booking\Contracts\CartPricing`. **Comprobado caso a caso el 2026-08-13: hoy coinciden** en las dos ramas de la Opción A (#227) | Un cambio de regla de señal o de complementos habría que portarlo a los dos sitios; el paso 2 ya encontró dos copias «iguales» que aplicaban políticas distintas sin que nadie lo hubiera decidido | Sin plan — **no se unificó a propósito** en el paso 4a: el panel calcula al AÑADIR la línea y el contrato al PINTARLA, así que migrarlo cambia su conducta ante un cambio de precio a media cesta |
| Dos normalizaciones del MISMO teléfono | `contact.phone` se normaliza de dos formas distintas según quién lo pinte: `preg_replace('/\s+/')` en el aviso de pausa del sidebar (hoy en `Platform\Services\MaintenanceSettings::reservationNotice()`) y `preg_replace('/[^0-9+]/')` en el composer global de vistas. **Medido al extraer el aviso en Fase 4 · paso 4.0b** | Un teléfono con puntuación (`968-12-34-56`) produce DOS `tel:` distintos en la misma web: uno conserva los guiones y el otro no. Solo se nota en instalaciones que puntúen el número | **Sin plan — decisión de owner**: unificarlas CAMBIA el `tel:` renderizado, así que es conducta de producto y no un refactor. Se dejó cada una como estaba a propósito al centralizar el aviso, para no colar un cambio visible dentro de una extracción |
| Escala tipográfica del sidebar sin canonizar | ⚠️ **No re-medido en la reconciliación del 2026-08-14**: acotar el recuento «al cajón» exige el mismo alcance que usa `SidebarTokenBudgetTest`, y un `grep` sobre `site.css` entero da otra cosa (55 valores en 259 declaraciones, todo el sitio). La cifra de abajo es la del 2026-08-14 en su medición original. El cajón usa **17 tamaños de letra distintos** en 74 declaraciones, con pasos de 1px (9, 9.5, 10, 11, 11.5, 12, 13, 14, 15, 16, 17, 18, 20, 22, 26, 40). Desde Fase 4 · paso 4.0c son tokens (`--fs-N`) sobre una unidad, así que **cambiarlos es tocar el `:root`, no 74 sitios**; lo que no existe es una escala de diseño con pocos pasos. **Medido el 2026-08-14**: canonizarla a 6–7 pasos movería el **52-55%** de los usos (47px de desviación total) | La coherencia tipográfica del cajón. No muerde a nadie hoy: el diseño actual es el que está en producción y funciona | **Sin plan — decisión de owner**: redondear es **rediseño visual**, que `sidebar-spa.md` §2 declara fuera del alcance de Fase 4. La indirección ya está hecha, así que el día que se decida es un cambio de N líneas en `:root` y una revisión visual, no un barrido |
| Las fechas del cajón las formatean dos motores distintos | El servidor usa Carbon (`isoFormat('ddd D MMM')`) y el cajón SPA `Intl`, porque §4.5 dejó la i18n de fechas **sin canal**. **Medido el 2026-08-14** con el patrón de formato fijado por nosotros (no por un preajuste de `Intl`, que en inglés invierte día y mes): `en` y `fr` salen **idénticos**; solo el español difiere — servidor `Sáb. 5 sep.`, cliente `Sáb 5 sept` (ICU no abrevia con punto y usa `sept`). Afecta a la línea de contexto de la banda de progreso y a las cabeceras del calendario | Solo al texto visible, y solo en español. No hay dato incorrecto: mismo día, mismo orden, misma hora | **Fase 4, si se decide**: se cierra publicando la etiqueta ya formateada en la oferta de días (`GET availability/{producto}/dates`), que es un cambio de CONTRATO y no de transcripción — por eso no entró en 4.3·1. Vigilada por `SidebarProgressParityTest`, que exige identidad en `en`/`fr` y fija la excepción española: si desaparece, el test obliga a retirar esta fila. ⚠️⚠️ **2026-08-22: cerrarla es ahora MÁS BARATO y más URGENTE.** Más barato porque el mecanismo ya existe —`DisplayTime::dayLabel()` es la fuente única y `me/orders`/`me/reservations` ya publican `date_label`, con el patrón sentado (`specs/area-cliente.md`)—: aquí solo falta `OfferedDateResource` y que `progress.js` use la etiqueta en vez de componerla. Más urgente porque el área de cliente **hace visible la incoherencia**: en español, el embudo pinta «Sáb 5 sept» y la zona «Mis reservas», «Sáb. 5 sep.» — dos pantallas del mismo cajón. ▶ **No se cerró en el paso 3 a propósito**: tocar la banda de progreso, su paridad y su manifiesto dentro del paso del área de cliente mezclaría dos trabajos, que es la regla que ha ordenado la fase entera |
| El formato de importe está quemado en español | `number_format($c / 100, 2, ',', '.').' €'` — coma decimal, punto de millares y el símbolo detrás— aparece **quemado** en `AddonResolver::viewModel()` (la nota de precio de cada complemento) y en una decena de sitios de `purchase.blade.php`. No mira el locale ni la moneda de la instalación. **Medido el 2026-08-14** al publicar los complementos por API: con `Accept-Language: en` el nombre llega traducido (`Grip socks`) y el precio sigue saliendo `2,00 €` | Toda instalación que venda fuera de un locale con esa convención, y ahora también por API: el endpoint hereda la nota tal cual. Hoy no muerde porque el producto solo se ha instalado en España | **Sin plan — decisión de owner**: cambia lo que ve el cliente en las tres superficies a la vez y arrastra la moneda (hoy `EUR` implícito), así que es trabajo de producto —no un refactor— y no se cuela dentro de un paso de API. Preexistente: el endpoint no lo introdujo, lo destapó |
| Dos formas de emparejar una respuesta del evento con su etiqueta | Desde Fase 4 · paso 4.0b·4b la composición es `TicketType::eventAnswers()` (esquema actual, filtrable por fase), y la usan la compra web y `GET orders/{code}/event-data`. `ReservationSlip::eventDataRows()` **sigue con la suya**: añade al final las claves HUÉRFANAS —las de campos que el pack ya no declara, con la etiqueta derivada de la clave— y castea valores no escalares con su propio `stringifyValue()` | La hoja de sala del panel. Un cambio en las reglas de composición (orden, saneo, i18n de etiquetas) habría que portarlo a los dos sitios | **Sin plan — no se unificó a propósito**: la divergencia es INTENCIONADA (el operador necesita ver todo lo que el cliente contestó, aunque el esquema haya cambiado; el cliente ve su reserva), y una respuesta huérfana no tiene `stage`, así que no se puede acotar a una fase. Unificar exigiría decidir qué hace la hoja de sala con las huérfanas, que es conducta del panel |
| N+1 de precio por complemento en `AddonResolver` | `AddonResolver::resolve()` pide el precio de cada complemento con `RateResolver::priceCents()`, que son 2 consultas por llamada (tarifa del día + importe). **Medido 2026-08-13 vía `orders/quote`: 1 complemento → 8 consultas, 3 → 12, 6 → 18** | Lo ejecuta también `OrderCreator` **dentro de la transacción que sostiene los locks de aforo**, donde alargar la txn es peor que en un presupuesto | Sin plan — es código de dinero compartido; su arreglo exige paso propio y los dos verificadores. `ApiOverheadTest::test_the_known_cost_per_addon_does_not_get_worse` impide que EMPEORE. ▶ **Desde `#413` lo paga también el POST-FORM**: `PostFormAddons::viewFor()` tarifica cada extra sin línea, y son las mismas 2 consultas. **Se midió antes de aceptarlo** y se dejó a propósito: esquivar `RateResolver::priceCents()` para ahorrarlas crearía un séptimo camino de tarificación (`#329`) y la pantalla podría enseñar un importe distinto del que se va a cobrar. Guarda de PENDIENTE: `Reservation\GuestFormTest::test_the_page_pays_at_most_two_queries_per_addon` |
| Un solo driver de pasarela (Redsys) | ~~La secuencia duplicada en la capa de entrega~~ **RESUELTA en el cierre de Fase 3** (`DECISIONES #37`): «admitir → crear → abrir cobro» vive en `Booking\Services\CheckoutOrchestrator` tras los puertos `ReservationCheckout` y `PaymentInitiation`, y `CheckoutSequenceTest` impide que reaparezca fuera del dominio. **Lo que queda es la otra mitad**: `PaymentInitiation` lo implementa solo `PaymentInitiator` (Redsys), no hay selección de driver por configuración ni un segundo driver | Un cliente fuera de España no se puede instalar sin escribir un driver nuevo. Ya NO hay riesgo de que una superficie se salte un paso: eso lo cierra el orquestador | **Fase 6**, con la app móvil — decisión del owner del 2026-08-13: hacer ahora la mitad MEDIDA (5 puntos de llamada duplicando el orden) y dejar el driver para cuando exista un lector real que revele su forma. El puerto ya está, así que el driver nuevo se enchufa sin tocar a ningún consumidor |
| Post-form web: el binding implícito filtra si una reserva existe | La ruta web usa *route model binding* (`OrderItem $reservation`), así que un id inexistente responde **404 antes** de comprobar la autorización, mientras que uno existente responde 403. Esa diferencia ES el oráculo que la escalada 403→410→404 quería evitar. **Medido 2026-08-13** al hacer la versión de API, que resuelve la reserva a mano y responde **el mismo 403** en ambos casos | Un tercero puede comprobar qué ids de reserva existen comparando códigos de estado. No expone datos, sí la existencia | Sin plan — no se tocó en el paso 5 porque cambia la conducta de una superficie en producción; la corrección es idéntica a la de la API (resolver a mano) y está a la vista en `Api\V1\GuestFormController` |
| Migraciones con lógica de datos del origen | **8 de 72** migraciones con backfill/seed dentro + **4 clases** `Legacy*`/`*Backfill` vivas (re-medido 2026-08-14; ⚠️ ya **no** en `app/Support`, que desapareció en Fase 2) | Decisiones del origen incrustadas en el esquema; ruido en instalación limpia | Sin plan |
| **Waiver · lo que el CLIENTE decide sobre su propia prueba** | Revisión `#169`, spec **§10.2**: (1) el **canal** que entra en el hash sale de `$request->bearerToken() !== null` (`MeWaiverController::signatureRequest()`, `AuthRegistrationController::register()`) — con cookie válida y `Authorization: Bearer basura` la firma consta como `api` sin que Sanctum valide nada; (2) la **IP** es el primer valor de `X-Forwarded-For` mientras `trustProxies(at: '*')` (`bootstrap/app.php` (`trustProxies`), medido: XFF `8.8.8.8` → `ip() = 8.8.8.8`); (3) se firma **con el correo sin verificar** — `SelfSignup::register()`/`SelfSignup::createAccount()` firma en la misma transacción que crea la cuenta y el correo sale después; la fila no guarda `email_verified_at`; §4.7 decía «correo verificado» y era falso; (4) **`POST /me/waiver` no es idempotente** (`WaiverAcceptance::accept()`): dos envíos del mismo id → dos firmas, dos `consents`, dos PDF (sonda `[201, 201, 2, 4, 4]`) | El valor probatorio: tres datos del PDF los fija quien firma, y una app nativa o un reintento duplica el registro. Hoy ninguna instalación está en `interno` | ✅ **(1) y (4) HECHOS** (`#174`, 2026-08-26 noche): el canal sale del token REAL (`currentAccessToken() instanceof PersonalAccessToken`; con sesión, `Bearer basura` sigue siendo `web`) y en el alta de si la petición se sirvió con sesión; `WaiverSigner` es **idempotente por versión y sujeto, DENTRO del lock** (segunda aceptación → la fila que hay; `waiver:verify-chain` firma ahora un menor por proceso y sigue cazando la bifurcación sin el lock: 15 `prev_hash` repetidos). 7 casos, mutaciones que muerden. ⏳ **(2) la IP** es infra: acotar `trustProxies` a los rangos del hosting (`ENTORNOS.md`) · ✅ **(3) el correo — HECHO** (`#179`, spec §7·5): `WaiverSigner` exige correo verificado (salvo firma declarada), el alta deja la aceptación pendiente y se firma al verificar; `409 waiver_email_unverified`; y desde `#183` la firma pendiente lleva la IP/UA de la ACEPTACIÓN y se registra tras el commit (S-1). **Queda solo (2), la IP** (infra). Precisión (S-4, `#183`): el canal `web` del alta lo decide que la petición se sirviera CON sesión, y eso lo decide el `Origin` de la web (`ApiOrigin`) — es el diseño, no un hueco |
| ~~**Waiver · el cajón: tres grietas entre lo que ENSEÑA y lo que FIRMA**~~ **RETIRADAS las tres 2026-08-26** (`#175`: el id que se firma es el del texto ENSEÑADO y el store relee si el estado apunta a otro —CAJ-1—; el 422 del alta relee y desmarca —CAJ-2—; tras el 409 la casilla se desmarca —CAJ-3—; 6 casos JS, 5 mutaciones que muerden, verificado en headless). ⏳ **Queda CAJ-4** (el 401 mudo, patrón de todos los formularios del área) | Revisión `#169`, spec **§10.3**: **CAJ-1** el id que se envía sale de `status.current_document_id` y el texto pintado de `legal.document`, cacheado para toda la página (`stores/waiver.js:52`, `:63`) — en la ruta del embudo (alta en el paso 5 → sesión sin recarga → Mi cuenta) puede enseñar la vieja y firmar la nueva sin 409 · **CAJ-2** el 422 `waiver_stale` del alta dice «vuelve a leerlo» y no hay forma de releer (`RegisterForm.vue:103`, `ensureLegal()` de una sola vez) · **CAJ-3** tras el 409 la tarjeta remonta con el texto nuevo **y la casilla sigue marcada** (`PrivacyZone.vue:73-80`) — ⚠️ **reproducido en headless el 26/08** (V34: «la casilla vuelve a estar desmarcada» ✗ y el botón habilitado) · CAJ-4 un 401 al firmar no cambia nada en pantalla (`form-outcome.js:44` marca `expired` y ningún `.vue` lo lee; patrón de todos los formularios del área) | Quien firma: puede aceptar un texto que no ha visto (CAJ-1, CAJ-3) o quedarse sin salida (CAJ-2) | Código acotado, carril A (`resources/js/sidebar/`): enviar el id del documento ENSEÑADO · invalidar `legal` en el 422 del alta y en el 409 · desmarcar la casilla al 409 · leer `expired` en las zonas. Tests de `node --test` para los tres |
| ~~**Waiver · la guarda de borrador caza UN marcador de tres**~~ **RETIRADA 2026-08-26** (`#174`: `DRAFT_MARKERS` con los tres del seeder + el neutro, y `mentionsDraftWords()` avisa de «borrador/draft/brouillon» en el modal de publicar sin bloquear; 2 casos, mutación que muerde) | `LegalDocumentPublisher::DRAFT_MARKER = '[pendiente'`; el seeder siembra `[PENDING: final wording]` (en, `LandingContentSeeder::legalDocs()`) y `[À COMPLÉTER : rédaction définitive]` (fr, `legalDocs()`) y **pasan** (`looksLikeDraft` → `false`, medido). En la BD del carril A hay una v1 publicada que dice «Este texto es un borrador…» sin corchetes (spec **§10.4**) | Publicar es irreversible: un operador con `content.manage` graba un borrador en la cadena probatoria quitando 30 caracteres. `#160` vendía una guarda de BORRADOR y es de MARCADOR | Código de una línea + test: los tres marcadores; y la palabra «borrador/draft/brouillon» como AVISO en la confirmación, no como bloqueo |
| ~~**Waiver · el badge del pedido lee el SELLO, la puerta lee el REGISTRO**~~ **RETIRADA 2026-08-26** (`#174`: el badge va por `WaiverStatus` — «No firmado» con sello sin registro en interno, «versión anterior» señalada, oculto en `desactivado`; 3 casos, mutación que muerde) | `OrderInfolist::detailsSection()` pinta `user.waiver_accepted_at` sin `WaiverStatus` (spec **§10.5**, PAN-5): en `interno` un sello heredado sin registro sale **verde** en el pedido y «falta firmar» en la puerta; una firma de versión antigua sale verde sin aviso; en `desactivado` sigue avisando. §8.7 exigía migrar «las dos» superficies y se migró una | Un operador que mira la ficha del pedido cree lo contrario de lo que le dirá la puerta | Código pequeño: el badge por `WaiverStatus` (modo, vigente/anterior) — carril del panel |
| **Waiver · `declared_by_user_id` entra en el hash y su FK es `nullOnDelete`** | La FK `declared_by_user_id` de `2026_08_25_130000_create_waiver_probatory_tables.php` + `WaiverSignature::HASHED_FIELDS` (spec **§10.5**, PAN-1/NUC-2, confirmado 2/2): un borrado físico del operador (`app:purge-customer-data` conservando al titular, o SQL; el panel no borra usuarios) deja `verifyHash() === false` **para siempre** en todas las firmas que declaró — la FK muta un campo hasheado de una fila «inmutable». Y PAN-4: el PDF lee `business.name` y el nombre del operador **en vivo** (`WaiverProof::businessName()`, `WaiverProof::declaredByName()`), así que renombrar cambia todos los PDF ya emitidos | Hoy ningún flujo lo dispara; el día que sí, la prueba de mostrador se rompe sin que nadie haya tocado la tabla | `RESTRICT` (como `user_id`), o copiar el nombre del operador en la fila como se copió el del titular; y decidir si la cabecera del PDF va congelada |
| ~~**Waiver · ningún gate vigila el lock de `WaiverSigner`**~~ **RETIRADA 2026-08-26** (`#174`: `WaiverSigner` en el `CRITICAL_RE` del `pre-push` y en `CriticalPathGateTest::CRITICAL_FILES`, con `WaiverAcceptance` de control negativo y `waiver:verify-chain` en el aviso del hook; mutación que muerde) | Spec **§10.5** (NUC-6): la suite corre en SQLite, donde `compileLock()` devuelve `''`; ningún test de `tests/Feature/Waiver/` menciona `lockForUpdate`; `WaiverSigner.php` **no está** en el `CRITICAL_RE` del `pre-push` ni en `CriticalPathGateTest::CRITICAL_FILES`. Retirar el lock pasa 72/72 y pasa el push | La única propiedad por la que existe la cadena (§8.5, vista fallar 3 de 3 sin el lock) depende de acordarse de `waiver:verify-chain` | Añadir `WaiverSigner` al `CRITICAL_RE` y a `CRITICAL_FILES` (con su control negativo), como pide la spec de lealtad para el canje |
| **Los `throttle:N,1` SIN nombre comparten UN cubo por usuario — y el reintento del pago está dentro** | Spec **§10.5** (API-2): `ThrottleRequests::handle()`/`ThrottleRequests::resolveRequestSignature()` — sin prefijo la clave es `sha1(user id)`, común a `POST /me/waiver` (10), el PDF propio (10), el guest-form (30), **`POST /orders/{code}/payment` (6)** y `verification.send` (6). Siete descargas del PDF en un minuto → 429 en el reintento del pago durante 60 s | El camino del dinero (`PAY-04`) se puede bloquear desde una superficie que no es de dinero. No nace en el waiver: la tanda 3a añadió dos rutas al cubo que ya compartían tres | ✅ **Las dos rutas del waiver ya van con PREFIJO** (`#174`: `throttle:10,1,waiver-sign` y `throttle:10,1,waiver-pdf` — el tercer parámetro del middleware es el prefijo del cubo; guarda en `MeWaiverTest`). ⏳ Quedan sin prefijo el guest-form (30), **el reintento del pago (6)** y `verification.send` (6): siguen compartiendo cubo entre sí. Una línea cada uno, pero el del pago está en el camino de `PAY-04`: se toca con su test |
| ~~**Waiver · la poda con firmas de MENOR intercaladas rompe la cadena**~~ **CERRADA 2026-08-27** (`#197` decide, `#198` ejecuta: cadena por (titular, sujeto); la guarda es `WaiverRetentionTest::test_pruning_one_subject_never_breaks_the_others_chain`, en las dos direcciones) | Spec **§10.5** (NUC-3): `prunable()` no poda `dependent` (`WaiverSignature::pruning()`) pero la cadena enlaza sobre la **última fila del titular sea cual sea el sujeto** (`WaiverSigner::sign()`): con un menor intercalado, la poda deja agujeros en medio y `WaiverChain::verify()` declara ROTA una cadena legítima. Hoy `dependent` es **código inalcanzable** (ningún escritor lo produce) | `menores-a-cargo.md` §8.3 el día que produzca la primera firma de menor: la poda se vuelve indistinguible de una manipulación | Decidirlo ANTES de esa primera firma: cadena por (titular, sujeto), o poda por cadena entera. Va en la spec C. ▶ **2026-08-27 (`#191`)**: la tanda 1 de menores está en el árbol **sin producir ninguna firma de menor**, a propósito; las opciones con su coste, para el owner, en `specs/menores-a-cargo.md` **§9.5·1** (recomendada: cadena por sujeto — el verificador de concurrencia pasa a medir la idempotencia bajo el lock). ▶ ✅ **`[DECIDIDO owner, 2026-08-27]` (`#197`): cadena por (titular, sujeto).** La tanda 2 de menores la ejecuta; la ficha se retira cuando esa tanda esté en el árbol |
| ~~**El contador de reservas próximas del índice de la cuenta NO se pinta nunca**~~ **RETIRADA 2026-08-26** (`#175`: `store.upcoming`, una palabra) | `AccountHomeZone.vue:63` compara `upcoming > 0` con un identificador **no declarado** en el `setup()` (`_ctx.upcoming` = `undefined`; dos líneas después sí usa `store.upcoming`). Compilado: `t.upcoming>0`. Lo cazó la revisión del waiver (`#169`, §10.3 CAJ-5) porque comparte componente con el aviso nuevo; **no es del waiver** | Quien tiene reservas futuras: la burbuja `.acct__count` y su `sr-only` no existen. Ninguna guarda lo ve: el diff de árbol no monta las zonas | Una palabra (`store.upcoming`) + un caso en el test del índice. Carril A |

## Baja

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **La API de Places devuelve instantáneas INCONSISTENTES entre llamadas** (2026-09-10, `#493`) | Medido con cinco consultas seguidas al mismo `place_id`, con segundos de diferencia: **cuatro dieron «5,0 · 1 reseña» y una «3,0 · 2 reseñas»**. La segunda reseña existe —es de 1 estrella y su texto llegó traducido en los tres idiomas—, así que no es un fallo de parseo ni de nuestra caché: **es la infraestructura de Google sirviendo vistas distintas**. ⚠️ No está documentado por ellos y no hay parámetro para forzar consistencia. | Con el umbral en **1** (`[DECIDIDO owner]`) y **dos** reseñas, la cifra que publica la portada **depende de qué instantánea pille el refresco de esa hora**: 5,0 o 3,0. Con un recuento alto el efecto se diluye —una reseña más no mueve una media de 300—; con dos, la mueve entera. | Es de Google y no se puede arreglar desde aquí. Las salidas son del owner: **(a)** subir el umbral, que es lo que el 10 evitaba; **(b)** no publicar la CIFRA y sí las opiniones, hasta tener volumen; o **(c)** aceptarlo a sabiendas. ⚠️ Una cuarta —quedarse con la instantánea «mejor»— **está descartada**: elegir cuál de las dos verdades se publica es exactamente el control editorial que §1.4 dice que no tenemos. |
| **Tres secciones de la portada NO desaparecen con el panel vacío** (2026-09-10, `#488`) | Regla dura del sistema de diseño: *«una sección cuyo contenido lo pone el panel desaparece con cero filas: ni rótulo, ni titular, ni caja vacía»*. Medido en `resources/views/home.blade.php`: la cumplen **`#events`** (`@if ($partyCards !== [])`) y **`#faq`** (`@if ($faqs->isNotEmpty())`, la que añadió esta tanda). **No la cumplen `#zones` (01), `#pricing` (02) ni `#rides-section` (03)** — y la 03 es la más clara: el mosaico SÍ está guardado (`@if ($rideMosaic !== [])`) y **la cabecera no**, así que con cero atracciones se pintan el rótulo, el titular, una entradilla que dice literalmente «0» y los **144 px de aire** de la sección, debajo de nada. | Hoy no muerde: esta instalación tiene zonas, tarifas y 23 atracciones. Muerde **en una instalación nueva** —el caso white-label, que es el producto— entre el `ProductionSeeder` y el día que el negocio da de alta su catálogo: la portada publica cabeceras que no presentan nada. ⚠️ Y 02 es la peor de las tres si pasa: la sección que vende. | Una pasada corta: envolver las tres en su `@if` y añadir el caso a la guarda de cada una. **No se hizo en `#488` a propósito**: toca tres secciones cerradas (`#478`, `#479`, `#481`/`#482`) y sus guardas, y meter eso en la tanda de otra sección es cómo se cuelan cambios que nadie revisa. ▶ El molde está escrito en `DudasSectionTest` (el caso del vacío, su control con una sola fila, y el de la fila desactivada). |
| **Waiver · la IP y el user-agent de un tercero se guardarían bajo el `target` del titular** (2026-09-01, revisión adversarial de `specs/waiver-por-reserva.md`, §4.14) | `AuditLogger::log()` persiste la IP y el user-agent **de la petición en curso**. Con el justificante por reserva (aún sin construir), esos valores son los del **padre desconocido** que firma, en una fila cuyo `target` es el responsable. Y `User::anonymize()` nulifica IP/UA **por ACTOR**, así que sobrevivirían a la supresión del responsable — que no es su dueño para borrarlos ni para conservarlos. | No muerde hoy: la feature no existe. Muerde el día que la T1 escriba el primer `waiver.signed` de un `guest_minor`. **Sin PII de nombre**, así que es menor. | La T1 de `specs/waiver-por-reserva.md`: usar el canal que no persiste IP/UA (`logSystem`) para ese sujeto, o dejar escrito por qué se conservan. Es una línea y una decisión. |
| ~~**El tipo de campo `age` puede llegar a `event_fields` y ahí rompe el contrato**~~ **CERRADA 2026-08-30** (`#282`: cada esquema declara SUS tipos —`TicketType::EVENT_FIELD_TYPES` / `GUEST_FIELD_TYPES`— y las tres puertas leen de ahí; `ApiContractTest` ata la lista del dominio al `enum` del contrato por los dos lados) | El `Select` del panel solo ofrece `age` en los datos por-niño, pero el saneo —`InteractsWithCatalogForm::sanitizeFieldSchema` y `TicketType::normalizeFieldSchema`— acepta los CUATRO tipos en los DOS esquemas. Medido: forzado, persiste, y `CatalogEventFieldResource` lo publica tal cual contra `openapi/v1.yaml` (`CatalogEventField`, `enum: [text, number, textarea]`, `additionalProperties: false`) | Solo con una petición fabricada por alguien con acceso al catálogo. Un cliente estricto de la API rechazaría la respuesta | Acotar el tipo `age` a `guest_fields` en el saneo (es la «regla 12» que el propio fichero invoca dos funciones más abajo), o ampliar el `enum` del contrato. Dos líneas |
| **El ÁREA DE CLIENTE no tiene casos de contrato de árbol, y los `.vue` no tienen lint** (2026-08-28, `#210`) | Medido: el login del área estuvo MUDO cinco días —`const auth` sombreaba la prop `auth` en `AccountSection.vue`— y un `watch` por encima de su `const` (TDZ) sobrevivió a un guion 19/19. `SidebarDomContractTest` cubre los ONCE pasos del embudo y **cero** de las 12 zonas de `ZONES` (11 componentes `*Zone.vue`); `node --test` no monta `.vue`. Hoy lo cubre `SidebarSetupBindingsTest`, que es un escáner de texto con alcance declarado, no un parser | Cualquier fallo de cableado de una zona (prop que no llega, texto que no se pinta, `watch` mal ordenado) solo lo ve un navegador con el caso «malo» delante | `[PENDIENTE: owner]` dos opciones con coste: **(1)** casos de contrato para las zonas del área (extender `render-sidebar.mjs` con sección+zona+estado de Pinia; ~1 tanda) · **(2)** ESLint en el gate (`no-use-before-define` + `vue/no-dupe-keys`; dependencia nueva de ~30 MB en `devDependencies`, un config y un paso en `pre-push`). Las dos son compatibles |
| **«Guardar» en Ajustes se queda MUDO si el error está en otra sección** | Medido con el owner delante el 2026-08-26: eligió el modo interno del waiver (sección «Puerta», plegada, al final) y «Guardar» no hacía nada visible. Reproducido con `Livewire::test(Settings::class)->call('save')`: el formulario ENTERO se rechazaba por `address.maps_url = '#'` («debe ser una URL válida»), un campo de la sección de dirección, media página más arriba. Filament pinta el error bajo ese campo y **ninguna notificación junto al botón**. ▶ La raíz de ESE `#` ya está retirada: lo sembraba `LandingContentSeeder` (la landing ya pinta `#` cuando el ajuste está vacío, así que era redundante), y hay guarda: lo que siembra el seeder de demo tiene que poder guardarse tal cual desde Ajustes. | Cualquier operador con un valor inválido en un campo que no está mirando: cree que el panel está roto. | **Sin plan.** Candidata barata: en `Settings::save()`, capturar la `ValidationException` de `getState()` y lanzar una notificación roja con los rótulos de los campos que fallan antes de relanzarla. |
| **El registro del pedido en CHINO conserva el defecto entero** | `lang/zh_CN/admin.php` existe (112 KB, versionado) y el chino **es un idioma soportado del panel** (`SetAdminLocale::SUPPORTED`). `DECISIONES #145` arregló **solo el español**. Medido el 2026-08-25: de las 25 acciones del catálogo, `zh_CN` etiqueta **18** → **15 saldrían como clave cruda** y **8 son huérfanas** — las mismas cuatro misarchivadas y cuatro muertas que tenía el español | Un operador con el panel en chino ve exactamente lo que el owner vio en staging: `orders.gate_credit_applied` y nada más. ⚠️ Y `AuditActionCatalogTest` **declara que no lo mira**, así que no lo va a avisar nadie | **Decisión del owner (2026-08-25): NO se arregla.** El chino es herencia del origen y **no lo usa nadie**. Traducir 25 etiquetas a un idioma que nadie del equipo puede revisar produce copy sin validar. ▶ Si vuelve a usarse: el alcance se amplía en `AuditActionCatalogTest` recorriendo `SetAdminLocale::SUPPORTED` |
| **El visor GLOBAL de incidencias enseña claves crudas** | Usa otro espacio de nombres (`admin.audit.actions.*`) y solo etiqueta las **8** acciones críticas — las que filtra por defecto. Las otras ~79 del catálogo salen en crudo si se quita el filtro | Al investigar un incidente, quitar el filtro «solo críticas» devuelve una lista de claves técnicas | Sin plan. ⚠️ Hueco declarado en `AuditActionCatalogTest`: `#145` cerró el registro DEL PEDIDO, no éste |
| ~~El reembolso del PEDIDO (sin cancelar) no preguntaba ni decía el importe~~ **CERRADA 2026-08-25** (`#153`, owner) | Decisión: SIN campo aquí (el canje legítimo devuelve el total; los parciales van por LÍNEA, `#149`). El modal ahora NOMBRA el importe exacto («Se devolverán X € — TODO lo cobrado») y, si el operador desactiva «también cancelar» —la forma exacta del error medido—, un aviso le señala la vía de los parciales | — | ✅ hecha (2 mutaciones muerden) |
| ~~El tope de reembolso por línea es incorrecto tras cambiar el PRECIO~~ **CERRADA 2026-08-25** (`#150`) | `itemOriginalOnlineCents` reconstruye ahora `cantidad_original × precio_original` — cada término del PRIMER cambio de su clase, con desempate por `id` — porque el cambio de precio viaja estructurado (`unit_price_change`) en el contexto del ajuste. Verificado en vivo (`R-VLRYUV`): 40→30, tope 40,00, y tras cancelar la RESERVA la línea devuelve TODO | — | ✅ hecha, con los CUATRO caminos de `#146` en `ItemPriceChangeReconstructionTest` y 6 mutaciones que muerden |
| ~~La bajada por cambio de fecha no deja fila en el desglose~~ **CERRADA 2026-08-25** (`#150`) | El marcador de reducción se dispara con CUALQUIER cambio reconstruible (cantidad o precio unitario), no solo `quantity_change`. Verificado en vivo: marcador 0 € con `changes=[slot_change,unit_price_change]` y etiqueta «Cambio de fecha a …» | — | ✅ hecha |
| ~~Dos textos del panel cuentan la causa vieja de una bajada~~ **CERRADA 2026-08-25** (`#150`) | El toast elige la variante VERDADERA por la causa que ya viaja (cantidad → «unidades canceladas» · precio → `success_edited_reduced_price`), los tres pies de «pendiente de devolución» citan también la bajada de precio, y el registro etiqueta `unit_price_change`. El TERCER texto de la familia (el modal de Reembolsar prometía una cancelación falsa desde `#157`) lo arregló `#149` | — | ✅ hecha |
| ~~«+2 Pack» — la línea de puerta narraba un cambio de FECHA como CANTIDAD (el sexto sitio de `PAY-18`)~~ **CERRADA 2026-08-25** (`#150`) | La etiqueta «+N producto» exige ahora un `quantity_change` real además de la divisibilidad. Verificado en vivo sobre `R-8STAH6` (pedido PRE-arreglo): el cliente lee «Cambio de fecha a 05/09/2026 10:00». ⚠️ Cargos anteriores a `#145` (contexto vacío) caen al respaldo honesto («Diferencia por cambios en X») | — | ✅ hecha |
| ~~Un pedido CANCELADO no tenía NINGUNA vía de reembolso en el panel~~ **CERRADA 2026-08-25** (`#152`, owner) | La LÍNEA se abre para cancelados con deuda (`refundItemBlockedReason` acepta `CANCELLED`; topes bajo lock intactos, cancelado sin deuda sigue bloqueado) y el banner dice CUÁNTO se debe y por dónde se devuelve. El TOTAL sigue vetado a propósito. E2E medido: bajada → cancelar pedido → reembolsar línea → «pendiente de devolverte» a CERO | — | ✅ hecha |
| ~~El email de una BAJADA no mencionaba el dinero que se le debe~~ **CERRADA 2026-08-25** (`#155`) | `OrderItemModified` cuenta las DOS mitades de la bajada con el reparto real del panel: «deja :amount € pendientes de devolverte» (mismo vocabulario que la pantalla) y «pagarás :amount € menos al llegar al parque» (absorbido, packs con señal). ES/EN/FR con guarda `Lang::has(..., false)` | — | ✅ hecha (3 mutaciones muerden) |
| ~~El cliente EN/FR veía claves en CRUDO en su desglose de dinero~~ **CERRADA 2026-08-25** (`#154`) | Las etiquetas del cargo de puerta viven ahora en `tickets.gate_change_line_slot`/`_product` (ES/EN/FR), junto a su respaldo de `#131`; las de `admin.*` (solo ES) se retiraron. Guarda con `Lang::has(..., false)` × 3 idiomas + composición real bajo `en` | — | ✅ hecha (2 mutaciones muerden). ⚠️ **Y una SEGUNDA guarda al lado** (`#157`, `ClientMoneyLabelsAreTranslatedTest`): existir no es estar traducido — con `lang/fr` relleno de castellano **esta guarda pasa (23 verdes) y aquélla cae**, medido. Añade además la guarda-de-la-guarda, la prohibición del mecanismo y el barrido ancho de todas las claves `tickets.*` del dominio. ❗ **Las dos se arreglaron EN PARALELO por los dos agentes sin saberlo**: el porqué y la regla que sale de ahí, en `#157` |
| ⚠️ El `redis.conf` de staging está de fábrica (14 bytes) | Medido el 2026-08-25 (`#137`): `maxmemory 0` (**sin techo de memoria**), `maxmemory-policy noeviction`, snapshots activos y `stop-writes-on-bgsave-error yes` | Dos formas de morder: la caché **crece sin límite** hasta que el contenedor sufre, y **si falla un volcado RDB deja de aceptar escrituras** — con `CACHE_STORE=redis` eso es el sitio caído por algo que no tiene que ver con la web | **Aplazado por el owner**, no olvidado. Contenido acordado en `#137` (`maxmemory 256mb` · `allkeys-lru` · `save ""`). ⚠️ Aplicarlo exige reiniciar el contenedor PHP desde el panel |
| `app:set-setting` pasa su eco de VERIFICACIÓN por el formateador de consola | Mismo mecanismo que `#135`: `«comprobado en BD: …»` sale por `info()`, y Symfony se come la barra de `\<` / `\>`. Con un valor que las lleve, el eco enseña algo distinto de lo guardado | Es un eco de **verificación**: el operador lo lee para creerse que la clave quedó bien. No bloquea (los secretos van enmascarados y el valor lo escribe él), pero **puede mentir justo cuando se le consulta** | Decisión del owner: mismo arreglo de una línea que `#135` (salida RAW) o dejarlo |
| Hueco test: throttle rutas Redsys | `throttle:120,1` vigente; 0 asserts | Quitarlo pasa verde → amplificación en el retorno de pago | Sin plan (PAY-15 ⚠️) |
| Hueco test: `after_commit` de la cola | `after_commit => true` vigente; 0 asserts | Si regresa: emails de pedidos cuya txn rollbackea | Sin plan (PAY-14) |
| Hueco test: frontera de fecha UTC↔Madrid | `DisplayTimeTest` no cruza medianoche UTC | Bug latente de «hoy operativo» 22:00–00:00 Madrid en verano | Sin plan (AFORO-09 ⚠️) |
| Hueco test: gate no-prod de `tableExists` | Código vigente; 0 asserts | +4 queries/request al hot path si regresa | Sin plan (PERF-03 ⚠️) |
| Hueco test: `hasPositivePrice` 0-queries | Solo cobertura indirecta (presupuesto de la home) | N+1 enmascarado hasta agotar el presupuesto | Sin plan (PERF-04) |
| `slots.seats_taken` muerta | Columna sin ningún escritor; el propio panel advierte «NUNCA seats_taken» | Trampa para agentes: parece la verdad del aforo y no lo es | Sin plan (candidata a drop) |
| `rooms` sin uso | Modelo+tabla+seed placeholder; cero consumo en flujos | Estructura muerta que confunde el diseño de Fase 2 | Sin plan (candidata a drop) |
| **El día elegido avanza solo en la SPA y no en Livewire** | Medido en el extremo a extremo (`DECISIONES #59`): `Purchase::selectDate()` solo MARCA el día y exige pulsar «Continuar» (`goToTime`); `selectDate()` del cajón SPA hace `store.go(STEPS.TIME)` y salta al paso 3. Consecuencia: el CTA «Continuar» del paso 2 es **inalcanzable** en la SPA | El embudo de compra. No rompe nada —se llega igual al paso 3— pero es un clic menos en un motor y no en el otro, y deja muerto un CTA que el pie sí compone. Ningún test lo ve: el diff de árbol renderiza cada paso por separado | **Sin plan — decisión de producto.** Quitar un clic puede ser mejor UX; lo que no vale es que no esté decidido. Si se adopta, hay que quitarlo también del Blade y retirar el CTA del paso 2 |
| **Sin foco al cambiar de paso en el cajón** | §6 del spec de Fase 4 lo pide y **el cajón no lo hace**: cero llamadas a `focus()` al mover de paso. Medido en su día en los DOS motores —`purchase.blade.php` y `resources/js/sidebar/`—; retirado el primero en `#112`, queda el segundo, y el hueco sigue igual. El foco de PANEL sí está —`a11yPanel` da el primer foco al abrir y atrapa el Tab—; lo que falta es mover el foco al contenido cuando el asistente avanza | Quien navega con teclado o lector de pantalla: al pasar de catálogo a calendario, el foco se queda donde estaba y el cambio no se anuncia | **Sin plan — hueco HEREDADO, no regresión de la SPA.** Se anota para que nadie lea §6 y crea que el motor nuevo lo perdió |
| **Reintento denegado sin aviso visible** | Fase 4 · 4.6·2, MEDIDO contra el HTML: en el paso 10, `retryPayment()` mete el motivo en `errors.cart` —pausa, límite de frecuencia o 502 de la pasarela— y **el bloque del paso 10 no tiene `@error('cart')` y el pie es nulo en los pasos de resultado**, así que el mensaje no aparece en ninguna parte. Para el cliente, el botón «Reintentar el pago» simplemente no hace nada | La pantalla en la que alguien con la tarjeta rechazada intenta pagar por segunda vez. Afecta a los DOS motores: el cajón SPA lo transcribe fiel a propósito (parity), así que arreglarlo es cambiar la WEB | **Sin plan — decisión de producto.** El arreglo es una línea de Blade (+ su espejo en `DeclinedStep.vue`), pero cambia la copia de una pantalla del camino del dinero en tres idiomas |
| Ciclo de `tickets` amputado | Solo `STATUS_PURCHASED`; sin canje digital (`qr_token` sin lector) | Una puerta digital (Fase 6) parte de un ciclo mutilado | Sin plan (¿backlog F6?) |
| `Order::STATUS_REFUNDED` legacy | Constante + **16 usos en 6 ficheros** (re-medido 2026-08-14; eran ~10): dominio (`OrderRefundFlags`, `Order`, `Payment`), presentación Filament (tabla y relation manager) y, desde Fase 3, **la API** (`OrderResource`) | Doble semántica status vs `refunded_at` que todo lector debe conocer — y ahora también todo cliente de la API | Sin plan |
| FKs ausentes a propósito | `ticket_types.zone_id` y `order_items.parent_item_id` sin constraint (limitación ALTER de SQLite) | Integridad solo en app; huérfanos posibles ante bugs | Sin plan |
| `seasons.name` sin i18n | Sin cast ni `HasTranslations` (asumida etiqueta interna) | Inconsistencia con el resto del CMS | Sin plan |
| CSP laxa + HSTS pendientes | `'unsafe-inline'/'unsafe-eval'` (los exigen Alpine/Livewire); HSTS no emitido | En el primer despliegue real de JumpWeb | Condición: despliegue propio (`SEGURIDAD.md`) |
| Mantenimiento: sin bypass de staff con Bearer | `EnsureSiteAvailable` resuelve el bypass con el guard por defecto (sesión), porque corre antes del `auth:` de ruta. Con la SPA funciona (comparte sesión); con un token no | Un admin no podría hacer QA del sitio en mantenimiento desde la app nativa | Fase 3 · paso 3 (cuando existan tokens que emitir) — hoy la tabla está vacía |
| Rutas de API con `auth:` fuera del limitador base | Medido: Laravel ordena `AuthenticatesRequests` antes que `ThrottleRequests`, así que el 401 no consume `throttle:api`. Se conserva el estándar a propósito (`DECISIONES #24g`): invertirlo empeoraría los `throttle` de la web | Un anónimo puede repetir peticiones a un endpoint autenticado sin techo; el 401 es barato y no toca BD | Sin plan (fijado por `ApiEnvelopeTest`; lo que necesita techo de verdad es público y sí lo tiene) |
| Slugs públicos en español | `/mi-cuenta`, `/cumpleanos`… (`routes/web.php`), verificado vigente el 2026-08-14 | Instalaciones no hispanohablantes | ⚠️ **Ya no es «decisión pendiente»: se DECIDIÓ en Fase 1** (`DECISIONES #12.a`) — español ahora, configurables cuando exista un cliente que lo pida. Queda como limitación conocida, no como tarea sin dueño |
| ~~Wordmark de emails no data-driven~~ **RETIRADA en Fase 1** (2026-08-12), verificado el 2026-08-14 | El header de mail, la marca del panel, el título de puerta y el wordmark de los dos PDFs leen `business.name` de BD con fallback neutro | — | ✅ hecha |
| «1 reservas próximas»: el contador de próximas reservas no concuerda | `account.sidecart.upcoming_count` es `:count reservas próximas` **sin forma plural**, así que con una sola reserva el texto dice «1 reservas próximas». **Preexistente**: el bloque `.acct` lo lleva diciendo desde `#221`; el área de cliente lo reproduce **a propósito** —su criterio es la paridad— y por eso ahora se ve en dos sitios. Solo afecta al `sr-only` del cajón y a la línea del bloque | Cosmético y de accesibilidad: un lector de pantalla lee una concordancia incorrecta. Ningún dato es falso | Añadir la forma plural en los tres idiomas (`:count reserva próxima\|:count reservas próximas`) y leerla con `trans_choice` en el servidor / `tp` en el cajón, que **ya sabe pluralizar**. Es un cambio de CONTENIDO en tres idiomas, no de código: por eso no entró en el paso 3b |
| ~~La API no publica el DESGLOSE financiero del pedido~~ **CERRADA 2026-08-22** (`#120(t)`) | Los cuatro huecos que `AccountPageCaptureTest` enumeraba se publican: `pending_at_gate_lines`, `total_final_cents`, `pending_refund_cents` y `has_deposit`. El test de captura se borró, como pedía su propia aserción | — | ✅ hecha |
| ~~La WEB no limita los intentos de `current_password`~~ **CERRADA 2026-08-22** (`DECISIONES #120(s)`) | **Medido el 2026-08-22**: ninguna de las cuatro gestiones que reconfirman la contraseña tenía `throttle`. Las cuatro lo tienen ya, y **sin escribir una línea de limitador en la web**: el paso 6 pasó `UpdatePassword` y `LogoutOtherDevices` por `AccountCredentials`, el 7 el perfil por `AccountProfile` y el 8 el borrado por `AccountPrivacy` | — | ✅ hecha. ▶ **Y la lección va más allá de la ficha**: una defensa que se hereda al bajar la lógica al dominio es la prueba de que bajarla no era ceremonia. ⚠️ Lo que el paso 8 destapó al cerrarla es que el techo estaba puesto y **su aviso no se pintaba en ninguna parte** (fila de abajo) |
| El aviso del limitador iba a `_global` y **ninguna vista de cuenta lo pintaba** | **Medido el 2026-08-22** al escribir el paso 8: los cuatro componentes de «Mi cuenta» mandan el aviso del límite a la clave `_global` —para que no se lea como «esta contraseña está mal» bajo el input, que es la convención del login— y de las cuatro vistas solo `update-profile` tenía banner (`$errors->any()`). En las otras tres, agotar los cinco intentos dejaba el formulario **sin hacer nada y sin decir nada** | Es la familia de `#117`: un camino que «no falla y no hace nada». El titular legítimo no sabía si estaba bloqueado o si el botón se había roto | ✅ **cerrada en el paso 8**: nace `<x-ui.global-errors>` y lo usan las tres vistas que faltaban, con un caso por vista **verificado por mutación**. ⚠️ `login`/`register` conservan su bloque a mano **a propósito**: son la referencia de las dos paridades de árbol (`#112(f)`) |
| ⚠️ El aviso de «formulario pendiente» no cubre un pack con campos de evento y SIN post-form | **Medido el 2026-08-22** al auditar la retirada (`#120(u)`): `OrderItem::guestFormStatus()` devuelve `null` si el pack no declara `guest_fields`, así que ese pack no ofrece enlace al post-form. La página retirada pintaba las respuestas del evento en línea y cubría el hueco por accidente | **Ninguno hoy**: el cajón las enseña bajo demanda desde `#120(u)`, así que el titular las ve igual. Lo que queda sin cubrir es **editarlas**: sin post-form no hay dónde | Si alguna instalación configura ese caso y pide editarlas, la salida es que `guestFormStatus()` contemple los packs con solo campos de evento — ⚠️ **toca el aviso de «formulario pendiente» de toda la web**, así que no es un cambio local |
| ~~«Tus consentimientos» solo existía en `/mi-cuenta`~~ **CERRADA 2026-08-22** (`#120(t)`) | Se publicaron con `GET /me/consents` y los pinta la zona `PRIVACY` del cajón —sin la IP, que viaja solo en el export—. La página se retiró después, ya sin perder nada | — | ✅ hecha |
| ~~**Waiver · el PDF afirma MÁS de lo que el diseño garantiza**~~ **RETIRADA 2026-08-26** (`#180`: «Coincide (comprobación interna)» + nota de alcance —cadena SHA-256 sin secreto, sin sello ni tercero—, pie sin «inmutable», datos/IP/UA del puesto en las firmas de mostrador, y `WaiverChain` cruza la versión; en es/en/fr con paridad. ⏳ Quedan como precisión de doc PAN-3 (§9.6 «enlace» = firma→versión) y NUC-7 (`body_hash` no cubre `published_at`)) | Spec **§10.6**: «Verificada» y «fila inmutable» con un `sha256` sin secreto ni anclaje (una fila inventada verifica `true`, WAI-02, 2/2) · «`integrityOk()` (fila + versión + enlace)» donde el enlace es firma→versión, no `prev_hash` (PAN-3) · `body_hash` no cubre `published_at`/`version`/`locale` y el PDF imprime esa fecha como cubierta (NUC-7) · `WaiverChain` no cruza la versión (NUC-8) · en una firma de mostrador el PDF dice «los datos los declaró la persona» y muestra la IP/UA **del puesto** sin decirlo (WAI-07) | El día del litigio: el abogado contrario ataca lo que el documento dice de más, no lo que el diseño hace | Texto del PDF y de las etiquetas (`lang/*/waiver.php`, `waiver-proof.blade.php`): decir exactamente lo que se garantiza. Y `WaiverChain::verify()` cruzando `document_hash` con la versión |
| **`docs-check` no detecta marcadores de CONFLICTO en la doc** | Medido el 2026-08-26 con `docs/ESTADO.md` en `UU` (seis líneas `<<<<<<<`/`=======`/`>>>>>>>`): `bash scripts/docs-check.sh` → «✓ doc coherente». Ninguno de los 8 checks mira marcadores y no hay `pre-commit` (spec del waiver §10.7, DOC-5) | Con dos agentes sobre `main`, un `ESTADO.md` a medio fusionar pasa el gate y llega al remoto: dos repartos contradictorios en el fichero que todo agente lee primero | Un check 9 de dos líneas (`grep -l '^<<<<<<<\|^>>>>>>>' docs/*.md docs/**/*.md`) |
| **Waiver · `pending` viaja en la API pero el cajón no lo pinta** | `#183` (S-2): `WaiverStatus.pending` y `account-context.waiver.pending` dicen «aceptado en el alta, a la espera del correo verificado». El cajón no lo usa: una cuenta sin verificar no entra en Mi cuenta por web (`verified`), así que el aviso «fírmala» solo lo vería un cliente NATIVO que lea el contexto sin la puerta del correo | Un cliente nativo enseñaría «fírmala» a quien ya aceptó y solo tiene que verificar | Cuando exista el cliente nativo: si `pending`, decir «verifica tu correo para completar la firma» en vez de pedir la casilla |
| **Turnstile · dos ramas del montaje inalcanzables hoy** | `#183` (TURN-CLAVE-VACÍA, TURN-ESPERA-DEFINITIVA): `mountTurnstile()` espera hasta `waitMaxTries` a que haya clave y nodo, y con clave vacía definitiva o nodo que nunca llega se queda callado (sin token, el envío responde «no eres un robot» al vencer). Hoy no ocurre: la clave sale de `GET /config` antes de pintar el formulario | Un ajuste a medio guardar (clave pública sin secreto) dejaría el alta sin mensaje claro | Al tocar el anti-bot: mensaje explícito al agotar la espera, y validar en Ajustes que las dos claves van juntas |
| **Waiver · 29 hallazgos BAJOS de la revisión, sin verificar por tope** | La revisión de `#169` verificó 40 hallazgos con escépticos y dejó **29 bajas sin verificar** (spec **§10.9**, con ids): entre ellas `waiver.view` no basta sin `users.manage` (PAN-7), la puerta con `deleted_N@deleted.local` dice «registrado con waiver» (PAN-10), ~~TOCTOU publicar-vs-firmar fuera del lock (WAI-06)~~ (✅ `#183`: re-comprobado dentro del lock), `waiver:verify-chain --keep` en interno sin versión **deja publicada una v1 de prueba** (WAI-14), `zh_CN` sin claves del waiver (PAN-8), foco y tamaño del texto legal en el cajón (CAJ-6/7), y ocho desfases de doc (§4 sigue con `retención_hasta` y `consents`; `RGPD-06`/`SEC-09` no citan el waiver; `SEGURIDAD`, `GLOSARIO`, `MODELO-DATOS`) | Ninguna bloquea; se listan para que no se crean inexistentes | Al retomar el waiver: verificar las cinco primeras y corregir los desfases de doc en una pasada |

## Ya no es deuda (verificado)

- ✅ **`PackStayExtensionTest` bajo `--parallel`** (abierta 2026-09-07, **CERRADA 2026-09-08, `#447`**) — cuatro rojos en dos métodos, y la causa resultó ser **del arnés**: el helper `edit()` le pasaba al editor el testigo optimista del **PEDIDO** (`$order->updated_at`) cuando el editor lo compara contra el de la **RESERVA** (`$item->updated_at`). Dos filas distintas que coinciden **solo dentro del mismo segundo** —`updated_at` tiene precisión de segundo—, así que con la máquina libre pasaba siempre y bajo carga el `save()` del helper cruzaba el segundo y el editor respondía `stale_item_version` **con razón**. ▶ **Lo que la cerró fue el INSTRUMENTO**: en cuanto el aserto imprimió `$outcome->reason`, la cuarta aparición trajo el nombre. Reproducida con control (`travel(2)->seconds()` antes del `save()`: 3 rojos) y verificada arreglada con el mismo control a 2 s y a 90 s.
`prices.deposit_cents` (columna eliminada) · `event_packages` (absorbida por packs) ·
`contact_messages` (tabla+modelo+resource eliminados) · marcadores TODO/FIXME en `app/`
(0 reales: los matches son «TODO/TODOS» en español) · `ci.yml` (retirado a propósito,
`DECISIONES #9`).

## Verificado y SIN cambios (reconciliación 2026-08-14)

## ▶ ~~Alta · `Sidebar.vue` es el segundo objeto-dios~~ **RETIRADA 2026-08-22** (`DECISIONES #119`)

**Lo que era, medido** (2026-08-15, `#90`): **618 líneas de CÓDIGO** en su `<script>` y **las 11
llamadas a la API del cajón**. Los otros 18 componentes sumaban 236 entre todos y ninguno tocaba la
API — la violación de `CE-6` era **un fichero**, con los MISMOS métodos que el `Purchase.php` que se
estaba retirando.

**Lo que es hoy**, tras la reorganización que el owner pidió antes de meter el área de cliente:

    Sidebar.vue                    16 líneas · 0 llamadas a la API   ← enruta y expone
    sections/PurchaseSection.vue  438 líneas · 2 llamadas            ← el embudo
    stores/  (nueve)                                                 ← el estado, por dominio

- **La causa raíz sigue vigilada**: `SidebarComponentBudgetTest`, con techo general por componente,
  excepción declarada que solo encoge y —desde `#119`— una guarda de que **la raíz no vuelve a pintar
  pantallas** ni a hablar con la API.
- **Lo que QUEDA tiene ficha propia arriba** (las cinco secuencias transversales del embudo) y es
  deuda **sin intereses**: tocan solo la compra, así que su coste no crece con el área de cliente.

⚠️⚠️ **Y se corrige aquí una afirmación de esta misma ficha que era FALSA, porque es la que más daño
podía hacer.** Decía: «hay red para hacerlo: el manifiesto de DOM compara el árbol byte a byte, así
que refactorizar el `<script>` sin tocar la plantilla se verifica solo». **No es cierto**, y se midió
el 2026-08-22: `scripts/render-sidebar.mjs` **no importa la raíz** —su propio comentario dice por qué:
lee `window.Alpine` y el idioma del documento— y monta los ONCE componentes de paso con props
construidas por los módulos planos. El contrato de árbol prueba que esos no cambian; **no ejerce el
orquestador en absoluto**.
▶ **En un refactor del orquestador la única red es el NAVEGADOR.** Confiar en la frase de arriba es
exactamente lo que habría dejado pasar los tres fallos vivos que el refactor destapó (`#117`, `#118`,
la navegación de mes) y las dos regresiones propias que se cazaron en un navegador y no en la suite.
Receta del andamio, con sus trampas medidas: `VERIFICACION-E2E-CAJON.md` §5.bis y §5.quater.

## ▶ Baja · los colores en crudo que QUEDAN, todos porque cambiarlos mueve píxeles (2026-08-25, `DECISIONES #143`)

⚠️ **Esta ficha decía «seis colores crudos en el CSS del cajón», y ése era también su ámbito. Ya no.**
El barrido de la tanda A midió las **dos hojas enteras** —234 ocurrencias— y convirtió las **144** que
repetían un token ya existente. Lo que queda es lo que el owner dejó fuera **a propósito**, y no es
deuda de descuido: es la lista de decisiones de diseño que nadie ha tomado todavía.

**a) Blanco y negro puros — 39 ocurrencias.** No tienen token, y **darles uno los repinta**:
- el **blanco de papel** (polaroids, tarjetas de evento, la de invitación, el post-form, `.bd-card`)
  no es `--bg`, que es crema `#F4EFE3`. Pediría un `--paper` propio;
- el **blanco sobre acento** (`rgba(255,255,255,.72)` de los CTA) tampoco es `--on-brand`: ése sigue
  al acento y **sobre un acento claro es tinta oscura**. Convertirlo cambiaría esos textos en toda
  zona clara, que es justo lo que la promesa de «píxel idéntico» excluía;
- el **negro de `mask`/`mask-image`** (8) no es un color visible, es un recorte. **Se queda.**

**b) Nueve huérfanos con nombre**, casi todos del envoltorio de regalo (`.offw-*`, que ya tiene su
paleta local `--box`/`--ribbon`): `#F0B33F` ×3 escapado de `--ribbon-dk`, `#FFF7EC`,
`rgba(255,247,236,.95)`, `rgba(255,196,140,.55)`, `#FDFBF4` y `#1A1915`.

**c) ❗ Y uno que además es un DEFECTO de coherencia, no solo deuda de tema:**
`.addons-mini__badge` pinta `color: var(--ok)` sobre `background: rgba(34,197,94,.15)` — **un verde de
otra familia**. Sus dos hermanas (`--included`, `--free`) sí usan `--ok`/`--ok-bg`. Cambiar `--ok`
mueve el texto y deja el fondo quieto. ▶ Arreglarlo es **una línea**, cambia píxeles (los dos verdes
no son el mismo) y por eso está aquí y no hecho: es decisión de producto, no refactor.

- **Ninguno se toca en un refactor**: cambiar un color exige verificación visual (DoD §4).
- `SidebarTokenBudgetTest::MAX_RAW_COLOURS` (hoy **5**, era 6) es un **trinquete estricto** sobre el
  ámbito del cajón: bajarlo es parte del commit que tokenice cada uno.
- ✅ **Lo que ya NO es deuda**: un literal *nuevo* que repita un token existente ya no puede entrar
  —`RawColourIsNotATokenTest` lo caza, y compara por VALOR RGB, no por nombre de token—.
- **Sin plan de fase**: no bloquea nada. Candidato natural a la tanda B, con el segundo cliente
  delante, que es cuando se sabrá si un `--paper` hace falta de verdad.

## ▶ Media · la suite sigue sin auditar contra la FECHA (2026-08-16, `DECISIONES #97`)

Segunda mordida medida. El `pre-push` cayó con **1 fallo** a las **00:02 de Madrid** —el contenedor
marcaba 22:02 UTC del día anterior— y el reintento inmediato salió **verde**. Es el mismo patrón de
`#64`: un caso que compone su fixture con fechas relativas y cruza la medianoche entre el montaje y la
aserción.

⚠️ **No se identificó cuál**, y esa es la parte evitable: la salida del gate no se volcó a fichero y se
perdió al reintentar. **Procedimiento a partir de ahora: si el `pre-push` cae, `git push > log 2>&1`
antes de volver a intentarlo.**

El barrido sigue pendiente y ahora tiene dos incidencias detrás, no una. Candidatos: todo test que
siembre franjas con `now()->addDays(n)` y asevere sobre `today`/`tomorrow` sin congelar el reloj.

Estas filas se comprobaron una a una contra el árbol actual y siguen **exactamente** como estaban; no
se tocan para no ensuciar el diff, pero constan como verificadas:

- **Media**: suite ciega a carreras InnoDB (`phpunit.xml` sigue en SQLite `:memory:`) · composer global
  `View::composer('*')` · 2FA inexistente (solo el comentario en `Filament/Pages/Settings.php`) · los
  dos huecos de test declarados —`lockZoneDaySlots` **no aparece en ningún test**, y ninguno de los 10
  ficheros que mencionan `redsys_secret_key` asevera que el formulario NO lo cargue— · «precio desde»
  sin filtro de tarifas activas · cacheabilidad del catálogo sin decidir · **las dos normalizaciones
  del mismo teléfono** (`MaintenanceSettings` con `\s+` y `AppServiceProvider` con `[^0-9+]`) · las dos
  formas de emparejar respuestas del evento (`ReservationSlip::eventDataRows` sigue con la suya) · N+1
  de precio por complemento · un solo driver de pasarela · post-form web con *binding* implícito
  (`/reserva/{reservation}/datos-invitados`) · el formato de importe quemado (1 `number_format` en
  `AddonResolver`; los **17** de `purchase.blade.php` se fueron con él en `#112`, y su equivalente en
  el cajón es `money.js`, que sí tiene paridad —`SidebarMoneyParityTest`—) · las fechas del cajón formateadas por dos motores.
- **Baja**: los cinco huecos de test (throttle de Redsys `throttle:120,1` sin un solo assert;
  `after_commit`; frontera UTC↔Madrid; gate de `tableExists`; `hasPositivePrice`) · `slots.seats_taken`
  **sigue muerta** (solo el cast del modelo y el comentario que advierte de ella; ningún escritor) ·
  `rooms` sin uso real (`Room::` aparece únicamente en el morphMap del `AppServiceProvider`) ·
  `seasons.name` sin `HasTranslations` · FKs ausentes · CSP laxa + HSTS · bypass de staff con Bearer ·
  rutas de API con `auth:` fuera del limitador base.

---

## ▶ ❗❗ ALTA · el GATE DOCUMENTAL puede dar FALSO VERDE en la shell de un agente (2026-08-27, `#193`)

> **No es una hipótesis: pasó, y estuvo pasando desde el 2026-08-26.**

| Qué (medido) | Dónde muerde | La retira |
|---|---|---|
| **`docs-check.sh` daba ✓ en la shell del agente y ✗ con el `grep` real.** El check 4 (citas `fichero.php:línea`, `CONVENCIONES §4`) no cazaba **37 citas en 2 documentos** que llevaban en `origin/main` desde el 26/08 —entraron con el waiver (`4e2e1c4`)—. La causa: en la shell de trabajo del agente **`grep` es una FUNCIÓN de bash** interpuesta por el harness, no `/usr/bin/grep`. `declare -F grep` lo confirma; y **no se exporta**, así que el `pre-push` sí usa el binario y por eso el gate acabó mordiendo en un push y no en el anterior | **Todo agente que corra `bash scripts/docs-check.sh` a mano y lea el ✓ como garantía.** Es el peor modo de fallo de un gate: no falla, **certifica**. Y `docs-check` es la red de la que este repo depende para que la doc no mienta al siguiente agente | ✅ **Las 37 citas, CONVERTIDAS a símbolo** (`#193`, carril C, con permiso del owner por ser ficheros de otro carril): `CustomerRegistrar.php:109-114` → `CustomerRegistrar::register()` (ejemplo), y así las 37, con el símbolo **resuelto leyendo el código**, no inventado. ⚠️ Esa muestra lleva «(ejemplo)» porque **el propio check la caza**: es el escape que el script declara, y la primera vez que hace falta de verdad. ⏳ **Lo que NO se ha arreglado y es lo importante: que el gate pueda mentir.** Hasta que se arregle, la regla es: **`docs-check` solo cuenta corrido con el binario real** — `env -i PATH=/usr/bin:/bin bash scripts/docs-check.sh` — o directamente dejando que lo corra el `pre-push` |

▶ **La lección, que es la de siempre con otra cara**: este repo lleva cinco instrumentos que resultaron ciegos (`#143` §8, la sonda de color y el diff de capturas de `#192`, y los dos de `#193`). **Éste es el primero que era el propio GATE.** Un instrumento que dice «todo bien» sin mirar es peor que no tenerlo: los otros cuatro dieron un inventario incompleto; éste dio permiso.

---

## ▶ ~~ALTA · el SENTINEL de los dos CTAs de compra es el mismo elemento que el CTA del hero~~ **RETIRADA 2026-08-27** (`#195`)

| Qué (medido) | Dónde muerde | La retira |
|---|---|---|
| **`.hero__stage-bottom` hace DOS trabajos a la vez y nadie lo declaró así**: es donde vive el CTA «prime» del hero **y** el sentinel que observan `navCtaReveal` (el botón «Comprar entradas» del nav en escritorio) y `mobileBookBar` (la barra flotante de reserva en móvil), los dos con `IntersectionObserver` en `resources/js/app.js`. Funciona hoy **por casualidad**: el sitio donde acaba el CTA del hero coincide con el sitio donde queremos que aparezcan los otros dos | **El camino de compra.** En cuanto el hero cambie de estructura —y el paso 3 de la tanda 2b lo pone en `position: sticky`— el sentinel deja de abandonar el viewport cuando alguien lo pensó: **cambia el momento en que se le ofrece comprar al visitante**, y no lo caza ningún test | **Decisión del owner, con tres opciones y su coste en `specs/tema-por-instalacion.md` §11.5.** Recomendada la **B**: un sentinel propio, invisible, al final del `<header>` y fuera del sticky. Es igual de barata que las otras y la única que deja el sentinel siendo *un sentinel* y no *el botón*. ⏳ **Bloquea el paso 3 de la 2b** |

---

## ▶ Baja · un comentario del contenedor promete algo que el binding no hace (2026-08-29, `DECISIONES #243`)

| Sev. | Qué | Por qué | Impacto | Salida |
|---|---|---|---|---|
| **Baja** | **`OperatingCalendar` se ata con `bind`, pero su comentario dice que «se comparte por petición»** | `bind` construye una instancia NUEVA en cada resolución, así que la memoria de `OperatingSchedule` nace vacía cada vez; y la clase **no usa `once()`** (medido: 0 apariciones). El memo solo funciona dentro de la vida de quien la recibe inyectada | Ninguno funcional: es una consulta de horarios repetida, no un dato incorrecto. Lo que engaña es el **comentario**, que el siguiente agente leerá como garantía | Cambiarlo a `scoped` (una línea, como se hizo con `GuestAgeMixReader`) **o** corregir el comentario. No se tocó en `#243` porque cambiar el ciclo de vida de un servicio que consumen la landing, el SEO y el hero es una conducta nueva, y esa sesión no iba de eso |

## ▶ Media/Baja · las TRES fuentes del cliente que no coinciden en su propio CTA (2026-08-28, `DECISIONES #213`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **Su sistema de color se contradice con su propia implementación** | El CTA fijo de `Landing PJP Modos` se pinta de **Amarillo Aviso** con el menú abierto. Pero `Colores de Marca` §02 dice de ese amarillo: «Resalte tipo marcador y anillo de foco. **Nunca fondo de sección ni botón**». Y §14 (orden de página) dice que la cabecera fija domina con **`cta.fill`** (Naranja Salto). Tres fuentes suyas, tres respuestas | En que cualquiera que implemente desde su canvas elegirá una de las tres **sin saber que hay tres**. Aquí se eligió la implementación por decisión suya (`#213`), pero la próxima pieza volverá a chocar | Es del owner: decidir cuál de las tres manda y corregir las otras dos en su canvas. Mientras, queda escrito en `specs/armazon-y-menu.md` §7.2 |
| **Baja** | **El rol de ACCIÓN se queda sin demostración VISUAL de su independencia de superficie** | `.cta-med` era el único botón de acción que entraba en un ámbito de tinta, y en `#213` salió del rol. El mecanismo sigue entero —`SurfaceScopeTest` lo fija y el bloque B del guion aún lo enseña— pero **la mitad A ya no se puede ver en el producto** | En nada funcional. Cuesta que el owner no pueda comprobar con el ojo la mitad del quinto mecanismo | Volverá sola el día que un botón de acción viva sobre una sección oscura — que es justo lo que traerá la tanda 3 (el cierre del mockup es un bloque de tinta con un botón dentro) |

---

## ▶ Media/Baja · lo que destapó el OJO del owner sobre la marca (2026-08-28, `DECISIONES #211`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **El icono del PRODUCTO lleva quemado el naranja del PRIMER cliente** | `public/favicon.svg` es un cuadrado `#FF5B22` con una «J». Es correcto que el producto tenga icono propio; **el color no**: `#FF5B22` es el acento histórico de la primera instalación, la misma fuga que `#139` cerró en el CSS | Toda instalación **sin** icono propio enseña en la pestaña el naranja de otro cliente. Y ninguna guarda de color lo mira: es un fichero, no CSS | Decidir el color del PRODUCTO (¿neutro? ¿la marca de JumpWeb?) y repintar los tres ficheros. No es urgente porque una instalación puede taparlo con su SVG desde `#211` |
| **Baja** | **`public/favicon.ico` está versionado y pesa 0 bytes** | Medido: `wc -c` = 0, y **ningún `<link>` lo referencia**. El navegador lo pide solo por convención | Poco: servir 0 bytes y no tenerlo dan el mismo resultado visible. Pero es un fichero versionado que no significa nada, y el siguiente que lo vea perderá el rato | Borrarlo, o ponerle contenido. Se dejó porque retirar ficheros tiene su protocolo (`CONVENCIONES §3.quater`) y esto no lo pedía nadie |
| **Media** | **Dos zonas de PRUEBA salen en la portada pública** | `zones` id 5 y 6, nombre «Cap», `slug` `cap`/`cap2`, `is_active = true`, color `#FF5B22`. Son sondas de aforo. **Salen como tarjetas de zona en `/` y `/entradas`** de esta máquina | Confunde la pasada de navegador del owner —parecen zonas reales con el color del cliente anterior— y falsea cualquier medida de color sobre la portada | Es dato de ESTA base de datos, no del producto: desactivarlas o borrarlas. **No se ha tocado**: son datos del owner y borrar datos no se hace por iniciativa propia |
| **Baja** | **El logotipo del cliente no se pudo reconstruir en vectorial** | Su lockup son 2 líneas en Lilita One con **6 capas por palabra** (3 contornos, escalera de 6 sombras, relleno por letra, 2 degradados recortados al texto) + una figura que **no es silueta plana**. La reconstrucción con contornos reales y geometría medida llegó a **29 % de píxeles distintos** | En que la marca sigue siendo el nombre en la fuente de rótulo | `[DECIDIDO owner, 2026-08-28]`: **lo exporta él desde Claude Design**. El hueco ya existe (`#206`). ▶ **Y falta un hueco para la variante sobre TINTA**: con el menú abierto el logo cae sobre superficie oscura, y una imagen no se adapta sola como sí hace el texto de hoy |

---

## ▶ Baja/Media · lo que el QUINTO mecanismo midió y NO arregló (2026-08-28, `DECISIONES #209`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Baja** | **`[data-surface="ink"] .cta-prime__ico` y `.cta-prime__s` pueden estar MUERTAS** | Las creó la tanda 2b para cuando el CTA grande vivía DENTRO del hero. **El hero perdió su CTA en `#195`** y `.cta-prime` hoy solo aparece en la barra de compra de móvil, que no declara superficie | En nada visible. Pero si un día vuelve un `.cta-prime` a una superficie de tinta **con** color de acción, esas dos reglas pintarían gris oscuro sobre el naranja (≈ 2,0 de contraste) | No se retiran por `grep`: `CONVENCIONES §3.quater` (clasificar por sujeto y **mutar, no leer**). Va con la auditoría de las ~50 reglas de la ficha de abajo |
| **Media** | **El cajón no refresca el contexto de cuenta al volver a la pestaña: tras verificar el correo en OTRA pestaña, la original sigue diciendo «tienes pendiente la exención»** | Lo vivió el owner el día del lanzamiento (`#326`, adenda): el servidor ya daba `signed=true · pending=false` (`WaiverStatus::for` + `CustomerAccountContext`, medido), pero el cajón cargó su contexto ANTES de la verificación y no lo releyó | El cliente recién verificado cree que le falta firmar, y el aviso lleva a un formulario que no tiene nada que firmar | Releer `/me/account-context` en `visibilitychange`/`focus` (o al cerrar y abrir el cajón). Es el SPA, aparcado `[DECIDIDO owner, 2026-09-01]` |
| **Baja** | **Un color de acción puede no alcanzar AA y el panel no avisa** | Aritmética, no defecto: con un relleno de luminancia ≈ 0,19, tinta y blanco **empatan en 4,31**. `onAction()` devuelve el mejor de los dos y calla | El operador elige un color en el panel, ve el botón bonito y **nunca sabe** que su rótulo no cumple. Afecta al control con más peso de la web | `[PENDIENTE: owner]`, spec §15.8: no hacer nada · decirlo en la ayuda del campo · un aviso vivo bajo el campo con el número medido |
| **Baja** | **El PRESS del rol de acción no existe** | El sistema del 2.º cliente declara `press #B85615` (×0,76 de su relleno) y el producto **no tiene estado de pulsado en color**: sus `:active` solo escalan | En nada hoy. El cliente no puede reproducir su tercer estado | Añadirlo cambia la conducta del producto en un estado que hoy no existe: es **diseño**, no mecanismo. Del owner |

---

## ▶ Media · ~50 reglas de CSS que PARECEN muertas y un `grep` no puede confirmarlo (2026-08-27, `#196`)

| Qué (medido) | Dónde muerde | La retira |
|---|---|---|
| Al clasificar las sombras salieron **14 reglas cuyo selector raíz no aparece en `resources/`** —`.invite-card`, `.modal__panel`, `.plan-select__trigger`, `.events__photo`, `.events__ticket`…—. ⚠️ **Pero eso NO prueba que estén muertas**: el cajón construye sus clases por concatenación en Vue, así que `cal__day--normal` **tampoco aparece** ni en `resources/` ni en el bundle minificado **y está viva** (sale en `tests/Fixtures/sidebar-dom-manifest.json`) | Peso muerto en las dos hojas que sirve todo visitante, y ruido para el siguiente que audite. Pero **borrar por `grep` sería exactamente el error que este repo persigue** | Auditoría propia con el rigor de `CONVENCIONES §3.quater`: clasificar por SUJETO y **mutar, no leer**. El instrumento tiene que ver las clases que se construyen dinámicamente — el manifiesto DOM del cajón es una fuente, y las páginas servidas por HTTP otra. ⚠️ En `#196` se retiraron **solo** las dos de `.cta-prime--onvideo`, porque su único uso lo había retirado yo mismo media hora antes: eso sí es certeza, no inferencia |

---

## ▶ Baja · lo que la tanda 2b dejó anotado (2026-08-27, `#194`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Baja** | **Tres excepciones del anillo de foco, y dos ya podrían caer** | `.skip-link:focus-visible` y `.hero__chip:focus-visible` pintan el anillo claro **a mano** (`var(--bg)`) porque su fondo oscuro no declaraba superficie. **El del chip ya no aplica: el hero declara `ink` desde `#194`**, así que `var(--focus-color)` ya rendiría claro solo | Un cliente que cambie `--focus-color` mueve 8 anillos y estos dos no | Se dejó fuera del paso 2 **a propósito**, para no mezclar un cambio de foco con la conversión de superficie. Entra con el paso 3. `FOCUS_EXCEPTIONS` **solo encoge** |
| **Baja** | **`.cta-prime__ico` tiene un literal blanco que ningún test caza** | `background: rgba(255, 255, 255, 0.10)`. `RawColourIsNotATokenTest` no lo marca porque **no repite el valor de ningún token existente** — es blanco puro, y el producto no tiene un token blanco puro salvo `--sheet`, que significa otra cosa (la hoja de una tarjeta) | Un cliente cambia su marca y la caja del icono del CTA sigue aclarándose con blanco puro. Dentro del hero **sí** obedece, porque ahí lo pinta una regla de ámbito con `--paper-fg` | Decidir qué token le toca. No es `--sheet` (rol distinto) ni `--bg` (crema). Puede que haga falta declarar que «aclarar sobre el propio botón» es un rol y darle nombre |
| **Baja** | **El sentinel y el CTA del hero eran EL MISMO elemento** ✅ **RETIRADA 2026-08-27** (`#195`) | Al decidir el owner que el hero no lleva CTA, `.hero__stage-bottom` desaparece y el sentinel se da aparte: `.hero__sentinel`, vacío, fuera del `sticky`, con un solo trabajo | — | ✅ hecha. ▶ La lección se queda: **que funcionara era una coincidencia** —el sitio donde acababa el botón coincidía con el sitio donde queríamos que aparecieran los otros dos— y una coincidencia sostiene hasta que algo se mueve, sin avisar |
| **Baja** | **El titular del hero sigue con `--onvideo`, y el nombre miente** | De las 10 reglas `--onvideo` que quedan, ninguna pinta color: son `font-size` (×5), `box-shadow` (×2), `text-shadow`, `opacity` y `transform`. Es decir, el modificador ya solo significa **«tamaño del hero» y «sombra sobre oscuro»**, no «sobre vídeo» | En nada hoy. Cuesta lectura: quien lo vea creerá que aún invierte colores | Renombrar toca la especificidad de `.hero__title.hero__title--onvideo`, que existe para ganarle a `landing.css`. Va con el paso 3, que toca el titular de todos modos |

---

## ▶ Baja/Media · lo que la tanda 2a del TEMA midió y NO arregló (2026-08-27, `DECISIONES #193`)

> Todo lo de aquí salió de instrumentos con guarda, no de leer. `specs/tema-por-instalacion.md` §10.

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| ~~**Media** · **No hay escala de ELEVACIÓN**~~ **RETIRADA 2026-08-27** (`#196`: no era una escala, eran **TRES ROLES** — `lift`/`float`/`modal`; 19 sombras se retiran porque una tarjeta quieta no está elevada, y de 53 formas propias quedan 6, todas justificadas) | 68 `box-shadow` → **55 de elevación · 9 anillos · 4 `none` · 0 duras**. Solo 8 formas se repiten ≥2 veces. Probadas todas las escalas de 3 a 6 escalones, la mejor (`18·28·38·48·60·80`) **mueve 35 de 55 y 125 px de blur** | Un cliente que redefina su tema **no mueve ninguna de las 55 sombras**. El color sí le obedece (60 de 68 ya van por `var()`); la forma, no | **Tanda 2b** del tema (el hero): la escala se decide cuando el armazón pida sombras concretas, no antes. `[DECIDIDO owner]` |
| **Media** | **El MOVIMIENTO no tiene escala** | **237** declaraciones `transition`/`animation` con **48 duraciones distintas** y **20 curvas**; el sistema del cliente declara **7 y 4**. `200ms` sola tiene 110 usos y `ease` 216. Existen ya 4 tokens (`--dur-collapse`, `--dur-fade`, `--ease-panel`, `--ease-bounce`) que cubren una parte mínima | Un cliente no puede dar a su web su propio tempo. Y 48 duraciones no son una decisión: son 48 decisiones sueltas | **Tanda 2d** del tema. ⚠️ No mueve píxeles, mueve TIEMPO: **no se revisa con una captura, se revisa interactuando** |
| **Baja** | **16 no-ops `var(--X, var(--X))`** | Un fallback de `var()` a **sí misma** no puede dispararse nunca: si `--X` no está definida, el fallback tampoco. Medido en `site.css`: 5 × `--bg`, 4 × `--fg`, 3 × `--bg-soft`, 2 × `--bg-card`, 1 × `--line`, 1 × `--fg-mute`. Restos del barrido de `#143` | En nada hoy: es ruido que hace parecer que una regla tiene un fallback pensado. Cuesta lectura, no píxeles | Un `replace` de dos líneas, cero riesgo y cero píxeles. **No se hizo en la 2a a propósito**: ampliar el alcance en silencio es lo que esta casa prohíbe |
| **Baja** | **`.cal__dot` está entre dos familias** | La muestra de color de la leyenda del calendario: lado 12 px, radio 4 px → ratio **3,00**, fuera de la ley del motivo cuadrado (`lado/4`, tolerancia 3,2–5,4). Está en la lista de dibujo de `ShapeScaleTest` con su porqué | En nada. Es una excepción **justificada**, no un olvido | Tokenizarlo a `--r-xs` lo movería **+1 px**. Es decisión de producto, no refactor: entra si el owner lo quiere en la pasada de navegador |
| **Baja** | **Tres excepciones del anillo de foco, y desaparecen solas** | `.skip-link:focus-visible` y `.hero__chip:focus-visible` pintan el anillo claro **a mano** (`var(--bg)`) porque su fondo oscuro **aún no declara superficie**; `.gf-fiche__head` usa un anillo interior en el acento de zona | Un cliente que cambie `--focus-color` mueve 8 anillos y estos dos no | **Tanda 2b**: cuando el hero declare `[data-surface="ink"]`, `--fg` ya vale claro ahí y las dos pasan a `var(--focus-color)` sin más. La lista de `FOCUS_EXCEPTIONS` **solo encoge** |
| **Baja** | **`--attn` y `--zone-2` valen lo mismo** | Los dos `#FFE14A`. No es un error —el amarillo de aviso y el acento secundario coinciden en la marca por defecto—, pero hace que un token semántico y uno de marca sean **indistinguibles** hasta que el cliente cambie uno | Cualquier sitio que use los dos juntos: se ven como uno. Cazado al diseñar la tira del pie, que por eso **no** usa semánticos | Decisión de producto: o se separan los valores por defecto, o se documenta que coinciden. Hoy solo está documentado en el CSS de la tira |
| **Media** | **El cajón lateral de móvil (`.mob-menu`) está APAGADO, no retirado** | Desde `#221` el menú a pantalla completa manda en los doce anchos, así que el cajón no lo enseña ningún ancho. Se apaga con una regla y su marcado se sigue sirviendo: **~200 líneas de CSS y un bloque de blade que nadie ve** | En peso y en lectura: el siguiente que abra `nav.blade.php` encuentra dos navegaciones y no sabe cuál manda. Cero en runtime visible | Retirarlo con el protocolo de `CONVENCIONES §3.quater` — clasificar sus tests por SUJETO antes de borrar nada. ⚠️ `armazon-y-menu.md` §1.3: **ningún test lo cubre hoy**, así que la red hay que construirla o aceptar que se va sin ella |
| **Media** | **`client-icon-512-maskable.png` no lo lee NADIE** | El paquete del 2.º cliente entrega un icono *maskable* y el hueco lo acepta (`#216`), pero **un maskable solo lo lee un manifiesto de aplicación web** y este producto no sirve ninguno. Se declara con `<link rel="icon">` y Android lo ignora | En que un cliente lo entrega, se copia al servidor y **no hace nada** — y nada avisa. Hoy está dicho en el componente y aquí, que es todo lo que se puede hacer sin manifiesto | O se sirve un `manifest.webmanifest` (y entonces el maskable entra, con `purpose: "maskable"`), o se retira el hueco. Lo primero es una decisión de producto: un manifiesto convierte la web en instalable |
| **Baja** | **El logotipo en línea son ~64 KB de marcado en las DOCE vistas** | `#254` lo incrusta para poder animarlo (`[DECIDIDO owner]`, con el número delante). Son **~64 KB sin comprimir y ~15 con gzip**, y se pagan en todas las vistas aunque solo la portada tenga coreografía. Para comparar: todo el JS de la landing son 24,5 KiB | En el peso del HTML de cada página. No hay medida de regresión: **ninguna guarda presupuesta el tamaño del HTML**, así que si el paquete de un cliente trae un SVG de 300 KB nadie se entera | Dos salidas, las dos de producto: **(a)** pedir el logotipo en dos piezas (dibujo sin la silueta + silueta) y volver a `<img>`, que pesa casi nada pero depende de un envío del diseñador; **(b)** un techo de tamaño en `InlineSvg` que caiga al `<img>` por encima de N KB. La (b) es media hora y quita el peor caso |
| **Baja** | **El punto estático del cierre NO cabe por debajo de ~700 px de ventana** | `#253` bajó lo que hacía falta —el pie perdió el selector de idioma y la tarjeta dejó de reservar en reposo una tira de juego que aún no existe— y con eso la composición **cabe en 8 de las 9 ventanas medidas**, desde 1920×1080 hasta 1280×720 y 390×844. La que falta es **390×667** (un iPhone SE): necesita 798 px y tiene 667 | En que en esa pantalla el owner sigue sin tener «el punto donde se ve todo junto»: el pie arranca detrás de la tarjeta. No es un fallo, es aritmética | Sin bajar contenido no se arregla: 423 de tarjeta + 365 de pie ya son casi todo. Las salidas son de PRODUCTO —menos enlaces en el pie, o un CTA en vez de dos en el cierre— y se deciden mirando, no maquetando. ▶ **`#264` le devolvió 30 px sin quitar nada**: el bloque legal pasó a tira y el pie mide **335**. Sigue sin caber (768 necesarios contra 667), pero el hueco bajó de 131 a **101** |
| **Media** | **La portada CRECE mientras se lee: 55 imágenes y ninguna declara su proporción** | Medido el 2026-08-29 al construir el imán (`#252`): el alto del documento pasa de **11.413 a 11.447 px a 1280** en la primera pasada, y a 390 **oscila entre 15.022 y 15.075 aunque se recorra cuatro veces**. La causa está contada: **55 imágenes, 51 de ellas `lazy`, y CERO declaran alto o `aspect-ratio`**, así que cada una reflowea al cargar | En dos sitios. **(1) El visitante**: el contenido salta bajo el dedo mientras lee — es CLS de manual. **(2) Cualquier medida en píxeles sobre la portada nace caducada**: dos sondas de esta sesión dieron falsos negativos por eso, y la única salida fue aseverar el ESTADO (`--cierre-q`, `--hero-p`) en vez de la posición | Declarar `width`/`height` o `aspect-ratio` en las imágenes de la landing. La proporción la sabe el servidor —las sirve él—, así que es dato, no maquetación. ⚠️ Antes de tocar, medir cuántas son de CMS y cuántas fijas: no es una regla de CSS, es un cambio en cómo se sirven |
| **Baja** | **La QUINTA contradicción del 2.º cliente: el CANTO de los CTA del cierre** | Su escala de forma declara **seis escalones — 0 · 6 · 10 · 16 · 24 · 999 — y 14 no está en ella**; su `client.css` lo traduce a `--r-btn: 10px` («el control y el botón comparten canto»). Pero los dos botones del hero del cierre de `Landing PJP Modos` llevan `border-radius:14px`. **Medido en su propio artboard: 12 de sus 19 botones usan 10 y solo 3 usan 14** — los del cierre. Van cinco: el color de su CTA fijo (`#213`), su anillo de foco (`#206`), la sombra de su mobiliario (`#217`), el color en el hover (`#222`) y ésta | En nada roto: **manda su escala declarada**, así que sale 10. Es la única de las 28 medidas del bloque que no coincide con su artboard (`#250`) | Es del owner. Si quiere 14, **es una línea en su `client.css`** (`--r-btn`), no código del producto — pero entonces 14 tendría que entrar en su escala, o quedan dos cantos de botón conviviendo |
| **Baja** | **La CUARTA contradicción del 2.º cliente: el COLOR en el hover** | Su sistema de movimiento dice «el color identifica; si cambia, deja de identificar. **Solo opacidad y masa**» y, del hover: «el color no cambia nunca» — el suyo desplaza 3 px y acorta la sombra dura. **Nuestra web anima color en 129 declaraciones**, y el hover del CTA de comprar pasa a su cian. Van cuatro: el color de su CTA fijo (`#213`), su anillo de foco (`#206`), la sombra de su mobiliario (`#217`) y ésta | En nada roto: `[DECIDIDO owner, 2026-08-28]` **gana lo que él ya validó**, así que el hover sigue cambiando de color. Pero su sistema de movimiento no se cumple entero | Preguntárselo si alguna vez quiere el hover «pegatina» de su artboard —desplazar y acortar la sombra, sin tocar el color—. Es un cambio de una regla por botón, no de mecanismo: la escala de la 2d ya está |
| **Baja** | **La TERCERA contradicción del 2.º cliente: la sombra de su mobiliario** | Su mockup pinta el racimo de la cabecera con una sombra **difusa** (`0 10px 28px rgba(16,20,24,.24)`); su propio hallazgo `M-05` dice que las difusas son **solo para modal**, y su `client.css` declara `--shadow-float: 5px 5px 0` (dura). Van tres: el color de su CTA fijo (`#213`) y su anillo de foco (`#206`) | En nada roto: `#216` sigue **el sistema** —la sombra entra por rol, así que el paquete decide— y sale la dura. Pero la cabecera no se ve como su artboard | Preguntárselo con las tres fuentes delante, como se hizo con el color del CTA. Si quiere la difusa, es **una línea** en su `client.css`, no código del producto |

---

## ▶ Media · el ARMAZÓN del panel no cumple el mínimo táctil (2026-08-28, `DECISIONES #240`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **La barra superior y el menú del panel están por debajo de 44 px** | Medido en iPad horizontal (1080×810) mientras se rediseñaba «Crear pedido»: **11–12 controles** del ARMAZÓN quedan bajo el mínimo táctil en toda pantalla del panel — el buscador **222×36**, el botón «Crear pedido» de la barra **127×32**, los dos iconos de la derecha **36×36** y **32×32**, y los ítems del menú lateral **273×40**. La página en sí ya está a **0** | En cualquier pantalla del panel usada con el dedo, que desde `#232` (la puerta) y `#240` (crear pedido) ya son dos. No lo arregla ninguna de las dos tandas: las dos acotaron su CSS a **su** pantalla a propósito | Una tanda propia, porque toca a TODAS: el CSS iría sin acotar en `theme.css`. ⚠️ **Antes hay que decidirlo con el owner**: agrandar la barra del panel cambia el aspecto en ordenador, que es donde se usa el resto |

---

## ▶ Baja/Media · lo que el SET DE ICONOS dejó anotado (2026-08-29, `DECISIONES #257`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| ~~**Baja**~~ | ~~**`chevron-down` no lo usa NADIE**~~ **RETIRADA en `#258`** | Se redibujó con la anatomía del set (trazo 3) y **se cableó en sus dos sitios**: el selector de idioma y el acordeón de invitados, que hasta entonces llevaban cada uno su propio chevron a 1,6 y 2,2 | — | — |
| **Media** | **DOS iconos siguen hablando el idioma anterior** (eran tres; `#258` resolvió `chevron-down`) | `devices` y `cookie` son línea fina (`fill="none"`, trazo 1.7–1.8) en un set que ahora es masa sobre rejilla 24. **El artboard del cliente no los dibuja.** ⚠️ Y no valen los tres que sí se dibujaron en `#258`: un chevron o un ojo son glifos MECÁNICOS que la anatomía determina casi entera; un `devices` —una pantalla y un portátil compuestos— es una decisión de dibujo | En la cookie banner y en la zona «Sesiones» del cajón, que son las dos superficies donde se ven al lado de los nuevos. `IconSetAnatomyTest` los declara en `TRAZO_FINO_HEREDADO`, y esa lista **solo encoge** | Pedírselos al diseñador con la anatomía delante (rejilla 24, masa 3, `currentColor`). Son dos dibujos |
| **Baja** | **El PANEL sigue en otro idioma entero** | Medido en `#258`: **13 `<svg>` propios** en las vistas de Filament (`filament/orders/**`, widgets) **más los Heroicons que Filament trae de serie** en cada acción, columna y navegación. Nada de eso es la rejilla de 24 del set | En nada roto: es herramienta de operador, no superficie de marca, y el owner lo usa en ordenador. Pero la puerta y «Crear pedido» ya son pantallas de tablet con su propio cuidado (`#232`, `#240`), así que la frontera se irá borrando | Decisión de producto antes que trabajo: o el panel adopta el set (y entonces hay que decidir qué pasa con los Heroicons de Filament, que no se pueden sustituir uno a uno sin un mapa), o se declara explícitamente que el panel tiene su propio idioma y se deja de comparar |
| **Media** | **Los 19 iconos de PARQUE no tienen dónde aterrizar** | `[DECIDIDO owner]`: los 5 «Del parque» (`saltador` `cama` `canasta` `bote` `altura`) y las **14 zonas** (rejilla **64**, masa 8) son del cliente, no del producto multi-sector. Pero **no existe mecanismo** para que una instalación sustituya el DIBUJO de un icono: `client.css` alcanza al color, la forma y lo que viaja por CSS, y los `<x-icons.*>` llevan el SVG en línea | En que la landing no puede ser 1:1 con el artboard en las piezas de zona, y en que hoy **ninguna pantalla las pinta** — así que no urge | El **séptimo mecanismo del tema**: sustitución de dibujos por instalación. Cuando exista, los 19 salen del artboard con el mismo extractor de `#257`, en minutos |
| **Baja** | **Ninguna guarda presupuesta cuántos iconos SE SIRVEN por página** | El set pasó de 26 a 57 componentes. Blade solo renderiza los que se usan, así que el coste en runtime es cero — pero **nada lo comprueba**, y un componente nuevo con `@include` en el layout se lo cobraría a las doce vistas sin que nadie se entere | En el peso del HTML, que **no tiene techo aseverado** en ninguna vista (es la misma ficha que el logotipo en línea de `#254`) | Un presupuesto de tamaño del HTML por vista. Cubriría de una vez esta ficha y la del logotipo |

---

## ▶ Media/Baja · lo que el INTERRUPTOR `6d` dejó anotado (2026-08-29, `DECISIONES #262`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **Un token en `em` se re-resuelve en cada elemento, y nada lo vigila** | Una custom property se sustituye como TEXTO: el `em` lo resuelve **el elemento que la USA**, contra su propio `font-size`. `--sw-u: 0.62em` valía en el rótulo del interruptor **un tercio** de lo que vale en la pista, porque el rótulo se cambia su cuerpo — el sangrado salió a **1,84 en vez de 5,6**. ⚠️ **No falla nada: solo queda mal puesto**, y no lo ve `RawColourIsNotATokenTest`, ni `ShapeScaleTest`, ni una captura | En cualquier pieza cuyo sistema de medidas viva en un token en `em` y tenga un descendiente que cambie su `font-size`. Hoy es UNA (`.hero__switch`), pero el patrón «una unidad y todo en `calc()`» es el que este carril usa desde `#261` | Dos caminos: (a) **`@property { syntax: '<length>' }`** sobre el token, que lo computa a longitud UNA vez y lo hace inmune — hoy **no se usa `@property` en ningún sitio del producto**, así que sería estrenar mecanismo; (b) una guarda que prohíba `var(--token-en-em)` en un selector que también declare `font-size`. Ninguna es urgente con una sola pieza |
| **Baja** | **El rótulo del interruptor cae por debajo del suelo tipográfico del cliente en pantallas pequeñas** | Su contrato dice «Bungee solo en mayúsculas y **nunca por debajo de 20 px**». Tras agrandar la pieza a la altura de las mayúsculas (`#262` §9), el rótulo mide **30,4 px a 1920 · 26,4 a 1280 — cumple** — y **21,1 a 1024 · 15,8 a 768 · 12,0 a 390 — no cumple**. ⚠️ Antes de esa vuelta incumplía en TODAS, así que la ficha ya no es «el cliente se contradice» sino «falta decidir el suelo» | En nada roto: el propio artboard avisa de que el rótulo «solo vive en el tamaño grande, a 20 px es una mancha y se cae», o sea que él mismo prevé retirarlo. Pero hoy **no se retira en ningún ancho** | Una línea: por debajo del ancho donde el rótulo baja de 20 px, ocultarlo y dejar solo la pista (el interruptor se sigue leyendo por relleno y por lado, que es lo que el artboard exige en `6c`). ⚠️ **Antes hay que decidir el corte con el owner**, y ojo: la palabra vive en `.sr-only` aparte, así que ocultar el rótulo visible **no toca el nombre accesible**. **Ninguna guarda vigila la tipografía hoy** |
| **Baja** | **El titular del hero cabe en un renglón porque la palabra mide lo que mide** | `#262` §10 puso `--hero-t-fit: calc((100vw - 52px) / 7.3)` para que el interruptor no se caiga debajo del texto en un teléfono. El **7,3** son 5,99 de la palabra + 0,25 del espacio + 1,20 del interruptor, y **los 5,99 son de «DIVERSIÓN»**. `hero.l1` es contenido de la instalación | En cualquier cliente cuyo rótulo del hero sea más largo que el actual: el interruptor volvería a caerse a la línea de abajo. Las otras dos traducciones vigentes («FUN», «DU FUN») son más cortas y caben de sobra | Medir texto desde CSS no se puede. Las salidas reales son: (a) JavaScript que mida el renglón y publique el tope —caro para una pieza decorativa—, (b) declarar un tope por instalación junto al rótulo, o (c) aceptar que con rótulos largos el interruptor baje. ⚠️ **No lo cubre ninguna guarda**, y el defecto es visible pero silencioso |

---

## ▶ Media/Baja · lo que el LOGOTIPO dejó anotado (2026-08-30, `DECISIONES #275`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **El logotipo se incrusta DOS veces en cada página y la segunda no se pinta nunca** | `nav.blade.php` monta `<x-site.brand>` dos veces: en el racimo del armazón y en la cabecera del cajón lateral. Medido sobre `GET /`: **2 copias · 128.232 B · el 42 % de los 305 KB del HTML**. La segunda vive dentro de `.mob-menu`, cuya regla en `site.css` la apaga con un `display: none` **incondicional** —no está dentro de ningún `@media`—: `getBoundingClientRect()` da `0×0` a los 17 anchos medidos. ⚠️ El comentario de `#254` dice «~64 KB en las doce vistas» y cuenta **una** copia | En el peso de las doce vistas, en todos los idiomas. No rompe nada: se paga en bytes | El cajón lateral quedó **apagado, no retirado** (`armazon-y-menu.md` §12). Retirarlo de verdad se lleva esta ficha por delante. Mientras tanto, servir el logotipo solo en el racimo visible es una línea de Blade — pero hay que comprobar antes que nadie reactiva `.mob-menu` |
| **Baja** | **La variante sobre TINTA es INERTE en la rama del SVG en línea** | Las reglas de `.nav__brand-logo--paper` / `--ink` conmutan bajo `[data-surface="ink"]`, pero desde `#254` lo que se sirve —cuando hay una sola imagen— lleva `.nav__brand-logo--inline`. Medido con el menú abierto: `querySelector('.nav__brand-logo--paper')` → `null`. Sobre tinta se sirve el mismo dibujo sin cambiar nada | En nada hoy: el asset de este cliente se lee bien sobre tinta gracias a su anillo cian (**6,85:1** sobre `#101418`). Muerde el día que una instalación entregue las dos variantes **y** el logotipo se sirva en línea | Decidir si la rama en línea admite pareja. Hoy el Blade solo inlina cuando hay **una** imagen, así que las dos ramas no se cruzan — pero eso es una coincidencia del `@if`, no una regla escrita |
| **Baja** | **El escalón de teléfono del logotipo corta en otro ancho que el mockup** | El nuestro a **≤ 719 px** (el `@media` que redeclara `--nav-logo-h`), el suyo a **< 620** (la constante `tel` de `Landing PJP Modos`). Entre esos dos anchos nuestro logotipo mide 46 px donde el suyo mide 70. ⚠️ **La proporción sí es fiel**: 46/70 = 0,657 contra su `scale(.66)`, 0,44 % de diferencia | En una franja de 100 px de ancho de ventana, y solo en el tamaño del logotipo. El racimo tiene además **tres cortes distintos** (logo 719, `--nav-btn-h` 619, `--nav-pad-block` 768) y solo el de los botones casa con el suyo | Alinear los tres cortes con los suyos, o decidir explícitamente que el racimo tiene su propia escalera. Es una decisión de diseño, no un arreglo |
| **Baja** | **Las constantes del velo inferior están AJUSTADAS contra un solo lockup** | `VELO_ALFA = .14` y los finales `0.200` / `0.188` de `scripts/logo-sombra.php` salen de ajustar el perfil medido contra el lockup de ESTE cliente. La derivación es general —la caja de línea contra la tinta—, pero los números concretos dependen de dónde caiga la línea base en la caja y de cuánto infle el giro la caja de cada palabra | En el segundo cliente que exporte un lockup con esta anatomía y otra tipografía o otro giro: el velo saldría cerca, no idéntico | Derivar los offsets del propio fichero (alto de trazado de cada palabra) en vez de llevarlos como constantes. Hace falta calcular la caja de un `<path>` en PHP, que hoy el guion no hace |

---

## ▶ Media/Baja · lo que las MICROANIMACIONES dejaron anotado (2026-08-30, `DECISIONES #277`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **El PRESUPUESTO de movimiento por pantalla, MEDIDO** (`#279`) | Su norma es «máximo **dos** elementos animándose en pantalla». Medido en navegador (7 vistas × 2 ventanas × 5 paradas de scroll, contando solo lo que **intersecta el viewport y se pinta**, y solo animaciones `running`; control: con `prefers-reduced-motion` da **0**): · **`/precios` llega a 9** — y **8 son UN SOLO icono**, el de calcetines: seis puntos que laten en secuencia escalonada (0 · 120 · 240 · 320 · 420 · 620 ms) más dos calcetines que se mecen. **No se pueden fusionar en uno sin perder la secuencia**: o se quedan o se van · · **el aro + la mitad que asoma del CTA (`#205`) corren en las DOCE vistas**, así que **el techo está gastado antes de que aparezca nada más** · · ~~el interruptor del titular añade 2 en `/` y `/entradas`~~ **RESUELTO en `#280`, y de paso CORREGIDO: eran 3, no 2** —contar animaciones en un instante subestima una pieza cuyo ciclo apaga una de sus partes (el rótulo pasa por `opacity: 0`), así que hay que **unir las muestras de varias paradas**— · · sueltos: la chapa «destacado» (`/precios`, `/entradas`), la llama de b1 (`/cumpleanos`), la marquesina (`/servicios`) y el pin del mapa (`/contacto`) | En que el sitio incumple el techo de su propio cliente. ▶ **Remedido tras `#280`**: `/` pasa de **5 a 2** —ya cumple— y `/entradas` de **6 a 4**; siguen por encima `/precios` (9), `/cumpleanos`, `/servicios` y `/contacto` (3) | `[DECIDIDO owner, 2026-08-30]`: **solo el informe, no podar todavía.** El mapa está en `storage/app/logo-probe/simultaneidad.json` (gitignorado; se regenera con la sonda de `#279`). ▶ **Cuando se retome**, el orden por rentabilidad es: (1) que la invitación del CTA **deje de ser eterna** —libera las 12 vistas y el argumento no es de gusto: *una invitación que no para no invita, es un tic*, que es lo que su artboard llama ruido—; (2) el icono de calcetines; (3) los sueltos. ⚠️ **Y `#280` deja el patrón hecho**: acotar iteraciones exige antes comprobar que el reposo de la pieza existe fuera del bloque de movimiento reducido y que el corte cae en un tramo constante |
| ~~**Media**~~ | ~~**El interruptor del titular tiene que PARAR, y no es cambiar `infinite` por un número**~~ **RETIRADA en `#280`** — para tras `--switch-cycles` (3) y **descansa encendido**, y vuelve a saltar cuando el hero regresa al viewport. Quedaba: | ⚠️⚠️ El estado ON de reposo vivía **SOLO dentro del `@media (prefers-reduced-motion: reduce)`**, y medido en navegador el defecto era peor que lo previsto: acotando iteraciones la pieza quedaba **apagada con la palabra ON al 100 % encima** (el rótulo no tenía `opacity` propia) | En la primera pantalla de la portada y de `/entradas` | ▶ Se resolvió en dos mitades: el reposo **sube a la regla base** (el bloque de accesibilidad se queda solo con `animation: none`) y el corte **cae dentro del tramo encendido** —`calc(var(--switch-cycles) - 1 + 0.6)`—, donde el valor es constante y coincide con el reposo: **0 px de salto**, medido. Guarda: `HeroSwitchRestsOnTest` (8 mutaciones) |
| **Media** | **Sus dos artboards se CONTRADICEN sobre los bucles** | `Microanimaciones PJP` dice «los únicos bucles permitidos son los tres cargadores» y prohíbe por su nombre «banners que respiran, flechas que se balancean, **iconos que laten**». El producto tenía **24 declaraciones en bucle** y solo **2** son cargador; **tras `#280` son 21** —los 3 del interruptor dejaron de serlo—. Entre las otras 19: el aro que invita al CTA (`[DECIDIDO owner]` en `#205`). ⚠️⚠️ **Y el interruptor era el caso que más contradecía al cliente**, porque salía de su propio artboard `6d`: hoy ya no cuenta, y **cómo se sacó de la lista es el patrón** para las que queden (`#280`). ⚠️ Y `#222` construyó una escotilla («ambiental») **precisamente** para dejarlos fuera de la escala: la escotilla y esta norma son incompatibles | En que la web no cumple una norma escrita del cliente, y en que nadie sabe cuál de sus dos artboards manda. No rompe nada | `[DECIDIDO owner, 2026-08-30]`: **medir y anotar, no podar.** Podar en bloque cambiaría **cuatro piezas que hacen trabajo** —la galería y la tira de servicios (habría que decidir cómo se recorre el contenido), el meneo del widget de ofertas (es lo que llama la atención sobre él) y el aro del CTA (es la pista de que el botón se expande)— y ocho que son puro adorno. El corte natural, si se hace, es **el adorno sí, el trabajo no**, y eso es negociarlo con el cliente, no aplicar la norma |
| **Media** | **El hover del sistema no es el que su artboard define** | Su norma es una **pegatina que se aplasta**: baja 3 px, la sombra dura pasa de 5 a 2, al pulsar 5 px y sombra 0, **y el color no cambia nunca**. Hoy `.btn` hace lo contrario —sube (`translateY(-3px) scale(1.02)`) y **cambia el fondo** a `--action-hover`—, y `.cta-pair__buy` y `.zone-card` no tienen regla de hover | En todo control de la web a la vez. ⚠️ Y el arreglo **no es CSS**: retirar el cambio de color toca el mecanismo de color de acción de `#209`, que llega también al panel y a los correos | Decisión de producto antes que trabajo. ⚠️ Y ojo: **el artboard se contradice aquí** —su curva «Salida» dice servir «todo el hover» y su propia tarjeta de iconos usa Bote a 120 ms—, así que hay que preguntarle cuál vale |
| ~~**Baja**~~ | ~~**Las otras dos «que sí cuentan algo» siguen sin existir**~~ **RETIRADA en `#278`** — el sello y el check están en el desenlace, secuenciados y **sin confeti** (`[DECIDIDO owner]`). Quedaba: | El **sello de plaza** (cae de −34 px, −16° → −6°, escala 1,35 → 1) y el **check de confirmación** (lona 420 ms, tras la espera y sin solaparse con ella). Su artboard los define al dígito | En el desenlace de la reserva, que hoy no tiene ningún movimiento propio: la pantalla de confirmación aparece sin celebrarse | La U4 de esta tanda, cuando el owner la pida. La coreografía está escrita; lo que falta es decidir dónde encaja con el confeti que ya existe (`celebrate()`, `#258`) |

## ▶ Media/Baja · lo que el HUECO DE ILUSTRACIÓN dejó anotado (2026-08-31, `specs/hueco-ilustracion.md`)

> Salieron de medir los cinco vehículos de entrega de un dibujo en Chrome real. **Tres son del
> producto de HOY** y no dependen de que ese hueco llegue a construirse.

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **El servidor no manda NI UNA cabecera de caché para los estáticos** | `curl -sI http://localhost:8081/img/client-tag.svg` devuelve `Content-Type` y `Content-Length` **y nada más**: sin `Cache-Control`, sin `ETag`, sin `Last-Modified`, y `Accept-Encoding: gzip` da el mismo tamaño (sin comprimir). `public/.htaccess` son **740 B con 0 directivas** de caché o compresión —solo el rewrite de Laravel— y es el ÚNICO fichero de configuración de servidor que el producto controla. Ese asset son **199.561 B que se re-descargan enteros en cada visita** | En el ancho de banda de todo visitante, en todas las vistas. ⚠️ Y en que cualquier diseño que se apoye en «una sola descarga cacheable» está apoyándose en una suposición sobre el hosting de cada cliente | Una tanda propia, pequeña, **y con este orden — al revés hornea contenido viejo en la marca de un cliente**: (1.º) poner `?v={filemtime}` a los **7 assets que hoy no lo llevan** —`client-tag.svg` (el único referenciado desde CSS), `favicon.svg`, `favicon-64.png`, `apple-touch-icon.png`, `og-image.jpg`, `historia-seguridad.png`—, de los cuales **DOS son assets DE CLIENTE que cambian al reinstalar**; (2.º) entonces sí, `mod_expires` + `mod_deflate`. ⚠️ El HTML no corre riesgo: ya va `no-store` por `NoStoreWebResponses`, y los estáticos **ni pasan por Laravel** (medido: su respuesta no lleva esa cabecera). Va a staging antes que a producción |
| **Media** | **El CSS versionado del producto lleva la GEOMETRÍA del 2.º cliente** | `.reserve__tag` escribe `aspect-ratio: 377 / 197`, y eso **es exactamente el `viewBox` de `client-tag.svg`** (verificado: `0 0 377 197`). El producto pone el hueco y el paquete el dibujo… pero la PROPORCIÓN del dibujo se quedó en la hoja del producto | Una instalación con un tag de otra proporción lo ve **encajado dentro de una caja con la forma del tag de Play Jump Park** —`background-size: contain` lo mete, con aire alrededor—: no rompe, queda mal, y **nadie se entera** | Un token del paquete (`--deco-tag-ratio`). ⚠️⚠️ **Y NO se puede hacer solo en el producto**: hoy PJP funciona *porque* el número está quemado, así que sacarlo a un token con defecto neutro **deja su tag mal proporcionado** hasta que su `client.css` declare el valor. El cambio de producto y el de paquete **se despliegan juntos**. Va DESPUÉS del hueco de ilustración, para que sea aplicar un patrón establecido y no inventar uno suelto |
| **Baja/Media** | **7,1 MB de fotos del PRIMER cliente, versionadas en el repo del producto** | `git ls-files public/images` = **41 ficheros, 7,1 MB**. ⚠️ **Es decisión declarada, no accidente**: `.gitignore` dice literalmente *«Las fotos ORIGINALES de atracciones (~67 MB) NO se versionan. Las versiones optimizadas para web SI se versionan en /public/images/attractions»* | No rompe nada y no es una fuga silenciosa —está por escrito—, pero es la MISMA clase que el naranja del favicon (ficha propia arriba): el repo del PRODUCTO lleva contenido de una instalación. Un cliente nuevo se descarga 7,1 MB de fotos del parque de otro | Cuando exista el hueco de ilustración, la alternativa existe: mismo patrón (fichero gitignorado + exclusión del `--delete`). **Es decisión del owner**, porque hoy está autorizado por escrito y cambiarlo mueve el despliegue de una instalación viva |
| **Baja** | **`<use>` externo no está medido en WebKit ni en Firefox** | La máquina de desarrollo solo tiene Chrome. Todo lo que `specs/hueco-ilustracion.md` §2 afirma del vehículo —que es inerte, que recolorea por instancia, que da los tres tratamientos— está medido **en Chrome 151 y solo ahí** | En nada hoy: el hueco no está construido. Mordería el día que alguien proponga **mover las manchas del menú** de `mask-image` a `<use>`, porque la máscara sí está soportada en todos los motores y sería cambiar una pieza sana por una sin verificar | Medirlo antes de tocar `--deco-blob-*`. Por eso §3.2 de esa spec escribe explícitamente que **no se toca lo que hoy funciona** |
| **Baja** | **El catálogo vacío le dice al cliente que no hay DÍAS** | `CatalogStep.vue` pinta su estado vacío con `t('no_dates')`, que en `lang/es/tickets.php` dice *«No hay días disponibles ahora mismo. Vuelve a intentarlo más tarde.»* — pero en ese paso **el día ya está elegido**: lo que falta son entradas. No existe rótulo propio para esto en el lado cliente (`no_items` solo está en `admin.php`, y es otra cosa: «Este pedido no tiene productos») | En el cliente, que lee un mensaje que no corresponde a lo que ve y puede volver a elegir día sin que eso arregle nada | Un rótulo nuevo en `tickets.php` × 3 idiomas y cambiar la llamada. **El texto lo decide el owner**, que es quien escribe la voz del sitio |
| **Baja** | **`attractions` y `park_rules` no tienen identificador estable** | Medido: `attractions` es `id · zone_id · name · description · image · age · badge · position · is_active · is_special · ticket_type_id`, y `park_rules` es `id · name · description · position · is_active`. **Ninguna tiene `slug`** — `zones` sí | En que no pueden tener dibujo por instalación con el mapeo por nombre de `specs/hueco-ilustracion.md` §4.2. Derivar la clave de `name` haría que **renombrar una atracción le cambiara el dibujo en silencio** | Una migración que les dé `slug`, el día que alguien quiera marcador ahí. ⚠️ No urge: `#257` ya midió que esos dibujos **no tienen hoy ninguna pantalla que los pinte** |

## ▶ Baja · lo que dejó la unificación de zonas y juegos (2026-08-31, `#302`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Baja/Media** | **`zones.image` se ha quedado SIN NINGÚN consumidor en la web** | `[DECIDIDO owner]` retiró las tarjetas de zona, que eran su única superficie: la rama con foto (`.zone-photo-card`) y la de respaldo. Verificado tras el cambio: **cero vistas** leen `$zone->image`. El panel sigue ofreciendo el campo, el seeder sigue asignando `park_jump.webp` y `kids_zone.webp`, y los casos del SEEDER siguen exigiendo que existan en disco | En que un operador sube la foto de una zona desde el panel y **no aparece en ninguna parte**, sin ningún aviso. No rompe nada; miente sobre lo que hace el campo | Decisión del owner, y son tres caminos: (a) devolverle una superficie —la foto de la zona ACTIVA junto al selector era la tercera opción que se le ofreció y no eligió—; (b) retirar el campo del panel con su migración; (c) dejarlo y decirlo en la ayuda del campo. ⚠️ **No se toca sin decidir**: el dato está poblado en la instalación viva |
| ~~**Baja**~~ **RETIRADA (`#304`)** | ~~**Dos zonas de prueba llegan a la portada**~~ — `[owner]`: «no existen». Borradas (0 atracciones, 0 productos, 1 franja vacía cada una); la portada vuelve a DOS pestañas. Quedaba: | `cap` y `cap2`: `show_in_landing = 1`, **cero atracciones**, el mismo nombre («Cap») y el acento de `kids`. La portada pinta por eso **cuatro** pestañas en vez de dos, dos de ellas sin dibujo y sin edad | En que la portada local enseña dos pestañas vacías. **Es DATO, no código**: el producto pinta lo que el panel dice | Desmarcarles «mostrar en la landing» desde el panel. ⚠️ Se deja anotado y **no se toca desde código**: cuántas zonas salen es decisión del operador, que es la línea data-driven del producto |

## ▶ Media/Baja · lo que dejaron los datos reales de Play Jump Park (2026-09-01, `#304`)

| Sev. | Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|---|
| **Media** | **Un día SIN PRECIO se sigue OFRECIENDO** | «Kids · Ilimitada no se vende findes» se expresa no teniendo precio en la tarifa `special`, y la venta **falla cerrado** (`OrderCreator` lanza `tickets.errors.unavailable`, verificado). ⚠️ Pero `AvailabilityReader::dates()` construye un `OfferedDate` por cada fecha ofertable **sin filtrar las de precio nulo**, y `DateStep.vue` solo oculta la etiqueta del precio (`v-if="cell.price_cents !== null"`), no el día | El cliente elige el sábado, avanza, y **solo al crear el pedido** lee «Esa franja ya no está disponible» — un mensaje que habla de la FRANJA, no del producto. ⚠️ **No se pudo reproducir**: la BD de demo solo tiene franjas los miércoles, y el CONTROL lo confirmó (un producto con los dos precios ofrece exactamente las mismas fechas). **Saldrá en cuanto entre el horario real** | Filtrar en `AvailabilityReader` las fechas de precio nulo **para productos de entrada**, y decirlo con un mensaje propio. ⚠️ Antes hay que comprobar que no rompe el caso legítimo de «sin precio» (un complemento incluido). Toca el read-model que alimenta cajón y API: con guarda |
| **Media** | **«Mostrar sin vender» no existe para un COMPLEMENTO** | La relación `TicketType::addons()` filtra `ticket_types.is_sellable = true` **y** `is_active = true`, así que apagar un complemento lo deja **invisible en todas partes**: no hay estado intermedio | En que no se puede enseñar una carta (combos, cubos) sin poder comprarla. ⚠️ Para una ATRACCIÓN ese estado sí existe —`AttractionComplementLandingTest` fija que degrada a informativa—, así que la asimetría es del modelo, no una limitación de fondo | `[DECIDIDO owner, 2026-09-01]` de momento **se venden**, así que no urge. Si vuelve a hacer falta: un tercer estado en la relación + su guarda, tocando cajón, API y panel |
| **Media** | **El máximo de antelación es UNO GLOBAL, no por producto** | `sales.purchase_horizon_months` = 6, y es el único tope. El mínimo SÍ es por producto (`min_advance_value`/`min_advance_unit`) | En que el owner pidió **6 meses para entradas y 8 para packs** y no se puede: o se sube el global y las entradas también llegan a 8, o los packs se quedan en 6 | `[DECIDIDO owner]`: **se queda en 6**. Si algún día hace falta, es un campo por producto con su respaldo al ajuste global |
| **Baja/Media** | **El tope de packs es por FRANJA, no por DÍA** | `packs.max_per_slot` (= 5) lo aplica `PackAvailability` por franja; no existe ningún tope diario | En que «5 cumpleaños al día» no se puede expresar: con N franjas al día son 5 × N | `[DECIDIDO owner]`: **5 por franja**, que es lo que ya había. Un tope por día sería un ajuste nuevo y un contador por fecha en `PackAvailability` |
| **Baja** | **La HORA EXTRA no está construida, y son CUATRO productos** | El owner dictó cuatro precios distintos: cumple Kids 4 € · cumple Jump 5 € · entrada Kids 5 € · entrada Jump 8 €. `[DECIDIDO owner]`: **alarga la reserva a 180 min**, no solo cobra | En que hoy **ningún complemento mueve la duración** de una reserva: un addon cobra y la franja sigue midiendo lo mismo, así que el calendario podría colocar otra reserva encima de la hora vendida. Toca AFORO | `[owner]`: «hay que iterar cómo lo haremos para que sea profesional». Diseño fino antes de una línea de código, con al menos cuatro preguntas resueltas: si bloquea la SALA, si consume aforo de zona, si se puede comprar DESPUÉS de reservar, y qué pasa si la hora extra se sale del cierre |
| **Baja** | **Dos huecos de edad entre las dos zonas** | `[owner]`: Kids **4–7 años y +1 m**, Jump **+8 años y +1,30 m**. Medido contra esas reglas: un niño de **3 años o menos** no encaja en ninguna zona, y uno de **8 años que mida menos de 1,30 m** tampoco —mayor para Kids, bajo para Jump— | No rompe nada del sistema: es una regla de negocio. Muerde en la web, que tendrá que contestar a esos padres (la FAQ de edades ya existe y hoy dice otra cosa) | Decisión del owner. Si los huecos son deliberados, la FAQ y las normas tienen que decirlo con esas palabras |
| **Baja** | **`guard-bash.sh` lee la PROSA como si fuera un comando** | Escribir la entrada de `DECISIONES.md` con un `cat` **quedó bloqueada** porque el texto NOMBRABA una orden destructiva de base de datos. El hook mira la cadena del comando y no distingue documentación de ejecución | En que documentar un incidente destructivo obliga a rodear el propio guardián (escribir a fichero y anexar). No es peligroso; es fricción, y sorprende | Si se toca ese hook: excluir el cuerpo de un heredoc, o al menos los que escriben en `docs/`. ⚠️ Es el mismo cuño que la guarda del cajón cazándose a sí misma por citar el marcado roto (`#303`): *un escáner que mira texto tiene que decidir a propósito qué hace con la prosa* |

## ▶ Alta/Media · lo que dejó la LANDING con los datos reales (2026-09-01, `#307`)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **`legal.jurisdiction` se LEE y no se puede ESCRIBIR** (Alta) | `LegalIdentity` interpola `:jurisdiction` en el **aviso legal** y en las **condiciones** («*las partes se someten a los juzgados y tribunales de :jurisdiction*»), y `ProductionSeeder` lo siembra como `[PENDIENTE: partido judicial]`. Pero la clave **no está en `Settings::MANAGED`** ni en ninguna sección del formulario, así que **no hay forma de rellenarlo desde el panel**. Verificado en la página servida el 2026-09-01: `/aviso-legal` y `/condiciones` sirven cada una un `[pendiente]`, y es el ÚNICO que queda tras cargar los datos reales. | Dos páginas legales publicadas con un marcador visible. Es el gemelo invertido de los tres campos de `#304`: aquéllos se podían escribir y no los leía nadie. | Añadir la clave a `MANAGED` y un `TextInput` a `fiscalSection()` (4 líneas, junto a NIF y domicilio). ⚠️ **Falta además el VALOR**, que es del owner: el partido judicial del domicilio social. |
| **Las franjas no cubren el horizonte de venta** (Alta) | Medido el 2026-09-01 con el catálogo real ya cargado: `slots` tiene **486 filas del 2026-06-24 al 2026-10-02**, pero **solo 15 días tienen más de 5 franjas**. Se preguntó al dominio (`AvailabilityReader::times`) por un producto real: el **sábado 2026-09-05 devuelve `[]`**. Las franjas que hay son de la generación de demo del 19-08 y `#304` cambió el horario sin regenerarlas. ⚠️ **CERO franjas tienen reservas**, así que regenerar no destruye nada hoy. | Con los datos reales, el embudo de compra **no tiene disponibilidad** más allá de unos pocos días. | `php artisan slots:generate-rolling` (idempotente, hasta el horizonte de 6 meses). **No se corrió en `#307` a propósito**: es AFORO y no estaba en el encargo. ⚠️ Antes de correrlo, mirar si `slot_templates` (231 filas, horas de demo 10:00–21:00) sigue describiendo el parque real: el filtro por horario efectivo recorta bien, pero las horas de inicio no se alinean con el 16:30 real. |
| **La ficha de Google se posa sobre el mapa y repite la dirección** (Baja) | La URL de inserción con identificador de ficha hace que Google dibuje su propia tarjeta («Play Jump Park / Ctra. de Granada, 30813 Lorca / No hay reseñas / Abrir en Maps») en la esquina superior izquierda del mapa, justo al lado de nuestra tarjeta de dirección. | Ruido visual y dato duplicado en «Visítanos». | Decisión del owner. Quitarla es cambiar la URL por la variante SIN identificador de ficha — **medido: entonces el mapa sale limpio pero pierde la chincheta**. Se conservó la ficha porque es lo que Google entrega en «Compartir → Insertar un mapa», que es lo que pegará cualquier instalación. |
| **`address.maps_embed_url` se construyó a mano** (Baja) | El owner dio un enlace de COMPARTIR (`maps.app.goo.gl`), no de inserción. La URL de inserción se compuso con las coordenadas resueltas del enlace corto (37.6527252, −1.7144879) y el identificador de ficha, y **se verificó en navegador con control**. Funciona, pero no salió de la interfaz de Google. | Nada hoy. Mordería si Google cambiara el formato opaco del parámetro `pb`. | Pedirle al owner el `<iframe>` de «Compartir → Insertar un mapa» y pegarlo en el panel; el campo ya acepta el iframe entero y `MapsEmbed::clean` extrae el `src`. |

## ▶ Media · lo que dejó el encargo de las cinco secciones (2026-09-01, `#309`)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **La categoría de cookies `social` ya no gatea nada** (Media) | Al retirar «En directo» (`[DECIDIDO owner]`) desapareció el **único** consumidor de la categoría. Se quedan sin uso: el ajuste `social.feed_embed_url` del panel, el servicio `SocialEmbed`, la entrada `social` de `CookieConsent::OPTIONAL` y **la línea del banner que promete «contenido de redes sociales»**. ⚠️ Es el defecto de `#304` —un campo que se rellena y no lee nadie— creado **a sabiendas**. | Un operador puede pegar un widget en el panel y no verlo en ninguna parte. Y el texto del banner nombra un tercero que ya no se carga, que es una promesa legal que no se cumple al revés. | **Dos salidas, y es del owner.** (a) Vuelve con las reseñas (`specs/google-reviews.md`), que es el hueco que deja esta sección — entonces no hay nada que hacer salvo esperar. (b) Se retira ENTERA: ajuste, servicio, categoría **y su frase del banner**, que es texto legal y no se toca suelto. ⚠️ Mientras tanto, `CookieGateBlockingTest` conserva la independencia de categorías **medida por el lado del mapa**. |
| **El `sticky` de normas casi no se engancha** (Media) | Medido en tres tamaños: la columna derecha es **99 px** más alta que la izquierda, y a 1280×900 la sección entera (830 px) **cabe en la ventana**, así que no llega a pegarse. | El efecto que se pidió —«al hacer scroll se mueve solo la columna de la derecha»— casi no se ve. | **Las dos cosas pedidas se estorban**: la pegajosidad necesita que la derecha sea mucho más alta, y el tope de 3/4 normas la deja corta. La palanca es **subir el tope**, y es decisión del owner. |
| ~~**`images/historia-seguridad.png` sin consumidor**~~ **CERRADA 2026-09-01** (`#313`) | 93.404 B. Era la lámina de doce pictogramas del cliente ANTIGUO en la columna izquierda de normas; su única vista la retiró `#309`. | — | ✅ **Borrada** al sustituir los assets por los del parque real, junto con las 14 fotos que la galería retirada dejó huérfanas. ▶ **Y con ella `public/images` deja de tener material del PRIMER cliente**: de 8,0 MB / 40 ficheros a 5,2 / 26. ⚠️ **Lo que NO se cierra**: las fotos optimizadas **siguen versionadas** (el `.gitignore` lo dice explícitamente desde Fase 1) — ahora son las del SEGUNDO cliente, así que la pregunta white-label sigue abierta y es del owner. Gitignorarlas dejaría a un clon limpio con las fotos ROTAS: `attractions.image` es una ruta de BD, no un hueco que falle hacia invisible como el kit. |

## ▶ Media/Baja · lo que dejó la lectura del LIBRO por el owner (2026-09-01, `DECISIONES #316`)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **La cancelación deja «pendiente de devolución» y exige una acción del operador** (Media) | Un pedido cancelado sin devolver (`LB-CANCEL`) queda con saldo `refund_pending` hasta que alguien devuelva por tarjeta (REST) o registre la devolución a mano. No hay visita, así que no se puede inferir nada; y no hay POLÍTICA de cancelación (100 % por tarjeta · gastos · no reembolsable según antelación) en el producto: hoy cancelar devuelve el valor entero. | En cada cancelación de un pedido cobrado, el operador tiene que abrir «Reembolsar»; si el parque quisiera retener algo, no hay forma de expresarlo y el cliente leerá «pendiente de devolución» de dinero que el parque no piensa devolver. | `[DECIDIDO owner, 2026-09-01]`: **se deja como está y en producción se verá qué solución necesita el operador** (`#316` decisión 3). Cuando llegue: una política de cancelación es spec propia (toca `OrderItemCanceller`, el libro —una línea «gastos de cancelación»— y las condiciones legales). |
| **Un libro que no cuadra no tiene salida en el panel** (Media) | `LB-REVISION` / `T4-PRB01`: el aviso rojo «Este desglose no cuadra» es un detector correcto (en producción solo puede venir de un bug o de un toque en la BD), pero no hay ninguna acción que escriba un asiento correctivo: se arregla en base de datos. | Un pedido en revisión se queda así hasta que un técnico lo toque; el cliente ve solo lo cobrado y la frase. | `[DECIDIDO owner, 2026-09-01]`: **aparcado** (`#316` decisión 4). El diseño propuesto queda en `#316`: una acción «Regularizar», visible solo con el libro sin cerrar, que enseña qué identidad falla y por cuánto y escribe un asiento fechado, firmado y con motivo — sin tocar nunca el pasado. Las tres sondas locales que lo disparan (`T4-PRB01`, `R-IBX8B1`, `R-D3AN8Q`) se borran. |
| **Una compensación seguida de la bajada que la justifica cuenta dos veces** (Baja) | Con la T4 (§6.4), «devolver lo debido» ya no puede fabricar una cortesía; pero una **compensación deliberada** (con motivo) seguida de la bajada que la explicaba resta dos veces: la cortesía y el delta de la bajada. El libro cierra y el Total queda por debajo de lo que vale. | Solo si el operador declara compensación sabiendo que la deuda viene de una bajada que aún no ha registrado. Raro, y con motivo escrito. | Fuera de la T4 a propósito: el editor podría avisar al bajar una línea con un descuento por cortesía posterior («¿esta bajada justifica el descuento de X del día Y? → anularlo con un asiento»). **La T4 está en el árbol (`#317`, 2026-09-01)** y la ficha sigue abierta: se decide cuando el owner la haya visto. Con la T4 el caso exige que el operador ELIJA «compensación» y escriba el motivo, así que ya no ocurre por descuido. |

## ▶ Media · lo que dejó `#329` (2026-09-01)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **El área de cuenta del cajón perdió la puerta de «verificado» al mudarse** (Media) | Las rutas web viejas del área declaran `middleware(['auth', 'verified'])` (`routes/web.php`), pero el cajón que las sustituyó corre sobre `/api/v1` con `auth:sanctum` y **cero `verified` en todo `routes/api.php`**. Medido: `POST /api/v1/auth/login` responde **200** con el correo sin verificar. No fue una decisión: la puerta se quedó en la casa vieja. | Un titular sin verificar entra a su cuenta entera. Hoy el daño visible lo tapa `#329` —el aviso ya no miente y ofrece reenviarse el correo—, pero sigue habiendo un área privada sin la condición que su versión anterior exigía. | ⛔ **NO se cierra bloqueando el acceso, y está decidido**: `[owner, 2026-09-01]` *«no quiero que se haga cola esperando que los usuarios verifiquen sus correos… imagínate 100 personas, 5 filas»*. Sin entrar no hay QR y sin QR no hay identificación en la puerta: bloquear cambia una cola por otra peor. ▶ **La salida propuesta, y es del owner: que la PUERTA firme.** Hoy hay DOS sucesos que convierten la aceptación retenida en firma —el enlace del correo y **el pago** (`autoVerifyBuyer`: «un pago real demuestra que el titular es una persona»)—; la pantalla de puerta ya acredita la visita **con la persona delante**, que es más prueba que un enlace, no menos. Añadir ese tercer suceso vacía el estado sin tocar `#179` ni crear cola. |
| **`orders.edit_item_below_minimum` gobierna dos puertas y su CLAVE dice solo una** (Baja) | Desde `#329` el permiso decide también si se puede vender por debajo del mínimo **al crear** un pedido manual, no solo al editar una reserva. El rótulo se actualizó en `lang/*/admin.php`; la clave no. | Nada funcional: el operador lee el rótulo, no la clave. Muerde solo al leer el código o `PermissionCatalog`. | `[DECIDIDO]` a sabiendas: renombrar la clave obliga a migrar la tabla de permisos **y los roles ya asignados en la instalación del cliente en producción**, a cambio de nada que se vea. Si algún día se hace, va con una migración que renombre la fila y conserve las asignaciones. |

## ▶ Media · lo que dejó la sesión del 2026-09-01 (noche)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **Un correo que sale del panel no deja rastro** (Media) | El owner mandó los dos formularios post-reserva desde la ficha del pedido y **no hay forma de comprobarlo**: esa acción no escribe en `audit_logs`, no marca ninguna columna del ítem (no existe `guest_form_sent_at`) y la cola del sistema ya estaba vacía. Reconocido por el owner: *«sí, es un hueco, no tenemos trazabilidad de los correos que salen»*. | Nadie puede responder «¿se le mandó a esta clienta y cuándo?» sin mirar el buzón del cliente. Y con un incidente de correo como el de esta noche —límite por hora del hosting— tampoco se puede saber a quién hay que reenviar. | Una entrada de auditoría por envío es lo barato (`orders.guest_form_sent`, con el ítem como target). Lo completo sería una columna `guest_form_sent_at` por reserva, que además dejaría al panel enseñar «enviado el …» en vez de un botón mudo. ⚠️ Aplica a TODOS los correos que el operador dispara, no solo a éste. |
| **Un padre sin el correo verificado no puede ni ACEPTAR la exención de sus hijos** (Media; ⚠️⚠️ **reescrita el 2026-09-02, `#338`: su redacción anterior era FALSA en las dos mitades**) | ⚠️ Decía «un titular con menores declarados tiene una aceptación retenida **por cada uno**»: **no existe tal cosa** — medido, `dependents` no tiene ninguna columna `waiver_pending_*`; la aceptación retenida vive **solo** en `users`. Y decía que ésas «siguen pidiendo la tablet»: **la tablet tampoco sirve**, porque es el mismo `POST /me/dependents/{id}/waiver` desde la misma cuenta y devuelve el mismo **409**. ▶ **Tres sondas con control**: sin verificar → `WaiverEmailUnverifiedException`; **control** verificado → firma; **control** sin verificar pero con `declaredBy` = operador → **firma**. El dominio YA lo permite (`WaiverSigner:89-90`). | Desde `#336` el padre sin verificar cierra **su** exención en la puerta y la de sus hijos no tiene ninguna vía: **pasa él y no pasan ellos, con él delante**. No es que falte el gesto del operador — falta el paso anterior, porque para los menores **nunca hubo aceptación que retener**. | Es DISEÑO y es del owner, no una línea de código: `[owner, 2026-09-02]` *«el cliente no le pasa la tablet, solo le avisa; ya la ha leído y aceptado — lo único que no podemos verificar es que sea una persona»*. El espejo de `#336` sería dejar que el padre acepte por sus hijos desde su cuenta **sin** el correo verificado, y que la puerta lo cierre igual. ⚠️ Toca la puerta, o sea el carril del justificante: coordinar antes. |

## ▶ Baja/Media · lo que ENTRAR CON GOOGLE dejó anotado (2026-09-02, `DECISIONES #342` y `#343`)

- ✅ ~~**El botón no lleva el logotipo de Google.**~~ — **CERRADA con la T5** (`#345`,
  `specs/auth-con-google.md` §21.1). `[DECIDIDO owner, 2026-09-02]`: el botón oficial, variante
  CLARA y píldora. Se hizo por donde esta ficha decía —**asset por proveedor**,
  `public/images/providers/google.svg`, no icono del set— y con la marca **descargada de Google**, no
  redibujada: 0 px distintos de 226.560 contra su asset, con control.
  ▶ **Lo que deja anotado para el día que entre Apple**: la carpeta `providers/` y la exención de las
  dos guardas de iconos ya existen, así que el segundo proveedor es un fichero más y una regla más.
- ⚠️⚠️ **`deploy.sh` corre `migrate --force` y NO hace copia de la base de datos** (Media, `#353`).
  Medido el 2026-09-02 al desplegar a producción: no existía ni carpeta de copias en el servidor. Se
  hizo un `mysqldump` a mano antes de aplicar cuatro migraciones estructurales sobre 69 clientes y 6
  pedidos reales. ▶ **Salida**: un paso más en el script, antes del `migrate`, con `mysqldump
  --single-transaction` a `~/backups/` y su comprobación de que el volcado acaba en `Dump completed`.
  `mysqldump` está disponible en el servidor y sobra disco (421 GB).
- **`/servicios` devuelve 503 y está en el sitemap** (Baja, `#353`). `maintenance.page.servicios = 1`
  es deliberado, pero `sitemap.xml` lista esa URL igual, así que se le está pidiendo a Google que
  indexe una página que le responde error. ▶ **Dos salidas y es del owner**: quitarla del
  mantenimiento, o que el sitemap salte las páginas en mantenimiento —que es lo que `SitemapController`
  debería hacer, porque el dato ya está en `Setting`—.
- **`robots.txt` no declara el sitemap** (Baja, `#353`). El sitemap existe y responde, pero un
  rastreador que no lo tenga enviado a mano no lo encuentra solo. ⚠️ **No es una línea en el fichero
  estático**: la URL del sitemap es ABSOLUTA y distinta en cada instalación, y `deploy.sh` reescribe
  ese fichero en staging. ▶ Salida: que el script lo componga con `DEPLOY_URL`, como ya hace con el
  `Disallow: /` de staging.
- **Una fecha especial se salta la TEMPORADA al resolver el horario** (Baja, latente, `#353`).
  `OperatingSchedule` da prioridad a `special_dates` y, sin horas propias, cae al horario **SEMANAL**
  —no al de la temporada—. Hoy no muerde: `seasons` tiene **0 filas** en producción. Mordería el día
  que se cree una temporada, y entonces las 30 fechas especiales cargadas usarían el horario semanal
  en vez del suyo, **sin que nada falle**.
- **La víspera de Jueves Santo salió de aplicar la regla al pie de la letra** (Baja, `#353`). El
  owner definió la tarifa especial como «viernes, sábado, domingo, vísperas y festivos», y eso genera
  un «Miércoles Santo» (24/03/2027) a precio de festivo. Se cargó porque es lo que la regla dice; si
  no lo quiere, es borrar esa fila de `special_dates`. ⚠️ Se omitieron 6 vísperas que ya caían en
  viernes, sábado o domingo: no aportaban nada.
- **El par de CTA de la cabecera desborda 5 px a 1080 px de ancho** (Baja, `#351`). A ese ancho el
  botón de la mitad expandida se estrecha de 224 a **182 px** y su contenido no cabe. ⚠️ **Es
  PREEXISTENTE y está medido con control**: el desborde es idéntico con el subtítulo nuevo y sin él,
  así que no lo trae `#351` — se encontró al medirlo. Solo ocurre en la franja estrecha justo por
  encima del punto donde el menú cambia de forma (1080). Salidas: dar al botón el ancho que ya tiene
  por encima de 1100, o acortar el rótulo en esa franja.
- ✅ ~~**Una desviación de la letra de la Q6, pendiente del owner**~~ — **CERRADA con la T8·c**
  (`#350`, `specs/auth-con-google.md` §21.4.3 y §19.6). La razón vieja desapareció con la tanda: esa
  pantalla ya no recoge teléfono ni condiciones, así que sin descargo se quedaría con **un solo
  campo** y la pregunta volvía a estar abierta. `[DECIDIDO owner, 2026-09-02]`: **se sigue pintando,
  por el NOMBRE** —Google devuelve a veces «Ana G.» y ese nombre viaja a la reserva y a la firma—, y la
  alternativa costaba construir una rama de alta directa que **ninguna instalación viva ejercita**.
- ✅ ~~**`identities.unlinked` no está catalogada**~~ — entró con la T3 (`#344`), con su emisor.
- **No se sabe si una cuenta TIENE contraseña propia**, y por eso el aviso de «créala con “he olvidado
  mi contraseña”» se pinta en las cuatro pantallas **a todo el mundo** (`#344`). Un hash aleatorio y
  uno elegido son indistinguibles, así que detectarlo exige una columna (`password_set_at`) mantenida
  en los **siete** sitios que escriben contraseñas: alta pública, alta de mostrador, alta con Google,
  reset, cambio, anonimización y la toma de una cuenta sin verificar (P12).
  ▶ **Lo que compraría**: el aviso solo donde hace falta, y de regalo el panel podría decir «cuenta
  sin contraseña propia». ▶ **Lo que cuesta**: una migración con backfill y siete puntos que no
  pueden divergir. Hoy el coste es un párrafo de más para quien sí tiene contraseña — y a ése también
  se le olvidan.
- ⚠️ **La política de privacidad no menciona el login con Google** (art. 13/14). Es **requisito de
  salida** de la Q7: el texto vive en la BD de cada instalación, así que en `playjump.es` es un paso
  manual y **no se anuncia el botón a clientes reales sin él**.

## ▶ Media · lo que la AUDITORÍA DE DISEÑO vio fuera de su alcance (2026-09-02, `DECISIONES #430`)

- ⚠️ **La plantilla de correo lleva el naranja del PRIMER cliente quemado cinco veces**:
  `resources/views/vendor/mail/html/themes/brand.css` escribe `#FF5B22` como color de enlace y como
  relleno/bordes del botón («mockup `--zone-1`»). Los correos no cargan `client.css` ni `landing.css`,
  así que en una instalación con otra marca **el botón del correo sale del color de otro parque**, y
  no falla nada. Es la misma fuga que `#138` cerró en la web (`--jump-*` heredado por toda
  instalación), por la puerta que aquella tanda no miró. ▶ Salida: que la plantilla lea el color de
  acción del tema como ya hace el panel (`ThemeSettings`), no un literal. Fuera del alcance de
  `specs/auditoria-diseno.md` (12 vistas públicas), por eso está aquí y no allí.
- Los hallazgos de la propia auditoría **no se duplican aquí**: viven en `specs/auditoria-diseno.md`
  §3–§5 con su plan (§8) y se retiran de allí al ejecutarse. Los dos que YA tenían ficha —
  `/servicios` 503 en el sitemap, y las imágenes sin proporción (`#252`)— siguen en sus fichas; la
  auditoría les añadió la medida (64 de 64 peticiones · 25 de 25 imágenes).

## ▶ Media · `focused-layout` no sirve el paquete visual del cliente (2026-09-04, `DECISIONES #416`)

| qué | lo medido | consecuencia | salida |
|---|---|---|---|
| **`focused-layout` no incluye `client.css`** | El layout general (`components/layout.blade.php`) lo enlaza; `focused-layout` **no** — solo inyecta el bloque `<style id="jj-theme">` con **9** variables de BD (`--brand`, `--zone-*`, la familia `--action-brand-*`). Medido comparando las hojas de `/entradas` (landing.css · spinner.css · site.css · **client.css**) con las del post-form (landing.css · site.css). Lo usan **dos** vistas: el post-form de invitados y la pantalla del justificante | Esas dos pantallas se pintan con la piel del PRODUCTO, no con la del cliente: se pierden `--interactive` (`#436`), `--on-ok`/`--on-err` (`#434`), los radios, las sombras y las fuentes por rol del paquete. Hoy **no se ve roto** —la escala base de `landing.css` es coherente y el logotipo llega como asset— pero la divergencia crece con cada tanda que añade un token al paquete. Lo destapó que el «Más info» nuevo saliera en tinta en vez del azul del cliente: el rol es correcto, lo que falta es quién lo define | Enlazar `client.css` en `focused-layout` igual que en `layout`, con su `@filemtime`. ⚠️ **Cambia el aspecto de dos pantallas** (colores de interacción, radios y sombras del cliente), así que es del carril de DISEÑO — hoy parado (`#452`) — y merece una pasada de ojo del owner, no un `<link>` a escondidas. ⚠️ Y hay que comprobar antes que ninguna regla del paquete pise la maqueta enfocada, que no se diseñó bajo él |

## ▶ ~~❗ ALTA · un producto SIN precio para la tarifa del día SE OFRECE, y el checkout lo rechaza~~ **RETIRADA: ARREGLADA** (2026-09-06, `DECISIONES #429`)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| ~~**Alta · la oferta de un producto no mira si hay precio ese día**~~ **ARREGLADA el mismo día** (`#429`: `passesOffer()` lo comprueba, con el atajo que deja el catálogo normal a coste cero) | Medido sobre el catálogo real: **«Kids · Ilimitada» no tiene precio en la tarifa `special`** (viernes, sábado y domingo) y aun así el calendario **ofrece sus 10 horas del sábado**; al pagar, `tickets.errors.unavailable`. El cliente elige día y hora y se lo tumban al final **sin decirle por qué**. ⚠️ `SlotOffer::passesOffer()` mira franja, horario y antelación —**no el precio**—, a diferencia de los COMPLEMENTOS, donde «sin tarifa ese día no se ofrece» es regla desde `#410`. | Venta pública **y** pedido manual del panel (misma fuente, `AFORO-02`). Hoy lo sufre un producto, pero el hueco es del mecanismo: **cualquier alta con una tarifa a medias se comporta igual y nada avisa** — ni el panel ni la suite. | **DOS salidas, y la primera es del owner**: **(a) DATO** — si la ilimitada se vende los findes, ponerle precio de festivo; si no se vende, hoy no hay forma de decirlo salvo desactivar el producto. **(b) PRODUCTO** — que `passesOffer()` descarte los días sin precio, como ya hace la oferta de complementos: cierra el hueco para todo el catálogo y convierte «sin precio» en el interruptor de calendario que ya es para los complementos. ⚠️ (b) toca `SlotOffer`, que está en el `CRITICAL_RE`: tanda propia con `VERIFY_CONC`. |

## ▶ Alta/Media · lo que destapó diseñar los complementos de venta posterior (2026-09-03, `DECISIONES #413`)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| ~~**Media · el editor del panel no sabe BAJAR la cantidad de un complemento**~~ **RETIRADA EL MISMO DÍA: ERA FALSA** (2026-09-03, revisión adversarial de `#413`) | Afirmé que `OrderItemEditor::edit()` ignora en silencio un 3 → 2. **No lo ignora: lo BLOQUEA.** `validateAddonEdits()` —que corre **antes** del cuerpo de `edit()`— devuelve `addon_partial_reduce_unsupported`; hay mensaje al operador en `lang/es` y `lang/zh_CN` («para reducir un complemento, quítalo y vuelve a añadirlo; el reembolso se hace aparte»), fila de auditoría y caso propio en `ManageItemAddonsTest`. **Mi error**: leí las dos ramas del guardado y me salté la validación de delante | En nada: no hay defecto. ⚠️ **Y el bloqueo no es un olvido, es una exclusión deliberada que sostiene el modelo de dinero**: para una línea nacida con el pedido `nac > 0`, así que bajarla **debe dinero**, y `#244` prohíbe moverlo online | Nada que retirar. La ficha se conserva **tachada** porque la afirmación llegó a `origin/main` y alguien podría encontrarla. ▶ Lo único que queda es una decisión estrecha de `complementos-post-reserva.md` §4.10: permitir 3 → 2 en el panel **solo** para líneas con `LineFacts::birthValue() === 0` |
| ~~**Alta · un `PUT` del post-form SIN `general` BORRA las respuestas generales**~~ **RETIRADA: ARREGLADA en la T0 de `#413`** (2026-09-03, commit `055d4781`) — ⚠️ **y era MÁS ancha de lo que esta ficha decía**: al construirlo se midió que `guests` tenía el mismo defecto y peor (un `PUT` sin esa clave vaciaba las ocho fichas de los menores, alergias incluidas). Las dos claves comparten ya la semántica «ausente = no la toques», con caso en las dos superficies, control de que `[]` sí vacía y 4 mutaciones mordiendo | Medido con una petición firmada real contra `localhost:8081`: sembradas `adults_approx` y `observations`, un `PUT {"guests":[]}` **sin la clave `general`** devuelve **200** y las dos desaparecen. La causa: `Api\V1\GuestFormController::update()` pasa `$validated['general'] ?? []` y el web `$request->input('general', [])`; `submitGuestForm()` sí distingue `null` (conservar) de `[]` (vaciar), pero **ese `null` solo lo pasa el PANEL**. ⚠️ **Y el contrato documenta hoy la conducta contraria**: la entrada de `OPTIONAL_BY_DESIGN` justifica los campos opcionales diciendo que «un formulario de invitados se guarda a trozos» — guardar a trozos **borra el otro trozo** | Pérdida de datos silenciosa por la API, sin ningún caso que la fije. Hoy el único cliente es el cajón (que manda las dos claves), así que no muerde; morderá con la app móvil o con cualquier guardado parcial. Y `complementos-post-reserva.md` iba a construir **dinero** encima de ese patrón | ✅ **ENTRA EN LA T0 de `complementos-post-reserva.md` §9** (`[DECIDIDO owner, 2026-09-03]`: la feature construye dinero encima de esta superficie). Decidir con `array_key_exists` en vez de `?? []`, pasar `null` de verdad, y fijarlo con dos casos en direcciones opuestas. Corregir además el porqué escrito en `OPTIONAL_BY_DESIGN` |
| ~~**Alta · la escalada 403 → 410 → 404 NO se cumple en la WEB**~~ **RETIRADA: ARREGLADA en la T0 de `#413`** (2026-09-03, commit `055d4781`) — `->missing(fn () => abort(403))` en las **cuatro** rutas públicas (post-form y justificante), con caso en los dos y verificado con `curl` fuera de la suite: 403 tanto con un id real como con uno inventado | Medido con `curl` sin sesión: `/reserva/{id}/datos-invitados` responde **403** con un id que existe y **404** con uno que no; `/autorizacion/{id}` igual. La API responde **403 en los dos casos**. La causa: el *route model binding* implícito lanza el 404 **antes** de que corra `AuthorizesGuestForm`, que es exactamente lo que el controlador de la API evita resolviendo a mano | Un desconocido puede **enumerar qué `order_items` existen**. `RGPD-03` y el docblock del trait afirman que ese orden es una propiedad de seguridad — y hoy solo se cumple en la API. Afecta también al justificante del menor invitado (`#335`) | ✅ **ENTRA EN LA T0 de `complementos-post-reserva.md` §9** (`[DECIDIDO owner, 2026-09-03]`). `->missing(fn () => abort(403))` en las rutas web **y en las del justificante**, o resolver a mano como la API. Barato; lo caro es que nadie lo había medido |
| **Media · la IP y el user-agent de las filas de auditoría SIN SESIÓN sobreviven al art. 17** (2026-09-03, `#413`) | `AuditLogger` guarda `ip` y `user_agent` de la petición y `user_id = Auth::id()`, que es `null` cuando se entra por enlace firmado. Medido: **368 de 373** filas de `orders.guest_form_submitted` tienen `user_id` nulo **y** una IP. `User::anonymize()` nulifica IP/UA **solo** `where user_id = <titular>` (su propio comentario: «solo donde es el ACTOR») | La IP de quien envió el post-form —incluido el propio titular entrando por su correo— **no se purga nunca**. Preexistente; `complementos-post-reserva.md` multiplicaría las filas con una acción nueva | ⏸️ **SE QUEDA COMO FICHA** (`[DECIDIDO owner, 2026-09-03]`): tiene dos lados y merece tanda propia, no colarse en la de complementos. Es una decisión con dos lados y es del owner: o `anonymize()` alcanza las filas cuyo *target* sea un pedido suyo para una lista declarada de acciones (y se pierde parte del rastro probatorio), o se conserva y **se documenta** como art. 17.3.e. Hoy no está escrito en ningún sitio |
| **Media · `max_qty = null` no tiene techo en el dominio** (2026-09-03, `#413`) | Medido: `AddonResolver::effectiveQuantity()` solo acota si `max_qty !== null`, y las **29** filas reales lo tienen a `null`; el `20` que parece un tope vive en `viewModel()` como `can_inc` (apaga el botón `+`) y `CartPayload` valida `'quantity' => ['required','integer','min:1']` **sin `max`**. `order_items.quantity` y `orders.total` son `int unsigned`: cabe de sobra | Una cesta forjada compra hoy cualquier cantidad de cualquier complemento. No es un agujero de dinero (el precio se calcula en servidor y el pedido hay que pagarlo), pero sí de aforo operativo y de basura en el pedido | Un tope por defecto en el dominio, o `max` en el esquema de la cesta. ⚠️ **`complementos-post-reserva.md` lo cierra solo para su fase** (`max_qty` obligatorio + cinturón): los 29 enganches `booking` se quedan como están |
| ~~**Baja · `markGuestFormCompleted()` bumpea `updated_at` en cada guardado completo**~~ **RETIRADA: ARREGLADA en la T0 de `#413`** (2026-09-03, commit `055d4781`) — se sella la primera vez y cuando el contenido cambia; con `travel()` en los casos, sin el cual pasarían con el defecto puesto | Sella `guest_form_completed_at` con `now()`, que siempre está sucio, así que siempre hay `UPDATE`. Y `updated_at` del ítem es el **token optimista** de cinco puertas del operador (`OrderItemEditor::edit`, `OrderItemCanceller::cancel`, `OrderItemRefunder::refund`, `OrderItemGuestDataWriter`, `OrderItemEventDataWriter`) | Un cliente que guarda su formulario varias veces invalida los modales abiertos del operador con `stale_item_version`. Hoy es raro; con los extras (steppers) la frecuencia de guardado sube | ✅ **ENTRA EN LA T0 de `complementos-post-reserva.md` §9** (`[DECIDIDO owner, 2026-09-03]`). Sellar solo si estaba `null` o si el contenido cambió. Una línea, y desbloquea al operador |
| **Baja · `ProductAddon.php` no está en el `CRITICAL_RE`** (2026-09-03, `#413`) | Los guards del pivote (los de la hora extra, y los siete que añadiría `complementos-post-reserva.md`) viven en `ProductAddon::booted()`, que **no dispara el gate del `pre-push`**. `AddonResolver` y `MixedPartySurcharge` sí están | Un cambio que retire un guard del pivote no exige `VERIFY_CONC=1` ni llama la atención de nadie. Hueco heredado de `#410`: hoy lo tapa el cinturón de `AddonResolver`, que sí está gateado | ✅ **CERRADA por la T3 de `#448`** (2026-09-08, §12.18): entró en el regex **y** en `CriticalPathGateTest` **en el mismo commit que re-apuntó su guard** — el propio test declara, medido por mutación, que añadirlo solo al regex deja el gate sin red. ⚠️ Verificado el 2026-09-08 ejecutando el `CRITICAL_RE` real: hoy `ProductAddon.php` **no está en ninguna de las listas** |
| **Media · un limitador medido con reloj de pared cae BAJO CARGA** (aviso del carril de diseño, 2026-09-03, leído y retirado según `CONVENCIONES §10.4`) | `Tests\Feature\Admin\Puerta\ValidarRegistroTest::test_rate_limit_is_per_user_not_per_ip` esperó `rate_limited` y recibió `not_registered` en un gate con **tres suites corriendo a la vez**; la suite tardó **8:49 en vez de 1:20**. Mismo árbol verde cinco minutos antes y verde en aislamiento después | En la confianza del gate: un rojo que no es del producto enseña a ignorar rojos. Es la familia del aviso del SSR («el pre-push puede caer si la máquina está cargada»): una ventana de `RateLimiter` medida con reloj de pared contra una máquina que puede tardar más que la ventana | Fijar el tiempo en el caso (`travelTo()`, `SUITE-03`) en vez de depender de que la ventana no expire, o subir la ventana del caso. ⚠️ Toca `SEC-05`: el limitador de la puerta se AUDITA una vez por ventana, así que el caso tiene que seguir ejercitando esa auditoría |

## ▶ Lo que dejó anotado el asistente de crear pedido (2026-09-04, `DECISIONES #463` y `#464`)

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| ~~**Media · el paso «Cuándo» cuesta ~700 ms de servidor, y el 90 % es hidratar el horizonte entero**~~ **RETIRADA: ARREGLADA en `#465`, el día siguiente** | Medido entonces: `calendarMonth()` **8 consultas / 343 ms** y `timeChips()` **39 / 371 ms**, con la consulta cruda en **9,9 ms** — el coste era PHP: hidratar **1.947 modelos** (68 ms) y resolver la ventana del día **once veces por día** (124 de los 167 ms del filtro). ⚠️ Y no era del panel: `AvailabilityReader` —la web y la API del cajón— llamaba a lo mismo, así que **cada cliente que abría su calendario pagaba ~420 ms** | Medido después: el paso **714 → 94,6 ms** y el read-model público **421 → 44,8** (fechas) y **396 → 40,3** (horas), con las MISMAS 176 fechas y las mismas horas y plazas por los dos caminos | Retirada por `#465`: las FECHAS leen filas crudas y resuelven la ventana una vez por día; las HORAS cargan solo el día que se pregunta. ⚠️ **Lo que quedó en su sitio y hay que respetar**: la consulta y el predicado son UNO para las dos vías, `clampToHorizon()` re-pone el techo que la consulta del horizonte ponía sola, y `SlotOfferPathParityTest` ata las dos vías por equivalencia |
| **Baja · `ticket_types.conditions` es una columna MUERTA** | Existe en la tabla y el modelo la castea a `array` (en el `casts()` de `TicketType`), pero **no la lee ninguna vista, ningún serializador y ningún servicio**, y **el catálogo del panel no ofrece campo para rellenarla**: cero escritores y cero lectores en todo el repo | Hoy, en nada. Muerde en cuanto alguien la pinte —fue la tentación al montar el modal «Más info» de la T2, y por eso se dejó fuera—: sería el **único sitio del producto donde aparece un texto que el operador no puede editar desde ninguna pantalla**, o sea un hueco que falla hacia invisible | Dos salidas, y es decisión del owner: **darle un campo en el catálogo** y entonces enseñarla donde el cliente ya lee las condiciones, o **retirar la columna** con su cast. Mientras no se decida, no se pinta |

## ▶ Media/Baja · lo que la REVISIÓN ADVERSARIAL de `#448`/`#449` dejó abierto (2026-09-08, `specs/hora-extra.md` §12.20)

42 hallazgos brutos → **25 confirmados, CERO bloqueantes**. Lo arreglado en el acto está en §12.20;
esto es lo que se decidió NO arreglar, con su motivo. ⚠️ **Ninguno es alcanzable con el catálogo de
HOY**, y ésa es justo la razón por la que se fichan en vez de construirse a ciegas.

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **Media · el N+1 de la pastilla de divergencia** (m1) | `OrderItem::addonUnitDivergesFromCatalogue()` encadena `parent` → `parent.ticketType` → `addons()->first()` **por hija**, sin memo ni eager-load. Medido sobre el render REAL de `ViewOrder`: 1 hija **65 → 68** consultas, 6 hijas **70 → 88** | **Hoy cuesta CERO**: sale por el `return false` con el sello `null`, que es el estado de todo el corpus. La regresión aparece **con las ventas posteriores al despliegue**, y ninguna guarda la ve — el único presupuesto de esa pantalla mide `AssignedDependents::forOrder` | Resolver los pivotes **una vez por `$item`** en la cabecera del blade (el molde de `AssignedDependents::forOrder`) o `setRelation('parent', $item)`, **y añadir el caso de presupuesto**, que es lo que impide que vuelva |
| **Media · el RE-SELLO por cambio de producto va DESPUÉS de escribir `extra_minutes`** (m2) | Los minutos se calculan con el sello VIEJO y se escriben antes de re-sellar. Reproducido: A(`per_guest`) → B(`fixed`) deja `qty=15 · sello fixed · extra_minutes 60`, y la SIGUIENTE edición lo sube a **900** | ⚠️ **NO es regresión** —antes de `#448` se leía ya el pivote del producto nuevo, así que el estado final era idéntico y peor en la ventana— y **no es alcanzable hoy**: exige el mismo extensor colgado de dos packs con modo distinto, y los dos cuelgan de uno solo, así que `orphan_addons` lo para | Mover el re-sello ANTES del cálculo de minutos. ⚠️ **Y decidir a la vez qué pasa con la CANTIDAD**: re-sellar sin re-escalar deja un número que ya no significa lo que dice. No es solo un orden |
| **Media · el censo vigila menos de lo que su docblock afirma** (m3) | `SoldLineUnitHasOneSourceTest` solo casa `->isPerGuest()` y `->quantityUnit()`. Se le escapan los helpers estáticos que leen el modo por dentro (`effectiveQuantity`, `freeUnits`, `blocksFor`, `extraMinutes`) y el acceso crudo `->quantity_mode`. ⚠️⚠️ **Agravante**: la lectura de AFORO original era `AddonOccupancy::extraMinutes(…, $pivot, …)` — o sea que **la puerta por la que entró la mitad del agujero es justo la que el censo no vería** | En una regresión futura que entre por esa forma. **Mitigado**: la capa de conducta (`AddonQuantityModeReadsTest`) sí muerde esas regresiones, y la mitad positiva ya cubre los **cuatro** lectores desde §12.20 | Añadir las agujas de los helpers estáticos y del acceso crudo. Es un deletreo más, no una clase nueva de fallo |
| **Baja · la idempotencia del relleno B no tiene caso ni mutación** (m5) | El único caso de idempotencia monta un EXTENSOR `fixed`, que el relleno B **ni siquiera casa**: quitando su `whereNull('c.addon_quantity_mode')` los seis casos siguen VERDES | Solo al reconstruir la BD desde cero o en un rollback, no en un despliegue normal. El estado que protege **sí** es alcanzable (el re-sello por cambio de producto deja hijas selladas divergentes) | Un caso con sujeto de B ya sellado + su mutación |
| **Baja · el ORDEN de los dos bucles del editor decide dinero y ningún caso lo fija** (m6) | Con cambio de producto Y de cantidad a la vez sobre packs de modo divergente, el orden actual deja `qty=12` e invertido `qty=16`. **El orden actual es el correcto** y está razonado en el código | La configuración **no existe hoy**: cero complementos con más de un `quantity_mode` distinto entre sus enganches | Una guarda que fije el orden, cuando exista un sujeto |
| **Baja · el relleno A sella el literal `fixed` sin mirar el modo vivo** (m7) | Su docblock razona un hecho COPIADO —«a los extensores no les ha cambiado el modo nadie»—, lo que solo autoriza «= el modo de HOY», y escribe el literal. Un extensor enganchado `per_guest` (configuración que el panel **ofrece** desde `#443`) vendido antes del despliegue quedaría sellado `fixed` → **900 minutos donde se vendieron 60** | Ventana **estrictamente pre-despliegue** y estrecha: exige usar el atajo del «complemento nuevo ya en `per_guest`» **y** vender bajo él antes de migrar. **Lo cierra el SELECT del paso de despliegue** (§12.20·M1) | Sellar con `pa.quantity_mode` en vez del literal, + el control del extensor `per_guest` que hoy falta en `AddonQuantityModeBackfillTest` |

## ▶ Alta/Media · lo que destapó el SELLO DEL MODO de un complemento (2026-09-08, `DECISIONES #448`)

Las cuatro salen de medir producción para `specs/hora-extra.md` §12 y **quedan fuera de esa tanda a
propósito** (§12.15): ninguna es «el mismo trabajo», y meterlas dentro habría mezclado un arreglo de
significado con tres decisiones distintas.

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| ~~❗❗ **ALTA · `GuestCountAdjuster` NO re-escala las hijas por-invitado**~~ **CERRADA por `#449`** (2026-09-08, el mismo día). Se corrigió también la spec, que lo afirmaba en tres sitios. Lo que sigue abierto es la lección del instrumento: el arnés dejó el árbol mutado **por segunda vez en la banda**, ahora por un fichero fuera de `FICHEROS` — `mutar()` aborta si no hay copia (2026-09-08, `#448`) | Verificado en el árbol: el fichero **no menciona** `isPerGuest`, ni `AddonResolver`, ni `free_quantity`; su única escritura toca `quantity`/`unit_price`/`seats` del **PADRE** y su único `recordEdit` también; su única mención a `children` es un eager-load. `invitados-en-post-form.md` dice lo contrario en dos sitios. **`GuestCountTest` no tiene ni un caso con complemento hijo** | ⚠️⚠️ **Hoy no muerde porque ningún extensor es `per_guest` — y se activa EXACTAMENTE con el cambio que el owner quiere hacer**: en cuanto la hora extra pase a «por invitado», un cliente que suba de 15 a 20 invitados desde el post-form **se queda con la hora extra cobrada a 15**, y el desfase lo absorberá el siguiente `recordEdit` del operador **como si fuera suyo**. El panel sí re-escala (`OrderItemEditor:856-879`): son dos puertas al mismo hecho y solo una lo mantiene | Tanda propia, y **conviene cerrarla ANTES de configurar el modo nuevo**. Toca dinero y `GuestCountAdjuster` está en el `CRITICAL_RE` → `VERIFY_CONC=1`. ▶ Lo primero es corregir la spec, que hoy **afirma una conducta que el código no tiene** |
| **Media · `is_included` reinterpreta lo vendido y NO tiene candado ninguno** (2026-09-08, `#448`) | Es el hermano exacto de `quantity_mode`: `AddonResolver::freeUnits()` es todo-o-nada en `per_guest`, así que **apagar «incluido» convierte N unidades gratis en N de pago** en la siguiente edición. Medido en producción: **18 de las 27 hijas vivas tienen `free_quantity > 0`**. ⚠️ `quantity_mode` al menos tiene el candado de `#443`; ésta **no tiene nada** | En silencio, y sobre el conjunto más grande: los calcetines incluidos de los dos packs. El síntoma sería un cargo que aparece en una fiesta ya vendida sin que nadie lo pidiera | **NO entra en `#448` a propósito** (§12.4: reinterpretar en silencio y bloquear son dos problemas distintos). Cerrarla obliga a decidir **qué pasa cuando el parque legítimamente deja de incluir algo** — es una decisión de negocio, no un arreglo, y es del owner |
| **Media · `max_qty` RECORTA una línea ya vendida desde el post-form** (2026-09-08, `#448`) | `PostFormAddons::apply()` capa la cantidad **deseada** del cliente contra el `max_qty` vivo. Bajar el tope hace que **un reenvío sin cambios REDUZCA la línea** y mueva dinero — el cliente no pidió nada y su pedido encoge | Estrecho pero silencioso: solo en complementos de fase `postform` cuyo tope baje entre dos guardados. Medido: **18 de 29 enganches tienen `max_qty`**, 14 de ellos en `postform` | El arreglo **no es sellar el tope**: es que **un techo no pueda REDUCIR una línea ya vendida** — la regla R1 del propio reconciliador (el estado deseado gobierna solo lo OFRECIDO) aplicada al techo. Una tanda pequeña, pero es dinero |
| ❗ **ALTA · 14 de los 22 arneses de mutación DEJAN EL ÁRBOL MUTADO si el proceso muere** (2026-09-08, `#448`, **reproducido de verdad, no en teoría**) | El molde de la casa es `TMP="$(mktemp -d)"` + `trap 'restaurar; rm -rf "$TMP"' EXIT`. Un `trap EXIT` **no corre con SIGKILL, ni si se cae la sesión, ni si el contenedor se va** — y entonces quedan (a) ficheros del producto con la mutación puesta y (b) la copia buena en un temporal que ya nadie sabe encontrar. Medido: `for f in scripts/mutar-*.sh` → **14 con el patrón, 8 sin él**. ▶ **PASÓ**: un agente ejecutó `mutar-sello-modo.sh`, la sesión murió, y `PostFormAddons` e `ItemEditPricing` se quedaron **mutados en el árbol de trabajo**. Lo cazó la suite completa con **1 fallo de 4.460** — y solo porque se volvió a correr entera antes de commitear | Silencioso y caro: el árbol queda con una mutación de dinero puesta y **`git status` la enseña como si fuera trabajo tuyo**. Si el corte ocurre en un fichero que la suite del filtro no toca, el `pre-push` es la última red — y si la mutación está en algo que el gate no cubre, no hay red | ✅ **`mutar-sello-modo.sh` YA está endurecido** y sirve de molde: copia en **ruta FIJA y gitignorada** (`storage/app/mutaciones/<arnés>/`) en vez de `mktemp`, **reparación al ARRANCAR** (si hay copia huérfana, restaura y lo dice), `trap` también en `INT`/`TERM`, y **comprobación final de integridad** que sale con código ≠ 0 si algún fichero quedó distinto del original. Verificado con el fallo real puesto: simulado el corte, el arnés avisa y repara. ▶ Queda **portar el molde a los otros 13**, que es mecánico pero toca ficheros de otros carriles: tanda propia |
| **Media · la doctrina del sello está aplicada A LA MITAD en las extensoras** (2026-09-08, `#448`) | `liveStayExtendingChildren()` (`OrderItemEditor::liveStayExtendingChildren()`) reconoce a las extensoras por el **catálogo VIVO** (`whereHas('ticketType', extends_parent_stay = true)`), mientras su hermana `liveOccupyingChildren()` lo hace por **HECHOS de la fila** y su docblock invoca «la doctrina del sello». ⚠️ Y el problema es más ancho: `extends_parent_stay`, `occupies_after_parent`, `duration_min` y `seats_per_unit` **viven en `ticket_types`, no en `product_addons`** (verificado con `SHOW COLUMNS`: el pivote tiene 14 columnas y ninguna es ésas), **así que el sello del modo NO las alcanza** | Obliga a un cerrojo permanente sobre `extends_parent_stay` que **no se abre nunca** — exactamente el problema que `#448` resuelve para el modo, por la otra puerta. **Es la siguiente pared contra la que se choca** | La misma doctrina llevada a `ticket_types`: o un segundo sello para lo que define la FORMA de la ocupación, o reconocer a las extensoras por un hecho de la fila (`extra_minutes > 0` no basta: vive en el padre y es una suma). ⚠️ No se coló en `#448` porque es **otro alcance**, no un remate |

## ▶ Media · SIETE colores del PRIMER cliente vivos en el producto, y los cazó el canvas del segundo (2026-09-09, `DECISIONES #474`)

**Qué es.** `#fbeaea` · `#e3b5b0` · `#8a2b22` · `#a93226` · `#e7f6ec` · `#b45309` · `#92400e` son
hexadecimales heredados del cliente de origen. Los nombra la v1.10 de los tokens del canvas al
explicar por qué crea las superficies de aviso **sobre papel**: los cuatro avisos del sistema solo
existían sobre tinta y el cajón es papel de arriba abajo, así que en sus 25 pantallas no había
ninguno que usar **y el hueco lo rellenaron éstos**, que no son de esta marca.

**Verificado aquí, y están**: 7 usos en `public/css/site.css`, más
`resources/views/emails/partials/book.blade.php` (4), `resources/views/pdf/reservation-slip.blade.php`
(8), `resources/views/pdf/waiver-proof.blade.php` (2) y el tema del panel.

⚠️⚠️ **Lo que lo hace deuda y no un descuido**: viven en **correos y PDF**, o sea en las superficies
que **ninguna guarda de color mira** — `SidebarTokenBudgetTest` vigila el cajón y las de la landing
vigilan las hojas públicas, y un correo no pasa por ninguna de las dos. Por eso llevan ahí desde el
desbrandeo sin que nada fallara, y por eso lo vio un tercero antes que nosotros.

**Salida.** El canvas ya publica el reemplazo con su receta (`tintePapel`: el color al 14 % sobre
papel para el fondo y al 30 % para el borde, con **cuerpo y título en tinta** porque sobre una
superficie teñida solo aguanta la tinta). Entra con la Fase 4 (el cajón) o antes si se toca un
correo. ⚠️ Y conviene que salga con **una guarda que alcance correos y PDF**, o el siguiente los
vuelve a meter por la misma puerta.

---

⚠️⚠️ **DOS CORRECCIONES MEDIDAS A ESTA FICHA** (2026-09-10, `#489`), y la primera cambia lo que hay
que hacer.

**(1) «Del PRIMER cliente» es engañoso: lo verificado fue su PRESENCIA, no su procedencia.** Medido
con `git log -S`: los siete entraron en el **commit fundacional** (`48b08c97`, la importación del
repo de origen), así que *vienen de allí* — pero **no son la marca de aquel cliente**: `#b45309` y
`#92400e` son literalmente **`amber-700` y `amber-800` de Tailwind**, y el propio código lo dice en
un comentario (`PresentsOrderActions::priceDiffPreview()`). El resto tiene la misma pinta de valor de paleta
ajena elegido a ojo — la declaración de `--err-bg` lleva escrito «consolida `#fde8e8`».
▶ **Y eso cambia la salida**: no es «sacar la marca de un cliente del producto» (un buscar y
reemplazar), es que **las cuatro superficies semánticas sobre papel nunca se diseñaron**. El producto
necesita un juego por defecto **decidido y con su contraste medido**, y el paquete encima. Es una
tanda, no una línea. ⚠️ Y su valor **no puede copiarse del canvas tal cual**: eso metería en el
producto la paleta del SEGUNDO cliente, que es el mismo defecto por la otra puerta (§2 del carril).

**(2) No son siete piezas del mismo tamaño, son cinco triviales y dos grandes.** Medido:

| | Dónde vive | Tamaño |
|---|---|---|
| `#fbeaea` · `#e3b5b0` · `#8a2b22` · `#a93226` · `#e7f6ec` | **una sola aparición cada uno**, todas declaración de token en `site.css` | trivial |
| `#b45309` · `#92400e` | `site.css` + el tema del panel + **el correo del libro** + **`reservation-slip` (8)** + **`waiver-proof` (2)** + **tres sitios de `PresentsOrderActions.php`** | **14 apariciones fuera del CSS** |

⚠️⚠️ **Y ahí un token no vale, que es lo que hace grande a los dos ámbar**: un cliente de correo y
dompdf **no resuelven `var(--token)`** de forma fiable, así que en esas superficies el color tiene que
seguir siendo un literal. Lo que hay que decidir es **de dónde sale ese literal** — hoy está escrito a
mano en cada plantilla, y el producto ya tiene el mecanismo para lo contrario (`ThemeSettings`
compone el color de acción y su hover para los correos). Sin esa decisión, «pasarlo a token» es
imposible en la mitad de los sitios.

---

## ▶ ~~Baja · TRES bloques de la portada sin cabecera numerada~~ **RETIRADA: ARREGLADA — y su premisa era FALSA** (2026-09-10, `DECISIONES #495`)

La ficha decía que a las secciones 06, 07 y 08 les faltaba cabecera de bloque. **Solo era cierto de
la 07.** Las otras dos la tenían desde siempre, con **otro formato** —`══ SECCIÓN 06 · «RESEÑAS» ══`
en vez de `══ 06 · RESEÑAS ══`—, así que el `grep` que las buscaba (`══ 0[1-8] ·`) no las veía.

⚠️⚠️ **La lección es del instrumento, no del código**: *un `grep` que no encuentra no demuestra que
no exista*, y sobre esa lectura se llegó a escribir una ficha entera con la premisa equivocada. El
proyecto ya lo tenía escrito al revés (`#302`: «un `grep` que SÍ encuentra tampoco demuestra que
exista»); es la misma trampa por la otra cara.

▶ **Lo que se hizo**: unificar las cuatro cabeceras que divergían —02 traía relleno de más, 06 y 08
el prefijo «SECCIÓN», y la 07 era del formato antiguo y **sin número**— al mismo molde
`══ NN · RÓTULO · matiz ══`. Y, ya que la numeración quedaba completa, **ponerle guarda**
(`HomeSectionOrderTest::test_the_template_numbers_its_eight_blocks_in_order`): vigila que sean ocho,
que vayan en orden y que **cada una esté delante de su sección** — sin eso se desincroniza y una
numeración a medias engaña más que ninguna.

---

## ▶ ❗ ALTA · Las reseñas de Google casi nunca se ven: la caché está VACÍA el 83 % del tiempo (2026-09-10, `DECISIONES #499`)

**El owner preguntó por qué no se muestran las reseñas de Google en la página.** Medido, y la causa
principal no es el consentimiento: es una **incoherencia entre el TTL y la cadencia del refresco**.

    TTL de la caché           1.800 s  =  30 min   (`GoogleSocialProof::CACHE_TTL_SECONDS`)
    cadencia del refresco     3 h      = 180 min   (`routes/console.php`, `everyThreeHours`)
    ──────────────────────────────────────────────
    la caché está VACÍA       150 de cada 180 min  =  **83 % del tiempo**

⚠️⚠️ **Los dos razonamientos son correctos por separado, y por eso nadie lo vio.**

- El docblock del TTL dice: *«Es la mitad del refresco **(que va cada hora)**, no el doble. Con un TTL
  más largo que la cadencia, una respuesta vieja sobreviviría a un refresco fallido»*. Escrito
  suponiendo cadencia **horaria**.
- El scheduler va **cada tres horas**, con su propia aritmética bien argumentada: *«la llamada es una
  por idioma… a esta cadencia salen 24 llamadas al día, que caben en el tope de 50; **cada hora serían
  72 y el tope las cortaría a media tarde**»*.

▶ **La cadencia bajó de 1 h a 3 h por el tope de la consola de Google, y el TTL no se ajustó.** Cada
mitad es coherente con la premisa que tenía delante; juntas dejan la sección cayendo al respaldo
propio cinco sextas partes del tiempo.

**La segunda causa, que sí es de diseño y está decidida** (`#491`): las RESEÑAS necesitan
consentimiento de cookies de terceros —su avatar viene de `lh3.googleusercontent.com`— y la CIFRA no.
Sin aceptar cookies se ve la chapa de Google sobre opiniones propias. Eso es correcto y no se toca.

**Las salidas, y son del owner porque tienen matiz:**

1. **Subir el TTL por encima de la cadencia** (p. ej. 3 h 30 min). ⚠️ Acepta a sabiendas lo que el
   docblock quería evitar: una respuesta puede sobrevivir a **un** refresco fallido. Sigue siendo
   «temporary caching», que es lo que R2 permite.
2. **Bajar la cadencia a una hora** y subir el tope de la consola de Google de 50 a ~100 llamadas/día.
   ⚠️ Requiere tocar la consola, que es lo que `#491` dejó pendiente del owner.
3. Dejarlo y aceptar que las reseñas de Google son ocasionales. ⚠️ Difícil de defender: se paga el
   SKU más caro de Places (`reviews` es Enterprise + Atmosphere) para enseñarlas el 17 % del tiempo.

⚠️ **Verificado que el scheduler SÍ corre en producción** (`crontab` tiene `schedule:run`), así que no
es eso. Y en local se dispara a mano: `php artisan social-proof:refresh`.
