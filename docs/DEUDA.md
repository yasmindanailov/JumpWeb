# Deuda técnica — registro único

> Estado: vivo · Última actualización: 2026-08-24 (auditoría del desglose de dinero, `specs/desglose-dinero-cliente.md`) ·
> **Reconciliado contra el CÓDIGO fila por fila el 2026-08-14** (no contra el tracker: se comprobó
> cada afirmación con el árbol actual, no con lo que una fase dijo que iba a hacer) ·
> Se invalida si: una fase retira un ítem sin actualizar su fila.

Vista de conjunto con severidad; el detalle vive en el doc citado (aquí no se duplica).
«Sin plan» = ninguna fase del tracker la retira hoy: candidatas a backlog o a decisión.

> ⚠️ **Lo que enseñó la reconciliación del 2026-08-14, para quien mantenga este documento.** Tres
> filas llevaban RETIRADAS desde Fase 1 y Fase 2 sin tachar —la jurisdicción legal ya es un ajuste de
> BD, `app/Support` **ya no existe** y el wordmark de los correos sale de `business.name`—, y cinco
> cifras habían envejecido en la dirección contraria a la que uno supondría: `Purchase` **creció** de
> 1.859 a 1.904 líneas mientras Fase 4 lo vacía, y la suite pasó de 213 a 270 ficheros. **Una fila que
> nombra un fichero o una cifra hay que abrirla, no creerla**: la ruta de la «cuarta copia de la
> aritmética» apuntaba a una carpeta en la que ese fichero ya no está.

## Alta

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| ~~Dependencias con avisos de seguridad~~ **RETIRADA 2026-08-13** (`DECISIONES #22`) | `composer audit` (2026-08-13): **26 avisos en 5 paquetes** — `guzzlehttp/guzzle` 9 (1 alta), `league/commonmark` 6 (4 altas, DoS y bypass de filtro de enlaces), `dompdf/dompdf` 6, `guzzlehttp/psr7` 4, `laravel/framework` 1. Todos son transitivos. | La app hace peticiones HTTP salientes (Redsys, Turnstile), renderiza Markdown y genera PDFs: las tres superficies afectadas están EN USO | **Un `composer update` normal los resuelve**: verificado con `--dry-run` que sube commonmark 2.8.2→2.10.0 (los avisos son `<2.9.0`), guzzle 7.10.4→7.15.3, psr7 2.10.1→2.13.0, dompdf 3.1.5→3.1.6 y framework 13.11.2→13.25.0 — **sin cambiar ninguna restricción de `composer.json` ni añadir nada**. ✅ **hecha**: `composer audit` y `npm audit` → 0; suite 2186 verde, Pint sin churn, verificadores sobre MySQL y PDF real generado |
| ~~Avisos npm en el árbol de build~~ **RETIRADA 2026-08-13** (`DECISIONES #25`) | `npm audit` daba **5 avisos — 2 críticas, 3 altas** (`shell-quote` vía `concurrently`, `vite`, `postcss`, `nanoid`), aparecidos DESPUÉS del saneado de `#22` y sin que `package.json`/el lock cambiaran | Cadena de construcción de assets; no el bundle servido a usuarios | ✅ **hecha**: `npm audit fix` sin `--force` → 0 avisos, `package.json` intacto (solo el lock); verificado con `npm run build`, suite 2224 verde y los assets servidos uno a uno por HTTP |
| **`docs-check.sh` no valida CONTENIDO, solo estructura y citas** | La auditoría del 2026-08-19 (`DECISIONES #103`) midió **794 afirmaciones** de la doc contra el código y encontró **52 desfases** con el gate **en verde** — incluidos uno de DINERO (`DEPOSITO` describía la guarda de reembolso más débil de lo que es), una invariante contradicha (`REDSYS` §14 mandaba deshacer `PAY-15`) y tres símbolos FANTASMA que nunca se portaron del origen. Ninguno se detecta leyendo: los siete graves salieron de EJECUTAR la afirmación | Todo agente arranca leyendo doc; un desfase de dinero o de invariante induce a escribir código incorrecto **y el gate lo bendice** | ⚠️ **La ficha sigue ABIERTA** —las 794 afirmaciones de `#103` siguen sin instrumento—, pero su **candidato acotado está CERRADO** (`#159`, 2026-08-25): `docs-check` gana el **check 8**, que exige que toda clase `*Test` citada en `INVARIANTES.md` exista en `tests/`. ❗ **Lo que lo hacía imposible hasta hoy no era la falta de ganas: una mención HISTÓRICA y una cita VIVA se escriben igual.** Los separa una convención nueva —el TACHADO, `~~ClaseTest~~`— documentada en `CONVENCIONES §4`. Verificado por mutación en sus DOS mitades. ⚠️ **Y la medida de partida de `#121` estaba caducada**: al re-medirla hoy había **CERO citas muertas** —`PAY-04` y `SEC-06` ya se re-apuntaron el 22 y el 23— y el primer instrumento que dio «cuatro» contaba menciones históricas y partía mal las filas con `\|` escapado. ▶ Historia: **candidato medido** en `DECISIONES #121`, 2026-08-22. Un `grep` de dos líneas encontró **dos citas muertas en `PAY-04`** —el reintento de pago— y una llevaba **un día** ahí sin que nadie la viera: el gate valida rutas de fichero, no nombres de clase sueltos. La cobertura estaba intacta, pero así es como una invariante se queda sin red sin hacer ruido |
| ~~**No hay mecanismo para configurar SECRETOS de BD**~~ **RETIRADA 2026-08-20** (`DECISIONES #109`) | Se leen solo de `settings` y el panel **no los edita a propósito** (docblock de `Settings.php`: «NUNCA editables aquí»). Medido el 2026-08-19: la única vía es escribir la fila a mano por `tinker`/SQL en el servidor. `ENTORNOS.md` §3 decía «van por el panel» y era falso (`#107`) | **Cada instalación de cliente lo necesita** —sin claves el anti-bot se autodesactiva en silencio— y es un paso a mano, no probado y fácil de olvidar | ✅ **hecha**: `app:set-setting`, idempotente, con guardas para las tres claves que cuestan dinero o corrompen la numeración, enmascarado de secretos (se ejecuta por SSH y su salida acaba en el log del despliegue), 13 casos y 5 mutaciones muertas |
| **`PAY-08` sin verificar de punta a punta: falta un `0900` REAL de Redsys** | La integración del reembolso REST **está verificada** contra el sandbox con las credenciales del owner (comercio `263100000`, terminal 45): firma `HMAC_SHA512_V2` aceptada por el banco, y el control con clave errónea sale en rojo desde `#127(b)`. Lo que NO se ha probado es un `Ds_Response=0900` de verdad, que exige una autorización previa en el sandbox | Es el único hueco que queda del reembolso REST. Todo lo demás de la cadena —conectividad, credenciales, firma, formato y parseo— está medido | **Pendiente del OWNER**: un pago de prueba con tarjeta en el sandbox desde el navegador (staging), y después `redsys:verify-sandbox --gateway-order=…`. ⚠️ Una medición anterior dio el sandbox por INALCANZABLE y era un error: se probó el puerto 443 y Redsys sirve el suyo en el **25443** |
| ~~**El DESGLOSE de dinero que ve el cliente descuadra**~~ **CERRADA 2026-08-24** (`DECISIONES #127`) | Lo que había: la API publicaba **2 de las 6** dimensiones, faltaba «Pagado en el parque» y la columna dejaba de sumar en cuanto la reserva se disfrutaba. Y debajo, **cuatro defectos de DOMINIO** que dos auditorías anteriores no llegaron a construir —un pedido cancelado que no anunciaba lo que se debía devolver, un reembolso total que dejaba el valor intacto, un pedido nunca pagado que declaraba dinero cobrado en puerta, y un reembolso total sin atribuir a ninguna reserva— más un **agujero de ingresos**: mover la fecha no re-tarificaba, así que comprar el día barato y pedir el cambio al sábado salía gratis | — | ✅ **Tandas A y B ejecutadas.** Medido al cerrar: **0 de 23** gestiones del panel dejan la columna ilegible (eran 18) y el eje del valor **cierra en 50 de 50** pedidos por HTTP real. Guardado por `PAY-16`, `PAY-17`, `PAY-18` y `LedgerSingleSourceTest`. ▶ **Queda la tanda C** (pantalla propia de «Mis pedidos»), que no toca dinero |
| God-class `ViewOrder` | `app/Filament/Resources/Orders/Pages/ViewOrder.php`. ⚠️ **La cifra de esta fila estaba CADUCADA**: re-medido el 2026-08-26 son **5.280 líneas**, no 5.029 — creció **+251 en doce días**, porque cada tanda de dinero (`#146`, `#149`, `#150`) entra por aquí. 98 métodos · **8 invocaciones reales** a dinero/aforo (⚠️ la fila decía «25 llamadas»: el grep contaba comentarios y definiciones, spec §8.6) · `executeItemEdit` sola tiene **~472 líneas** | Toda operación de dinero/aforo del panel roza INVARIANTES §1/§2. ⚠️ **Y el panel tiene su PROPIA disponibilidad**: cero `Booking\Contracts\*`, `Slot::query()` a mano — misma familia que la «cuarta copia de la aritmética de cesta». ⚠️ Y una SEGUNDA duplicación: `lockZoneDaySlots` re-implementa a mano la receta anti-sobreventa de `AFORO-01` (spec §8.9) | 🟦 **YA TIENE PLAN**: `docs/specs/desmontar-view-order.md` (2026-08-26), ✅ **revisión adversarial HECHA y CORRECCIONES INCORPORADAS el 2026-08-26 (spec §8 · `#167`/`#168`): el cuerpo describe el código real**; ❗ pendiente SOLO el ✅ del owner. ▶ **Lo que lo hace pensable, medido**: solo **5 propiedades públicas** —4 del calendario—, así que es GRANDE pero **no enmarañado**; y hay red, **16 ficheros / ~285 casos**. Cuatro extracciones por riesgo creciente, el dinero la última |
| ~~God-class `Purchase` + puente Alpine~~ **RETIRADA 2026-08-21** (`DECISIONES #111`, `#112`) | Eran **1.904 líneas** en `app/Livewire/Tickets/` (re-medido 2026-08-14; 1.859 el 13, 2.049 antes) más su blade de 713 y el puente `Alpine.store('purchase')`↔`$wire.step` sincronizado a mano. ⚠️ **Creció mientras Fase 4 lo sustituía** —cada paso transcrito añadía su espejo en el motor viejo— y esa es la lección: un sustituto a medias es DOS implementaciones, no una y media | Todo el checkout web | ✅ **hecha** en 4.7·2b·3: borrados el componente, su vista, el placeholder y el flag `SidebarSettings`; el cajón SPA queda como motor ÚNICO. La condición no fue el contador de `PurchaseRetirementTest` sino «todas las entradas clasificadas» (`#98`) más los cuatro caminos de navegador verificados en staging (`#100`→`#110`). ⚠️ Queda **`Sidebar.vue`**, el segundo objeto-dios, con su propia fila más abajo |
| ~~Sin morphMap~~ | **RETIRADA en Fase 2** (2026-08-12): `enforceMorphMap` con alias para los 30 modelos + migración de datos + barrido de `::class` en columnas morph + `MorphMapTest` (verificado también en MySQL dev: 0 FQCN restantes) | — | ✅ hecha |
| ~~Datos de negocio del cliente origen~~ **RETIRADA en Fase 1** (2026-08-12), verificado contra el código el 2026-08-14 | La jurisdicción **ya no está quemada**: es el ajuste `legal.jurisdiction` en BD (hoy `[PENDIENTE: partido judicial]`) que `LegalIdentity` interpola con el token `:jurisdiction`, y `ProductionSeeder` es la instalación **en frío neutra** —el seed real de SaltoPark vive aparte, en `LandingContentSeeder` (`DECISIONES #12.c`)— | — | ✅ hecha |
| ~~**El alta SUELTA no termina con el anti-bot activo: el widget de Turnstile NUNCA se monta en `/registro`**~~ **RETIRADA 2026-08-26** (`DECISIONES #171`) | Medido en headless el 2026-08-26 (`DECISIONES #169`, spec del waiver §9.10): en `/registro` el cajón nace abierto y `RegisterForm` se monta a los **756 ms**; `GET /config` —que trae la `turnstile_site_key`— responde a los **794 ms**. `RegisterForm.vue:97-104` llama a `mountTurnstile(captchaEl, {sitekey})` **solo en `onMounted`**, con la clave aún vacía → apaño inerte. Cuando la clave llega (`PurchaseSection.vue:221` → `authStore.setSignupSiteKey`), el `v-if="turnstileSiteKey"` pinta el contenedor —está en el DOM, **vacío**— y nadie monta el widget: ni script de Cloudflare, ni iframe, ni token. El envío va con `turnstile_token: ''` y el servidor responde **422 «No hemos podido verificar que no eres un robot»**, dos veces de dos. ⚠️ En local las claves de prueba se pusieron el 26/08 a las 14:48; el guion V17 del 23/08 corrió **sin** anti-bot y por eso nunca se vio. El paso 5 del embudo NO lo sufre (monta el formulario mucho después de `/config`) | **Toda instalación con claves de Turnstile** —staging las tiene— desde `#122` (2026-08-23): nadie puede crear cuenta desde `/registro` ni desde «Crear cuenta» del nav si llega antes que `/config`. Un fallo que ninguna guarda ve: el diff de árbol no monta el widget y la suite nunca siembra las claves | ✅ **hecha** (`#171`, 2026-08-26 noche, carril A): la decisión vive en el MÓDULO, no en el componente — `mountTurnstile` acepta **funciones** para el nodo y la clave, las re-lee en cada sondeo, **no inyecta el script de Cloudflare hasta tener clave y nodo delante** (con el anti-bot apagado la clave no llega nunca y no se carga un tercero), y se rinde en silencio a los 60 s. `RegisterForm` solo cambia dos líneas (pasa `() => captchaEl.value` y `() => props.turnstileSiteKey`; sigue en 37 de 40). **5 casos nuevos en `turnstile.test.js` (18), 4 mutaciones, las 4 muerden**; chunk +0,13 KiB (225,85 / 226). **Verificado en headless con Turnstile ENCENDIDO**: script inyectado a 1.323 ms (tras `/config` a 1.178), token a 3,1 s, `POST /auth/register → 201`, correo en Mailpit |
| ~~**El alta MANUAL del panel registra una firma «declarada por el operador» que el operador NO declara**~~ **RETIRADA 2026-08-26** (`#178`: casilla «Le he enseñado al cliente la exención vigente y declara que la acepta» con el texto a la vista en el modal del alta manual; sin marcarla NO hay firma — `CustomerRegistrar::register(…, waiverDeclared:)`; 4 casos, 2 mutaciones que muerden) | Revisión adversarial del waiver (`#169`, spec **§10.1**, confirmado 2/2): `CreateManualOrderPage.php:252` tiene **una sola casilla** (`privacy_informed`, la de privacidad; `grep -i waiver` → 0) y `CustomerRegistrar.php:109-114` firma en modo interno con versión publicada y operador con sesión — sin enseñar el texto ni pedir nada. El PDF imprime «el operador X declara que la persona aceptó el texto en persona» (§8.4 `[DECIDIDO owner]`): le atribuye un acto que no hizo | Cualquier alta presencial en una instalación en modo `interno` con versión publicada (hoy ninguna). El día que haya que enseñar el PDF, el operador no podrá sostener la declaración — es §4.5 («fingir lo contrario es peor que decirlo») aplicado al operador | ✅ **`[DECIDIDO owner, 2026-08-26]` (spec §7·4): casilla del waiver en el alta manual, con el texto vigente a la vista; sin marcarla, NO hay firma.** Código pequeño (un campo en `CreateManualOrderPage`, la condición en `CustomerRegistrar`, su test) — carril A, detrás del anti-bot |

## Media

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| Suite ciega a carreras InnoDB | SQLite `:memory:`; doble-cobro/sobreventa solo en comandos on-demand; el pre-push exige el flag `VERIFY_CONC=1`, no ejecuta los comandos | Un cambio de locks pasa verde y sobrevende en producción | Sin plan (mitigada por la guarda del hook; desde Fase 3 · paso 0 `CriticalPathGateTest` verifica además que esa guarda sigue cubriendo lo que debe). ⚠️ La mitigación es **parcial**: ver la fila de abajo |
| ~~**`purchase:verify-oversell` solo verifica ENTRADAS: el aforo de PACKS nunca se ha probado bajo concurrencia**~~ **RETIRADA el 2026-08-25** (`DECISIONES #147`) | Ya no es cierto: el comando tiene **tres escenarios** —`entry`, `pack` (cupo de FIESTAS) y `pack-guests` (cupo de INVITADOS)— y los tres se ejecutaron sobre **MySQL real** con 8 y 16 procesos concurrentes. **Los dos topes del pack aguantan**: 1 compra creada y N−1 `sold_out` en todas las pasadas. ⚠️ **Y el verde está respaldado por su CONTROL NEGATIVO**: retirando el `lockForUpdate()` de `OrderCreator::lockSlots`, el mismo instrumento cazó **8 fiestas donde cabía 1** y **48 invitados donde caben 10** | — | ✅ hecha, y **los tres huecos que dejó también** (`#148`): son **cinco** escenarios, con `pack-prep` (tramo multi-franja y montaje/limpieza **ACTIVOS**) y `mixed` (los dos pools a la vez), los dos con su control negativo. ❗ **Y `mixed` destapó que los dos aforos NO son independientes**: una fiesta de 8 invitados deja una franja de 10 plazas en 2. Lo fija `PackConsumesEntrySeatsTest`; **si debe ser así es decisión del owner** |
| **Cinco secuencias transversales del embudo, todavía en el componente** | `addToCart` (27 líneas, toca 5 dominios), `confirmReservation` (26), `retryPayment` (25), `poll` (17) y `showLineProblems` (17). Son las transiciones del embudo: piden a la API, mueven el paso y tocan varios stores a la vez. `sections/PurchaseSection.vue` se queda en **438 líneas** por ellas | Nada, hoy. `CE-6` mide sobre todo las llamadas a la API, y ya son **2** (el montaje) | **Sin plan, y a propósito.** Medido el 2026-08-22: las cinco viven **íntegramente en el embudo** —cesta, desenlace, catálogo, fecha, hora—, así que el ÁREA DE CLIENTE no las rozará y **su coste no crece con el tiempo**: es deuda SIN intereses. Sacarlas dentro de un año cuesta lo mismo que hoy. ⚠️ Y `addToCart` toca **cinco** stores: en un módulo plano serían cinco dependencias inyectadas, donde la ceremonia empieza a competir con el beneficio (`DECISIONES #119(e)`) |
| **El techo del bundle tiene margen sin usar** | Subido de 160 a **168 KiB** el 2026-08-22 con su medición (ocho stores ≈ 8,5 KB de fuente → +3,9 KiB minificados, ~0,5 KiB por store), dejando sitio para lo que faltaba de la reorganización | `SidebarBundleBudgetTest`: un techo con holgura sobrante deja de apretar, que es lo que un presupuesto no debe hacer | **Al cerrar la reorganización**: volver a medir el chunk y BAJAR el número a lo que de verdad ocupe. Está escrito en el propio test |
| **La mitad de ZONA de los enlaces profundos** | `catalog.js::toItem()` **descarta el campo `zone`** que la API sí publica (`GET /catalog/products` lo trae con `id`/`slug`/`name`), y `CatalogStep.vue` no lo menciona. El motor retirado hacía scroll a la ZONA concreta (`catalog-open-entries-zone`); la SPA aterriza en la sección «Entradas» y para ahí. Se devuelve marcado `exact: false` en vez de fingir paridad (`intent.js`, `DECISIONES #117`) | El CTA «Comprar» de una tarjeta de zona en la home. **No falla, hace menos**: el cajón abre y muestra las entradas, pero el cliente tiene que buscar la suya | **Sin plan — es una DECISIÓN, no una tarea.** Devolver la zona al modelo cambia el árbol que pinta `CatalogStep.vue`, y eso **toca el manifiesto de árbol CONGELADO**. ⚠️ Y hoy **no se puede verificar en staging**: su seed no tiene productos `type=entry`, así que la sección «Entradas» ni se pinta |
| ✅ ~~**El bloque de cuenta del cajón es Livewire, no Vue**~~ — **RETIRADA el 2026-08-23** | El bloque lo pinta ahora `sidebar/account/AccountPanel.vue`, teletransportado al hueco que el layout emite; la clase Livewire y su Blade **ya no existen** y la página sirve **0 atributos `wire:`**. `specs/account-context-vue.md`, `DECISIONES #123`. ⚠️ **Lo que NO cambió, y conviene no leerlo de más**: Livewire sigue instalado y sigue trayendo Alpine, así que `@livewireScripts` **no se puede retirar** — lo que cambia es que ahora es la fuente ÚNICA, y su guarda (`SidebarMountTest::test_livewire_scripts_are_still_served`) **por fin discrimina**: medido, retirarla la pone roja, y hasta el 2026-08-23 no lo hacía |
| El ámbito del presupuesto de tokens se define RASCANDO una plantilla | **Re-medido con la lógica real del test el 2026-08-15** (`DECISIONES #74`), corrigiendo una lectura anterior: `SidebarTokenBudgetTest` deriva el ámbito CSS del cajón de las clases que aparecen en `purchase.blade.php`. Al añadir las fuentes Vue el ámbito pasa de **1.073 a 1.307 declaraciones** y el presupuesto de 75%→70% y de 3→4 colores crudos. **Pero lo que entra NO es CSS del cajón mal tokenizado**: son 60 declaraciones del bloque `auth__*`/`form__*`/`check`/`pwd-input` —los formularios de login y alta, compartidos con el modal de auth y con `/mi-cuenta`— más `eyebrow`, `icon` y `tk`, que son del sitio. **Del cajón solo había UNA**, el velo `.jj-loading`, ya tokenizada | Rascar una plantilla mete en el «ámbito del cajón» toda clase compartida que su marcado use. Con Vue eso crece, porque los formularios de auth viven DENTRO del cajón | **Antes de 4.7·2b·3, y es una DECISIÓN, no una tarea**: definir el ámbito por las familias propias del cajón (`purchase__`, `cart__`, `bk-`, `cal__`, `wiz__`, `qtybox`, `catalog-acc`, `jj-`, `sidecart`…) en vez de por lo que diga una plantilla — así el presupuesto deja de depender del motor. Tokenizar el bloque de formularios del sitio es **Fase 5** («theming como paquete coherente»), no la retirada |
| Suite no auditada contra la FECHA | **Medido el 2026-08-15** (`DECISIONES #64`): al cruzar medianoche UTC aparecieron **3 fallos que nadie había causado** —los 2 casos del calendario de `SidebarDomContractTest`, cuya foto congelada caduca **cada día**, y `CatalogTest`, que falla **sábados y domingos** porque su fixture declara una tarifa `weekdays: [0,6]`—. Los tres se arreglaron congelando el reloj. **Lo que NADIE ha hecho es barrer el resto**: ningún mecanismo impide que otro test solo pase ciertos días | El gate de `pre-push`: un rojo que no viene del cambio hace perder una sesión entera —y, peor, enseña a regenerar fixtures sin mirar, que es como se pierde un contrato congelado en silencio— | ⚠️ **La ficha sigue ABIERTA, pero por un motivo distinto al de antes** (`#162`, 2026-08-26). El barrido **se ha hecho**: `Tests\TestCase` lee `TEST_CLOCK` y `scripts/audit-clock.sh` corre la suite entera en diez fronteras, con las fechas **relativas a hoy** para que la lista no caduque. Encontró **DOS** defectos reales y los dos están arreglados: una ventana de temporada escrita a mano que **iba a tumbar el gate el 2026-09-01** —seis días después de medirla— y la **aritmética de meses de PHP, que desborda** (`2026-08-31 +3 meses = 2026-12-01`; `2026-12-31 +2m` y `+3m` caen los dos en marzo). Detalle en `TESTING.md` §2.septies. ▶ **Lo que queda abierto es el DISPARO**: no hay CI que lo ejecute, así que depende de que alguien corra el script al cerrar una tanda con fixtures de calendario. Medido: meterlo en el `pre-push` multiplicaría por diez el tiempo de cada push. ⚠️ **Y un hueco declarado**: el reloj no llega a `tests/Unit/` (extienden `PHPUnit\Framework\TestCase` por `CONVENCIONES §3.ter`). Medido: 4 ficheros, ninguno con calendario. ▶ **Y `#164` cerró la caza de `#97`**: el reloj **queda EXONERADO** —10 fronteras congeladas, 4 cruces de medianoche a mitad de pase con el modo nuevo `TEST_CLOCK_START`, y la suite entera en 2 órdenes aleatorios: todo verde—, así que la explicación que la doc le daba no sobrevive a la medición. **La causa sigue sin nombre**, pero el `pre-push` ya **anuncia la ruta de su log al caer** (antes sobrevivía y nadie lo decía), así que la próxima vez se responderá con datos |
| `$guarded = []` masivo | **24 de 30 modelos sin `$fillable`** y **27 declaran `$guarded = []`** (re-medido 2026-08-14; la fila decía 22). `User` sigue sin ninguno de los dos | Mass assignment en código nuevo; `preventSilentlyDiscardingAttributes` solo en no-prod | Sin plan |
| Branding residual | **RETIRADO en Fase 1** (2026-08-12): quedan solo 6 líneas intencionales (regla 7, guard-bash, cron de ejemplo neutro, y el `assertDontSee('jumpingjump.com')` que actúa de GUARD) + fixtures `JJ-…` autoconsistentes en tests (cosmético, sin valor de marca) y códigos forenses en comentarios | — | ✅ hecha |
| ~~`app/Support` cajón desastre~~ **RETIRADA en Fase 2** (2026-08-12), verificado el 2026-08-14 | **La carpeta `app/Support` ya no existe**: sus 57 ficheros se repartieron entre los cinco módulos de `app/Domain/` con frontera ejecutable | — | ✅ hecha |
| Composer global `'*'` | `View::composer('*')` vigente, memoizado por request (sin memo: ~1.900 queries/GET) | El rendimiento de toda la web pende de un memo; acopla vistas al provider | Fase 5 |
| **2FA de admin inexistente** | Ninguna implementación; solo un comentario «queda para sus sub-fases» en la página Settings | Panel con PII de menores + reembolsos protegido solo por contraseña | **Sin plan — decisión de owner** |
| Hueco test: alcance del lock zona/día | `lockZoneDaySlots` sin assert de ALCANCE (SQLite no reproduce la carrera) | Fase 2 podría estrechar el lock sin que nada falle → sobrellenado | Sin plan (INVARIANTES AFORO-05 ⚠️) |
| Hueco test: no-legibilidad de secretos | Solo se asevera la escritura; nada asevera que el form no CARGUE `redsys_secret_key` | Una regresión pintaría el secreto en el HTML del panel | Sin plan (INVARIANTES SEC-11 ⚠️) |
| Una sola factory | `database/factories/` = solo `UserFactory` para 30 modelos y **270 ficheros de test** (re-medido 2026-08-14; eran 213). Fixtures a mano: **1.252 `::create(`** (eran ~1.100) | Cada test nuevo escribe su propio fixture; helpers `admin()`/`staff()` duplicados | Sin plan (ver `TESTING.md` §datos) |
| Vocabulario de sector en código | `PuertaSettings` vigente y `waiver` en **14 ficheros** de `app/` y `routes/`; zonas/cumpleaños en clases, rutas y BD (verificado 2026-08-14) | Multi-sector exige renombrar clases y valores morph | ⚠️ **Fase 1 y Fase 2 están CERRADAS y no lo retiraron**, así que la fila decía una fase que ya pasó: hoy es **sin plan**. Fase 1 decidió el criterio (`DECISIONES #12.a`: español ahora) y Fase 2 movió los ficheros sin renombrar el vocabulario |
| «Precio desde» no filtra tarifas activas | El mínimo sale de TODOS los `prices` del producto (`CatalogReader::fromPriceCents`, y así era ya en `Purchase`). `RateResolver` solo elige entre tarifas `is_active`, así que un precio colgado de una tarifa desactivada se anuncia pero no se puede comprar | Una instalación que desactive una tarifa barata sin borrar sus precios anunciaría un «desde» inalcanzable — en web y en API por igual | **Sin plan — decisión de owner** (toca lo que se le anuncia al cliente; no se cambió al extraer el read-model, Fase 3 · paso 1b) |
| Cacheabilidad del catálogo público sin decidir | `GET /api/v1/catalog/*` responde con el `Cache-Control: no-cache, private` por defecto de Symfony; ni CDN ni proxy pueden cachearlo | La superficie pública de más tráfico previsible va sin caché HTTP; hacerlo cacheable exige decidir el `Vary` del idioma | Sin plan (Fase 3 · paso 1b lo dejó anotado, no resuelto) |
| Cuarta copia de la aritmética de cesta en el panel | ⚠️ **La ruta de esta fila estaba mal**: es `app/Filament/Pages/CreateManualOrderPage.php`, no `Resources/Orders/Pages/` (corregido 2026-08-14). `CreateManualOrderPage::estimateLineCents()` + `cartOnlineDueCents()` + `cartHasDeposit()` reproducen las reglas de precio, complementos y señal que desde Fase 3 · paso 4a viven en `Booking\Contracts\CartPricing`. **Comprobado caso a caso el 2026-08-13: hoy coinciden** en las dos ramas de la Opción A (#225) | Un cambio de regla de señal o de complementos habría que portarlo a los dos sitios; el paso 2 ya encontró dos copias «iguales» que aplicaban políticas distintas sin que nadie lo hubiera decidido | Sin plan — **no se unificó a propósito** en el paso 4a: el panel calcula al AÑADIR la línea y el contrato al PINTARLA, así que migrarlo cambia su conducta ante un cambio de precio a media cesta |
| Dos normalizaciones del MISMO teléfono | `contact.phone` se normaliza de dos formas distintas según quién lo pinte: `preg_replace('/\s+/')` en el aviso de pausa del sidebar (hoy en `Platform\Services\MaintenanceSettings::reservationNotice()`) y `preg_replace('/[^0-9+]/')` en el composer global de vistas. **Medido al extraer el aviso en Fase 4 · paso 4.0b** | Un teléfono con puntuación (`968-12-34-56`) produce DOS `tel:` distintos en la misma web: uno conserva los guiones y el otro no. Solo se nota en instalaciones que puntúen el número | **Sin plan — decisión de owner**: unificarlas CAMBIA el `tel:` renderizado, así que es conducta de producto y no un refactor. Se dejó cada una como estaba a propósito al centralizar el aviso, para no colar un cambio visible dentro de una extracción |
| Escala tipográfica del sidebar sin canonizar | ⚠️ **No re-medido en la reconciliación del 2026-08-14**: acotar el recuento «al cajón» exige el mismo alcance que usa `SidebarTokenBudgetTest`, y un `grep` sobre `site.css` entero da otra cosa (55 valores en 259 declaraciones, todo el sitio). La cifra de abajo es la del 2026-08-14 en su medición original. El cajón usa **17 tamaños de letra distintos** en 74 declaraciones, con pasos de 1px (9, 9.5, 10, 11, 11.5, 12, 13, 14, 15, 16, 17, 18, 20, 22, 26, 40). Desde Fase 4 · paso 4.0c son tokens (`--fs-N`) sobre una unidad, así que **cambiarlos es tocar el `:root`, no 74 sitios**; lo que no existe es una escala de diseño con pocos pasos. **Medido el 2026-08-14**: canonizarla a 6–7 pasos movería el **52-55%** de los usos (47px de desviación total) | La coherencia tipográfica del cajón. No muerde a nadie hoy: el diseño actual es el que está en producción y funciona | **Sin plan — decisión de owner**: redondear es **rediseño visual**, que `sidebar-spa.md` §2 declara fuera del alcance de Fase 4. La indirección ya está hecha, así que el día que se decida es un cambio de N líneas en `:root` y una revisión visual, no un barrido |
| Las fechas del cajón las formatean dos motores distintos | El servidor usa Carbon (`isoFormat('ddd D MMM')`) y el cajón SPA `Intl`, porque §4.5 dejó la i18n de fechas **sin canal**. **Medido el 2026-08-14** con el patrón de formato fijado por nosotros (no por un preajuste de `Intl`, que en inglés invierte día y mes): `en` y `fr` salen **idénticos**; solo el español difiere — servidor `Sáb. 5 sep.`, cliente `Sáb 5 sept` (ICU no abrevia con punto y usa `sept`). Afecta a la línea de contexto de la banda de progreso y a las cabeceras del calendario | Solo al texto visible, y solo en español. No hay dato incorrecto: mismo día, mismo orden, misma hora | **Fase 4, si se decide**: se cierra publicando la etiqueta ya formateada en la oferta de días (`GET availability/{producto}/dates`), que es un cambio de CONTRATO y no de transcripción — por eso no entró en 4.3·1. Vigilada por `SidebarProgressParityTest`, que exige identidad en `en`/`fr` y fija la excepción española: si desaparece, el test obliga a retirar esta fila. ⚠️⚠️ **2026-08-22: cerrarla es ahora MÁS BARATO y más URGENTE.** Más barato porque el mecanismo ya existe —`DisplayTime::dayLabel()` es la fuente única y `me/orders`/`me/reservations` ya publican `date_label`, con el patrón sentado (`specs/area-cliente.md`)—: aquí solo falta `OfferedDateResource` y que `progress.js` use la etiqueta en vez de componerla. Más urgente porque el área de cliente **hace visible la incoherencia**: en español, el embudo pinta «Sáb 5 sept» y la zona «Mis reservas», «Sáb. 5 sep.» — dos pantallas del mismo cajón. ▶ **No se cerró en el paso 3 a propósito**: tocar la banda de progreso, su paridad y su manifiesto dentro del paso del área de cliente mezclaría dos trabajos, que es la regla que ha ordenado la fase entera |
| El formato de importe está quemado en español | `number_format($c / 100, 2, ',', '.').' €'` — coma decimal, punto de millares y el símbolo detrás— aparece **quemado** en `AddonResolver::viewModel()` (la nota de precio de cada complemento) y en una decena de sitios de `purchase.blade.php`. No mira el locale ni la moneda de la instalación. **Medido el 2026-08-14** al publicar los complementos por API: con `Accept-Language: en` el nombre llega traducido (`Grip socks`) y el precio sigue saliendo `2,00 €` | Toda instalación que venda fuera de un locale con esa convención, y ahora también por API: el endpoint hereda la nota tal cual. Hoy no muerde porque el producto solo se ha instalado en España | **Sin plan — decisión de owner**: cambia lo que ve el cliente en las tres superficies a la vez y arrastra la moneda (hoy `EUR` implícito), así que es trabajo de producto —no un refactor— y no se cuela dentro de un paso de API. Preexistente: el endpoint no lo introdujo, lo destapó |
| Dos formas de emparejar una respuesta del evento con su etiqueta | Desde Fase 4 · paso 4.0b·4b la composición es `TicketType::eventAnswers()` (esquema actual, filtrable por fase), y la usan la compra web y `GET orders/{code}/event-data`. `ReservationSlip::eventDataRows()` **sigue con la suya**: añade al final las claves HUÉRFANAS —las de campos que el pack ya no declara, con la etiqueta derivada de la clave— y castea valores no escalares con su propio `stringifyValue()` | La hoja de sala del panel. Un cambio en las reglas de composición (orden, saneo, i18n de etiquetas) habría que portarlo a los dos sitios | **Sin plan — no se unificó a propósito**: la divergencia es INTENCIONADA (el operador necesita ver todo lo que el cliente contestó, aunque el esquema haya cambiado; el cliente ve su reserva), y una respuesta huérfana no tiene `stage`, así que no se puede acotar a una fase. Unificar exigiría decidir qué hace la hoja de sala con las huérfanas, que es conducta del panel |
| N+1 de precio por complemento en `AddonResolver` | `AddonResolver::resolve()` pide el precio de cada complemento con `RateResolver::priceCents()`, que son 2 consultas por llamada (tarifa del día + importe). **Medido 2026-08-13 vía `orders/quote`: 1 complemento → 8 consultas, 3 → 12, 6 → 18** | Lo ejecuta también `OrderCreator` **dentro de la transacción que sostiene los locks de aforo**, donde alargar la txn es peor que en un presupuesto | Sin plan — es código de dinero compartido; su arreglo exige paso propio y los dos verificadores. `ApiOverheadTest::test_the_known_cost_per_addon_does_not_get_worse` impide que EMPEORE |
| Un solo driver de pasarela (Redsys) | ~~La secuencia duplicada en la capa de entrega~~ **RESUELTA en el cierre de Fase 3** (`DECISIONES #37`): «admitir → crear → abrir cobro» vive en `Booking\Services\CheckoutOrchestrator` tras los puertos `ReservationCheckout` y `PaymentInitiation`, y `CheckoutSequenceTest` impide que reaparezca fuera del dominio. **Lo que queda es la otra mitad**: `PaymentInitiation` lo implementa solo `PaymentInitiator` (Redsys), no hay selección de driver por configuración ni un segundo driver | Un cliente fuera de España no se puede instalar sin escribir un driver nuevo. Ya NO hay riesgo de que una superficie se salte un paso: eso lo cierra el orquestador | **Fase 6**, con la app móvil — decisión del owner del 2026-08-13: hacer ahora la mitad MEDIDA (5 puntos de llamada duplicando el orden) y dejar el driver para cuando exista un lector real que revele su forma. El puerto ya está, así que el driver nuevo se enchufa sin tocar a ningún consumidor |
| Post-form web: el binding implícito filtra si una reserva existe | La ruta web usa *route model binding* (`OrderItem $reservation`), así que un id inexistente responde **404 antes** de comprobar la autorización, mientras que uno existente responde 403. Esa diferencia ES el oráculo que la escalada 403→410→404 quería evitar. **Medido 2026-08-13** al hacer la versión de API, que resuelve la reserva a mano y responde **el mismo 403** en ambos casos | Un tercero puede comprobar qué ids de reserva existen comparando códigos de estado. No expone datos, sí la existencia | Sin plan — no se tocó en el paso 5 porque cambia la conducta de una superficie en producción; la corrección es idéntica a la de la API (resolver a mano) y está a la vista en `Api\V1\GuestFormController` |
| Migraciones con lógica de datos del origen | **8 de 72** migraciones con backfill/seed dentro + **4 clases** `Legacy*`/`*Backfill` vivas (re-medido 2026-08-14; ⚠️ ya **no** en `app/Support`, que desapareció en Fase 2) | Decisiones del origen incrustadas en el esquema; ruido en instalación limpia | Sin plan |
| **Waiver · lo que el CLIENTE decide sobre su propia prueba** | Revisión `#169`, spec **§10.2**: (1) el **canal** que entra en el hash sale de `$request->bearerToken() !== null` (`MeWaiverController.php:98`, `AuthRegistrationController.php:161`) — con cookie válida y `Authorization: Bearer basura` la firma consta como `api` sin que Sanctum valide nada; (2) la **IP** es el primer valor de `X-Forwarded-For` mientras `trustProxies(at: '*')` (`bootstrap/app.php:39`, medido: XFF `8.8.8.8` → `ip() = 8.8.8.8`); (3) se firma **con el correo sin verificar** — `SelfSignup.php:127-131`/`:244-249` firma en la misma transacción que crea la cuenta y el correo sale después; la fila no guarda `email_verified_at`; §4.7 decía «correo verificado» y era falso; (4) **`POST /me/waiver` no es idempotente** (`WaiverAcceptance.php:47-56`): dos envíos del mismo id → dos firmas, dos `consents`, dos PDF (sonda `[201, 201, 2, 4, 4]`) | El valor probatorio: tres datos del PDF los fija quien firma, y una app nativa o un reintento duplica el registro. Hoy ninguna instalación está en `interno` | ✅ **(1) y (4) HECHOS** (`#174`, 2026-08-26 noche): el canal sale del token REAL (`currentAccessToken() instanceof PersonalAccessToken`; con sesión, `Bearer basura` sigue siendo `web`) y en el alta de si la petición se sirvió con sesión; `WaiverSigner` es **idempotente por versión y sujeto, DENTRO del lock** (segunda aceptación → la fila que hay; `waiver:verify-chain` firma ahora un menor por proceso y sigue cazando la bifurcación sin el lock: 15 `prev_hash` repetidos). 7 casos, mutaciones que muerden. ⏳ **(2) la IP** es infra: acotar `trustProxies` a los rangos del hosting (`ENTORNOS.md`) · ✅ **(3) el correo — HECHO** (`#179`, spec §7·5): `WaiverSigner` exige correo verificado (salvo firma declarada), el alta deja la aceptación pendiente y se firma al verificar; `409 waiver_email_unverified`. **Queda solo (2), la IP** (infra) |
| ~~**Waiver · el cajón: tres grietas entre lo que ENSEÑA y lo que FIRMA**~~ **RETIRADAS las tres 2026-08-26** (`#175`: el id que se firma es el del texto ENSEÑADO y el store relee si el estado apunta a otro —CAJ-1—; el 422 del alta relee y desmarca —CAJ-2—; tras el 409 la casilla se desmarca —CAJ-3—; 6 casos JS, 5 mutaciones que muerden, verificado en headless). ⏳ **Queda CAJ-4** (el 401 mudo, patrón de todos los formularios del área) | Revisión `#169`, spec **§10.3**: **CAJ-1** el id que se envía sale de `status.current_document_id` y el texto pintado de `legal.document`, cacheado para toda la página (`stores/waiver.js:52`, `:63`) — en la ruta del embudo (alta en el paso 5 → sesión sin recarga → Mi cuenta) puede enseñar la vieja y firmar la nueva sin 409 · **CAJ-2** el 422 `waiver_stale` del alta dice «vuelve a leerlo» y no hay forma de releer (`RegisterForm.vue:103`, `ensureLegal()` de una sola vez) · **CAJ-3** tras el 409 la tarjeta remonta con el texto nuevo **y la casilla sigue marcada** (`PrivacyZone.vue:73-80`) — ⚠️ **reproducido en headless el 26/08** (V34: «la casilla vuelve a estar desmarcada» ✗ y el botón habilitado) · CAJ-4 un 401 al firmar no cambia nada en pantalla (`form-outcome.js:44` marca `expired` y ningún `.vue` lo lee; patrón de todos los formularios del área) | Quien firma: puede aceptar un texto que no ha visto (CAJ-1, CAJ-3) o quedarse sin salida (CAJ-2) | Código acotado, carril A (`resources/js/sidebar/`): enviar el id del documento ENSEÑADO · invalidar `legal` en el 422 del alta y en el 409 · desmarcar la casilla al 409 · leer `expired` en las zonas. Tests de `node --test` para los tres |
| ~~**Waiver · la guarda de borrador caza UN marcador de tres**~~ **RETIRADA 2026-08-26** (`#174`: `DRAFT_MARKERS` con los tres del seeder + el neutro, y `mentionsDraftWords()` avisa de «borrador/draft/brouillon» en el modal de publicar sin bloquear; 2 casos, mutación que muerde) | `LegalDocumentPublisher::DRAFT_MARKER = '[pendiente'` (`:28`); el seeder siembra `[PENDING: final wording]` (en, `LandingContentSeeder.php:790`) y `[À COMPLÉTER : rédaction définitive]` (fr, `:797`) y **pasan** (`looksLikeDraft` → `false`, medido). En la BD del carril A hay una v1 publicada que dice «Este texto es un borrador…» sin corchetes (spec **§10.4**) | Publicar es irreversible: un operador con `content.manage` graba un borrador en la cadena probatoria quitando 30 caracteres. `#160` vendía una guarda de BORRADOR y es de MARCADOR | Código de una línea + test: los tres marcadores; y la palabra «borrador/draft/brouillon» como AVISO en la confirmación, no como bloqueo |
| ~~**Waiver · el badge del pedido lee el SELLO, la puerta lee el REGISTRO**~~ **RETIRADA 2026-08-26** (`#174`: el badge va por `WaiverStatus` — «No firmado» con sello sin registro en interno, «versión anterior» señalada, oculto en `desactivado`; 3 casos, mutación que muerde) | `OrderInfolist.php:153-159` pinta `user.waiver_accepted_at` sin `WaiverStatus` (spec **§10.5**, PAN-5): en `interno` un sello heredado sin registro sale **verde** en el pedido y «falta firmar» en la puerta; una firma de versión antigua sale verde sin aviso; en `desactivado` sigue avisando. §8.7 exigía migrar «las dos» superficies y se migró una | Un operador que mira la ficha del pedido cree lo contrario de lo que le dirá la puerta | Código pequeño: el badge por `WaiverStatus` (modo, vigente/anterior) — carril del panel |
| **Waiver · `declared_by_user_id` entra en el hash y su FK es `nullOnDelete`** | Migración `2026_08_25_130000:55` + `WaiverSignature.php:72-76` (spec **§10.5**, PAN-1/NUC-2, confirmado 2/2): un borrado físico del operador (`app:purge-customer-data` conservando al titular, o SQL; el panel no borra usuarios) deja `verifyHash() === false` **para siempre** en todas las firmas que declaró — la FK muta un campo hasheado de una fila «inmutable». Y PAN-4: el PDF lee `business.name` y el nombre del operador **en vivo** (`WaiverProof.php:184`, `:133`), así que renombrar cambia todos los PDF ya emitidos | Hoy ningún flujo lo dispara; el día que sí, la prueba de mostrador se rompe sin que nadie haya tocado la tabla | `RESTRICT` (como `user_id`), o copiar el nombre del operador en la fila como se copió el del titular; y decidir si la cabecera del PDF va congelada |
| ~~**Waiver · ningún gate vigila el lock de `WaiverSigner`**~~ **RETIRADA 2026-08-26** (`#174`: `WaiverSigner` en el `CRITICAL_RE` del `pre-push` y en `CriticalPathGateTest::CRITICAL_FILES`, con `WaiverAcceptance` de control negativo y `waiver:verify-chain` en el aviso del hook; mutación que muerde) | Spec **§10.5** (NUC-6): la suite corre en SQLite, donde `compileLock()` devuelve `''`; ningún test de `tests/Feature/Waiver/` menciona `lockForUpdate`; `WaiverSigner.php` **no está** en el `CRITICAL_RE` del `pre-push` ni en `CriticalPathGateTest::CRITICAL_FILES`. Retirar el lock pasa 72/72 y pasa el push | La única propiedad por la que existe la cadena (§8.5, vista fallar 3 de 3 sin el lock) depende de acordarse de `waiver:verify-chain` | Añadir `WaiverSigner` al `CRITICAL_RE` y a `CRITICAL_FILES` (con su control negativo), como pide la spec de lealtad para el canje |
| **Los `throttle:N,1` SIN nombre comparten UN cubo por usuario — y el reintento del pago está dentro** | Spec **§10.5** (API-2): `ThrottleRequests.php:98`/`:224-227` — sin prefijo la clave es `sha1(user id)`, común a `POST /me/waiver` (10), el PDF propio (10), el guest-form (30), **`POST /orders/{code}/payment` (6)** y `verification.send` (6). Siete descargas del PDF en un minuto → 429 en el reintento del pago durante 60 s | El camino del dinero (`PAY-04`) se puede bloquear desde una superficie que no es de dinero. No nace en el waiver: la tanda 3a añadió dos rutas al cubo que ya compartían tres | ✅ **Las dos rutas del waiver ya van con PREFIJO** (`#174`: `throttle:10,1,waiver-sign` y `throttle:10,1,waiver-pdf` — el tercer parámetro del middleware es el prefijo del cubo; guarda en `MeWaiverTest`). ⏳ Quedan sin prefijo el guest-form (30), **el reintento del pago (6)** y `verification.send` (6): siguen compartiendo cubo entre sí. Una línea cada uno, pero el del pago está en el camino de `PAY-04`: se toca con su test |
| **Waiver · la poda con firmas de MENOR intercaladas rompe la cadena** | Spec **§10.5** (NUC-3): `prunable()` no poda `dependent` (`WaiverSignature.php:241`) pero la cadena enlaza sobre la **última fila del titular sea cual sea el sujeto** (`WaiverSigner.php:50-53`): con un menor intercalado, la poda deja agujeros en medio y `WaiverChain::verify()` declara ROTA una cadena legítima. Hoy `dependent` es **código inalcanzable** (ningún escritor lo produce) | `menores-a-cargo.md` §8.3 el día que produzca la primera firma de menor: la poda se vuelve indistinguible de una manipulación | Decidirlo ANTES de esa primera firma: cadena por (titular, sujeto), o poda por cadena entera. Va en la spec C |
| ~~**El contador de reservas próximas del índice de la cuenta NO se pinta nunca**~~ **RETIRADA 2026-08-26** (`#175`: `store.upcoming`, una palabra) | `AccountHomeZone.vue:63` compara `upcoming > 0` con un identificador **no declarado** en el `setup()` (`_ctx.upcoming` = `undefined`; dos líneas después sí usa `store.upcoming`). Compilado: `t.upcoming>0`. Lo cazó la revisión del waiver (`#169`, §10.3 CAJ-5) porque comparte componente con el aviso nuevo; **no es del waiver** | Quien tiene reservas futuras: la burbuja `.acct__count` y su `sr-only` no existen. Ninguna guarda lo ve: el diff de árbol no monta las zonas | Una palabra (`store.upcoming`) + un caso en el test del índice. Carril A |

## Baja

| Ítem | Qué (medido) | Dónde muerde | La retira |
|---|---|---|---|
| **«Guardar» en Ajustes se queda MUDO si el error está en otra sección** | Medido con el owner delante el 2026-08-26: eligió el modo interno del waiver (sección «Puerta», plegada, al final) y «Guardar» no hacía nada visible. Reproducido con `Livewire::test(Settings::class)->call('save')`: el formulario ENTERO se rechazaba por `address.maps_url = '#'` («debe ser una URL válida»), un campo de la sección de dirección, media página más arriba. Filament pinta el error bajo ese campo y **ninguna notificación junto al botón**. ▶ La raíz de ESE `#` ya está retirada: lo sembraba `LandingContentSeeder` (la landing ya pinta `#` cuando el ajuste está vacío, así que era redundante), y hay guarda: lo que siembra el seeder de demo tiene que poder guardarse tal cual desde Ajustes. | Cualquier operador con un valor inválido en un campo que no está mirando: cree que el panel está roto. | **Sin plan.** Candidata barata: en `Settings::save()`, capturar la `ValidationException` de `getState()` y lanzar una notificación roja con los rótulos de los campos que fallan antes de relanzarla. |
| **El registro del pedido en CHINO conserva el defecto entero** | `lang/zh_CN/admin.php` existe (112 KB, versionado) y el chino **es un idioma soportado del panel** (`SetAdminLocale::SUPPORTED`). `DECISIONES #145` arregló **solo el español**. Medido el 2026-08-25: de las 25 acciones del catálogo, `zh_CN` etiqueta **18** → **15 saldrían como clave cruda** y **8 son huérfanas** — las mismas cuatro misarchivadas y cuatro muertas que tenía el español | Un operador con el panel en chino ve exactamente lo que el owner vio en staging: `orders.gate_credit_applied` y nada más. ⚠️ Y `AuditActionCatalogTest` **declara que no lo mira**, así que no lo va a avisar nadie | **Decisión del owner (2026-08-25): NO se arregla.** El chino es herencia del origen y **no lo usa nadie**. Traducir 25 etiquetas a un idioma que nadie del equipo puede revisar produce copy sin validar. ▶ Si vuelve a usarse: el alcance se amplía en `AuditActionCatalogTest` recorriendo `SetAdminLocale::SUPPORTED` |
| **El visor GLOBAL de incidencias enseña claves crudas** | Usa otro espacio de nombres (`admin.audit.actions.*`) y solo etiqueta las **8** acciones críticas — las que filtra por defecto. Las otras ~79 del catálogo salen en crudo si se quita el filtro | Al investigar un incidente, quitar el filtro «solo críticas» devuelve una lista de claves técnicas | Sin plan. ⚠️ Hueco declarado en `AuditActionCatalogTest`: `#145` cerró el registro DEL PEDIDO, no éste |
| ~~El reembolso del PEDIDO (sin cancelar) no preguntaba ni decía el importe~~ **CERRADA 2026-08-25** (`#153`, owner) | Decisión: SIN campo aquí (el canje legítimo devuelve el total; los parciales van por LÍNEA, `#149`). El modal ahora NOMBRA el importe exacto («Se devolverán X € — TODO lo cobrado») y, si el operador desactiva «también cancelar» —la forma exacta del error medido—, un aviso le señala la vía de los parciales | — | ✅ hecha (2 mutaciones muerden) |
| ~~El tope de reembolso por línea es incorrecto tras cambiar el PRECIO~~ **CERRADA 2026-08-25** (`#150`) | `itemOriginalOnlineCents` reconstruye ahora `cantidad_original × precio_original` — cada término del PRIMER cambio de su clase, con desempate por `id` — porque el cambio de precio viaja estructurado (`unit_price_change`) en el contexto del ajuste. Verificado en vivo (`R-VLRYUV`): 40→30, tope 40,00, y tras cancelar la RESERVA la línea devuelve TODO | — | ✅ hecha, con los CUATRO caminos de `#146` en `ItemPriceChangeReconstructionTest` y 6 mutaciones que muerden |
| ~~La bajada por cambio de fecha no deja fila en el desglose~~ **CERRADA 2026-08-25** (`#150`) | El marcador de reducción se dispara con CUALQUIER cambio reconstruible (cantidad o precio unitario), no solo `quantity_change`. Verificado en vivo: marcador 0 € con `changes=[slot_change,unit_price_change]` y etiqueta «Cambio de fecha a …» | — | ✅ hecha |
| ~~Dos textos del panel cuentan la causa vieja de una bajada~~ **CERRADA 2026-08-25** (`#150`) | El toast elige la variante VERDADERA por la causa que ya viaja (cantidad → «unidades canceladas» · precio → `success_edited_reduced_price`), los tres pies de «pendiente de devolución» citan también la bajada de precio, y el registro etiqueta `unit_price_change`. El TERCER texto de la familia (el modal de Reembolsar prometía una cancelación falsa desde `#157`) lo arregló `#149` | — | ✅ hecha |
| ~~«+2 Pack» — la línea de puerta narraba un cambio de FECHA como CANTIDAD (el sexto sitio de `PAY-18`)~~ **CERRADA 2026-08-25** (`#150`) | La etiqueta «+N producto» exige ahora un `quantity_change` real además de la divisibilidad. Verificado en vivo sobre `R-8STAH6` (pedido PRE-arreglo): el cliente lee «Cambio de fecha a 05/09/2026 10:00». ⚠️ Cargos anteriores a `#145` (contexto vacío) caen al respaldo honesto («Diferencia por cambios en X») | — | ✅ hecha |
| ~~Un pedido CANCELADO no tenía NINGUNA vía de reembolso en el panel~~ **CERRADA 2026-08-25** (`#152`, owner) | La LÍNEA se abre para cancelados con deuda (`refundItemBlockedReason` acepta `CANCELLED`; topes bajo lock intactos, cancelado sin deuda sigue bloqueado) y el banner dice CUÁNTO se debe y por dónde se devuelve. El TOTAL sigue vetado a propósito. E2E medido: bajada → cancelar pedido → reembolsar línea → «pendiente de devolverte» a CERO | — | ✅ hecha |
| ~~El email de una BAJADA no mencionaba el dinero que se le debe~~ **CERRADA 2026-08-25** (`#155`) | `OrderItemModified` cuenta las DOS mitades de la bajada con el reparto real del panel: «deja :amount € pendientes de devolverte» (mismo vocabulario que la pantalla) y «pagarás :amount € menos al llegar al parque» (absorbido, packs con señal). ES/EN/FR con guarda `Lang::has(..., false)` | — | ✅ hecha (3 mutaciones muerden) |
| ~~El cliente EN/FR veía claves en CRUDO en su desglose de dinero~~ **CERRADA 2026-08-25** (`#154`) | Las etiquetas del cargo de puerta viven ahora en `tickets.gate_change_line_slot`/`_product` (ES/EN/FR), junto a su respaldo de `#131`; las de `admin.*` (solo ES) se retiraron. Guarda con `Lang::has(..., false)` × 3 idiomas + composición real bajo `en` | — | ✅ hecha (2 mutaciones muerden). ⚠️ **Y una SEGUNDA guarda al lado** (`#157`, `ClientMoneyLabelsAreTranslatedTest`): existir no es estar traducido — con `lang/fr` relleno de castellano **esta guarda pasa (23 verdes) y aquélla cae**, medido. Añade además la guarda-de-la-guarda, la prohibición del mecanismo y el barrido ancho de todas las claves `tickets.*` del dominio. ❗ **Las dos se arreglaron EN PARALELO por los dos agentes sin saberlo**: el porqué y la regla que sale de ahí, en `#157` |
| ⚠️ El `redis.conf` de staging está de fábrica (14 bytes) | Medido el 2026-08-25 (`#137`): `maxmemory 0` (**sin techo de memoria**), `maxmemory-policy noeviction`, snapshots activos y `stop-writes-on-bgsave-error yes` | Dos formas de morder: la caché **crece sin límite** hasta que el contenedor sufre, y **si falla un volcado RDB deja de aceptar escrituras** — con `CACHE_STORE=redis` eso es el sitio caído por algo que no tiene que ver con la web | **Aplazado por el owner**, no olvidado. Contenido acordado en `#137` (`maxmemory 256mb` · `allkeys-lru` · `save ""`). ⚠️ Aplicarlo exige reiniciar el contenedor PHP desde el panel |
| `app:set-setting` pasa su eco de VERIFICACIÓN por el formateador de consola | Mismo mecanismo que `#135`: `«comprobado en BD: …»` sale por `info()`, y Symfony se come la barra de `\<` / `\>`. Con un valor que las lleve, el eco enseña algo distinto de lo guardado | Es un eco de **verificación**: el operador lo lee para creerse que la clave quedó bien. No bloquea (los secretos van enmascarados y el valor lo escribe él), pero **puede mentir justo cuando se le consulta** | Decisión del owner: mismo arreglo de una línea que `#135` (salida RAW) o dejarlo |
| Hueco test: throttle rutas Redsys | `throttle:120,1` vigente; 0 asserts | Quitarlo pasa verde → amplificación en el retorno de pago | Sin plan (PAY-15 ⚠️) |
| Hueco test: `after_commit` de la cola | `after_commit => true` vigente; 0 asserts | Si regresa: emails de pedidos cuya txn rollbackea | Sin plan (PAY-14) |
| Hueco test: frontera de fecha UTC↔Madrid | `DisplayTimeTest` no cruza medianoche UTC | Bug latente de «hoy operativo» 22:00–00:00 Madrid en verano | Sin plan (AFORO-09 ⚠️) |
| Hueco test: gate no-prod de `tableExists` | Código vigente; 0 asserts | +4 queries/request al hot path si regresa | Sin plan (PERF-03 ⚠️) |
| Hueco test: `hasPositivePrice` 0-queries | Solo cobertura indirecta (presupuesto de la home) | N+1 enmascarado hasta agotar el presupuesto | Sin plan (PERF-04) |
| `slots.seats_taken` muerta | Columna sin ningún escritor; el propio panel advierte «NUNCA seats_taken» | Trampa para agentes: parece la verdad del aforo y no lo es | Sin plan (candidata a drop) |
| `rooms` sin uso | Modelo+tabla+seed placeholder; cero consumo en flujos | Estructura muerta que confunde el diseño de Fase 2 | Sin plan (candidata a drop) |
| **El día elegido avanza solo en la SPA y no en Livewire** | Medido en el extremo a extremo (`DECISIONES #59`): `Purchase::selectDate()` solo MARCA el día y exige pulsar «Continuar» (`goToTime`); `selectDate()` del cajón SPA hace `store.go(STEPS.TIME)` y salta al paso 3. Consecuencia: el CTA «Continuar» del paso 2 es **inalcanzable** en la SPA | El embudo de compra. No rompe nada —se llega igual al paso 3— pero es un clic menos en un motor y no en el otro, y deja muerto un CTA que el pie sí compone. Ningún test lo ve: el diff de árbol renderiza cada paso por separado | **Sin plan — decisión de producto.** Quitar un clic puede ser mejor UX; lo que no vale es que no esté decidido. Si se adopta, hay que quitarlo también del Blade y retirar el CTA del paso 2 |
| **Sin foco al cambiar de paso en el cajón** | §6 del spec de Fase 4 lo pide y **el cajón no lo hace**: cero llamadas a `focus()` al mover de paso. Medido en su día en los DOS motores —`purchase.blade.php` y `resources/js/sidebar/`—; retirado el primero en `#112`, queda el segundo, y el hueco sigue igual. El foco de PANEL sí está —`a11yPanel` da el primer foco al abrir y atrapa el Tab—; lo que falta es mover el foco al contenido cuando el asistente avanza | Quien navega con teclado o lector de pantalla: al pasar de catálogo a calendario, el foco se queda donde estaba y el cambio no se anuncia | **Sin plan — hueco HEREDADO, no regresión de la SPA.** Se anota para que nadie lea §6 y crea que el motor nuevo lo perdió |
| **Reintento denegado sin aviso visible** | Fase 4 · 4.6·2, MEDIDO contra el HTML: en el paso 10, `retryPayment()` mete el motivo en `errors.cart` —pausa, límite de frecuencia o 502 de la pasarela— y **el bloque del paso 10 no tiene `@error('cart')` y el pie es nulo en los pasos de resultado**, así que el mensaje no aparece en ninguna parte. Para el cliente, el botón «Reintentar el pago» simplemente no hace nada | La pantalla en la que alguien con la tarjeta rechazada intenta pagar por segunda vez. Afecta a los DOS motores: el cajón SPA lo transcribe fiel a propósito (parity), así que arreglarlo es cambiar la WEB | **Sin plan — decisión de producto.** El arreglo es una línea de Blade (+ su espejo en `DeclinedStep.vue`), pero cambia la copia de una pantalla del camino del dinero en tres idiomas |
| Ciclo de `tickets` amputado | Solo `STATUS_PURCHASED`; sin canje digital (`qr_token` sin lector) | Una puerta digital (Fase 6) parte de un ciclo mutilado | Sin plan (¿backlog F6?) |
| `Order::STATUS_REFUNDED` legacy | Constante + **16 usos en 6 ficheros** (re-medido 2026-08-14; eran ~10): dominio (`OrderRefundFlags`, `Order`, `Payment`), presentación Filament (tabla y relation manager) y, desde Fase 3, **la API** (`OrderResource`) | Doble semántica status vs `refunded_at` que todo lector debe conocer — y ahora también todo cliente de la API | Sin plan |
| FKs ausentes a propósito | `ticket_types.zone_id` y `order_items.parent_item_id` sin constraint (limitación ALTER de SQLite) | Integridad solo en app; huérfanos posibles ante bugs | Sin plan |
| `seasons.name` sin i18n | Sin cast ni `HasTranslations` (asumida etiqueta interna) | Inconsistencia con el resto del CMS | Sin plan |
| CSP laxa + HSTS pendientes | `'unsafe-inline'/'unsafe-eval'` (los exigen Alpine/Livewire); HSTS no emitido | En el primer despliegue real de JumpWeb | Condición: despliegue propio (`SEGURIDAD.md`) |
| Mantenimiento: sin bypass de staff con Bearer | `EnsureSiteAvailable` resuelve el bypass con el guard por defecto (sesión), porque corre antes del `auth:` de ruta. Con la SPA funciona (comparte sesión); con un token no | Un admin no podría hacer QA del sitio en mantenimiento desde la app nativa | Fase 3 · paso 3 (cuando existan tokens que emitir) — hoy la tabla está vacía |
| Rutas de API con `auth:` fuera del limitador base | Medido: Laravel ordena `AuthenticatesRequests` antes que `ThrottleRequests`, así que el 401 no consume `throttle:api`. Se conserva el estándar a propósito (`DECISIONES #24g`): invertirlo empeoraría los `throttle` de la web | Un anónimo puede repetir peticiones a un endpoint autenticado sin techo; el 401 es barato y no toca BD | Sin plan (fijado por `ApiEnvelopeTest`; lo que necesita techo de verdad es público y sí lo tiene) |
| Slugs públicos en español | `/mi-cuenta`, `/cumpleanos`… (`routes/web.php`), verificado vigente el 2026-08-14 | Instalaciones no hispanohablantes | ⚠️ **Ya no es «decisión pendiente»: se DECIDIÓ en Fase 1** (`DECISIONES #12.a`) — español ahora, configurables cuando exista un cliente que lo pida. Queda como limitación conocida, no como tarea sin dueño |
| ~~Wordmark de emails no data-driven~~ **RETIRADA en Fase 1** (2026-08-12), verificado el 2026-08-14 | El header de mail, la marca del panel, el título de puerta y el wordmark de los dos PDFs leen `business.name` de BD con fallback neutro | — | ✅ hecha |
| «1 reservas próximas»: el contador de próximas reservas no concuerda | `account.sidecart.upcoming_count` es `:count reservas próximas` **sin forma plural**, así que con una sola reserva el texto dice «1 reservas próximas». **Preexistente**: el bloque `.acct` lo lleva diciendo desde `#221`; el área de cliente lo reproduce **a propósito** —su criterio es la paridad— y por eso ahora se ve en dos sitios. Solo afecta al `sr-only` del cajón y a la línea del bloque | Cosmético y de accesibilidad: un lector de pantalla lee una concordancia incorrecta. Ningún dato es falso | Añadir la forma plural en los tres idiomas (`:count reserva próxima\|:count reservas próximas`) y leerla con `trans_choice` en el servidor / `tp` en el cajón, que **ya sabe pluralizar**. Es un cambio de CONTENIDO en tres idiomas, no de código: por eso no entró en el paso 3b |
| ~~La API no publica el DESGLOSE financiero del pedido~~ **CERRADA 2026-08-22** (`#120(t)`) | Los cuatro huecos que `AccountPageCaptureTest` enumeraba se publican: `pending_at_gate_lines`, `total_final_cents`, `pending_refund_cents` y `has_deposit`. El test de captura se borró, como pedía su propia aserción | — | ✅ hecha |
| ~~La WEB no limita los intentos de `current_password`~~ **CERRADA 2026-08-22** (`DECISIONES #120(s)`) | **Medido el 2026-08-22**: ninguna de las cuatro gestiones que reconfirman la contraseña tenía `throttle`. Las cuatro lo tienen ya, y **sin escribir una línea de limitador en la web**: el paso 6 pasó `UpdatePassword` y `LogoutOtherDevices` por `AccountCredentials`, el 7 el perfil por `AccountProfile` y el 8 el borrado por `AccountPrivacy` | — | ✅ hecha. ▶ **Y la lección va más allá de la ficha**: una defensa que se hereda al bajar la lógica al dominio es la prueba de que bajarla no era ceremonia. ⚠️ Lo que el paso 8 destapó al cerrarla es que el techo estaba puesto y **su aviso no se pintaba en ninguna parte** (fila de abajo) |
| El aviso del limitador iba a `_global` y **ninguna vista de cuenta lo pintaba** | **Medido el 2026-08-22** al escribir el paso 8: los cuatro componentes de «Mi cuenta» mandan el aviso del límite a la clave `_global` —para que no se lea como «esta contraseña está mal» bajo el input, que es la convención del login— y de las cuatro vistas solo `update-profile` tenía banner (`$errors->any()`). En las otras tres, agotar los cinco intentos dejaba el formulario **sin hacer nada y sin decir nada** | Es la familia de `#117`: un camino que «no falla y no hace nada». El titular legítimo no sabía si estaba bloqueado o si el botón se había roto | ✅ **cerrada en el paso 8**: nace `<x-ui.global-errors>` y lo usan las tres vistas que faltaban, con un caso por vista **verificado por mutación**. ⚠️ `login`/`register` conservan su bloque a mano **a propósito**: son la referencia de las dos paridades de árbol (`#112(f)`) |
| ⚠️ El aviso de «formulario pendiente» no cubre un pack con campos de evento y SIN post-form | **Medido el 2026-08-22** al auditar la retirada (`#120(u)`): `OrderItem::guestFormStatus()` devuelve `null` si el pack no declara `guest_fields`, así que ese pack no ofrece enlace al post-form. La página retirada pintaba las respuestas del evento en línea y cubría el hueco por accidente | **Ninguno hoy**: el cajón las enseña bajo demanda desde `#120(u)`, así que el titular las ve igual. Lo que queda sin cubrir es **editarlas**: sin post-form no hay dónde | Si alguna instalación configura ese caso y pide editarlas, la salida es que `guestFormStatus()` contemple los packs con solo campos de evento — ⚠️ **toca el aviso de «formulario pendiente» de toda la web**, así que no es un cambio local |
| ~~«Tus consentimientos» solo existía en `/mi-cuenta`~~ **CERRADA 2026-08-22** (`#120(t)`) | Se publicaron con `GET /me/consents` y los pinta la zona `PRIVACY` del cajón —sin la IP, que viaja solo en el export—. La página se retiró después, ya sin perder nada | — | ✅ hecha |
| **Waiver · el PDF afirma MÁS de lo que el diseño garantiza** | Spec **§10.6**: «Verificada» y «fila inmutable» con un `sha256` sin secreto ni anclaje (una fila inventada verifica `true`, WAI-02, 2/2) · «`integrityOk()` (fila + versión + enlace)» donde el enlace es firma→versión, no `prev_hash` (PAN-3) · `body_hash` no cubre `published_at`/`version`/`locale` y el PDF imprime esa fecha como cubierta (NUC-7) · `WaiverChain` no cruza la versión (NUC-8) · en una firma de mostrador el PDF dice «los datos los declaró la persona» y muestra la IP/UA **del puesto** sin decirlo (WAI-07) | El día del litigio: el abogado contrario ataca lo que el documento dice de más, no lo que el diseño hace | Texto del PDF y de las etiquetas (`lang/*/waiver.php`, `waiver-proof.blade.php`): decir exactamente lo que se garantiza. Y `WaiverChain::verify()` cruzando `document_hash` con la versión |
| **`docs-check` no detecta marcadores de CONFLICTO en la doc** | Medido el 2026-08-26 con `docs/ESTADO.md` en `UU` (seis líneas `<<<<<<<`/`=======`/`>>>>>>>`): `bash scripts/docs-check.sh` → «✓ doc coherente». Ninguno de los 8 checks mira marcadores y no hay `pre-commit` (spec del waiver §10.7, DOC-5) | Con dos agentes sobre `main`, un `ESTADO.md` a medio fusionar pasa el gate y llega al remoto: dos repartos contradictorios en el fichero que todo agente lee primero | Un check 9 de dos líneas (`grep -l '^<<<<<<<\|^>>>>>>>' docs/*.md docs/**/*.md`) |
| **Waiver · 29 hallazgos BAJOS de la revisión, sin verificar por tope** | La revisión de `#169` verificó 40 hallazgos con escépticos y dejó **29 bajas sin verificar** (spec **§10.9**, con ids): entre ellas `waiver.view` no basta sin `users.manage` (PAN-7), la puerta con `deleted_N@deleted.local` dice «registrado con waiver» (PAN-10), TOCTOU publicar-vs-firmar fuera del lock (WAI-06), `waiver:verify-chain --keep` en interno sin versión **deja publicada una v1 de prueba** (WAI-14), `zh_CN` sin claves del waiver (PAN-8), foco y tamaño del texto legal en el cajón (CAJ-6/7), y ocho desfases de doc (§4 sigue con `retención_hasta` y `consents`; `RGPD-06`/`SEC-09` no citan el waiver; `SEGURIDAD`, `GLOSARIO`, `MODELO-DATOS`) | Ninguna bloquea; se listan para que no se crean inexistentes | Al retomar el waiver: verificar las cinco primeras y corregir los desfases de doc en una pasada |

## Ya no es deuda (verificado)
`prices.deposit_cents` (columna eliminada) · `event_packages` (absorbida por packs) ·
`contact_messages` (tabla+modelo+resource eliminados) · marcadores TODO/FIXME en `app/`
(0 reales: los matches son «TODO/TODOS» en español) · `ci.yml` (retirado a propósito,
`DECISIONES #9`).

## Verificado y SIN cambios (reconciliación 2026-08-14)

## ▶ ~~Alta · `Sidebar.vue` es el segundo objeto-dios~~ **RETIRADA 2026-08-22** (`DECISIONES #119`)

**Lo que era, medido** (2026-08-15, `#90`): **618 líneas de CÓDIGO** en su `<script>` y **las 11
llamadas a la API del cajón**. Los otros 18 componentes sumaban 236 entre todos y ninguno tocaba la
API — la violación de `CE-6` era **un fichero**, con los MISMOS métodos que el `Purchase.php` que se
estaba retirando.

**Lo que es hoy**, tras la reorganización que el owner pidió antes de meter el área de cliente:

    Sidebar.vue                    16 líneas · 0 llamadas a la API   ← enruta y expone
    sections/PurchaseSection.vue  438 líneas · 2 llamadas            ← el embudo
    stores/  (nueve)                                                 ← el estado, por dominio

- **La causa raíz sigue vigilada**: `SidebarComponentBudgetTest`, con techo general por componente,
  excepción declarada que solo encoge y —desde `#119`— una guarda de que **la raíz no vuelve a pintar
  pantallas** ni a hablar con la API.
- **Lo que QUEDA tiene ficha propia arriba** (las cinco secuencias transversales del embudo) y es
  deuda **sin intereses**: tocan solo la compra, así que su coste no crece con el área de cliente.

⚠️⚠️ **Y se corrige aquí una afirmación de esta misma ficha que era FALSA, porque es la que más daño
podía hacer.** Decía: «hay red para hacerlo: el manifiesto de DOM compara el árbol byte a byte, así
que refactorizar el `<script>` sin tocar la plantilla se verifica solo». **No es cierto**, y se midió
el 2026-08-22: `scripts/render-sidebar.mjs` **no importa la raíz** —su propio comentario dice por qué:
lee `window.Alpine` y el idioma del documento— y monta los ONCE componentes de paso con props
construidas por los módulos planos. El contrato de árbol prueba que esos no cambian; **no ejerce el
orquestador en absoluto**.
▶ **En un refactor del orquestador la única red es el NAVEGADOR.** Confiar en la frase de arriba es
exactamente lo que habría dejado pasar los tres fallos vivos que el refactor destapó (`#117`, `#118`,
la navegación de mes) y las dos regresiones propias que se cazaron en un navegador y no en la suite.
Receta del andamio, con sus trampas medidas: `VERIFICACION-E2E-CAJON.md` §5.bis y §5.quater.

## ▶ Baja · los colores en crudo que QUEDAN, todos porque cambiarlos mueve píxeles (2026-08-25, `DECISIONES #143`)

⚠️ **Esta ficha decía «seis colores crudos en el CSS del cajón», y ése era también su ámbito. Ya no.**
El barrido de la tanda A midió las **dos hojas enteras** —234 ocurrencias— y convirtió las **144** que
repetían un token ya existente. Lo que queda es lo que el owner dejó fuera **a propósito**, y no es
deuda de descuido: es la lista de decisiones de diseño que nadie ha tomado todavía.

**a) Blanco y negro puros — 39 ocurrencias.** No tienen token, y **darles uno los repinta**:
- el **blanco de papel** (polaroids, tarjetas de evento, la de invitación, el post-form, `.bd-card`)
  no es `--bg`, que es crema `#F4EFE3`. Pediría un `--paper` propio;
- el **blanco sobre acento** (`rgba(255,255,255,.72)` de los CTA) tampoco es `--on-brand`: ése sigue
  al acento y **sobre un acento claro es tinta oscura**. Convertirlo cambiaría esos textos en toda
  zona clara, que es justo lo que la promesa de «píxel idéntico» excluía;
- el **negro de `mask`/`mask-image`** (8) no es un color visible, es un recorte. **Se queda.**

**b) Nueve huérfanos con nombre**, casi todos del envoltorio de regalo (`.offw-*`, que ya tiene su
paleta local `--box`/`--ribbon`): `#F0B33F` ×3 escapado de `--ribbon-dk`, `#FFF7EC`,
`rgba(255,247,236,.95)`, `rgba(255,196,140,.55)`, `#FDFBF4` y `#1A1915`.

**c) ❗ Y uno que además es un DEFECTO de coherencia, no solo deuda de tema:**
`.addons-mini__badge` pinta `color: var(--ok)` sobre `background: rgba(34,197,94,.15)` — **un verde de
otra familia**. Sus dos hermanas (`--included`, `--free`) sí usan `--ok`/`--ok-bg`. Cambiar `--ok`
mueve el texto y deja el fondo quieto. ▶ Arreglarlo es **una línea**, cambia píxeles (los dos verdes
no son el mismo) y por eso está aquí y no hecho: es decisión de producto, no refactor.

- **Ninguno se toca en un refactor**: cambiar un color exige verificación visual (DoD §4).
- `SidebarTokenBudgetTest::MAX_RAW_COLOURS` (hoy **5**, era 6) es un **trinquete estricto** sobre el
  ámbito del cajón: bajarlo es parte del commit que tokenice cada uno.
- ✅ **Lo que ya NO es deuda**: un literal *nuevo* que repita un token existente ya no puede entrar
  —`RawColourIsNotATokenTest` lo caza, y compara por VALOR RGB, no por nombre de token—.
- **Sin plan de fase**: no bloquea nada. Candidato natural a la tanda B, con el segundo cliente
  delante, que es cuando se sabrá si un `--paper` hace falta de verdad.

## ▶ Media · la suite sigue sin auditar contra la FECHA (2026-08-16, `DECISIONES #97`)

Segunda mordida medida. El `pre-push` cayó con **1 fallo** a las **00:02 de Madrid** —el contenedor
marcaba 22:02 UTC del día anterior— y el reintento inmediato salió **verde**. Es el mismo patrón de
`#64`: un caso que compone su fixture con fechas relativas y cruza la medianoche entre el montaje y la
aserción.

⚠️ **No se identificó cuál**, y esa es la parte evitable: la salida del gate no se volcó a fichero y se
perdió al reintentar. **Procedimiento a partir de ahora: si el `pre-push` cae, `git push > log 2>&1`
antes de volver a intentarlo.**

El barrido sigue pendiente y ahora tiene dos incidencias detrás, no una. Candidatos: todo test que
siembre franjas con `now()->addDays(n)` y asevere sobre `today`/`tomorrow` sin congelar el reloj.

Estas filas se comprobaron una a una contra el árbol actual y siguen **exactamente** como estaban; no
se tocan para no ensuciar el diff, pero constan como verificadas:

- **Media**: suite ciega a carreras InnoDB (`phpunit.xml` sigue en SQLite `:memory:`) · composer global
  `View::composer('*')` · 2FA inexistente (solo el comentario en `Filament/Pages/Settings.php`) · los
  dos huecos de test declarados —`lockZoneDaySlots` **no aparece en ningún test**, y ninguno de los 10
  ficheros que mencionan `redsys_secret_key` asevera que el formulario NO lo cargue— · «precio desde»
  sin filtro de tarifas activas · cacheabilidad del catálogo sin decidir · **las dos normalizaciones
  del mismo teléfono** (`MaintenanceSettings` con `\s+` y `AppServiceProvider` con `[^0-9+]`) · las dos
  formas de emparejar respuestas del evento (`ReservationSlip::eventDataRows` sigue con la suya) · N+1
  de precio por complemento · un solo driver de pasarela · post-form web con *binding* implícito
  (`/reserva/{reservation}/datos-invitados`) · el formato de importe quemado (1 `number_format` en
  `AddonResolver`; los **17** de `purchase.blade.php` se fueron con él en `#112`, y su equivalente en
  el cajón es `money.js`, que sí tiene paridad —`SidebarMoneyParityTest`—) · las fechas del cajón formateadas por dos motores.
- **Baja**: los cinco huecos de test (throttle de Redsys `throttle:120,1` sin un solo assert;
  `after_commit`; frontera UTC↔Madrid; gate de `tableExists`; `hasPositivePrice`) · `slots.seats_taken`
  **sigue muerta** (solo el cast del modelo y el comentario que advierte de ella; ningún escritor) ·
  `rooms` sin uso real (`Room::` aparece únicamente en el morphMap del `AppServiceProvider`) ·
  `seasons.name` sin `HasTranslations` · FKs ausentes · CSP laxa + HSTS · bypass de staff con Bearer ·
  rutas de API con `auth:` fuera del limitador base.
