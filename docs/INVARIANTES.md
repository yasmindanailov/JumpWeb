# INVARIANTES de no-regresión — endurecimiento heredado

> Adaptado del proyecto origen (2026-08-12). Describe la BASE HEREDADA: el refactor
> (`00-REFACTOR.md`) puede haberla cambiado. Verifica contra el código antes de construir
> encima (CONVENCIONES §7).

Destilado de las auditorías por sistema del origen (6 sistemas, hallazgos verificados
adversarialmente y reproducidos con tinker/curl/fork) + el endurecimiento pre-go-live.
**Cada invariante costó un bug real o cerró uno latente: NINGÚN agente debe deshacerlos.**
Formato: enunciado imperativo · dónde vive · cómo se verifica. Se cita por clase/método
(CONVENCIONES §4), nunca por número de línea. **Verificado contra el código: 2026-08-12**
(las 54 celdas de verificación resueltas a tests reales existentes; los huecos de cobertura
encontrados están marcados ⚠️ en su celda — son candidatos a tests nuevos, no adornos).

Vocabulario: «franjas» (slots), «packs/cumpleaños», «puerta», «waiver», «zonas» = vocabulario
del sector origen; su generalización se decide en `00-REFACTOR.md` Fase 1/2. El código heredado
aún se llama así.

---

## 1 · Dinero / pagos (Redsys)

| ID | Invariante (NO deshacer) | Código | Verificación |
|---|---|---|---|
| PAY-01 | **`RedsysReturnHandler` es el ÚNICO autorizado a pasar una `Order` a `paid`.** Verifica la firma con `hash_equals` ANTES de tocar BD; canarios `Ds_Amount`/`Ds_Currency` contra el importe esperado; idempotencia por-`Payment` de PAID y FAILED. | `app/Domain/Payments/Services/RedsysReturnHandler.php` | `RedsysReturnHandlerTest`; concurrencia real: `php artisan redsys:verify-concurrency --workers=16` (fork `pcntl` sobre MySQL → 1 cobro, 1 ticket, 1 `authorized` + N-1 `idempotent_paid`) |
| PAY-02 | **Guarda `$canFulfil = status===PENDING && !isExpiredInPractice()`:** una notificación autorizada sobre una Order **no viva** (PAID / CANCELLED / REFUNDED / expirada en la práctica) captura el pago como INCIDENCIA — **sin** emitir tickets, **sin** reenviar confirmación, **sin** re-transicionar estado (una CANCELLED sigue CANCELLED). Evita doble cobro con tickets duplicados y resurrección de pedidos cancelados (sobreventa de plaza liberada). | `RedsysReturnHandler` (guarda tras el lock de la Order); `Order::isExpiredInPractice()` | Tests con DOS Payments sobre la misma Order (`RedsysReturnHandlerTest`) + `redsys:verify-concurrency` |
| PAY-03 | **`TicketIssuer` es idempotente:** no-op si la Order ya tiene tickets (cinturón del anterior). | `TicketIssuer` | `RedsysReturnHandlerTest::test_ticket_issuer_is_idempotent` + `::test_second_authorized_payment_on_already_paid_order_does_not_duplicate` |
| PAY-04 | **Al reintentar un pago, los Payments pending previos pasan a `STATUS_SUPERSEDED`** (estado terminal propio, no `failed`). La extensión de `expires_at` del reintento es un **UPDATE atómico condicionado** (no check+save separados). **El código viajó en Fase 3 · paso 2**: el `SUPERSEDED` vive en `PaymentInitiator::reopen()` y el UPDATE atómico en `ReservationAdmissionPolicy::admitPaymentRetry()`, uno solo de cada. **Y el ORDEN entre los dos también es la regla** (cierre de Fase 3): admitir —que valida y extiende— va SIEMPRE antes de reabrir; invertirlo marcaría `SUPERSEDED` los intentos de un pedido cuyo hold aún no se ha validado. Ese orden vive en `CheckoutOrchestrator::retry()`, uno solo, y lo consumen las tres superficies que reintentan (antes: una copia del orden en cada una). Los tres ficheros están en el `CRITICAL_RE` del `pre-push`. | `Payment::STATUS_SUPERSEDED`; `Payments\Services\PaymentInitiator`; `Booking\Services\ReservationAdmissionPolicy`; `Booking\Services\CheckoutOrchestrator` | `PaymentInitiatorTest` (supersede sí en `reopen`, **no** en `open`) + `ReservationAdmissionPolicyTest` (extiende / no resucita un hold cruzado / no toca pedidos ajenos) + `CheckoutOrchestratorTest` (**el orden, verificado por mutación**: invertirlo deja 2 tests en rojo) + **en la superficie**: `Api\V1\OrdersTest` (el reintento abre un cobro nuevo y extiende el hold; y su techo propio) y `Api\V1\PaymentStatusTest` (un intento superado se reporta como tal). ⚠️ **Esta lista citaba dos ficheros que ya no existen** —~~`PurchaseRetryAndPollingTest`~~, que murió con `Purchase.php` en `#112`, y ~~`PaymentRetryFromOrdersTest`~~, con la página en `#120(u)`— y **nadie lo notó**: `docs-check` valida rutas de fichero citadas, no nombres de clase sueltos. La cobertura estaba intacta; lo viejo era la cita. Corregido el 2026-08-22 barriendo las 55 |
| PAY-05 | **Las incidencias de cobro son VISIBLES, no solo log:** `duplicate_or_dead_capture` y `overbooked_capture` se registran en `audit_logs` vía `AuditLogger::logSystem` (**sin PII**, sin IP/UA del cliente) + email al operador (`PaymentIncidentMail` → setting `incidents.alert_email`, fallback al email de contacto) + página «Incidencias» del panel (`AuditLogResource`, solo lectura, permiso `audit.view`). ⚠️ **Corolario descubierto al cerrar Fase 3**: el rastro `orders.payment_init_failed` lo escribe `PaymentInitiator` justo antes de lanzar, así que **`CheckoutOrchestrator` NO puede envolver la secuencia en una transacción** — la compensación haría rollback del audit junto con todo lo demás y la incidencia desaparecería en silencio (el otro motivo es `AFORO-01`: el `lockForUpdate` de las franjas quedaría sostenido durante la firma del payload). | `RedsysReturnHandler` + `AuditLogger::logSystem` + `AuditLogResource`; `CheckoutOrchestrator` (sin transacción, por contrato) | `RedsysReturnHandlerTest` (duplicate/overbooked/sin-alert-email) + `IncidentSettingsTest` + `AuditLogResourceTest` |
| PAY-06 | **La clave secreta de Redsys vive en `config/services.php` (`redsys.secret_key` = `env(...)`), NUNCA `env()` en código de runtime** — `env()` devuelve `null` tras `config:cache` y caería a la clave sandbox pública (apagón de cobros en despliegue). `Redsys::config()` lee `config(...)`. | `config/services.php` + `Redsys::config()` | `RedsysSecretKeyConfigTest::test_reads_secret_key_from_config_so_it_survives_config_cache` + smoke post-`config:cache` en deploy |
| PAY-07 | **Guarda de go-live:** el panel bloquea `redsys_environment=live` si la clave secreta efectiva (`Redsys::config()['secret_key']`) no mide **32 chars** o sigue siendo `Redsys::SANDBOX_SECRET_KEY`. La clave real va al vault del hosting, nunca a BD ni repo. | Guarda en el save de la página Settings | `SettingsPageTest` (2 bloqueos + 1 permite con clave válida) |
| PAY-08 | **La respuesta REST de devolución exige firma:** `Ds_Signature` ausente o inválida → malformed (el `!== ''` que cortocircuitaba la verificación se eliminó). | `Redsys::executeRefund` | `OrderExecutePartialRefundTest::test_rest_refund_with_unsigned_response_is_rejected_not_accepted` + `php artisan redsys:verify-sandbox` (la firma INVÁLIDA — no solo ausente — la cubre su control A/B) |
| PAY-09 | **La capacidad de reembolso cuenta también los reembolsos PENDING** (un total en vuelo reserva capacidad → un parcial concurrente no puede devolver más de lo cobrado) **y hay tope per-item** (`itemRefundableRemainderCents`) bajo lock. | `Order::refundableCapacityCents()` / `itemRefundableRemainderCents()` | `DepositRefundCoherenceTest` + `OrderExecutePartialRefundTest` (total-pending reserva capacidad; tope per-item) |
| PAY-10 | **Señal (depósito) gateada por `hasDeposit()`:** valor 0 → se cobra el total (nunca `DS_MERCHANT_AMOUNT=0`); backstop `amount>0` en la ida a Redsys; el panel exige `deposit_value>=1` si el tipo ≠ none. Un complemento cobrado íntegro en puerta bajo un principal con señal tiene online original = **0** (`LineFacts::onlineAtBirth`, desde la T3·4 del libro, `#315`) — y desde la T1 del libro (`specs/desglose-libro.md` §4.1, 2026-09-01) ese «online original» es un HECHO, `fila − Σ deltas − reparto deposit_split`, que **nunca consulta `depositCents()` del catálogo vivo** (era la raíz del fantasma de la señal: la señal del producto cambia y la reconstrucción cambiaba con ella) → sin saldo «a devolver» fantasma ni techo de reembolso inflado. | `TicketType::depositCents()`/`hasDeposit()`; `OrderCreator`; `Booking\Services\LineFacts::onlineAtBirth()` · `Order::itemRefundableRemainderCents()` | `DepositRefundCoherenceTest` + `LineFactsTest` + `EditMovementTest` (el nacimiento como hecho, guardas A–C) |
| PAY-11 | **`orders:expire` es un UPDATE atómico CAS** (`WHERE status=PENDING AND expires_at<now()`); solo notifica si afectó 1 fila. Nunca pisa a EXPIRED una Order que el handler acaba de marcar PAID. | `ExpireOrders` | `ExpireOrdersTest::test_does_not_clobber_an_order_that_becomes_paid_after_being_loaded` |
| PAY-12 | **El precio se calcula SIEMPRE en servidor** y el cap de líneas del carrito es invariante de servidor (`cart_too_large` en `OrderCreator`), no solo de UI. `#[Locked]` en la cesta Livewire se descartó a propósito: la cesta es client-syncable y `OrderCreator` re-valida cada línea. | `OrderCreator` | `OrderCreatorTest` (`test_rejects_a_cart_with_more_than_max_lines`; precio desde BD: `test_rejects_when_the_product_has_no_price_for_the_day`) |
| PAY-13 | **Backstop intra-día en el checkout:** `OrderCreator` rechaza una franja de HOY cuya hora ya pasó (`too_late_line`), factorizado en `SlotOffer::passesIntradayFloor()` — oferta y checkout usan la MISMA fuente y no pueden divergir. Ídem la antelación mínima: enforce único en `TicketType::meetsMinAdvance()` consumido por `SlotOffer` y `OrderCreator`. | `SlotOffer::passesIntradayFloor()`; `TicketType::meetsMinAdvance()` | `SlotOfferTest` + `OrderCreatorTest` |
| PAY-14 | **Emails en COLA:** todas las notificaciones/mailables (18 en el origen) son `ShouldQueue` — el SMTP no bloquea al cliente; el worker corre vía el `schedule:run` del cron (`queue:work --stop-when-empty`, sin servicio nuevo); **`after_commit=true`** (nada se encola antes del commit de la txn). Cualquier Mailable/Notification NUEVA debe ser `ShouldQueue`. | Notificaciones/Mailables + config de cola | `QueuedEmailsTest` (barrido `ShouldQueue` de notificaciones y mailables + worker en scheduler; `after_commit` sin assert propio) |
| PAY-16 | **EL LIBRO DEL PEDIDO CIERRA — las identidades de VALOR (I1 · I3).** Desde la T3·4 del libro (`DECISIONES #315`, 2026-09-01; `specs/desglose-libro.md` §4.1) el ÚNICO compositor del dinero de un pedido es `Booking\Services\OrderBook`, por pedido y por reserva, y lo que se asevera es: **I1** `Order.total == Σ nac(i)` —lo facturado al nacer es lo que las líneas dicen que nacieron: `fila − Σ deltas de sus gestiones` (`LineFacts::birthValue`), canceladas incluidas— e **I3** `Total == Σ líneas de valor` (alta + gestiones ± cancelaciones − cortesías), por pedido y por reserva, con `Σ Total(r) == Total`. **Un pedido CANCELADO no tiene valor vivo** (cancelarlo cancela sus reservas por los DOS caminos) y **el saldo es UNO, con signo**: `Total − Pagado` — positivo se paga en el parque, negativo se devuelve (`#305`); no hay cubos que netear ni canales que sumar. Si una identidad no cierra, el libro se sirve «en revisión» y el parque se entera (`ledger.no_cuadra`). ⚠️ Hasta la T3·4 aquí vivía el eje VALOR del modelo de DOS EJES (`valor = pagadoOnline + pendienteOnline + aCobrarPuerta + cobradoPuerta + compensado`), retirado con `OrderFinancialSummary`/`ReservationFinancials`: tres compositores del mismo dinero eran cómo el panel y el cliente divergieron (`#127`). | `Booking\Services\OrderBook` (`forOrder`/`forReservation` · `isConsistent` · `movementsSumCents`) · `Booking\Services\LineFacts` · `Order::birthValueCents()` | `Admin\Orders\OrderFinancialInvariantsTest` (`assertBookCloses` sobre **16 escenarios**, incluidos los cuatro que la tercera auditoría destapó y los del descuento mixto) + `OrderBookTest` (guardas G–K) + `EditMovementTest` (A · B · C) + `LineFactsTest`. **Verificado por mutación** (`specs/desglose-libro.md` §6.3.7): fabricar el total, ignorar un delta, saltar una cortesía |
| PAY-17 | **EL LIBRO DEL PEDIDO CIERRA — las identidades de CAJA (I2 · I4).** **I2** `cobrado online == Σ online_nac(i)`: lo que entró por la pasarela es lo que cada línea APORTÓ al nacer —`nac − reparto deposit_split`, `LineFacts::onlineAtBirth`, un HECHO que nunca consulta el catálogo vivo—; **I4** `refund_amount_cents == Σ payment_refunds con éxito` (la columna agregada es **siempre** Σ filas: la guarda de construcción que sustituye a la legacy-safety, `DECISIONES #127`). `Pagado = cobrado − devuelto + liquidado en el parque`, y **sin cobro no hay cobro**: la liquidación «en el parque» solo consta si `paid_at !== null` y la visita pasó (D9, en UN sitio: `OrderBook::reservation()`). ⚠️ **Todo reembolso se atribuye a una reserva**, también el total —que se escribe sin atar a línea—, a prorrata de lo que cada línea APORTÓ al cobro (`onlineAtBirth`, D-T3·24: una línea reducida no aportó menos por reducirse) y por resto mayor, **sin fuga de céntimos**. ⚠️ Y **la CORTESÍA de un reembolso se ESCRIBE al ocurrir y SOLO con `compensation`** (T4, `DECISIONES #316`/`#317`, `[DECIDIDO owner]`: **el motivo manda**): `courtesy`, en la misma transacción que la fila del reembolso, por lo devuelto por encima de lo que se le DEBE en su ámbito —la reserva si va atado a línea, el pedido si es total: `OrderBook::owedToCustomerCents()`, la misma cifra que el panel sugiere y capa—, con el motivo del operador obligatorio (`payment_refunds.reason` → `context.note`, INTERNO: el panel lo pinta, la API no lo publica). `value_returned` **no puede exceder lo debido** (`exceeds_owed`, bajo lock y ANTES de la pasarela) y nunca la genera; `paid_in_person` tampoco (re-canaliza el dinero al parque); sin intención, tampoco. ⚠️ **La liquidación en el parque es SIMÉTRICA (D9 bis)**: con la visita pasada, lo que quedaba por devolver se da por entregado en recepción («Devuelto en el parque», `gate` NEGATIVO) y el saldo queda `settled`; y «lo debido» es lo debido EN DINERO (`cobrado − devuelto − Total`, sin lo inferido), así que un reembolso posterior cabe como «lo debido» y hace desaparecer lo inferido en su importe — el libro no cuenta el dinero dos veces (D-T4·6). | `Order::collectedPaymentFacts()`/`refundFacts()` (la costura con `Payments`) · `Order::unattributedRefundShareFor()` · `Order::recordCourtesyForRefund()` · `Order::cancelLiveItems()` · `Order::itemRefundedCents()` | `OrderFinancialInvariantsTest` (I2/I4 en cada escenario + la prorrata entre DOS reservas) + `CourtesyMovementTest` (las ocho formas de la cortesía, `LB-ORDEN` bloqueado incluido) + **`RefundIntentGovernsTest`** (el tope por línea y total, el motivo obligatorio, la cortesía = exceso, el motivo que no viaja, los modales por conducta) + `OrderAdminActionsTest` (que conduce las ACCIONES REALES: una guarda sobre el helper suelto no ve el cableado, y de hecho no lo vio) + `OrderBookTest` (D9 bis y la inferencia que cede). **Verificado por mutación**: repartir a partes iguales, medir lo debido en el pedido en vez de en la reserva; y las de la T4 (`specs/desglose-libro.md` §6.4.1, 11/12 con control): la regla vieja de la cortesía, quitar el tope, motivo opcional, publicar la nota, restaurar el `max(0, …)`, no restar lo devuelto de lo inferido, medir lo debido con el saldo |
| PAY-19 | **UNA FIESTA SE VENDE CON UNAS CONDICIONES Y LAS MANTIENE; NINGÚN CAMBIO DE CATÁLOGO LA MUEVE, Y NINGUNA AUSENCIA DE DATOS DESTRUYE UN CARGO COMUNICADO.** (`[DECIDIDO owner, 2026-08-31]`, `#284` D1/D2 y `#288`.) Cada línea principal de un pack guarda al nacer en `order_items.age_family_seal` la familia por edad, sus tramos y los precios de cada pack para el día de la franja; el veredicto de fiesta MIXTA y su suplemento **derivan del SELLO, nunca del catálogo vivo**. Solo cambia de condiciones lo que cambia de producto: **pack nuevo → sello nuevo** (catálogo de hoy); **día nuevo → el MISMO sello re-preciado** (los tramos de la compra se conservan, cada precio pasa al del día destino: es lo que `PAY-18` hace con el precio de la propia fiesta); cantidad, edades, complementos o cualquier cambio de catálogo → **nada**. El re-sello va en la MISMA transacción que la mutación, nunca en el post-commit. Un invitado declarado tras una subida entra al precio comunicado (`[DECIDIDO owner]`, «14,00 € y no 24,00 €»), y el PRIMER cargo sale del catálogo del día de la compra, no del día del formulario (medido antes: 10,00 € donde se pactaron 5,00). ⚠️ **El dinero solo se mueve al guardar con TODAS las edades declaradas** (`#289`, `#285` §20.6): con alguna **EN BLANCO** no se escribe nada en ninguna dirección —ni crece (hasta el 2026-08-31 crecía, `#268`), ni encoge, ni se retira—, tampoco en los disparos del panel; lo escrito se congela y se dice al cliente y al operador. Una edad **SIN PRODUCTO** en las condiciones selladas NO es una ausencia (`#284` D6): esa ficha no genera línea, no frena a las demás y no deja completar el formulario. Con el veredicto **sin tarificar** —algún régimen sellado sin precio ese día— lo escrito puede crecer pero **nunca encoger ni retirarse**: una ausencia no es una corrección (`#268`). ⚠️ Tres formas de «no aplica» y no son intercambiables: **sin sello** (línea anterior al sello, entrada, complemento) y **sello CADUCADO** (`booked_type_id`/`priced_on` ≠ la fila: se movió por fuera del sellador) son SILENCIO —no se afirma ni se mueve nada, y el segundo se enseña en rojo—; **sello sin familia** («se vendió sin condiciones») es una AFIRMACIÓN y retira el suplemento. ⚠️ El aviso de `#283` («este cambio de tramos afecta a N fiestas vendidas») se retiró con el sello: no tenía nada que avisar. ▶ **Desde la T3 (`#294`, 2026-08-31) el PARQUE puede mover el HECHO, con rastro PROPIO**: el operador corrige las edades por la MISMA puerta que el cliente (`OrderItemGuestDataWriter` → `submitGuestForm`, permiso `orders.edit_guest_data` re-exigido `SEC-04`, `via: panel`, el operador de actor en la reconciliación —`REASON_PANEL_GUEST_FORM`, el correo habla con la voz del parque— y `applied_by` = operador) y puede bajar del mínimo del pack SOLO con `orders.edit_item_below_minimum` y un interruptor explícito, quedando auditado (`below_pack_minimum` + `pack_min_qty`; el máximo y el `>= 1` siguen; `OrderCreator` sigue exigiendo el mínimo al vender). La hoja de sala y la pantalla de puerta imprimen lo ESCRITO del suplemento —nunca el veredicto derivado—, con coste de consultas vigilado. ▶ **Desde la T4 (`#296`, 2026-08-31) el descuento del caso barato es REAL y es el ESPEJO del cargo** (`[DECIDIDO owner]` D5, §20): una línea hija `is_credit` (su subtotal RESTA vía `chargedSubtotalCents()`, el único sitio del signo) con su hecho `mixed` gemelo NEGATIVO del mismo importe. ⚠️ **Hasta la T3·3 del LIBRO iba acotada al dinero de puerta** (`crédito_escrito = min(derivado, cobertura)`, con el exceso enseñado como «a tu favor»); **desde `#312` (D4 de `#305`) se escribe ENTERA**: lo que la puerta no absorbe es saldo «a devolver en el parque» en el libro de la reserva (`specs/desglose-libro.md` §4.4), y el «a tu favor» no existe en ninguna superficie. ⚠️ La asimetría del silencio es distinta a la del cargo, a propósito: el cargo puede CRECER en silencio (`#268`); **el crédito no se mueve en NINGUNA dirección sin un veredicto que gobierne**. ⚠️ El `context` del ajuste del crédito JAMÁS lleva `changes.*`: la marca `mixed_party` es lo que hace que el libro lo etiquete como DESCUENTO (`MovementLabel::mixed`) y no como una gestión de cantidad (hasta la T3·4 era además la trampa medida de §16.5.bis: un `quantity_change` hacía «reconstruir» un original al modelo viejo). ⚠️ El libro emite la línea NEGATIVA con su frase (I3: Σ líneas de valor == Total). | `Booking\Services\AgeFamilySealer` (escribe; `CRITICAL_RE` desde el 2026-08-31) · `AgeFamilySeal`/`SealedRegime` (el valor) · `GuestAgeMixReader` (deriva del sello) · `MixedPartySurcharge::derivationGoverns()` · `OrderCreator` (nace) · `OrderItemEditor::{edit,changeSlot,sealUpdateFor,validateItemEditTarget}()` (re-sella; D7) · `OrderItemGuestDataWriter` (T3 · F; control negativo del gate) | `AgeFamilySealTest` (nacimiento por `OrderCreator`, el primer cargo, el caso espejo en las DOS direcciones, la etiqueta, el re-precio que conserva tramos, el cambio de pack, el sello caducado, `anonymize()`, el día sin tarifa) + `MixedPartySurchargeTest` (las ausencias, los controles de día y pack, **el guardado parcial que no escribe nada, la edad sin producto que no congela, el panel que también congela**) + `GuestAgeMixTest` (los tres «no aplica», `allAgesDeclared()` y los tres casos de «sin producto») + `MixedPartyBadgeTest` (la ficha) + `GuestFormTest` web y API (el formulario que no se completa, los textos por instalación, el congelado dicho) + **`ManageItemGuestDataTest`** (T3: la puerta única con el rastro del operador, el permiso, el token, los generales conservados, el mínimo con su interruptor y su audit, el modal por Livewire) + **`MixedPartyParkSurfacesTest`** (T3: la hoja y la puerta imprimen lo escrito, y el presupuesto de consultas no crece con las filas) + `mixed-party:verify-concurrency` sobre MySQL, **visto FALLAR sin el sello en la siembra y sin el lock**. **Verificado por mutación**: 13 mutaciones de la T1 (`specs/cumple-mixto.md` §21.13) + 12 de la T3 (§23.10) |
| PAY-18 | **Mover la FECHA de una reserva RE-TARIFICA al precio del día destino.** «Pagas el precio del día que elijas»: si sube, la diferencia se cobra **en el parque** como cualquier otra subida; si baja, **se le abona**. ⚠️ Antes conservaba la tarifa pagada, y eso **no era una política sino un arbitraje**: comprar el día barato y pedir el cambio al caro salía gratis, con el previo del operador **afirmando** «sin cambio de precio». ⚠️ **LÍMITE**: solo re-tarifica el cambio de FECHA — una edición que no mueve el día conserva la tarifa histórica del ítem. | `ItemEditPricing::computeEditPricing()` (desde la 4b, `#184`; antes en `ViewOrder`) + el despacho de `ViewOrder::executeManageItemSave()` hacia `OrderItemEditor::edit()` (`#189`) | `ItemDateChangeRetariffTest` (las dos direcciones × entrada y pack, el **control** —cambiar de producto sigue re-tarificando— y el **límite**). **Verificado por mutación**, incluida la que pierde el límite |
| PAY-15 | **Rutas `/pago/redsys/*` con `throttle:120,1`.** | `routes/web.php` | ⚠️ Sin test dedicado (verificado 2026-08-12): nada asevera el throttle de `payments.redsys.*` — candidato a test nuevo |

---

## 2 · Aforo / concurrencia

| ID | Invariante (NO deshacer) | Código | Verificación |
|---|---|---|---|
| PAY-20 | **El precio unitario puede depender de la CANTIDAD, y los SEIS resolutores tienen que dar el mismo número** (`#324`, `specs/precio-por-tramo.md`). `price_tiers` es `(producto, tarifa, min_qty) → importe`, UNIFORME y no escalonado: 70 a 13 € son 910 €. ⚠️ Los seis son `CartPricer` (presupuesto) · `OrderCreator` (checkout) · `AvailabilityReader` (calendario) · `CreateManualOrderPage` · `ItemEditPricing` · `AgeFamilySealer`; la regla vive en `RateResolver::priceCents()` y `TicketType::priceCentsForRate()` —las dos con `$quantity = 1` por defecto— y el llamante solo pasa su cantidad. ⚠️⚠️ **Los COMPLEMENTOS quedan fuera y es regla, no optimización**: un addon es una fila de `ticket_types`, así que un `instanceof` los alcanza y cada uno pagaba una consulta por unos tramos que no puede tener (10 → 16 consultas en la ficha, cazado por el presupuesto de la API). ⚠️ **Editar la cantidad RE-TARIFICA solo si el producto declara tramos**: excepción estrecha a `#127(d)` («subir cantidad sin mover el día conserva la tarifa histórica»), porque en un producto cuyo precio es función de la cantidad conservarla contradice al propio producto. ⚠️ **Tramos y `guest_age_family` son excluyentes**, con guarda en las DOS direcciones. | `PriceTier::resolve()`; `RateResolver::priceCents()`; `TicketType::priceCentsForRate()`/`displayPriceCentsForRate()` | `PriceTierTest` (10 casos; **5 mutaciones, las 5 muerden**, con CONTROL) + `ApiOverheadTest` (el presupuesto que cazó el N+1) + `VERIFY_CONC=1` |
| AFORO-01 | **⚠️ EL MÁS SUTIL — lock de franjas con `zone_id` LITERAL, resuelto FUERA de la transacción, como PRIMERA sentencia de la txn.** Bajo REPEATABLE READ, cualquier SELECT previo (incluida una **subconsulta** dentro del propio `FOR UPDATE`) fija el snapshot ANTES del lock → el recuento de aforo lee datos pre-commit del rival → **SOBREVENTA REAL** (reproducida con fork en el origen: N compras de la última plaza, todas con éxito). El fix «lock primero» con subconsulta fue INSUFICIENTE; solo el literal es correcto. No reintroducir subconsultas ni SELECTs antes de `lockSlots()`. | `ZoneDaySlotLock::acquire()` (la receta, UNA vez desde la 4b — `#186`) vía `OrderCreator::lockSlots()` (primera sentencia de la txn; zonas resueltas antes de abrirla) | `php artisan purchase:verify-oversell --workers=16` (fork real sobre MySQL → 1 compra + N-1 `sold_out`, asientos == aforo) + regresión por query log. ⚠️⚠️ **Son SIETE escenarios, no uno** (`#147`, `#148`, `#177`, `#410`): `entry` · `pack` (cupo de FIESTAS) · `pack-guests` (cupo de INVITADOS) · `pack-prep` (tramo multi-franja con montaje/limpieza ACTIVOS — la configuración por defecto de producción) · `mixed` (los dos pools a la vez) · **`panel-edit`** (N ediciones del panel hacia la última plaza — el lock de `OrderItemEditor`) · **`extra-hour`** (la HORA EXTRA, `specs/hora-extra.md`: la última plaza disputada entre entradas directas y la línea HIJA que ocupa — visto FALLAR sin la validación de la hija: 5 asientos en una franja de 1, SIN carrera). Hasta el 2026-08-25 solo existía el primero. ⚠️ Esta celda decía **CINCO** hasta el 2026-08-29 (`panel-edit` quedó fuera del recuento, `#244`) y **SEIS** hasta el 2026-09-03: el recuento envejece — quien añada un escenario lo sube AQUÍ y en `SUITE-04` (lo vigila `OversellVerifierCoversEveryQuotaTest`). **Correr tras tocar `OrderCreator::lockSlots`, `OrderItemEditor` o cualquier aforo, y los SIETE: un cupo correcto no dice nada de los demás.** ⚠️ `mixed` mide TOPES y no ganadores: su nº de ganadores VARÍA entre ejecuciones y es correcto (`#148`). |
| AFORO-02 | **Fuente ÚNICA de oferta web↔panel↔API:** fechas/horas ofrecibles salen SOLO de `SlotOffer` (consumido por la compra pública, por el pedido manual del panel y —desde Fase 3 · paso 4b— por `availability/*` a través de `Booking\Contracts\AvailabilityOffer`) → no pueden divergir (el panel aplicaba antes su propia aritmética y ofrecía días sin franja u horas cerradas en vivo). **La oferta de horas LLEVA la cesta**: `offerableTimes()` descuenta los ocupantes provisionales que el propio cliente ya retiene, así que una consulta sin cesta ofrece horas que el checkout rechaza. Esa derivación (líneas de cesta → ocupantes) vivía en `Livewire\Tickets\Purchase` y ahora es del dominio. `SlotOffer` entró en el `CRITICAL_RE` del `pre-push` en ese mismo paso. ▶ Desde la extracción 3 del desmontaje de `ViewOrder` (2026-08-26), la oferta de **RE-PROGRAMACIÓN** de un ítem ya comprado —otra pregunta: MOVER, no comprar— también es del dominio: `ItemRescheduleOffer`, sobre las mismas primitivas y con sus reglas propias declaradas (`[DECIDIDO owner]`; spec desmontar-view-order §9.4). El panel ya no compone NINGUNA oferta. | `app/Domain/Booking/Services/SlotOffer.php`; `Booking\Services\AvailabilityReader`; `Booking\Services\ItemRescheduleOffer` (re-programación) | `SlotOfferTest` (paridad web↔panel) + `AvailabilityReaderTest` (la cesta descuenta; pools de entrada y pack independientes) + `Api\V1\AvailabilityTest` (paridad web↔API con cesta no vacía, y su mutación) + `ManageItemSlotChangeTest` (la oferta de re-programación, con sus 4 mutaciones vistas morder) |
| AFORO-11 | **El horario efectivo de la venta es el de LA ZONA, no el del recinto** (`#322`, `specs/horario-por-zona.md`): `zones.opens_at`/`closes_at`/`ignores_venue_closure` son nulables y `null` = hereda, igual que el cupo de packs. ⚠️ **`OperatingSchedule::effectiveFor()` NO se toca: es la cara pública** que alimenta `OperatingCalendar` (landing, «Abierto ahora», horarios publicados) — si la zona se colara ahí, la web anunciaría horarios que el parque no tiene. ⚠️⚠️ **El punto de estrangulamiento es `ProductAvailability::allowsStart()`**, por donde pasan `SlotOffer` (oferta), `OrderCreator` (checkout), `OrderItemEditor` (ediciones) e `ItemRescheduleOffer`: arreglarlo ahí los cubre a los cuatro, y **un `grep` de `OperatingSchedule` NO los encuentra** porque preguntan por un tercero. ⚠️ **Generar y podar leen la MISMA resolución** (`$wanted` se construye en el bucle que genera) o la poda cierra franjas con ventas dentro (`AFORO-04`). ⚠️ **Ignorar el cierre sin declarar horas NO abre la zona**: con el recinto cerrado no hay ventana que heredar y el fallback histórico es «abierto sin restricción». | `OperatingSchedule::effectiveForZone()`/`isOpenOnForZone()`; `ProductAvailability::allowsStart()`; `SlotGenerator::generate()`; `ItemRescheduleOffer` | `ZoneOwnScheduleTest` (10 casos; **4 mutaciones, las 4 muerden**, con pasada de CONTROL) + `VERIFY_CONC=1` con los siete escenarios de `purchase:verify-oversell` |
| AFORO-03 | **Las franjas se regeneran solas:** scheduler diario de `slots:generate-rolling` (`withoutOverlapping`) — sin él el calendario «se agota». El seed solo crea ~2 semanas; el comando puebla el horizonte en deploy y el cron lo mantiene. | `routes/console.php` + `SlotGenerator::generateRollingHorizon()` | `SlotGeneratorTest` |
| AFORO-04 | **La poda de franjas re-verifica dependientes DENTRO de la txn bajo `lockForUpdate`** (si apareció una venta en vuelo → cerrar la franja, no borrarla; `order_items.slot_id` es cascade y el hard-delete destruiría la venta). | `SlotGenerator::pruneDay` | `SlotGeneratorTest::test_prune_rechecks_dependents_under_lock_to_survive_a_concurrent_checkout` (+ cerrar-no-borrar con ventas) |
| AFORO-05 | **Las ediciones de ítem del panel bloquean toda la zona/día** (`lockZoneDaySlots()`, mismo alcance que `OrderCreator::lockSlots`), no solo la franja destino — dos ediciones con tramos solapados no sobrellenan una franja intermedia. | `ZoneDaySlotLock::acquire()` (el MISMO helper que la compra desde la 4b, `#186`) vía `OrderItemEditor::withZoneDayLock()` — el punto ÚNICO de lock del editor para `changeSlot()` (`#187`) y `edit()` (`#189`); lo guarda `OrderItemEditorSingleLockPointTest` | `ManageItemQuantityProductTest` (aforo + excluye huella propia) + `ManageItemSlotChangeTest`; ⚠️ el ALCANCE zona/día del lock no tiene assert (SQLite no reproduce la carrera) |
| AFORO-06 | **Re-agendar un pack pasa `excludeItemId`** a la revalidación de cupo (no se cuenta a sí mismo → sin auto-bloqueo, paridad entre las dos rutas del modal Gestionar). | `OrderItemEditor::changeSlot()` (desde la 4b, `#187`; antes `ViewOrder::executeItemSlotChange`) | `ManageItemSlotChangeTest` |
| AFORO-07 | **`seats_per_unit` forzado a 1 para packs** (guard en create/edit del catálogo + migración normalizadora) — el checkout valida el cupo en unidades y lo consume en plazas; con `spu>1` sobrevendería. | Guardado del catálogo | `CatalogCreateTest` |
| AFORO-08 | **Las ventanas de disponibilidad clampan a `[00:00:00, 24:00:00)`** cuando la aritmética cruza medianoche (sin fail-open de cupo ni fail-closed de venta). | `PackAvailability::window()` y `SlotAvailability::spanEnd()` | `PackAvailabilityTest` |
| AFORO-09 | **«Hoy operativo» usa `DisplayTime::today()/now()`** (timezone de display), NUNCA `Carbon::today()` crudo en el subsistema de venta, y NUNCA cambiar `config('app.timezone')` global (la BD persiste en UTC). | `DisplayTime` | `OrderCreatorTest::test_rejects_a_today_slot_whose_start_time_already_passed` + `SlotOfferTest::test_past_times_today_are_not_offered` + `DisplayTimeTest` + `ManageItemSlotChangeTest::test_reschedule_offer_anchors_today_in_park_timezone_not_utc` (cruza la frontera UTC↔Madrid a las 00:30 del parque — el calendario del panel ancla en el día del parque desde la extracción 3, 2026-08-26); ⚠️ la oferta de COMPRA sigue sin su propio caso de cruce |
| AFORO-10 | **El pedido pendiente nace auto-liberable:** el hold se pasa a `createPendingOrder` (no en un `save` posterior) — un crash no deja retención de aforo perpetua. **Desde el cierre de Fase 3 la ventana la POSEE el dominio**: `CheckoutOrchestrator::start()` la fija con `OrderCreator::checkoutHoldUntil()` y no la recibe por parámetro, así que ninguna superficie puede alargarla ni olvidarla (antes se calculaba a mano en cada una). ⚠️ Alcanza al CHECKOUT, no a toda creación de pedidos: el pedido manual del panel (`ManualOrderFulfiller`) nace FIRME y sin hold a propósito, y por eso el default `?Carbon $hold = null` se conserva. | `Booking\Services\CheckoutOrchestrator::start()`; `OrderCreator::checkoutHoldUntil()` | `CheckoutOrchestratorTest::test_the_created_order_always_carries_its_hold` (**verificado por mutación**: quitar el hold deja 4 tests en rojo) + `OrderCreatorTest::test_can_create_a_provisional_held_order` + `RedsysIdaTest::test_order_expires_at_is_set_to_hold_window_when_redirecting` |

---

## 3 · RGPD / PII

Contexto heredado: los pedidos guardan datos de **menores** (nombres + **alergias** = dato de
salud, art. 9 RGPD) en `order_items.guest_data`/`event_data`.

| ID | Invariante (NO deshacer) | Código | Verificación |
|---|---|---|---|
| RGPD-01 | **`User::anonymize()` es la purga CENTRAL y completa (art. 17)**, y es **IDEMPOTENTE**: sobre una cuenta ya anónima devuelve `false` sin ejecutar nada (`isAnonymized()` mira el dominio del email). En su txn hace OCHO cosas: (1) **sobrescribe la fila del propio titular** — `name` a «Cliente eliminado», `email` a `deleted_{id}@deleted.local`, `phone`, `pending_email` y su sello, `last_login_at`, `email_verified_at` y `remember_token` a `null`, `locale` a «es», `marketing_opt_in` a `false`, `password` a un `Str::random(60)` que el cast `hashed` convierte en hash inservible (login imposible), y los TRES sellos legales `privacy_accepted_at`/`terms_accepted_at`/`waiver_accepted_at` —y, desde `#179`, la aceptación PENDIENTE del waiver (`waiver_pending_document_id`/`_channel`/`_ip`/`_user_agent`, `#183`), que es PII del alta—; (2) **borra sus `consents` y desvincula sus `roles`**; (3) nulifica `guest_data`/`event_data` de TODOS los `order_items` del titular (incl. cancelados; conserva importes/fechas); (4) **invalida TODA credencial** —`sessions` y `personal_access_tokens`— vía `revokeAllAccess()`, por las dos vías (self-service y panel); (5) borra el token de `password_reset_tokens` con el email original; (6) **redacta** los payloads legacy de `audit_logs` del titular (`event_data_updated`, `orders.email_resent`); (7) nulifica **IP/user-agent** de sus filas de `audit_logs`; (8) **borra sus `user_identities`** —el vínculo con Google— y lo hace **escrito a mano y por BORRADO** (`#342`, `specs/auth-con-google.md` §11): el `cascadeOnDelete` de esa FK **no se dispara nunca** por esta vía (la fila de `users` sobrevive), y redactar en vez de borrar dejaría el `sub` ocupado en su `UNIQUE`, así que **esa persona no podría volver a registrarse con su Google nunca más** — el derecho de supresión le habría cerrado la puerta de entrada. ⚠️ **La fila NO se borra nunca**: la FK de los pedidos es `RESTRICT` y la factura tiene que seguir vinculada (art. 17 vs. deber fiscal). ❗ **Cualquier PII nueva que se persista debe añadirse aquí — y desde `#159` eso está EXIGIDO, no pedido**: el censo de `AnonymizeCoversEveryUserColumnTest` declara columna a columna qué se purga y qué se conserva **con su razón**, y una columna NUEVA en `users` pone la suite en ROJO hasta que alguien la declare. ⚠️ Hasta `#159` esta celda enumeraba CINCO operaciones y el código hacía OCHO: callaba (1), (2) y la idempotencia — los tests sí las cubrían, así que **el agujero era el MAPA, no la red**. ▶ **Fase 6 · waiver — lo que NO purga, a propósito** (`DECISIONES #160`, `specs/waiver-probatorio.md` §4.6 y §9.2): el registro probatorio `waiver_signatures` **se conserva VINCULADO** —conservación con tratamiento restringido y plazo (art. 17.3.e + 18)—: fuera de toda superficie normal (bloque de cuenta, `GET /me/consents`, export del art. 20), con permiso propio y consulta auditada (tanda 2), y purgado solo por `waiver.retention_months` (sin plazo fijado, nada). El sello `waiver_accepted_at` de (1) **sí se nulifica**: es presentación, no prueba — la prueba vive en su propia tabla (`user_id` RESTRICT), no en una columna de `users`, y por eso el censo no cambia. ⚠️ Y desde `#161` **la firma lleva copia del nombre y el email del titular tal y como estaban al firmar** (`[DECIDIDO owner, 2026-08-26]`): tras la supresión la fila de `users` dice «Cliente eliminado», y una prueba que no identifique a la persona no prueba quién firmó. Esa copia es PII conservada **a propósito** bajo el régimen restringido, y no se purga con la cuenta: solo con el plazo. ▶ **Fase 6 · menores a cargo** (`#191`, `specs/menores-a-cargo.md` §5): cada fila de `dependents` **sigue el régimen de su waiver** — con una firma detrás se conserva VINCULADA y desvinculada (`removed_at`, fuera de toda superficie: lista, export), y la poda la retira cuando su última firma vence; sin firma es PII de un menor sin nada que la justifique y **se borra**. Y la firma EN NOMBRE de un menor (`#198`) lleva copiados su nombre y su fecha de nacimiento (esquema canónico v3): PII conservada a propósito bajo el mismo régimen restringido, purgada solo por su propio plazo (`waiver.dependent_retention_months`, contado desde los 18). ▶ **Fase 6 · el JUSTIFICANTE de un menor INVITADO** (`#328`, `specs/waiver-por-reserva.md` §4.14): `guardian_authorizations` sigue el régimen de su firma —se conserva vinculada al anonimizar al RESPONSABLE y la retira la poda cuando se queda sin ninguna—, y la firma lleva copiada además la identidad de **quien firmó**, que no es el titular. ⚠️⚠️ **Esa prueba NO es del responsable para que él la borre**: son datos de un menor de otra familia y de otro adulto, y el parque se quedaría sin la prueba de una visita que ocurrió (art. 17.3.e). ⚠️ **Y por eso tampoco son suyas para VERLAS**: `User::waiverSignatures()` es fail-closed (acotada a `SUBJECTS_OF_HOLDER`) y el PDF, que llega por ruta, lleva su propia condición — antes de acotarlo, medido, `GET /me/waiver` devolvía el nombre del hijo de otra familia y servía su PDF. El censo de `users` **no cambia**: no hay columna nueva en `users`. ▶ **T5 · mixtos (`#298`, `#284` D8): la supresión NO se ejecuta con una reserva POR CELEBRAR** — pagada, principal activo, franja sin terminar (el criterio EXACTO de `CustomerReservations::hasUpcomingFor()`, que es el de la pantalla «próximas» del titular; NO el complemento de `terminated()`: una línea pagada sin franja jamás «termina» y bloquearía el art. 17 para siempre, y una cesta `PENDING` caduca sola). La puerta cubre **las TRES vías** (`[DECIDIDO owner]`): `AccountPrivacy::anonymize()` tras `verify()` (la API responde `409 account_has_upcoming_reservations`, el cajón pinta el mensaje) y la acción del panel con su re-comprobación y su audit (`users.anonymize_blocked`, `upcoming_reservations`) — el escape del operador es cancelar primero. ⚠️ **NUNCA en `User::anonymize()`**: metería Booking en un modelo de Identity y arrastraría el censo y la idempotencia. Con todas las reservas finalizadas o canceladas, la supresión sigue intacta — y de esta puerta sale que **ninguna reserva anonimizada se reconcilia** (cierra la ficha del «techo tras anonimizar», `PAY-19`). | `User::anonymize()`, invocado por `Identity\Services\AccountPrivacy` desde las DOS superficies (web y `DELETE /api/v1/me`); la puerta de D8 también en `ViewUser::anonymizeUserAction()` | `PrivacyTest` + `ApiTokenRevocationTest` + `MePrivacyTest` (la vía de API: purga de PII de terceros y revocación de TODAS las credenciales, incluida la de la petición) + **`AnonymizeCoversEveryUserColumnTest`** (el CENSO de `users`: simétrico —una columna que deja de purgarse y una conservada que empieza a purgarse caen igual— y con su guarda-de-la-guarda) + **`WaiverRetentionTest`** (Fase 6: `anonymize()` conserva `waiver_signatures` **y** sigue purgando todo lo demás — los dos asserts, verificado por mutación) + **`DependentPrivacyTest`** (Fase 6 · menores: borra las personas a cargo sin firma y desvincula las que la tienen; verificado por mutación) + `WaiverRetentionTest` (la poda del menor desde los 18 y que podar un sujeto no rompe la cadena del otro) + **`MePrivacyTest`** (T5 · D8: reserva futura → 409 y NADA purgado; cancelada, sin franja, `PENDING` y pasada → siguen purgando; **fechas RELATIVAS** — el fixture llevaba `2026-09-05` clavado y tres tests habrían cambiado de significado con el calendario) + **`AnonymizeUserActionTest`** (la puerta del panel bloquea, notifica y audita; el control con la reserva pasada; **verificado por mutación en las dos vías**) + **`GuestMinorAuthorizationTest`** (`#328`: la autorización y su firma sobreviven al `anonymize()` del responsable, y el hash sigue verificando) + **`GuestMinorIsolationTest`** (que esas firmas NO son suyas en ninguna superficie, con control) |
| RGPD-06 | **Invalidar el acceso de un titular tiene UN solo sitio: `User::revokeAllAccess()` / `revokeOtherAccess()`** (Fase 3 · paso 3a), y alcanza a las TRES credenciales: filas de `sessions`, `personal_access_tokens` y —desde Fase 6 · subsistema A (`#208`)— el **carné QR** (`customer_cards.revoked_at`, con motivo; `anonymize()` lo revoca como `anonymized`; ⚠️ `revokeOtherAccess()` lo conserva a propósito: escanear no autentica y el carné impreso no debe morir con un cambio de contraseña). Nació de un hueco medido: la purga de sesiones estaba copiada en cuatro ficheros —supresión RGPD, cambio de contraseña, «cerrar otras sesiones» y reset— y **ninguna revocaba tokens**, porque Sanctum llegó después; con un emisor de Bearer (Fase 6) un token habría sobrevivido a las cuatro, incluido el art. 17. `revokeOtherAccess()` conserva la credencial en curso (sesión o token) para que el titular no se autoexpulse al defenderse. La limpieza de go-live borra además los tokens por su cuenta: `personal_access_tokens` es morph y **no tiene FK**, así que borrar el usuario los dejaría huérfanos. | `User::revokeAllAccess()`/`revokeOtherAccess()`; `PurgeCustomerData` | `ApiTokenRevocationTest` (las 5 vías, **verificado por mutación**) + `AccessRevocationTest` (nadie más escribe en esas tablas —`customer_cards` incluida—; excepciones con nombre) + `CustomerCardTest` (el carné cae con `revokeAllAccess()` y `anonymize()`, verificado por mutación) |
| RGPD-02 | **`audit_logs` NUNCA lleva PII en claro:** email del actor en sha256 (`AuditLogger::logSensitive`); los diffs de `event_data` registran solo las **CLAVES** cambiadas, jamás valores (nombre del menor); el payload de reenvío de email no incluye el email; `logSystem` omite IP/UA del cliente. | `AuditLogger`; `OrderItemEventDataWriter` (eventDataDiffKeys — desde la 4b, `#185`; antes en `ViewOrder`) | `OrderItemUpdateEventDataTest`, `OrderAdminActionsTest`, `PrivacyTest` |
| RGPD-03 | **El enlace del post-form de invitados CADUCA** (`temporarySignedRoute`, expiración = fecha del evento + 14 días, fuente única `Order::guestFormLinkExpiresAt`) y **devuelve 410 si el titular está anonimizado** (GET y POST — la supresión cierra el canal de re-introducción de PII). La URL firmada no lleva PII. ▶ **Y desde `#335` lo mismo vale para el enlace del JUSTIFICANTE de un menor invitado** (`specs/waiver-por-reserva.md` §4.6): `Order::guardianAuthorizationSignedUrl()` firma con **la misma fuente de caducidad** —`Order::guestFormLinkExpiresAt()`, no un plazo nuevo— y su trait repite la escalada **403 → 410 → 404**, cuyo ORDEN es la propiedad: autorizar ANTES de comprobar la anonimización es lo que impide deducir por el código de estado si un pedido ajeno existe. ⚠️ Aquí el acceso sin sesión **es el caso normal**, no la excepción: quien firma no tiene cuenta. | `GuestFormRequest` (notificación); **`Http\Concerns\AuthorizesGuestForm`** — el trait que usan los DOS controladores (web y `Api\V1`); ahí vive el `abort_if(...isAnonymized(), 410)` · **`Http\Concerns\AuthorizesGuardianAuthorization`** (`#335`) | `GuestFormTest` + **`GuardianAuthorizationScreenTest`** (la escalada y su orden, por mutación) |
| RGPD-04 | **`Cache-Control: no-store` en toda superficie NO-Livewire con PII:** los 2 PDF operativos (hoja de reserva, resumen del día — llevan alergias de menores), **el PDF del registro probatorio del waiver** (Fase 6, `#161`: nombre, email, ip y user-agent del firmante; además permiso propio `waiver.view` y consulta auditada), la página pública del post-form y **la del justificante de un menor invitado** (`#335`; middleware `NoStore` en sus dos rutas: lleva el nombre y la fecha de nacimiento de un menor y se sirve sin sesión), el export RGPD de «Mi cuenta» —y su gemelo `GET /api/v1/me/export`, que lo hereda del grupo; desde Fase 6 · menores (`#191`) lleva también las personas a cargo ACTIVAS (`dependents[]`), nombre y fecha de nacimiento de menores— y el feed JSON del calendario del panel (alias `no-store` en ruta). Las páginas Livewire/Filament NO lo necesitan: Livewire ya estampa `no-store` globalmente (verificado empíricamente en el origen — no añadirlo redundante, pero no confiar en él para controladores planos). **Toda respuesta AUTENTICADA de `/api/v1` lo lleva por defecto** (Fase 3 · paso 0): en una superficie que solo crece, lo que hay que recordar poner ruta a ruta se acaba olvidando en alguna. | Middleware `NoStore` + alias en `routes/web.php`; `Api\NoStoreWhenAuthenticated` en el grupo `api` | `PrivacyTest` + `CalendarEventsTest::test_feed_is_served_with_no_store` + `GuestFormTest` + `ReservationSlipTest`/`DailySummaryTest` (no-store en los 2 PDF) + `WaiverProofPdfTest` (no-store, permiso propio, IDOR y auditoría del PDF del waiver) + `MeTest::test_the_authenticated_response_is_not_stored` |
| RGPD-05 | **El consentimiento de cookies es atómico:** `decided=true` y el evento que inyecta el `src` de los iframes de terceros solo se disparan si el POST de persistencia devolvió `res.ok` (la fila-prueba del art. 7.1 existe ⇔ el iframe cargó). El bloqueo previo server-side (iframe con `data-src`, no `src`, hasta consentir) se mantiene. | `resources/js/app.js` (`cookies.persist()`); `CookieConsentController` | `CookieGateBlockingTest` (bloqueo previo); el camino JS se verificó por revisión + build |

---

## 4 · Seguridad web

| ID | Invariante (NO deshacer) | Código | Verificación |
|---|---|---|---|
| SEC-01 | **`SecurityHeaders` cubre TAMBIÉN el panel admin y el grupo `api`** (está en el stack del `AdminPanelProvider` y en el grupo de middleware de `/api/v1`, no solo en `web`) — sin él, las superficies que gestionan PII/reembolsos/roles quedan sin cabeceras ni CSP. En la API va la PRIMERA del grupo, para que también estampe sus cabeceras en el 429 del limitador y en el 503 de mantenimiento. | `AdminPanelProvider` (middleware) + `bootstrap/app.php` | `SecurityHeadersTest::test_admin_panel_login_also_sends_the_base_security_headers` + `ApiEnvelopeTest::test_security_headers_reach_the_api_even_on_errors` |
| SEC-02 | **El kill-switch de mantenimiento NUNCA bloquea el login del panel:** `EnsureSiteAvailable::isExcluded()` excluye el endpoint Livewire por **NOMBRE de ruta** (`*livewire.update` — el path lleva hash aleatorio; el form de login de Filament POSTea ahí, no a `/admin/*`). Sin esto: auto-lockout del admin con mantenimiento ON, recuperable solo por BD/CLI. No reabre la web pública (sus GET ya dan 503). | `EnsureSiteAvailable::isExcluded()` | `SiteMaintenanceTest` — el test hace el **POST real** al endpoint Livewire (un GET enmascararía la regresión) |
| SEC-03 | **El pivote de permisos re-filtra SERVER-SIDE al persistir** (`array_intersect` con `PermissionCatalog::assignable()`, por grupo) — `access.manage` y nombres inventados nunca se cuelan aunque el form se manipule. Anti-lockout del último admin bajo `DB::transaction`+`lockForUpdate`. | `InteractsWithRoleForm::extractPermissionMatrix` | `RolePermissionMatrixTest` (payload manipulado → intersección con `assignable()`) + `ManageUserRolesActionTest` (anti-lockout del último admin) |
| SEC-04 | **Cada acción sensible re-autoriza en el MOMENTO de ejecutarla, no solo en `mount()`** (la revocación en vivo surte efecto): p. ej. la puerta re-valida el permiso en `search()`/`clear()`; la rama «también cancelar» del reembolso re-exige `orders.cancel` en el handler. **Y aplicado al TIEMPO** (Fase 6 · subsistema A, `identidad-qr-puerta.md` §4.8): la FICHA de puerta caduca en el SERVIDOR —`ValidarRegistro::ensureFresh()` en cada método público y en `render()`— porque un temporizador de navegador no es garantía (la pestaña se duerme, el JS se pausa, y el dato ya está en el DOM). | `ValidarRegistro::authorizeAccess()` / `ensureFresh()`; `ViewOrder` (refund) | `ValidarRegistroTest`, `ValidarRegistroProfileTest` (caducidad por mutación), `OrderAdminActionsTest` |
| SEC-05 | **El rechazo por rate-limit de la búsqueda en puerta SE AUDITA** (1.ª vez por ventana, sin PII) — una enumeración masiva deja rastro. Desde Fase 6 · subsistema A (`identidad-qr-puerta.md` §4.6·1) son **DOS limitadores por empleado, nunca por IP**: el de toda búsqueda por MINUTO (`registrations.validate_rate_limited`) y el de la búsqueda TECLEADA que abre la ficha por HORA (`puerta.lookup_rate_limited`, acción CRÍTICA: aviso al operador). El escaneo del carné solo pasa por el primero. | `ValidarRegistro::search()` / `limited()` | `ValidarRegistroTest::test_rate_limit_rejection_is_audited_once_per_window`, `ValidarRegistroProfileTest` (el segundo limitador, por mutación) |
| SEC-06 | **Anti-bot/anti-enumeración en auth:** login con 2.º `RateLimiter` SOLO por IP (además de la clave `email\|ip`, que sola no frena stuffing distribuido); reset con mensaje genérico único (no distingue email inexistente de token inválido) + rate-limit por IP; `/contacto` con `throttle:5,1` + Turnstile (no-op sin claves). ▶ **Y desde `#335` la superficie más expuesta del producto**: `POST /autorizacion/{order}` —pública, sin sesión y que **CREA PERSONAS**— con `throttle:10,1` por IP, Turnstile y honeypot, más el tope por pedido y la ventana temporal, que impone el DOMINIO bajo el lock. ⚠️⚠️ **Y ahí el fallo del anti-bot se DICE, al revés que en `/contacto` y el alta**: copiar aquel silencio fue un defecto REAL, encontrado en navegador —sin token el formulario no escribía nada y anunciaba «Listo»—. Turnstile falla también a PERSONAS (extensión, red, JS), y un padre que cree tener firmada la autorización de su hijo se entera en la puerta del parque. El HONEYPOT sí calla, y la asimetría es deliberada: un campo invisible relleno es señal de bot y de nada más. ⚠️ Su honeypot **no se llama `website`**: ese nombre mapea al tipo de autocompletado `url` y un gestor de contraseñas puede rellenárselo a una persona. | **`Identity\Services\PasswordLogin`** (los DOS limitadores del login: `email\|ip` + solo-IP) · **`Identity\Services\PasswordRecovery`** (reset) · **`Identity\Services\SelfSignup`** (señuelo, límites del alta y anti-bot) · `ContactController` · **`GuardianAuthorizationController`** (`#335`) — ⚠️ **ninguna superficie los contiene**: se movieron a Identity en Fase 3 · paso 3b, y el modal de auth que los consumía se retiró en `DECISIONES #122` | `Auth\PasswordLoginTest` + `Api\V1\AuthSessionTest` (los dos limitadores del login y la indistinguibilidad de credenciales) · `Api\V1\PasswordRecoveryTest` + `Auth\ResetPasswordTest` (no-enumeración del reset) · `Api\V1\AuthRegistrationTest` (señuelo indistinguible byte a byte, límites por IP y por correo) · `ContactPageTest` (throttle + Turnstile) · **`GuardianAuthorizationScreenTest`** (honeypot silencioso vs. anti-bot que habla, y la asimetría en un caso propio; 14 mutaciones). ⚠️ **Cita re-apuntada el 2026-08-23 y MEDIDA, no supuesta**: nombraba ~~`LoginTest`~~/~~`ForgotPasswordTest`~~, que conducían el modal retirado. Se mutó cada limitador uno a uno para comprobar que estos ficheros los cazan (`specs/auth-en-cajon.md` §8.bis) |
| SEC-07 | **Ninguna URL externa editable llega a un `href`/`content` sin `safeExternalUrl`** (maps, redes sociales, registration_url, og_image) — defensa de render uniforme, no depender solo del `->url()` del form. Los embeds pasan por allowlist de host (`MapsEmbed`/`SocialEmbed::clean`); el color de tema por `ThemeSettings::hex` (`/^#[0-9a-fA-F]{6}$/` — es el sink de máximo valor: se pinta en `<style>:root{}`). El render público escapa con `{{ }}`; los `{!! !!}` existentes son i18n del desarrollador pre-escapados — no añadir ninguno nuevo con contenido CMS. | `AppServiceProvider::safeExternalUrl`; `MapsEmbed`/`SocialEmbed`; `ThemeSettings::hex` | `SeoTest`, `Detalles216Test`, `MapsEmbedTest`, `SocialEmbedTest` |
| SEC-08 | **`lang.switch` no hace `back()` ciego:** solo redirige al `Referer` si es del MISMO host (o ruta relativa); si no, a la home (open-redirect por Referer reproducido en el origen). Los tests deben aseverar el DESTINO del redirect, no solo que redirige. | Ruta `lang.switch` en `routes/web.php` | `LocaleDetectionTest` |
| SEC-09 | **Las páginas legales no pueden desactivarse desde el panel** (`Page::PROTECTED_ACTIVE_SLUGS`; el form fuerza `is_active=true`) — sitemap/footer/enlaces de consentimiento del registro nunca apuntan a un 404. | `Page::PROTECTED_ACTIVE_SLUGS` + `InteractsWithPageForm` | `PageResourceTest` |
| SEC-10 | **Allowlists `$fillable` en los modelos de dinero/autorización** (Order, Payment, Role, Permission, Setting) + `preventSilentlyDiscardingAttributes` en no-prod (la suite verde con el guard activo prueba que los allowlists están completos). No volver a `$guarded=[]` en estos modelos. | Modelos citados + `AppServiceProvider` | La propia suite con el guard estricto |
| SEC-11 | **Los secretos NO son legibles ni editables desde el panel** (la página Settings los excluye). | Página Settings | `SettingsPageTest::test_secrets_and_counter_are_untouched_on_save`; ⚠️ la NO-legibilidad (que el form no cargue el secreto) no tiene assert |

---

## 5 · Rendimiento

| ID | Invariante (NO deshacer) | Código | Verificación |
|---|---|---|---|
| PERF-01 | **`Setting::value()` memoiza POR PETICIÓN** (`self::$memo`, invalidado en `saved`/`deleted`): las múltiples lecturas de un request (CSP, tema, flags) colapsan en 1 query. ⚠️ Acarrea el invariante de suite: `flushMemo()` en `TestCase::setUp()` (ver §6). | `app/Domain/Platform/Models/Setting.php` | Suite + `HomePageTest::test_anonymous_home_get_stays_within_query_budget` |
| PERF-02 | **El composer global de vistas memoiza su payload por petición y lee `settings` con UN `pluck` por request.** Antecedente: `View::composer('*')` corría ~79 veces por render de la home reconstruyendo ~20 `Setting::value()` cada vez → **~1.900 queries / ~4 s de BD por CADA GET anónimo** de la ruta de mayor tráfico y sin throttle (DoS por amplificación, reproducido). Con el memo: ~50 queries. | `AppServiceProvider::sharedViewData()/buildSharedViewData()` (memo en `request()->attributes` — ámbito por petición, sin fugas entre tests) | **Test-presupuesto** `HomePageTest::test_anonymous_home_get_stays_within_query_budget` (< 120 queries totales, < 50 lecturas de `settings`) — si un cambio lo rompe, ES una regresión de este invariante |
| PERF-03 | **Los guards `Schema::hasTable` del arranque solo se consultan FUERA de producción** (−4 queries a `information_schema` por request en el hot path). | `AppServiceProvider::tableExists()` | ⚠️ Sin test dedicado (verificado 2026-08-12): nada asevera el gate no-producción — candidato a test nuevo |
| PERF-04 | **Sin N+1 en el listado de servicios:** `isSellablePackForLanding()` reutiliza la relación `prices` ya eager-loaded (0 queries); solo cae al `EXISTS` puntual si no está cargada. | `TicketType::hasPositivePrice()` | Indirecta: presupuesto de queries de la home (`HomePageTest`); sin assert directo del «0 queries con `prices` eager» |
| PERF-05 | **La caché del «desde X €» (`cta.min_price_cents`) se invalida SIEMPRE que cambie la comprabilidad**, no solo el precio: toggle `is_active`/`is_sellable`, borrado del producto, cambio de `zones.is_active`. Y su query (como las de cards de la landing) aplica `->inOperationalZone()` — mismo predicado que el catálogo de compra: la landing nunca anuncia/oferta lo que el checkout no vende. | `EditCatalog::afterSave`, `deleteProductAction`, `EditZone::afterSave`; `ctaMinPriceCents()`; `HomeController`/`PricingController` | `HomePageTest`, `CatalogEditTest`, `ZoneResourceTest` |
| PERF-06 | **Los scripts de Vite/Livewire son módulos ES (`type="module"`):** cualquier optimizador proxy/CDN que reescriba scripts (p. ej. Rocket Loader) los mutila. Mantener esos reescritores desactivados en cualquier despliegue. | — (gotcha de infraestructura) | Comparar el HTML del origen vs el servido por el proxy |
| PERF-07 | **Anti-patrón documentado:** en el origen, un «tarda 10 s la 1.ª carga» se demostró **stall DNS del lado cliente** (origen caliente, CrUX aprobado). No añadir keep-warm de OPcache ni tuning especulativo de servidor sin medir primero (curl con desglose DNS/TTFB, origen directo vs proxy). | — | Metodología: medir antes de tocar |

---

## 6 · Suite de tests

| ID | Invariante (NO deshacer) | Código | Verificación |
|---|---|---|---|
| SUITE-01 | **`Http::preventStrayRequests()` activo en `setUp()`:** ningún test puede hacer red real. Toda salida externa se simula: Redsys → `Http::fake([Redsys::REST_URL_TEST => ...])`; Turnstile → sin claves o fake; HIBP → sustituir `UncompromisedVerifier` en el contenedor. | `tests/TestCase.php` | La suite completa pasa con la guarda activa |
| SUITE-02 | **`Setting::flushMemo()` en `TestCase::setUp()`:** el rollback de `RefreshDatabase` NO dispara eventos `saved`/`deleted`, así que sin el flush el memo estático de `Setting` contamina los tests siguientes del mismo proceso (en el origen: 31 fallos fantasma que no eran de lógica). Si se añade OTRO memo estático, necesita su flush aquí. | `tests/TestCase.php::setUp()` | Suite completa verde en secuencial Y paralelo |
| SUITE-03 | **Tiempo determinista:** `travel()`/`travelTo()`, nunca `sleep()`. | Tests | — |
| SUITE-04 | **La suite corre en SQLite `:memory:` → NO reproduce los locks de InnoDB.** Las carreras de concurrencia (doble-cobro, sobreventa) NO se ejercitan en CI: existen comandos on-demand dev-only con `pcntl_fork` sobre MySQL. **Obligatorio correrlos tras tocar el código que cubren:** `redsys:verify-concurrency` (tras tocar `RedsysReturnHandler`/locks de pago) · `purchase:verify-oversell` **en sus SIETE escenarios** (`#147`, `#148`, `#177`, `#410`; el recuento decía CINCO hasta `#244` y SEIS hasta `#410`) tras tocar `OrderCreator::lockSlots`, `OrderItemEditor`, `CartOccupants`/`AddonOccupancy`/`AddonResolver` (la hora extra) o el aforo · `redsys:verify-sandbox` (refund REST contra sandbox real) · **`mixed-party:verify-concurrency` en sus DOS escenarios** (`charge` · `credit`, desde la T4 `#296`) tras tocar `MixedPartySurcharge`: sin su lock, N guardados del post-form escriben N líneas — medido en los dos lados, 12 líneas y 84,00 € donde había 7,00 € (cargo) y 12 líneas y **−84,00 €** donde había −7,00 € (descuento). | Comandos artisan (gateados a no-producción; limpian lo que crean) | Los propios comandos (incluyen control negativo) |
| SUITE-05 | **Paralelo:** `php artisan test --parallel` (paratest, BD `:memory:` por proceso; ~6,8× más rápido en el origen). Resultado idéntico a secuencial es parte del contrato de aislamiento. `npm run build` una vez antes de la suite completa (vistas → `ViteManifestNotFound` sin manifest). | `phpunit.xml` + `brianium/paratest` | Comparar recuento secuencial vs paralelo |
| SUITE-06 | **Entorno de test:** `CACHE_STORE=array`, `SESSION_DRIVER=array`, `QUEUE_CONNECTION=sync`, `MAIL_MAILER=array`, `BCRYPT_ROUNDS=4`. Detalle en `TESTING.md`. | `phpunit.xml` | — |

---

## Cómo usar este documento

- Antes de tocar dinero, aforo, PII o el composer global: relee el grupo correspondiente.
- Si un cambio legítimo del refactor NECESITA alterar un invariante: registra la decisión en
  `DECISIONES.md`, actualiza aquí el enunciado y asegúrate de que la verificación equivalente
  (test o comando) sigue existiendo. Un invariante sin verificación es una regresión en espera.
- Los nombres de clase/tabla citados son de la base heredada; si el refactor los renombra,
  el invariante VIAJA con el código renombrado (actualiza la referencia, no borres la fila).
