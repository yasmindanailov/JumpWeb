# Índice de la documentación — JumpWeb

> **No leas toda la doc.** Usa la tabla de enrutado de `../CLAUDE.md`: para cada tarea,
> solo su fila. Este índice es el mapa completo con el estado de cada documento.

## Vivos (se actualizan cada sesión)
| Doc | Qué es |
|---|---|
| `ESTADO.md` | Foto viva mínima: dónde estamos / qué sigue. **Carga obligatoria al arrancar.** Fuente única del recuento vivo de la suite. |
| `00-REFACTOR.md` | Tracker VIVO del refactor de generalización (fases + checklists). Sus marcadores de fase MANDAN sobre ESTADO. |
| `DECISIONES.md` | Cronológico de decisiones con su porqué. Buscar por número, no cargar entero. Revertida = «Sustituida por #N» en la antigua. |
| `ENTORNOS.md` | Los dos entornos (local y **staging**, que es 0 LIVE / 0 PRODUCCIÓN), sus seis guardas y el procedimiento de despliegue. |
| `CONVENCIONES.md` | Reglas y protocolo de los agentes (DoD §3.bis, arranque/handoff §7, git §8, escalado al owner §9, **dos agentes sobre `main` §10**). |
| `GLOSARIO.md` | Lenguaje ubicuo: término de dominio ↔ artefacto real de código/BD, con estado de generalización. |
| `DEUDA.md` | Registro único de deuda técnica con severidad, medida y fase que la retira. |
| `INSTALACION-CLIENTE.md` | Checklist de instanciar un cliente white-label (settings, tema, contenido, Redsys, cron). |
| `VERIFICACION-E2E-CAJON.md` | Guion OPERATIVO del extremo a extremo del cajón SPA con la pasarela en sandbox: la forma ejecutable de `specs/sidebar-spa.md` §6. Empieza diciendo qué NO hace falta mirar porque ya tiene red automática. Nació como lo único que separaba al motor SPA de poder desplegarse; retirado el flag en `#112`, es el guion con el que se comprueba el cajón en navegador tras cada despliegue. |
| `specs/PLANTILLA.md` | Plantilla de spec para diseños previos a implementación (`specs/`). |
| `specs/vocabulario-dominio.md` | 🟦 Diseño de generalización del vocabulario (entrada de Fase 2; decisión final con los módulos). |
| `specs/modulos-dominio.md` | ✅ Arquitectura de módulos de Fase 2 APROBADA (revisión multi-agente) — orden, contratos y checklists de mudanza. |
| `specs/api-v1.md` | 🟦 Diseño de la API v1 (Fase 3), v2 tras revisión adversarial. **EJECUTADO**: los 6 pasos del corte cerrados; §9 lleva el avance y §10 → §10.sexdecies lo que el código enseñó al implementar (83 puntos; los tres últimos lotes ya de Fase 4). |
| `specs/checkout-orquestado.md` | ✅ Diseño APROBADO (v2, revisión adversarial ×3) del cierre de Fase 3: la secuencia «admitir → crear → abrir cobro» baja al dominio tras los puertos `ReservationCheckout` y `PaymentInitiation`. El segundo driver de pasarela queda para Fase 6. |
| `specs/sidebar-spa.md` | 🟦 Diseño de Fase 4: el sidebar como SPA (Vue 3 + Pinia), primer consumidor real de la API v1. Alcance = solo el cajón; el tema es **tokens + hoja de estilos por instalación**, lo que convierte los nombres de clase del sidebar en un contrato público. |
| `specs/area-cliente.md` | 🟦 Diseño del **área de cliente dentro del cajón** (`DECISIONES #66`/`#120`), tanda 1 = **solo lectura**. Mide que «el servidor ya está» solo vale para leer, elige el modelo de navegación (índice + zonas libres, NO el grafo del embudo) y declara por qué aquí **la red no es el diff de árbol** sino la paridad de datos y el navegador. |
| `specs/auth-en-cajon.md` | ✅ **EJECUTADO** (`DECISIONES #122`, 2026-08-23) — el **último trozo de `#66`**: entrar, darse de alta y recuperar contraseña viven en el cajón y el **modal de la cabecera está retirado**. Se conserva porque su valor está en lo que MIDIÓ: la revisión adversarial encontró dos bloqueantes (los textos del área viajan solo con sesión; el contexto del alta estaba quemado) y la auditoría de tests destapó **un hueco de seguridad vivo** —el desenlace de pago de otra persona sobrevivía a un login en dispositivo compartido— más dos huecos de guardia. §8.bis es el método: clasificar mutando, no leyendo. |
| `specs/desglose-dinero-cliente.md` | ✅ **EJECUTADA Y CERRADA** (2026-08-24, `#127`→`#134`): las tres tandas y los cuatro defectos de lectura, hechos — el último, **`L6`** (§23): «Importe al reservar» dice **hacia dónde y cuánto**, y su frase `null` **es la condición de enseñar la línea**. `L4` aparcado, `L5` retirado (no era un defecto). El desglose de dinero que ve el cliente. ⚠️ **Contesta la pregunta del owner a favor del código**: NO hay dos fuentes de verdad — medido sobre **11 pedidos REALES creados por los flujos reales**, 8 de 9 identidades se cumplen en los once y panel y cliente enseñan el mismo número bajo el mismo rótulo. El riesgo está en la **proyección**: la API publica 2 de las 6 dimensiones. ⚠️⚠️ **§4.ter.1 es el hallazgo**: el descuadre solo aparece **cuando la franja ya pasó**, o sea justo cuando el cliente entra a repasar lo que pagó. **§4.quater** deja la receta reproducible y las cuatro puertas de validación de `OrderCreator`. |
| `specs/landing-white-label.md` | 🟦 **EN REVISIÓN** (`DECISIONES #136`, 2026-08-25) — **qué es DATO, qué es PÁGINA y qué es TEMA** en una landing multi-cliente. ⚠️ **Empieza por §1**: de los cinco problemas que enunció el owner, **uno era falso** (el CMS solo necesita 2 modelos más), **otro iba al revés** (el copy vive en el REPO) y **otro era peor** (`/servicios` tiene 0 productos vinculados y 2 tablas de precios TECLEADAS). La línea sale de una medida: landing actual y mockup del cliente nuevo **coinciden en 3 de 6 secciones, y son las 3 del motor de reservas**. ⚠️⚠️ **§4.5: el tema son TRES mecanismos, no uno** —y el compartido cajón↔landing que se temía imposible ya existe y ya tiene guarda—. ▶ **TANDA A CERRADA** (`#138`→`#143`); la **B** en curso. ⚠️ **Este documento acumula TRES afirmaciones PROPIAS desmentidas al medirlas** (§4.5.1 ×2 y §4.5.2): lee cada corrección antes que el texto que corrige. ⚠️ **La tanda C toca AFORO y PAY y NO se implementa desde aquí** (§7). |
| `specs/auth-con-google.md` | ⬜ **BORRADOR — pendiente del owner** (2026-09-02). Entrar y registrarse con **Google**, por encargo del owner («0 fricción al registrarse»). ▶ **Empieza por §4**: `[DECIDIDO owner]` **hay pantalla intermedia y la exención se pide al 100 %** — la fricción que se ahorra online *se muda al empleado*, y sin aceptación el operador de puerta ya no confirma, **acredita**. ⚠️⚠️ **Pasó una revisión adversarial de cinco lentes que encontró OCHO bloqueantes, varios en la propia sección de «hechos medidos»: lee las marcas ✱ antes que el texto que corrigen.** Los dos peores: **la raíz de confianza no estaba escrita** (§6.3, nueva) y **la vinculación automática permitía heredar la cuenta que un tercero había creado con tu correo** (§5.2, P12). ⚠️ Destapa además dos incumplimientos **preexistentes**: una cuenta de Google no podría usar ninguna de las **cuatro** acciones que exigen contraseña (art. 12.2, §3.4) y el consentimiento de marketing **no se puede retirar** (art. 7.3, §3.5). **§15 son ONCE preguntas del owner; la Q1 bloquea la T2.** |
| `specs/telefono-del-cliente.md` | 🟦 **REVISADA — código no empezado** (2026-09-06, `#440`). De dónde sale el TELÉFONO del cliente y quién lo pide. ❗❗❗ **Conseguirlo de Google se DESCARTA y §3 conserva la medición**: no hay claim de teléfono en el `id_token`, el ámbito que lo daría es **sensible** (verificación con vídeo + «app no verificada» + tope de 100 usuarios) y **el dato puede venir vacío**; además el mecanismo escrito **no era implementable** —una sola ida a Google, mismo botón en login y registro—. ▶ Lo que se construye es el **mostrador**. ⚠️⚠️ **Empieza por §1.3**: no existe `EditUser`, así que hoy **el panel no tiene por dónde arreglar** un cliente sin teléfono. ⚠️ Las puertas son **cuatro** (`addLineToCart()` es público y no mira el paso) y **`create()` avisa pero COBRA**. |
| `specs/firma-al-declarar-menor.md` | 🟦 **REVISADA — código no empezado** (2026-09-06, `#441`). Declarar un menor y aceptar su exención pasan a ser **un solo gesto**. ❗❗❗ **`#179` NO se relaja: se REUTILIZA** — la revisión **reprodujo** que relajarla convierte fichas borrables en PII indeleble de niños de terceros (20 menores declarados con la cuenta de otro, firmados, heredados al reclamarla). Si el correo está verificado se firma; si no, la aceptación queda **retenida** y se sella al verificar. ▶ **Empieza por §1.2**, un defecto reproducido con control (la tarjeta ofrece un botón que solo puede dar 409). ⚠️⚠️ **Dos afirmaciones propias eran falsas**: la garantía de la transacción (→ 500 medido) y «`RGPD-01`: ninguno» (cambia la **población**, y hoy no se poda nada). ⚠️ Los plazos de retención pasan a **requisito de salida**. |
| `specs/google-reviews.md` | ⬜ **BORRADOR — pendiente del owner** (2026-08-25). Traer a la landing las reseñas y la valoración de **Google**, por encargo del owner al dimensionar la tanda B. ⚠️⚠️ **Empieza por §1.3: CINCO restricciones DURAS verificadas contra la documentación oficial**, y tres chocan con invariantes ya endurecidos aquí —máximo **5 reseñas sin poder elegirlas** (el parque pierde el control de su portada, §1.4) · **prohibido cachear o almacenar** salvo el `place_id` · **atribución con avatar obligatoria** contra una CSP `img-src 'self' data:` · SKU más caro, **el coste escala con el tráfico de la home**—. **§3 son las TRES decisiones que necesita el owner** y sin las que §4 no se puede cerrar; una de ellas (la caché) es **cumplimiento, no ingeniería**. |
| `specs/mis-reservas-por-reserva.md` | ✅ **EJECUTADA** (`DECISIONES #126`, 2026-08-23) — «Mis reservas» se lista **por RESERVA, no por pedido**, con el pasado en un **historial aparte** tras un CTA. Se conserva por lo que MIDIÓ: ⚠️ **§3.4 es lo que hay que leer** —partir la pantalla en dos cambia un problema de ORDEN por uno de PÉRDIDA, y por eso los dos ámbitos son **un solo predicado con dos lados**—, más la lección de que dos `whereNotNull` redundantes **se tapan entre sí y ninguna se puede medir mutándola** (`#112` otra vez). La cabecera enumera las cuatro cosas que la ejecución cambió del diseño, todas a mejor. |
| `specs/account-context-vue.md` | ✅ **EJECUTADO** (`DECISIONES #123`, 2026-08-23) — el **último componente Livewire del layout** pasa a Vue. Se conserva porque su valor está en lo que MIDIÓ: la revisión adversarial ×3 lo declaró **INSUFICIENTE** y destapó dos cosas que nadie vigilaba —el **`no-store` de todas las páginas web lo ponía un accidente de Livewire**, y **`route('logout')` aparece UNA sola vez en toda la aplicación**— más un gate de iconos **ciego a 10 de sus 32 componentes**. §8.1 enumera las cuatro afirmaciones del autor que no salían del código. |
| `specs/waiver-probatorio.md` | 🟦 **Subsistema B de Fase 6 — CÓDIGO COMPLETO** (`DECISIONES #142`; ejecución `#160` · `#161` · `#163` · `#166`, spec **§9**; subsistema revisado de forma adversarial —spec §10, `#169`—; **la tanda 4 que exigió esa revisión, hecha** —§9.11, `#171`→`#180`— **y su propia revisión aplicada** —§9.12, `#183`—; guion `VERIFICACION-E2E-CAJON.md` §5.nonies recorrido en headless dos veces, la última con la conducta definitiva, 111/111 ✓). Sigue 🟦 por el owner: ✅ en navegador, texto definitivo, retención. |
| `specs/waiver-por-reserva.md` | 🟦 **REVISADA Y CORREGIDA — el justificante de un menor invitado a una reserva («waiver offshore»), carril P3** (2026-09-01). Un adulto **sin cuenta** autoriza a un menor concreto desde un enlace que le pasa quien reservó; el **responsable es el que reserva** y por eso su cuenta es el ancla de la cadena de firmas — lo que hace que `user_id` siga `NOT NULL` y **no haya migración destructiva**. ❗ **§1.2 CORRIGE el alcance de `ESTADO.md`**: no es una feature de excursiones de colegio, es del waiver. ⚠️ **§1.4(b) es el bloqueo que decide el diseño y no estaba medido**: `subject_id` tiene FK dura a `dependents`, así que un tercer sujeto **no puede reutilizarla**. ⚠️ **§10 es un hallazgo colateral MEDIDO** del subsistema viejo (un `SET NULL` dentro del hash). ❗❗ **REVISADA de forma adversarial (§11): DOS BLOQUEANTES** —la idempotencia de `WaiverSigner` se cruzaba entre autorizaciones (el 2.º padre recibía la firma del 1.º y su hijo se quedaba sin justificante, **mudo**) y `WaiverChain` metía a todos en UNA cadena— **y una afirmación central del diseño que era FALSA** («no cambia una línea del mecanismo»). Corregida; **nueve `[DECIDIDO owner]`** en §7. ▶ **T1 (dominio, `#328`), T2 (pantalla pública, `#335`) y T3 (las seis superficies, `#337`) EN EL ÁRBOL**; §8.1–§8.3 son lo ejecutado. ⚠️ El defecto de la T2 lo encontró la sonda de NAVEGADOR y no la suite: el anti-bot **decía «Listo» sin escribir nada**. ⚠️ En la T3, tres guardas de arquitectura cazaron tres defectos del autor. **Queda el OJO del owner** (guion en `VERIFICACION-E2E-CAJON.md` §5.septies). |
| `specs/desmontar-view-order.md` | 🟦 **Diseño del desmontaje del god-class del panel**, pendiente de revisión adversarial y del ✅ del owner (2026-08-26). ⚠️ **La cifra de `DEUDA` estaba caducada**: son **5.280** líneas, no 5.029. ▶ **Lo que decide el diseño, medido**: solo **5 propiedades públicas** —4 del calendario—, así que el fichero es GRANDE pero **no enmarañado**, y hay red (**16 ficheros / ~285 casos**). Cuatro extracciones **por riesgo creciente** y el dinero la última. ❗ **§1.4: el panel tiene su PROPIA disponibilidad** —cero contratos de Booking, `Slot::query()` a mano— y §4.2 avisa de que sustituirla puede destapar una diferencia de CONDUCTA, que es un hallazgo y no un error de la mudanza. |
| `specs/menores-a-cargo.md` | 🟦 **Subsistema C de Fase 6 — TANDAS 1, 2 y 3 EN EL ÁRBOL** (`#191` el núcleo + la API · `#198` la FIRMA DEL MENOR con la cadena por (titular, sujeto) —`#197`, owner— · **`#199` la ZONA DEL CAJÓN**, medida y subida por feature, guion en headless 20/20). ▶ **Empieza por §9**: §9.1/§9.7/§9.8 qué existe, §9.5 las cinco decisiones TOMADAS, §9.8.4 lo que queda — la **tanda 4 (el embudo)** y el ✅ del owner. Nombre y fecha de nacimiento; la edad se **deriva y nunca se persiste**; «quitar» es desvincular si hay firma detrás. ⚠️ **§4.6**: Booking **no puede mirar a Identity**. §4.7: **dos puertas** para asignar. |
| `specs/identidad-qr-puerta.md` | 🟦 **Subsistema A de Fase 6**. El carné QR del cliente y la pantalla que ve el empleado. ⚠️ **Contiene la segunda reversión de `#142`**: la puerta deja de ser «privacy-by-design mínima». ⚠️⚠️ **§4.4 es el hallazgo**: `RGPD-06` nació porque una credencial nueva no entró en la purga; un carné es exactamente la siguiente, y entra en `revokeAllAccess()` desde el primer commit. §3 mide por qué el payload **no puede ser una URL** y §4.8 por qué **un temporizador de navegador no es una garantía**. |
| `specs/lealtad-jumppoints.md` | 🟦 **Subsistema D de Fase 6**. Ledger append-only con saldo derivado; vale **en especie** canjeado **en puerta**, que es la decisión que lo mantiene fuera del núcleo de dinero. ⚠️ **§4.2 es el corazón**: los puntos se abren **después de la visita**, lo que elimina por construcción el agujero comprar→canjear→reembolsar sin necesitar ninguna tarea programada. ⚠️ **§4.5**: no es dinero, **pero se protege como si lo fuera** — el canje entra en el `CRITICAL_RE` del `pre-push`. |
| `specs/tema-por-instalacion.md` | 🟦 **LOS CUATRO MECANISMOS EN `main`** (`DECISIONES #192` → `#196`, 2026-08-27): color y superficie · forma (cantos, motivo cuadrado, foco, tira del pie) · el hero · y la **ELEVACIÓN**. Son los mecanismos que `landing-white-label.md` §4.5 dio por supuestos y **no existían**. ❗ **Sigue 🟦 por UNA sola cosa, y es del owner: su pasada de NAVEGADOR** — `#195` (el hero pierde su CTA, gana eslogan y encoge al bajar) y `#196` (19 elementos pierden su sombra, cambia media web) **no los ha visto nadie**. ⚠️⚠️ **DOS SECCIONES CADUCADAS, y la corrección va ANTES que el texto**: **§1.2** (la alternancia de superficies **ya no existe**: papel continuo de arriba abajo y el contraste lo dan las TARJETAS — hallazgo `S-00` del owner) y **§7** (la tanda 2 se partió en **2a** hecha, **2b** hecha, **2c** el menú → `specs/armazon-y-menu.md`, y **2d** el movimiento). ⚠️ **Empieza por §13**: la elevación son **TRES ROLES, no una escala** —53 sombras, 42 formas distintas, y la mejor escala de cinco escalones movía 47 de 53—, y eso **corrige la medición de §10.3**, que miró solo el difuminado. ⚠️ **§10.6 es la lección más cara**: dos instrumentos propios salieron rotos y las dos veces parecía lo contrario. ⚠️ **§1.3 no es preferencia, es aritmética**: el gris único da **3,28 sobre tinta** (falla AA) y ninguno pasa en los dos fondos. |
| `specs/armazon-y-menu.md` | 🟦 **EL ARMAZÓN, COMPLETO SALVO EL MENÚ EN MÓVIL** — 2c·0, 2c·1, 2c·2, 2c·3 y 2c·4a en el árbol (`DECISIONES #200`, `#201`, `#203`, `#204` y `#205`, 2026-08-27; siete decisiones del owner tomadas y cuatro pendientes). ⚠️⚠️ **§8.4: la tanda 2c·2 corrigió DOS VECES a su propia spec** —`M-05` ya estaba cumplido (nuestro mobiliario no tenía sombra desde `#196`) y la coreografía «nace oculto bajo el hero» **no se implementó**, porque el hero dejó de ser a sangre en `#195`—. **Lee §4.4 y §4.8 con su corrección delante.** ▶ **La barra ya no lleva enlaces: la navegación es un MENÚ A PANTALLA COMPLETA** y la hamburguesa está en todos los anchos; el cajón de móvil sigue intacto hasta que el owner suba su artboard. **Tanda 2c** de `tema-por-instalacion.md`, y es la que cambia **por dónde se navega el sitio**: la barra fija se retira y la sustituyen **dos racimos flotantes + un menú a pantalla completa**, en las **12** vistas (`[DECIDIDO owner]`). ▶ **Empieza por §1.6** —lo que el mockup **no contesta**, porque solo dibuja la landing— **y §1.7**, tres defectos que se importan solos si se copia tal cual: el menú cerrado **deja sus enlaces en el orden de tabulación** (cero `inert`, cero `aria-hidden`), el hallazgo `M-05` del propio cliente **choca con el rol `--shadow-float` que acaba de crear `#196`**, y el eslogan a rotulador sale dos veces. ⚠️ **§1.5: la auditoría del cliente EXCLUYE el menú y el logotipo** por indicación suya — misma trampa que con el hero. ❗ **§1.3: la pieza que el owner más quiere revisar —el móvil— es la única sin NINGÚN test**, y por eso la primera tanda no cambia nada: construye la red. ⚠️ **§1.2: el 17 % de la CSS del armazón está MUERTA** (33 reglas · 121 declaraciones, ya retiradas en la 2c·0) — y el primer instrumento dijo 9 clases en vez de 11 porque **dos «vivían» dentro de un comentario de Blade**. ❗ **El MÓVIL está `[PENDIENTE: owner]`**: lo guía él con un artboard, y sin él no se empieza la 2c·4. |
| `specs/panel-navegacion.md` | 🟦 **TANDA 1 EN EL ÁRBOL** (`DECISIONES #223`, 2026-08-28) — la FORMA del panel de admin: **menú PLANO de cinco sitios** (Hoy · Calendario · Pedidos · Clientes · Puerta) y las **19 pantallas de puesta en marcha** fuera del camino, en `/admin/ajustes`, a la que se entra por el menú del avatar. ⚠️ **La primera medición fue FALSA**: Filament MEMOIZA la navegación y dijo que el empleado veía las 24 entradas del admin (no era cierto; el gateo estaba bien). ⚠️⚠️ **§4.2 es la invariante que sostiene el rediseño**: esconder no es autorizar —eso ya lo hacía `canViewAny()`— sino arriesgarse a dejar una pantalla **huérfana**, y por eso `AdminNavigationTest` exige que toda pantalla registrada esté en el menú, en `AdminSettingsHub::areas()` o en la lista de excepciones. ⚠️ **§4.5: las utilidades de color de Tailwind habrían dejado el aro de foco INVISIBLE** —`--color-primary-500/600` no están declaradas aunque sus clases sí compilan—, el mismo fallo de `#217`. ▶ **§6: lo que NO arregla el menú** — la pantalla «Hoy» y la búsqueda global (⌘K), que son las dos mitades del «todo está separado». |
| `specs/cajon-en-movil.md` | 🟦 **El embudo de compra a 390 px — unidades 1–3 EN EL ÁRBOL** (`#237` la medición y el aviso de cookies · **`#239`** la FECHA como tira de días reservables, la HORA como tira deslizable y el aviso «Casi llena»). ▶ **Empieza por §1.1**: corrige una premisa de la propia medición anterior — no faltaban días (**hay 182**), sobraba rejilla. ⚠️⚠️ **§4.2 lleva una objeción MEDIDA que el owner mantuvo**: las 11 horas cabían a la vez y la tira enseña 4; queda escrito para que no se lea como un descuido. ⚠️ **§5.2: la vela sola no bastaba** —`--bg` y `--bg-card` casi coinciden— y lo que dice «hay más» es el chip **cortado por el borde**. ⚠️ **§7.4: 366 px vacíos en fecha y 452 en hora**, medidos y sin resolver a propósito: `[PENDIENTE: owner]`. |
| `specs/cumple-mixto.md` | ⬜ **BORRADOR apuntado el 2026-08-28, pendiente del owner.** Un cumpleaños KIDS con un niño por encima de la edad del pack pasa a **MIXTO**: etiqueta «MIXTA» para cliente y operador, y la diferencia de precio por persona. ⚠️⚠️ **§2 mide TRES cosas que el encargo da por hechas y no existen**: no se pide la edad en el post-formulario (`guest_fields` son `name·allergy·notes·special_menu`), los dos packs **cuestan lo mismo** en el producto (15/18) y **comparten zona** (la 4), lo que contradice «cada zona tiene su hora». ❗ **§5 son las seis preguntas del owner** —y la del COBRO no se puede elegir por defecto: es dinero después de un pedido pagado—. ⚠️ **§4: el post-formulario es editable**, así que «mixto» va y viene: tratarlo como un sello irreversible se rompe con el primer cliente que corrija una edad. |
| `specs/elementos-fachada.md` | ⬜ **BORRADOR — es una VALORACIÓN, no un plan de obra** (`DECISIONES #281`, 2026-08-30). El kit de material gráfico del 2.º cliente sacado del mural de su entrada: texturas, manchas, bandas, siluetas, **9 poses de adulto + 3 de niño** y las piezas de página montadas con todo ello. Método de `#277`: primero qué de esto YA está. ✅ **§1.1 YA NO APLICA: el canvas se refrescó el 2026-08-31 y por fin llegó SANO.** Verificado: UTF-8 válido, **cero** marcas de mojibake, **40 piezas** con la distribución exacta que la spec predecía (A·4 B·6 C·2 D·8 F·11 G·6 E·3), y `A4`/`C1`/`D9` ausentes como estaba medido. ⚠️ **El daño lo traía el PEGADO, no el canvas** (esta vez se descargó como fichero, no se pegó) — la trampa de `tema-por-instalacion.md` §25.1 sigue vigente para la próxima. ▶ Medido sobre la copia nueva: `F1` aporta **9 geometrías** y `G1` otras **3** —las 12 poses—, `F2`–`F11` **casi ninguna nueva** (confirma «una geometría, doce composiciones»), y `F8` tiene **14 paths con 9 distintas**, que reproduce su contradicción nº2. ▶ **§1.2: `D9` desapareció porque el cliente resolvió su propio hueco**, y lo resolvió MEJOR: pedía 4 recortes PNG y entregó **12 poses en SVG a un color plano**, que sí se recolorean, escalan y recortan. ▶ **§3: el vocabulario de base YA ESTÁ** — la trama A1 es `.menu__grain` **al dígito** (1,4 / 1,6 / 20×20), la tira C2 coincide **en orden 5 de 5** con `--strip-1..5`, la sombra dura de E1 **es** `--shadow-float`, y las cuatro tipografías son los cuatro tokens. Lo nuevo no es la técnica: es el REPERTORIO. ❗❗ **§5 es la decisión que manda**: manchas y poses son ARTE DE ESTE CLIENTE y **F10/G5 caen justo encima de la deuda abierta de `#257`** («no existe mecanismo para sustituir un dibujo por instalación»). Sin ese hueco, meter esto clava el mural de un parque dentro del producto. ▶ **§6: una geometría, doce composiciones** — plano, contorno y troquel salen del MISMO path (verificado en F6/F7/G6), y las composiciones van con `<use>`: medido, `D8` pesa **100.374 B para 24 figuras** y con `<use>` son ~5,4 KB (**18,6×**). ✅ **`[DECIDIDO owner]`: el grupo D · Siluetas se retira ENTERO y SIN EXCEPCIONES** (kit de 32 sobre 40) — ⚠️ y con `D6` se va **la única pieza que contaba un movimiento**: de aquí no sale ninguna animación — su propio `D9` ya decía el porqué («la misma pose transformada, y se nota cuando se repite»), **seis de los ocho tratamientos sobreviven dentro de F/G**, y **no toca el logotipo** (medido). ⚠️ §8: **seis contradicciones medidas** del propio artboard, entre ellas que titula «Cuatro reglas» y son cinco, y que su F8 **rompe su propia regla 05** (14 copias, 9 poses, 5 repetidas). |
| `specs/idioma-visual-heredado.md` | 🟦 **TANDA A EN EL ÁRBOL** (`DECISIONES #290`, 2026-08-31) — sustituir el idioma visual del cliente ANTIGUO por el de éste, **cambiando lo que hay** en vez de añadir. Tanda A: el **sistema de etiquetas** (antes había cinco formas y cuatro giros para la misma función) y el motivo «foam» del cliente antiguo fuera. Quedan la marquesina→cinta, los cubos del cumple, la galería y el SPA |
| `specs/hora-extra.md` | 🟦 **CÓDIGO COMPLETO — las cuatro tandas en el árbol** (`#410`/`#411`, 2026-09-03; **empieza por §8**, la ejecución). Queda el ✅ del owner y el alta del producto en producción, que es DATO. ⚠️ *Esta fila decía «⬜ BORRADOR pendiente del ✅» hasta el 2026-09-03: el índice envejeció y `docs-check` no lo ve.* Un complemento que **OCUPA**: «hora extra» para que el cliente alargue su visita sin duplicar el catálogo (`[owner]`: *«no tener 4 productos tipo entrada 1 hora, entrada 2 horas»*). ❗❗ **§1.1 corrige a su propia primera versión**: se leyó como «la reserva dura más» y es **«uno de los cuatro se queda»** — o sea **menos plazas, más tarde**, un ocupante nuevo y no una duración distinta. Esa lectura tira el diseño caro (duración por línea: 50 referencias en 14 ficheros) y deja uno pequeño, porque **`occupancyMap` ya cuenta ocupantes sin filtrar por tipo** (medido, `SlotAvailability::occupancyMap()`). ▶ La línea hija recibe **franja, plazas y duración**; la cantidad son **ENTRADAS**, de donde sale gratis el precio por persona y el tope físico. ⚠️ Lo que hay que construir es **el LOCK** (hoy `OrderCreator::createPendingOrder()` salta los complementos) y **recalcular la oferta al añadirla**, o se ofrece lo que el checkout rechaza (`AFORO-02`). |
| `specs/invitados-en-post-form.md` | 🟦 **CÓDIGO COMPLETO — el cliente sube y baja sus INVITADOS desde el post-form** (`DECISIONES #444`, 2026-09-07; queda el OJO del owner). ▶ **EMPIEZA POR §8, la ejecución**, y sigue por §7.1, la revisión adversarial. ❗❗❗ **Es la PRIMERA puerta por la que el CLIENTE mueve AFORO**, que es justo lo que `complementos-post-reserva.md` §4.3·5 evitó a propósito («esta feature no toca aforo, y esa es la mitad de su coste»): por eso NO reutiliza `OrderItemEditor::edit()` —aquél exige `orders.edit_item` y además mueve producto, fecha y complementos, así que dárselo al titular sería `SEC-04` por la puerta de atrás— y sí reutiliza su receta de lock (`slots` → `order_items`, dinero POST-COMMIT). ▶ El hueco estaba REPRODUCIDO: 12 fichas sobre una línea de 8 guardaban 8 y **descartaban 4 en silencio**. ▶ **Cuatro `[DECIDIDO owner, 2026-09-07]`**: subir Y bajar · plazo de **un día como mínimo** (`packs.guest_count_cutoff_hours`) · techo el `max_qty` del pack · basta con el libro. ⚠️⚠️ **El SUELO son DOS motivos y no se funden** (el mínimo del pack se resuelve llamando; «ya has asignado más plazas», quitando a alguien de la lista), y con eso se cierra por su lado la ficha de la hoja de sala con **plazas negativas**. ⚠️ **El ORDEN de LOCKS se midió antes de construir** (§7.1·A1): la forma `items→orders` contra `orders→items` se interbloquea —4 de 8 procesos—, los dos órdenes ya están en el post-form de hoy, y esto **no añade ninguna arista**; el par preexistente queda con ficha propia. ⚠️ `Booking` no puede mirar a `Identity`, así que lo ya asignado llega por `ReservationPlacesTaken` con el binding en el composition root. ▶ **16/16 mutaciones** y un escenario propio de `purchase:verify-oversell` (`guest-count`) **visto FALLAR**: 96 invitados donde caben 30. |
| `specs/complementos-post-reserva.md` | 🟦 **CÓDIGO COMPLETO — el extra que se elige DESPUÉS de reservar, las CUATRO tandas en el árbol** (`DECISIONES #413`, 2026-09-03; queda el OJO del owner). ▶ **EMPIEZA POR §9.4 (la T3)**: la página con su suelo sin JS, el correo agrupado, las tres superficies de DEMANDA de D15 y el limitador por RESERVA. ❗❗❗ **Y sus dos defectos de navegador**: el testigo optimista **se invalidaba a sí mismo** —`submitGuestForm()` mueve `updated_at` en la misma petición, así que un guardado normal no compraba nada—, invisible en la suite porque `updated_at` tiene precisión de SEGUNDO; y un **N+1 preexistente** en `OrderItemResource` que salía rojo **también con el control**. — La revisión adversarial que precedió al código: ❗❗❗ **EMPIEZA POR §8**: seis lentes encontraron **TRES afirmaciones de la propia spec que eran FALSAS** (el editor del panel **bloquea** la bajada parcial y no la ignora · «no hay inversión de locks posible» — la hay, y el interbloqueo se **reprodujo** con dos conexiones MySQL · `general` **no** tiene semántica *nullable* en el `PUT`: ausente **BORRA**) y **dos bloqueantes**. ⚠️⚠️ **El cambio de diseño que trajo (D9)**: la propiedad «quitar es neutro» es un hecho de la **LÍNEA** (`birthValue() === 0`), no del eje — el eje es configuración **mutable** y pasar «Tarta» a `postform` habría puesto en manos del cliente líneas ya cobradas, **con el libro cerrando en verde**. ⚠️ Segundo bloqueante: **tres listas blancas del panel** entre el formulario y la fila, y las tres callan (la peor **revierte la fase al tocar otro campo**). ▶ **Tres desacuerdos entre lentes, resueltos midiendo y no votando** — incluido que **bajar SÍ escribe movimiento y retirar NO**, que decidió la aritmética. ⚠️ Seis defectos **preexistentes** destapados, con ficha en `DEUDA.md`. — El diseño original: | `[owner]`: *«el cliente reserva un cumpleaños, va a su form post reserva y puede añadir ahí complementos tipo cubo de refrescos para los adultos, tapas…»*. ▶ **El mecanismo ya existe**: el panel añade complementos a una reserva pagada desde `#170` y el LIBRO lo pinta como «+2 Cubo de refrescos» con su fecha y saldo «A pagar en el parque»; **el post-form ya mueve dinero hoy** (el suplemento mixto). Falta un eje que diga **cuándo se vende** y la puerta del cliente. ▶ **`product_addons.stage`** (`booking` por defecto | `postform`), en el PIVOTE por la doctrina del origen y espejando `event_fields.stage`. ⚠️⚠️ **La definición es «cuándo se VENDE», no «dónde se ve»**, y de ahí sale la propiedad que lo sostiene: un `postform` **no nace nunca con el pedido** —tampoco en el alta manual—, así que `nac = 0` (verificado en `LineFacts`) y **quitarlo es NEUTRO en dinero**. ▶ Cuatro `[DECIDIDO owner, 2026-09-03]`: se paga en el parque (`#244`) · plazo de corte **por complemento** · el cliente puede quitar mientras el plazo esté abierto · **la puerta es «hay post-form», no «es un pack»** (§4.2). ⚠️ **§4.9: defecto PREEXISTENTE medido** — `isFinishedInPractice()` cierra el post-form **1–2 h TARDE** (parsea hora de pared del parque como UTC); ficha en `DEUDA.md` y el plazo nuevo usa `DisplayTime::now()`. ⚠️ **§4.10: el editor del panel no sabe BAJAR la cantidad de un complemento** (3→2 no hace nada, sin rastro). |
| `specs/hueco-ilustracion.md` | ⬜ **BORRADOR — diseño medido, sin implementar** (2026-08-31). El **tercer** hueco por instalación, tras el logotipo y el icono, y el **primero para ILUSTRACIÓN y no para marca**: la instalación entrega **un** sprite (`client-kit.svg`) y el producto lo pinta con `<use>`, poniendo él color, tamaño y tratamiento. ❗ **CORRIGE tres afirmaciones de `elementos-fachada.md` y van en §12**: (1) §10·1 dice que el hueco «no está diseñado» y **ya existía DOS veces** (`--deco-blob-*` máscara y `--deco-tag` imagen, `INSTALACION-CLIENTE.md` §4.d/§4.e); (2) §4 clasifica las manchas como arte ausente y **dos de las seis ya viajan instaladas** —verificado byte a byte: `--deco-blob-a` **es** `splash-1.svg` y `--deco-blob-b` **es** `splash-4.svg`—; (3) el «18,6× menos» de `<use>` es cifra **CRUDA**: comprimido son **1,4×**. ▶ **§2: los cinco vehículos medidos en Chrome real con control** — `<use>` externo es el único que junta ser **INERTE** (ni script ni baliza salen; el control en línea disparó las dos) y dar **los tres tratamientos desde una geometría** (plano 18,9 % · contorno 8,8 % · troquel 78,8 %). ⚠️⚠️ **§2.2: sus cuatro límites** — el `<style>` interno del cliente **gana** al `fill` del producto (fuga white-label, no agujero de seguridad: se cierra al instalar) · el `stroke-width` propio del símbolo **gana** al del `<use>` · un `fill` clavado hace imposible recolorear **y los ficheros sueltos del canvas lo traen (7 de 7), el artboard no (1 de 138)** · no existe al `DOMContentLoaded`. ⚠️ **§8: el troquel sobre un símbolo con `fill="currentColor"` pinta el 100 % de la caja** — el rectángulo que `site.css` documenta como peor que no tener default, por otra puerta. ✅ **`[DECIDIDO owner, 2026-08-31]`**: mapeo **por nombre desde el paquete** (sin panel, sin migración) y **sin suelo del producto** (sin paquete no se pinta nada). ⚠️ **§2.3: `<use>` externo NO está medido en WebKit ni Firefox** — por eso **no se toca** lo que hoy funciona por máscara. ❗ **§13: seis fichas de deuda**, tres del producto actual y verificadas — el servidor **no manda ni una cabecera de caché**, `site.css:2421` escribe el `viewBox` del 2.º cliente, y `public/favicon.svg` conserva el naranja del primero. |
| `specs/desglose-libro.md` | 🟦 **T1 (los HECHOS) EN EL ÁRBOL** (`DECISIONES #306`) · spec ✅ del owner (`#305`, 2026-09-01). El desglose pasa de BALANCE (dos ejes, `#127`) a **LIBRO**: cada gestión con su línea + o − y su fecha, un Total y un SALDO que se liquida EN EL PARQUE (`[DECIDIDO owner]`: nada se cobra ni se devuelve online post-reserva; el descuento mixto entra en el saldo; el reembolso manual del panel se queda). Medido antes de diseñar: el libro coincide con `OrderLedger` en **19/22** pedidos locales y los 3 restantes son datos sucios que las identidades nuevas marcan. Tres tandas: hechos · libro (con el modelo viejo de oráculo) · superficies + tope de la T4 + retirada. |
| `specs/auditoria-diseno.md` | 🟦 **LA SEGUNDA AUDITORÍA DE DISEÑO DE LA WEB PÚBLICA, EN EL REPO — tandas C, E, H, D y F EJECUTADAS y medidas (§11–§14, `#434`→`#437`); queda la G (interiores y normas: D2, D3)** — ⏸️ **y el carril está PARADO (`#452`, 2026-09-03)**: el owner rehace el sistema en Claude Design y la G espera a su base (`DECISIONES #430`, 2026-09-02) — `hallmark audit` sobre las 12 vistas × 8 anchos con Chromium real y control en cada sonda: **3 críticos · 10 mayores · 6 menores**. ⚠️ **Sustituye a «Un solo idioma» (01-09), que se publicó como artefacto y se PERDIÓ con su URL** — por eso ésta es un fichero y el artefacto («Costuras a la vista») es solo la presentación. ▶ **Empieza por §0**: la tesis ya no es «dos generaciones» sino **costuras** — la capa de tokens es buena y las tandas A/B se sostienen; lo que queda son literales, roles prestados y hovers viejos por debajo del sistema. Lo peor es LEGIBILIDAD medida: el cian de zona pinta la FAQ a **2,45 : 1**, «O INICIA SESIÓN» sale a **2,61** en las doce vistas. **§6 son las seis puertas de Hallmark que fallan A PROPÓSITO** (decisiones del owner, no hallazgos), **§7 las seis decisiones que son suyas**, **§8 el plan C→H**. §9 son las cuatro trampas de instrumento pagadas. |
| `specs/asistente-crear-pedido.md` | 🟦 **EL ASISTENTE DE CREAR PEDIDO, DE TRES PASOS A SIETE — T1 y T2 en el árbol** (`#462` · `#463`, 2026-09-04). Encargo del owner tras la auditoría del panel: auto-avance al elegir cliente, producto y hora; productos en tarjetas; calendario grande; carrito a pantalla completa; pantalla de desenlace. ▶ **Empieza por §2, que son las tres medidas que CAMBIAN el encargo**: dos de cada tres pasos saldrían vacíos (16 de 18 productos no tienen ni un campo), el calendario con plazas por día cuesta **709 consultas y 11,4 s** sin vía agregada posible, y **el panel no le pasa la cesta a `SlotOffer` aunque la web sí** —única corrección de dominio, va en la T3—. §3 son las tres decisiones del owner; §4 el mapa de pasos; §5 las cuatro tandas; **§7 lo ejecutado de la T1 y sus tres tropiezos** (el indicador que afirmaba lo que no sabía · dos mutaciones que no mordían porque las guardas miraban el predicado y no el movimiento · un caso que pasaba por el motivo equivocado con un fixture de una sola franja); **§8 lo ejecutado de la T2**, que cierra el crítico **C1** —el `Select` plano de 18 opciones pasa a tarjetas AGRUPADAS por tipo, con el rango de invitados como marca del pack y un «Más info» que NO elige— y sus tres tropiezos (las ventajas en los tres idiomas por saltarse `tr()` · dos mutaciones que no mordieron, una por falta de sujeto y otra porque el `sed` nunca se aplicó · el modal que `assertSee` no ve). |
| `specs/auditoria-panel-admin.md` | 🟦 **AUDITORÍA UI/UX DEL PANEL DE ADMIN, EN EL REPO — informe entregado; ejecutados el SHELL (`#461`) y el crítico C1 (`#463`)** (2026-09-03, banda `#460`–`#469`). Encargo del owner: «que cualquier persona intuitivamente pueda hacer la operativa del parque; quitar ruido y cada acción en su sitio; normalmente se usa en TABLET». Playwright + Chromium sobre el panel real: **10 pantallas × 3 tamaños** (iPad h. 1080×810, iPad v. 810×1080, escritorio de control) + cinco sondas de estados de trabajo. **4 críticos · 10 mayores · 11 menores**; D1, D2 y D3 decididas (§9), D4 pendiente. ▶ **Empieza por §0.** **C1 ✅ CERRADO por `#463`** (tarjetas agrupadas por tipo; el diagnóstico se conserva porque justifica la forma, y **el DESHACER sigue sin resolver**) — **C1 es un error que YA PASÓ en el parque**: una admin vendió un cumpleaños como diez entradas sueltas — **119,00 € en vez de 180,00 €**, la sala sin reservar, 60 min en vez de 120 y sin formulario de invitados; la causa es **un desplegable plano de 18 opciones donde nada dice qué es entrada y qué es pack**, y **el error no se puede deshacer** (solo se cambia un producto por otro del mismo tipo y zona). Luego: en «Gestionar» **«Cancelar producto» es el botón más ancho y saturado y está a 12 px de «Guardar cambios»**; **la acción principal de una reserva es un icono gris de 40×40 sin rótulo**, uno de hasta seis iguales; y en la puerta **«Nueva búsqueda» (que es PRIVACIDAD) cae fuera de pantalla** con un cliente con menores. **§5.bis mide el «montón de accionadores y datos»**: 14 acciones registradas, **66 controles** sobre un pedido con UNA reserva, la reserva en 572 px contra 1.503 de administración, y el email y el descargo del titular plegados dentro de «Detalles». §1 son las **cinco trampas de instrumento** pagadas —la peor, medir un 404 durante tres tamaños—; **§7 lo que está bien y no se toca**; **§8 lo que `PANEL-ADMIN.md` promete y el código no tiene**; **§9.bis lo que falla a propósito**. |
| `../openapi/v1.yaml` | El **contrato** de la API v1 (OpenAPI 3.0.3, escrito a mano). No vive en `docs/` porque no es documentación: es el artefacto contra el que se validan los tests y, en Fase 6, la app móvil. Manda sobre el código (`DECISIONES #21`). |

## Archivo (`archivo/`) — 📜 histórico, NO se mantiene
> Lo que se paró a propósito. Se lee para saber qué se midió y qué se decidió, no para construir encima.

| Doc | Por qué está aquí |
|---|---|
| `archivo/design-producto-2026-09-03.md` | Era el **`design.md` de la raíz** (`#431`): el sistema del producto en dos capas (roles y mecanismos aquí; el perfil de PlayJump con su paquete, gitignorado). **Archivado por `#452`** (2026-09-03, `[DECIDIDO owner]`): el owner rehace el sistema en **Claude Design** y avisará con la base. Sale de la raíz a propósito (la skill `hallmark` leería un `design.md` de raíz como sistema bloqueado). Lo que describe de `site.css`/`landing.css` (`#434`→`#437`) SIGUE en código con sus guardas. |
| `archivo/guion-de-la-portada.md` | La **fase 2 del diseño** (`#431`→`#433`, `#438`): el guion de la portada, la línea base medida (6,3 pantallas hasta decir qué es el parque), las tres formas sobre la web real, A descartada y **B en artefacto** vista por el owner en su móvil (§6.9). **Archivado por `#452`**: los prototipos `/_diseno/…` y `scripts/prototipo-b/` se retiraron; sus medidas siguen valiendo como dato. |
| `archivo/auditoria-cajon.md` | La **auditoría Hallmark del cajón de compra** con el flujo entero (`#438`: 100 medidas, 3 críticos · 10 mayores · 7 menores), las cinco decisiones del owner (§7, `#439`) y las tandas A y B (`#450`, `#451`). **Archivado por `#452` y las dos tandas REVERTIDAS**: el cajón está como antes de `#450`. Lo que vale es la medición y las decisiones. |

## Base heredada (adaptada del proyecto origen el 2026-08-12)
> Describen la base tal como se heredó; el refactor puede haberlas cambiado.
> **Verificar contra el código antes de construir encima** (`CONVENCIONES §7`).

| Doc | Qué es |
|---|---|
| `INVARIANTES.md` | 62 invariantes de no-regresión (dinero · aforo · RGPD · seguridad · rendimiento · suite). **Leer antes de tocar esas áreas.** |
| `ARQUITECTURA.md` | Stack real, estructura (`app/Domain/<Contexto>/`), white-label 3 capas, composer global memoizado. |
| `MODELO-DATOS.md` | Mapa de BD **regenerado desde el código** (39 modelos · 106 migraciones), por dominios, con rarezas heredadas. |
| `SEGURIDAD.md` | Estándar transversal nivel Reforzado (ASVS/NIST): 12 reglas + estado heredado. |
| `TESTING.md` | Suite (paralelo paratest), anti-red, fakes por proveedor, Unit/Feature, comandos de verificación con MySQL real. |
| `FLUJOS.md` | Los 6 recorridos de usuario heredados + reglas transversales. |
| `REQUISITOS.md` | Roles + inventario funcional de los 6 módulos heredados. |
| `MAPA-PAGINAS.md` | Rutas públicas/cuenta/admin con permisos (contrastar con `routes/`). |
| `PANEL-ADMIN.md` | Capacidades del panel Filament + inventario real de Resources/Pages. |
| `OPERATIVA-SECTOR-ORIGEN.md` | Sector origen (parque): sistemas físicos, canje en puerta, supuesto «cupo online ≠ aforo físico». Explica POR QUÉ el dominio es como es. |

## Sistemas implementados (`sistemas/`)
| Doc | Sistema |
|---|---|
| `sistemas/COMPRA-PRODUCTOS.md` | Catálogo unificado («todo es producto») + flujo de compra tipo ROLLER. |
| `sistemas/REDSYS.md` | Integración de pagos: firma, retorno/notificación idempotente, reembolso REST. |
| `sistemas/DEPOSITO.md` | Señal/depósito (pago parcial online, resto presencial). |
| `sistemas/POSTFORM-INVITADOS.md` | Datos por invitado post-reserva (signed URL, PDF, RGPD menores). |
| `sistemas/SERVICIOS-CMS.md` | Entidad CMS `LandingService` (clasificación de superficie, modelo A). |
| `sistemas/OFERTAS-WIDGET.md` | Widget de ofertas informativo + CMS `Offer` (primer FileUpload real). |
| `sistemas/COOKIES.md` | Consentimiento de cookies (banner 2 capas + bloqueo previo). ✅ implementado. |
| `sistemas/UI-SPINNER.md` | Sistema de feedback de carga. |

## Qué NO hay aquí (y dónde está)
Los trackers del ciclo de vida del cliente origen (producción, handoffs, audits de copys,
datos reales) viven SOLO en el repo origen — no se portaron a propósito (`DECISIONES #8`).

### Punteros fantasma en comentarios del código (equivalencias)
El código heredado cita ~71 veces docs y decisiones del **repo origen** que aquí no existen.
(Convención: en la doc, el corpus del origen se menciona **sin** prefijo `docs/`, para que
`docs-check` no lo confunda con un enlace de este repo.) Si un comentario te manda a uno de
estos, su equivalente actual es:

| Referencia en el código | Equivalente en este repo |
|---|---|
| `PLAN-REDSYS.md` | `sistemas/REDSYS.md` |
| `PLAN-COMPRA-PRODUCTOS.md` | `sistemas/COMPRA-PRODUCTOS.md` |
| `PLAN-FASE-7-PANEL.md` | `PANEL-ADMIN.md` |
| `PLAN-COOKIES.md` | `sistemas/COOKIES.md` |
| `04-MODELO-DATOS.md` | `MODELO-DATOS.md` (regenerado; el del origen estaba desfasado) |
| `UI-SPINNER.md` (sin ruta) | `sistemas/UI-SPINNER.md` |
| `10-DESPLIEGUE.md`, `08-OPERATIVA-FISICA.md`, `02-CUESTIONARIO.md` | no portados (ciclo de vida del cliente) |
| `DECISIONES.md #N` en comentario HEREDADO (fichero no tocado desde la portación 2026-08-12) | ledger del origen, no portado — NO resolver contra el `DECISIONES.md` local aunque el número exista; el porqué relevante suele estar inline en el comentario |

Los punteros se reescriben al equivalente actual **al tocar cada fichero** (no en barrido).
