# Decisiones #600–#699

> Parte del registro de decisiones de JumpWeb: **`docs/DECISIONES.md` es el índice** y explica las bandas por
> carril. Las entradas van en orden de NÚMERO y una nueva se añade al final. Una decisión revertida lleva
> «Sustituida por #N (fecha)» en su primera línea; sin marca, sigue vigente. Se busca por número, no se lee de
> arriba abajo. Partido el 2026-09-16 desde el fichero único (`DECISIONES #617`) sin tocar una entrada: ese día
> tenía 7 entradas, de #610 a #616; el recuento vivo lo da `grep -cE '^## #[0-9]+ ·' docs/decisiones/600-699.md`.

## #610 · 2026-09-16 · `[DECIDIDO owner]` El programa «producto e instancias»: un producto, N instancias, y la banda 610–639
JumpWeb se separa en **producto** (`main`: API, cajón y panel; la app en repo propio) e **instancias** (un repo por
cliente con landing, tema, configuración, semillas, build de la app y documentos propios), unidos por un **contrato
de instancia** con versión. La regla que decide dónde vive cada cosa: «¿sería distinto para un segundo cliente?».
`JumpWeb` conserva nombre y ruta —renombrar cuesta 6 rutas, 10 nombres, el remoto, el clon del otro ordenador y la
memoria del agente, ligada a la ruta absoluta—; solo se crea `jumpapp`. Spec: `docs/specs/producto-e-instancias.md`.
Carril «plataforma», banda **610–639**.
**Por qué**: medido el 2026-09-16, 79 de 526 decisiones y 14 de 44 specs nombran al cliente, y el mecanismo de
ficheros del cliente (rama huérfana, `aplicar.sh`, 15 rutas ignoradas) no da casa a sus decisiones ni a su estado.
**Consecuencia**: nada de un cliente en `main`, nada de código de producto en una instancia; la rama
`cliente/playjump` es la semilla del primer repo de instancia; la plantilla de instancia la publica el producto.

## #611 · 2026-09-16 · `[DECIDIDO owner]` La landing sale del producto, una por instancia y a mano, y el panel se queda sin CMS
La landing es de cada instancia y consume del producto **solo hechos por la API pública**: precios (catálogo),
horario, los cinco documentos legales, normas y reseñas. La presentación es manual. Del panel salen Attractions,
Faqs, Testimonials, LandingServices, Offers y BarImages y las secciones «textos de la landing» y «bar»; se quedan
ParkRules y Pages. Attractions arrastra el complemento por atracción (0 de 23 en uso) y se retira con él; los campos
de presentación de `ticket_types` se quedan porque los consume el cajón. **El cajón sigue siendo un cajón sobre la
landing**: pasa a ser un paquete con contrato de incrustación, y el producto gana un anfitrión mínimo para las nueve
rutas que hoy renderizan la portada solo para abrirlo.
**Por qué**: diseñar y editar una landing data-driven era insostenible (owner); medido, las specs de landing y diseño
son el 27 % de la doc y 73 de 489 tests son guardas de landing. **Consecuencia**: F5 del programa y v2.0.0; cuatro
endpoints públicos nuevos (horario, legales por clave, normas, prueba social); las 79 decisiones que nombran al
cliente se marcan y no se migran ahora.

## #612 · 2026-09-16 · `[DECIDIDO owner]` La app es nativa, y nace después de la instancia atada a una versión del contrato
Modificada por #627 (2026-09-17): la pila ya está decidida (React Native + Expo, TypeScript); la prueba corta de F6 confirma esa, no compara candidatas.
Opción B: app nativa con código propio contra la API v1, una base y N builds white-label. El repo `jumpapp` se crea
desde la plantilla en F6, no antes: un repo vacío sin reglas es un repo que un agente empieza a llenar a su manera.
La pila se decide en la spec de F6 con una prueba corta por candidata.
**Por qué**: el owner prefiere el tacto nativo a una cápsula sobre el cajón, con el coste de una segunda
implementación de las 25 pantallas asumido. **Consecuencia**: antes de la app, la API necesita login por token —hoy
declara un único esquema de seguridad, la cookie, y ningún endpoint emite tokens aunque el modelo los admita— y un
registro de dispositivo para notificaciones; cada cliente publica en las tiendas con su propia cuenta (guía 4.2.6 de
Apple).

## #613 · 2026-09-16 · `[DECIDIDO owner]` Versionado semántico del producto: v1.0.0 es producción, y producción despliega solo etiquetas
Modificada por #624 (2026-09-17): v1.0.0 es `1272cb93`, no `b0ea5a16`; todo lo demás sigue vigente.
Un solo número, `vMAYOR.MENOR.PARCHE`, sobre el producto entero. **MAYOR**: una instancia tiene que actuar para
actualizar, porque el contrato cambió; su versión es el MAYOR del producto. **MENOR**: capacidad nueva sin acción.
**PARCHE**: arreglo. Etiqueta anotada en `main`; **producción despliega solo etiquetas** (guarda 8 del despliegue) y
staging despliega `main`. `CHANGELOG.md` por versión con dos mitades, «para las instancias» e «interno». La versión
desplegada queda escrita en el servidor y en el estado de la instancia. El contrato OpenAPI sube en MENOR al añadir
y una ruptura es `/api/v2`. **v1.0.0 = `b0ea5a16`**, lo que corre en producción desde el 13-09; la separación de la
landing publica v2.0.0.
**Por qué**: medido, cero etiquetas de versión (las dos que hay son una copia de seguridad y un wip), el contrato
declara 1.0.0 sin moverse en 75 commits que lo cambiaron, y siete despliegues en once días identificados por hash.
**Consecuencia**: con dos agentes empujando a `main` cada hora, la etiqueta separa «esto está listo» de «esto es lo
último»; F3 del programa.

## #614 · 2026-09-16 · `[DECIDIDO owner]` La capa de agente es un plugin compartido, las skills arrancan sin barra, y el estándar de `/carril` es calidad profesional
Skills y hooks van en un **plugin de Claude Code** instalado en las dos máquinas y activado por repo; cada repo lleva
solo su `CLAUDE.md` corto. Las skills arrancan **sin escribir la barra** por tres capas: la descripción como lista de
situaciones y frases del owner (techo 1.536 caracteres), una tabla momento → skill en el enrutador, y hooks
deterministas (`SessionStart`, `UserPromptSubmit`, `Stop`) que inyectan la sugerencia al contexto. **`/carril`
sustituye a `/arranque-sesion` con un estándar**: calidad de código profesional como norma; antes de tocar un
subsistema se miden sus dependencias y sus fronteras, y si su arquitectura es mejorable se propone en spec antes de
codificar. Entran las **reglas 8 y 9 de `CLAUDE.md`**: leer con Read y editar con Edit/Write, y ningún workflow sin
permiso del owner en ese turno. Las herramientas de análisis estático (Larastan, Rector, ESLint) son dependencia
nueva: **pendiente del owner**.
**Por qué**: el modo «auto» del harness inyecta la orden de preferir Bash, y la regla contraria vivía solo en la
memoria de una máquina, copiada a mano al arranque del otro ordenador; la skill de arranque, con «usar SIEMPRE» en su
descripción, no se ejecutó en la sesión del 16 porque el modelo juzgó que no era una sesión de trabajo. Solo hay
Pint y PHPUnit como herramientas de calidad. **Consecuencia**: F2 del programa; salida medible, seis frases del
owner en sesión nueva de cada máquina, 6 de 6.

## #615 · 2026-09-16 · `[DECIDIDO owner]` Techos para la documentación caliente, estado por carriles, y el contador de la suite fuera del estado
Lo que carga la SESIÓN se mide en KB y tiene gate; lo que carga la TAREA se lee bajo demanda desde su spec.
Techos: enrutador ≤ 12 KB con una línea por fila; un fichero por carril en `docs/carriles/` ≤ 24 KB, que cada agente
escribe solo el suyo, con buzón para el otro; tracker ≤ 16 KB; §0 «antes de tocar» de cada spec ≤ 2 KB; decisión
nueva ≤ 1,5 KB, con el registro partido por centenas. Entra la comprobación 9 del gate documental (tamaños y
longitud de fila). El histórico del estado se borra: git es el archivo. **El contador de la suite sale de
`ESTADO.md`** y queda en el trailer del commit de cierre, lo que cambia la parte del hook de `DECISIONES #10` que lo
compara. La mudanza del contenido de las filas del enrutador se verifica con huella: cada frase con aviso localizable
en su spec antes de borrarse.
**Por qué**: arranque en frío de 1,57 MB (enrutador 314 KB inyectado siempre, estado 866 KB, tracker 389 KB), con
crecimiento de +17, +38 y +8 KB al día; la instrucción «mantener CORTO» no frenó nada en 32 días porque el gate no
mide tamaño; cada trampa se escribe en 2 a 6 ficheros. **Consecuencia**: F1 del programa; salida, arranque en frío
≤ 60 KB y huella 100 %.

## #616 · 2026-09-16 · `[DECIDIDO owner]` Las reseñas son mecanismo del producto: Business Profile, API pública sin avatares, y el CMS de testimonios fuera
Las reseñas de Google se quedan en el producto como herramienta del panel: la conexión con Google Business Profile
(`specs/google-business-profile.md`, `#524`, sigue vigente; sus tandas T1 y T2 van después de F5) sobre el contrato
`SocialProof` que ya existe, expuestas a la landing por un endpoint público con valoración, recuento y reseñas con
autor, enlace al perfil, marca de traducción y fuente, **sin avatares**. El CMS manual de testimonios se retira: una
instancia que quiera testimonios escritos los escribe en su landing.
**Por qué**: la mayoría de instancias tendrán reseñas (owner), y un mecanismo idéntico para todos es producto;
copiarlas a mano no es legítimo y envejece; sin avatares no hay petición del visitante a terceros y el consentimiento
deja de hacer falta. **Consecuencia**: el endpoint nace en F5 con la fuente de hoy (Places) y la fuente cambia
después sin tocar consumidores; requisito previo del owner, el proyecto de Google de JumpWeb verificado y «en
producción», porque en prueba el permiso caduca a los siete días.

## #617 · 2026-09-16 · Las decisiones se parten por CENTENAS y `DECISIONES.md` pasa a ser el índice
F1 de `specs/producto-e-instancias.md` (§4.8). El registro único pesaba 2,4 MB con 536 entradas, no monótonas
por las bandas de `CONVENCIONES §10.6` y con cuatro números repetidos (#217, #430, #431, #432). **Se parte sin
reescribir una entrada**: `scripts/partir-decisiones.py` reparte cada entrada a `docs/decisiones/` (un fichero
por centena, `000-099.md` … `600-699.md`) ordenada por número (orden estable: los repetidos quedan adyacentes y se conservan los dos), y
no escribe nada si las entradas reunidas en su orden original no reproducen el fichero byte a byte (control 1)
o si lo escrito en disco no contiene cada entrada verbatim (control 2). `DECISIONES.md` queda como índice:
dónde está cada número, cómo se escribe una decisión y las bandas. **Toda cita «DECISIONES #N» sigue
valiendo**: el check 6 del gate busca la entrada en las partes. Techo de 1,5 KB por entrada para lo posterior
a F0 (número > 616 o fecha posterior a hoy), medido por el check 10; las 498 entradas anteriores que lo
superan no se tocan (spec §3: reescribir cambia el sentido). Descartado: partir por carril (una cita no dice
de qué carril es) y renumerar los repetidos (renumerar ya costó 46 citas reapuntadas).

## #618 · 2026-09-16 · El contador de la suite sale del estado y vive en el trailer del commit; el hook lo lee de ahí
Sustituye la mitad de `#116` que comparaba la suite con la línea «Suite **N en verde**» de `docs/ESTADO.md`.
Esa línea era la que los dos carriles reescribían en cada cierre (711 commits sobre el estado,
`producto-e-instancias.md` §1.3), y este arranque la buscó donde la cabecera de `ESTADO.md` decía que estaba
y no la encontró en su parte viva: solo el trailer del último commit tenía la cifra. **Regla**: el número tiene
UNA copia, el trailer «Verificación: suite N tests / M aserciones (Xs)» de `CONVENCIONES §8`. El `pre-push`
recorre los commits que el push lleva a `main` del más nuevo al más viejo, toma el primero que lo declare y
exige que coincida con lo que la suite acaba de dar; sin ningún trailer no hay número comprobado y el push se
corta (fail-closed, como antes). Consecuencia: ningún fichero de carril lleva el contador, y un cierre sin la
suite corrida no puede empujar. Verificado el extractor contra `7d88538b` (con trailer) y `d898c76a` (sin él,
control); el hook entero, en el push de F1.

## #619 · 2026-09-16 · El enrutador baja a una línea por fila; cada spec gana un §0 y la fila se muda VERBATIM a un anexo
`CLAUDE.md` medía 316.874 B y 64 filas (spec §1.1). Medido antes de mover nada, con
`scripts/huella-enrutador.py`: **1 de las 974 frases con aviso (⚠️ ❗ ⛔) de las filas era localizable en su
spec** (0,1 %) y 6 en las decisiones — las filas eran redacción propia, no copias. Por eso la mudanza no
resume: el guion anexa a cada destino (la primera spec que la fila cita; `sistemas/AFORO-FRANJAS.md`, nueva,
para la fila de aforo que apuntaba a las invariantes) lo que la fila decía, tal cual, como «Anexo · La fila
del enrutador, mudada el 2026-09-16», con guarda por FILA (dos filas comparten spec en dos casos). Huella
después: **974/974**. El **§0 · Antes de tocar** (≤ 2 KB, obligatorio en las 46 specs, medido por el check
10) lo escribe el agente leyendo la spec y la fila: es lo único que carga el arranque. El enrutador queda en
≤ 12 KB: tema → documento y su §0. Regla nueva: una trampa nueva va al §0 o al cuerpo de su spec, nunca a la
fila del enrutador.

## #620 · 2026-09-16 · El gate documental mide TAMAÑO: comprobación 10 — y la 9 ya existía
La spec (§1.1, §4.7, §4.9, §6) contaba «ocho comprobaciones» y llamaba «comprobación 9» a la de techos; al
arrancar F1 se midió que `scripts/docs-check.sh` ya tenía una 9 en el cuerpo —los marcadores de conflicto de
merge, `#506`— aunque su cabecera enumeraba ocho. **La de techos es la 10** y la cabecera enumera las diez.
Techos: `CLAUDE.md` ≤ 12 KB · `00-REFACTOR.md` ≤ 16 KB · `ESTADO.md` ≤ 4 KB (queda como índice de carriles)
· `docs/carriles/*.md` ≤ 24 KB · §0 obligatorio y ≤ 2 KB en cada spec (de `## §0` a la siguiente `## `) ·
decisión posterior a F0 ≤ 1,5 KB (número > 616 o fecha posterior al 2026-09-16; la fecha se busca en toda
la cabecera porque siete entradas del carril del SPA no la llevan, y sin fecha decide el número). Bytes con
`wc -c` y `LC_ALL=C`. Visto en ROJO antes de mudar nada (50 errores: los tres ficheros y 45 specs sin §0) y
con una mutación de tamaño al cerrar F1. Un techo que solo se declara no vigila nada: «mantener CORTO»
llevaba 32 días en la cabecera del enrutador mientras crecía 17 KB al día.

## #621 · 2026-09-16 · El estado pasa a UN fichero por carril; `ESTADO.md` es un índice, el tracker no narra y git es el archivo
`docs/ESTADO.md` (870 KB, once bloques «si entras nuevo» apilados) y `docs/00-REFACTOR.md` (390 KB) se
sustituyen. **`docs/carriles/<carril>.md`** (≤ 24 KB: banda y último usado · ficheros del carril · foto · por
dónde retomar · buzón), uno por carril, y **cada agente escribe solo el suyo**: un mensaje al otro va en el
fichero del EMISOR, el receptor anota «atendido» en el suyo y el emisor lo retira en su siguiente cierre; cero
escrituras cruzadas. `ESTADO.md` (≤ 4 KB) solo lista carriles, máquinas, bandas y dónde está producción: no
cambia al trabajar. El tracker (≤ 16 KB) conserva cada fase con su marcador y sus casillas (el check 7 sigue
mandando) y pierde la narrativa. Medido antes de borrar: 0 de 103 frases con aviso del bloque vivo del estado
y 4 de 477 del tracker eran localizables en los documentos que citan; son crónicas de sesión cuyas decisiones
van por número, y el texto completo queda en `git log -p` de los dos ficheros hasta este commit.
`CONVENCIONES §2, §4, §5, §7, §8 y §10` se reescriben a esta forma; `CARRIL-SPA.md` sigue siendo el arranque
del otro ordenador.

## #622 · 2026-09-16 · `[DECIDIDO owner]` El ciclo de vida de la documentación: referencia, proceso y registro — y archivar no reescribe citas
El owner preguntó qué pasa al terminar un carril o una feature y si la doc sabe borrarse o moverse: tras F1
sabía escribirse con techo, no cerrarse. Medido: `docs/archivo/` existía sin regla de entrada, una spec
marcada histórica seguía entre las vivas, los anexos de `#619` son 300 KB sin fecha de retirada, y la
referencia también acumula (`DEUDA.md` 277 KB, `VERIFICACION-E2E-CAJON.md` 186 KB). **Regla**
(`CONVENCIONES §11`): tres familias. REFERENCIA (`sistemas/`, invariantes, glosario, instalación) se
mantiene con el sistema y tiene techo —48 KB por doc de `sistemas/`, hoy entre 12 y 31; si crece se parte—;
PROCESO (specs, tracker, carriles) se archiva al cerrar: una spec ✅ sin trabajo pendiente se destila en su doc
de sistema y va a `docs/archivo/` con 📜, y su anexo se borra al destilar; REGISTRO (decisiones, deuda) se
marca y no se reescribe, y la deuda cerrada se borra. **Archivar no reescribe citas**: un documento archivado
conserva su nombre y el gate resuelve su ruta antigua bajo `docs/specs/` contra el archivo (las 10 citas del
registro no se tocan); el enrutador solo cita vivos. Comprobación 11 del gate. Aplicado hoy a `desglose-dinero-cliente.md`.
Descartado: un techo para `DEUDA.md` y la verificación E2E hoy, porque ya lo superan; primero se podan y
después se les pone. Las skills de F2 (`/handoff`, `/dod`) ejecutan la mudanza.

## #623 · 2026-09-16 · La capa de agente toma forma: repo-marketplace, hooks en Python fail-open, `Stop` avisa una vez, las skills viejas conviven hasta el 6 de 6
F2 de `specs/producto-e-instancias.md`, sesión 1. El plugin `jumpweb-agente` vive en su repo privado, que es el
marketplace (`.claude-plugin/marketplace.json`) con el plugin bajo `plugins/` (sitio para un segundo; no hay dos
manifiestos en una carpeta); fuente `url` HTTPS, porque el owner empuja con credenciales HTTPS y no SSH (la
actualización en segundo plano no autentica en privados: a mano). Sin `version`: cada push es una versión por
hash. Hooks en Python 3 (jq no está; el guard de Bash ya exige python3), deterministas y fail-open.
`SessionStart` inyecta «ejecuta /carril» y las reglas del owner (también tras compactar); `UserPromptSubmit`
sugiere por `reglas/momentos.json` y nunca bloquea; `Stop` bloquea UNA vez por estado y solo con commits sin
empujar, porque es lo único que el owner ve por ese evento (medido en la doc de hooks); un árbol sucio no avisa.
Las skills de plugin se llaman `/jumpweb-agente:x` y `/x` si nadie más usa el nombre: `arranque-sesion`,
`cierre-sesion` y `dod` del repo siguen hasta el 6 de 6. **Medido**: arnés 38/38 y una sesión real
`claude -p --plugin-dir` con los tres hooks disparando y las once skills listadas. **Trampa**: el clasificador
del modo «auto» deniega escribir los seis ficheros que inyectan contexto; se resuelve con permiso del owner, no
rodeándolo. Referencia: `sistemas/CAPA-DE-AGENTE.md`.

## #624 · 2026-09-17 · `[DECIDIDO owner]` v1.0.0 es `1272cb93`, lo que corre en producción; y la guarda 8 toma forma
F3 de `specs/producto-e-instancias.md`. `#613` fijó v1.0.0 en `b0ea5a16` el 16-09 por la mañana y esa noche el
octavo despliegue subió `1272cb93` (`#569`–`#571`): el principio —«v1.0.0 es producción»— sigue, el hash cambia.
El owner eligió una sola etiqueta sobre lo que corre hoy. **Guarda 8** de `scripts/deploy.sh`, lo PRIMERO del
pre-vuelo local: con `DEPLOY_PRODUCTION=1`, HEAD tiene que ser una etiqueta ANOTADA `vX.Y.Z` exacta y estar en
`origin`; con `--go` aborta, en seco avisa y sigue (como el árbol sucio); staging despliega `main`. La versión
queda escrita en `storage/app/version` del servidor (`versión hash fecha`; `storage/` no viaja, el `--delete`
no la toca) y la salud la relee. `CHANGELOG.md` nace con la v1.0.0.
**Por qué**: producción no tiene `.git`; una etiqueta solo local es un hash con nombre, y la otra máquina no
sabría qué corre allí. **Medido**: un `--go` de producción sin etiqueta sale con 1 antes de la primera conexión;
`DeployScriptGateTest` EJECUTA el script en un repo de usar y tirar (ligera, `v1.0`, `-rc1`, sin empujar, un
commit por delante: todas abortan); `scripts/mutar-guarda8.sh` 9/9.
**Descartado**: v1.0.0 en `b0ea5a16` más v1.1.0 o v1.0.1 en `1272cb93` (el changelog nacería con dos versiones y
`#571` pidió cuatro tokens al `client.css`, que roza el MAYOR); abortar también en seco (el plan no se podría
mirar antes de versionar). **Consecuencia**: el noveno despliegue exige `/release` antes.

## #625 · 2026-09-17 · `[DECIDIDO owner]` Entra el análisis estático: Larastan y ESLint, con línea base y dentro del gate
Dos dependencias de desarrollo nuevas (`CONVENCIONES §9`): **Larastan** (PHPStan para Laravel) en nivel 5 sobre
`app/`, y **ESLint** con las reglas de Vue sobre `resources/js/sidebar/` (el cajón). Las dos con **línea base**:
lo que ya hay se congela en un fichero versionado y solo bloquea un error NUEVO. Las dos corren en el `pre-push`
de `main`, detrás de Pint y antes de la suite. **Rector no entra**: reescribe código; se usa puntual y a mano,
nunca como gate.
**Por qué**: hoy la calidad la vigilan Pint, la suite y las guardas de arquitectura, y ninguna ve un tipo mal
pasado ni una rama muerta; F4 añade credenciales (tokens Bearer) y conviene llegar con la red puesta.
**Descartado**: solo Larastan (el cajón son 25 pantallas sin más red que sus tests); ninguna hasta F4.
**Consecuencia**: tarea propia del carril de plataforma, ANTES de activar nada se mide el tiempo que añade al
gate y el tamaño de la línea base; ESLint toca `package.json`, que es compartido: se avisa en el buzón al
carril del SPA antes de empujarlo. `PrePushGateTest` fija los dos pasos nuevos.
**Ejecutado (Larastan, 17-09)**: 459 errores congelados, 10 s en frío y 2 s con caché; la línea base lleva
trinquete (`StaticAnalysisGateTest`: solo baja); `scripts/mutar-analisis-estatico.sh` 8/8. ESLint: `#629`.

## #626 · 2026-09-17 · `[DECIDIDO owner]` El harness sigue en modo «auto», con reglas `allow` estrechas — y producción fuera
El modo de permisos no cambia. Lo que el clasificador deniega siendo legítimo y LOCAL se abre con reglas
`allow` exactas en los ajustes de USUARIO de cada máquina (`~/.claude/settings.json`), no en los del producto:
`Bash(claude plugin marketplace update jumpweb-agente)`, `Bash(claude plugin update jumpweb-agente@jumpweb-agente:*)`
y `Edit`/`Write` sobre `~/proyectos/jumpweb-agente/**`. **Las escribe el owner**: un agente que se amplía sus
propios permisos es justo lo que el clasificador debe parar. **Producción queda FUERA a propósito**: `scp`,
el `--go` del despliegue y cualquier `ssh` de escritura los ejecuta o los aprueba el owner en el momento.
**Por qué**: medido tres veces (hooks el 16-09, `scp` y `--go` el 16-09, `claude plugin update` el 17-09 con
la petición del owner delante): el clasificador no distingue «lo pidió el owner». **Descartado**: el modo por
defecto (un aviso por comando sensible, todo el día); dejarlo como está (el owner de recadero). **Consecuencia**:
requiere `claude` en el PATH (el enlace `~/.local/bin/claude` estaba roto); en la extensión de VSCode `/plugin`
no existe: todo lo del plugin va por la terminal.

## #627 · 2026-09-17 · `[DECIDIDO owner]` La pila de la app: React Native + Expo, en TypeScript
La app nativa (`#612`) se escribe en **TypeScript** con **React Native + Expo**: una base de código, Android e
iOS, y un build por instancia. Cierra el pendiente F6a de `specs/producto-e-instancias.md` §7; la recomendación
era del agente y el owner la hace suya.
**Por qué**: una persona y sus agentes mantienen ya 25 pantallas en JavaScript (el cajón: 127 `.js` y 51 `.vue`)
y la app es su segunda implementación (spec §4.10): misma familia de lenguaje, y de `openapi/v1.yaml` sale un
cliente tipado. El desarrollo corre en WSL2 sobre Windows e iOS no se compila fuera de macOS: Expo trae la
compilación en la nube (EAS Build). La guía 4.2.6 pide un build por cliente, y la configuración dinámica de Expo
lo da desde la carpeta de la instancia (`build-cliente`, spec §4.7). Del tema se reutilizan los 65 tokens, no el CSS.
**Descartado**: Flutter (Dart, un lenguaje ajeno a todo lo que hay en la casa); dos apps nativas, Swift y Kotlin
(tres implementaciones de las mismas pantallas); la cápsula sobre el cajón (fuera desde `#612`).
**Consecuencia**: modifica `#612` en una frase: la prueba corta de F6 ya no compara candidatas, CONFIRMA esta
(token de F4, una pantalla contra `/api/v1`, cámara para el QR, una notificación, build de iOS sin Mac, coste de
EAS) y, si falla en algo medido, vuelve al owner. TypeScript es nuevo en la casa y vive solo en el repo de la
app, `jumpapp` (futuro), que nace en F6 desde la plantilla; a este repo no entra ninguna dependencia.

## #628 · 2026-09-18 · `[DECIDIDO owner]` La promo «−20 % online» es una CHAPUZA declarada: precios como dato, el «antes» y el recuadro por dos ajustes, sin sistema
El owner (17-09, 22:30 → 18-09, 00:30): las 5 entradas de PlayJump bajan un 20 % **como dato** (las 9 filas de
`prices` × 0,8 y el badge «−20 % online», con copia previa y `audit_logs`; `ENTORNOS.md` §6), la web enseña el
**precio de antes tachado** en la card de la portada y en `/precios`, y un **recuadro con el copy** encima del
carril. *«No quiero spec, ni sistema ni nada; es una chapuza rápida, más adelante haremos un sistema de
ofertas.»* El cajón no se toca.
**Cómo**: dos ajustes de la instalación sin pantalla en el panel —`promo.percent` (el «antes» deshace esa
rebaja: `Setting::promoPercent()`, `WritesLandingValues::antes()`) y `promo.banner.{es,en,fr}`— leídos por
`RateCards`, `RateTable` y el payload compartido. Cero dinero: ningún precio que se cobre cambia y ningún
fichero del `CRITICAL_RE` se toca. Apagar la promo = borrar las filas, sin desplegar.
**Por qué**: el owner quiere la promo esta semana y una sola vez; taquilla cobra de la misma tabla, así que el
copy dice «online», no «solo online». **Descartado**: la spec del mecanismo (`archivo/promo-precio-anterior.md`:
`compare_at_cents` + promo con fecha de fin), aparcada, y el descuento por canal en el resolutor (ocho servicios
del núcleo). **Consecuencia**: v1.1.0 (`/release`, despliegue de noche); el «antes» solo es cierto mientras el
porcentaje del ajuste sea el que se aplicó al catálogo; al subir los precios se borran las filas EL MISMO DÍA.

## #629 · 2026-09-18 · `[DECIDIDO]` ESLint del cajón: `flat/essential`, `no-use-before-define` a coste cero y la línea base NATIVA
La mitad de `#625` que faltaba. `eslint.config.js`: reglas base de ESLint + `vue flat/essential` sobre
`resources/js/sidebar/**/*.{js,vue}`, más `no-use-before-define` con `variables: false`. Línea base = las
**supresiones nativas** de ESLint (`eslint-suppressions.json`, `--suppress-all`): **12 errores** congelados el
18-09. Paso `npm run lint:js` en el `pre-push`, detrás de Larastan y antes de los builds (~2 s sin caché).
**Por qué**: medido, los tres juegos de Vue dan los MISMOS 12 errores; `strongly-recommended` y `recommended`
añaden 4.364 y 4.375 avisos de formato, y el cajón no tiene formateador: eso no es una red. `variables: true`
daba 16 errores que son funciones que corren después de existir la constante; con `false`, cero. La línea
base nativa trae el trinquete puesto: un error congelado que se arregla sin podar sale con código 2.
**Descartado**: una línea base casera (un JSON propio y un guion que compare); apagar
`vue/multi-word-component-names` (3 de los 12) en vez de congelarlo: apagar reglas es la puerta que la guarda cierra.
**Consecuencia**: `StaticAnalysisGateTest` fija reglas, alcance, comando exacto y `FROZEN_JS_ERRORS = 12` (solo
baja); `mutar-analisis-estatico.sh` 20/20. El primer día cazó un defecto VIVO del SPA (`addBtn` sin declarar
en `DependentsZone.vue`), avisado en el buzón. Cada clon necesita `npm install` tras traerlo.

## #630 · 2026-09-18 · `[DECIDIDO owner]` Lo técnico se decide por el estándar profesional; el emisor de Bearer queda aprobado, con rotación
El owner, al abrir F4: *«las decisiones técnicas acepto tu recomendación basándote en el estándar profesional…
y si afectan al tipo de producto que será este proyecto dame las opciones más profesionales y yo las elijo»*.
Regla de trabajo desde hoy: **técnica → decide el agente y lo justifica con medida; tipo de producto → opciones
al owner, la recomendada primero**. Con ella queda ✅ `specs/token-bearer.md`: `POST /auth/tokens` (correo,
contraseña, `device_name`), `PasswordLogin::verify()` sin sesión con los DOS limitadores y las MISMAS claves que
el login, ability `api-v1` exigida en el grupo autenticado, tope de 10 tokens por cuenta, respuesta `no-store`,
y duración **B: 30 días con rotación** (`POST /auth/tokens/rotate`). Contrato 1.1.0.
**Por qué**: medido, casi todo existía desde la Fase 3 (Sanctum, caducidad, poda, revocación por cinco vías);
faltaba el emisor, y `attempt()` abre sesión, así que no servía tal cual. **Descartado**: JWT (no se revoca),
30 días sin rotación (la app pide la contraseña cada mes), 180 días (un token perdido vive medio año), `['*']`
como ability. **Consecuencia**: implementa plataforma en el orden del §0 de la spec; el cajón NO usa token;
Google y el alta desde la app quedan para F6, con nombre.

## #631 · 2026-09-18 · `[DECIDIDO owner]` La landing consume un MENÚ DE HECHOS y todo es opcional; identidad y contacto por API; el widget de ofertas se retira
Matizada por #684 (2026-09-24): vuelven las promociones al panel como ETIQUETAS (texto e icono) y un aviso arriba.
El owner: todo lo que la landing pueda consumir es **opcional** —cada cliente diseña la suya y lo usa o no—;
el menú es más ancho que precios, normas y legales: **identidad y contacto van por API**, y habrá ficha de
producto (título, descripción). El sistema de ofertas de hoy (imágenes y texto en un icono flotante) **se quita**.
**Diseño** (`specs/cajon-empaquetable.md` §4.5 y §4.6, técnico): el cajón no depende de que la landing lea nada
—su arranque son dos lecturas propias, `cajon/boot` pública y cacheable y `cajon/session` privada y `no-store`—;
entra al menú lo que el negocio OPERA desde el panel y perjudica si se equivoca; **un modelo de lectura, dos
transportes** (HTTP y la vista de la vía B reciben el mismo payload); **lista blanca por `Resource`**, nunca un
volcado de `settings` (medido: la tabla mezcla `contact` con `redsys_secret_key`); caché pública con `ETag`
(hoy `no-cache, private`); «oferta» = hecho de precio (`was_price_cents`).
**Descartado**: un endpoint de «textos de la landing» (es presentación: de la instancia); el cargador con el
arranque incrustado por petición. **Consecuencia**: `specs/producto-e-instancias.md` §4.4 crece (`site`,
`prices`, `policies`); abiertas P1–P3 (ficha con imagen, kit declarativo, atracciones); alcance fino en la spec de F5.

## #632 · 2026-09-18 · `[DECIDIDO owner]` Tres rasgos del producto: ficha con descripción e imagen, API ahora y kit después, y las atracciones fuera del panel
Preguntado con opciones cerradas tras `#631`, el owner eligió las tres recomendadas.
**P1 · La ficha de producto gana descripción e imagen**: cada producto y cada zona llevan en el panel una
descripción traducible y UNA imagen opcional, servidas por la API. **Por qué**: la app nativa (F6) no tiene
landing y venderá con lo que dé la API; sin esto habría que empaquetar material de cada cliente en su build.
*Descartado*: solo texto; nada nuevo.
**P2 · API ahora, kit después**: F5 publica la API con su contrato; un kit declarativo (atributos que el
cargador del cajón rellena solo) se construye cuando una segunda instancia lo pida. **Por qué**: diseñarlo hoy
es diseñarlo sin quien lo valide. *Descartado*: solo API para siempre; kit desde F5; componentes ya maquetados
(devuelve presentación al producto: el CMS que `#611` sacó).
**P3 · Las atracciones salen del panel**: medido, las 23 son presentación (nombre, descripción, imagen, chapa,
etiqueta de edad en texto; sin aforo ni venta). Las restricciones que importen viven en Normas. Se retira con
ellas el complemento por atracción (0 de 23 en uso). *Descartado*: hechos mínimos; dejarlas enteras.
**Consecuencia**: P1 añade campos e imagen al catálogo del panel (F5, sin dinero ni aforo) y `catalog/*` crece
en MENOR. Escrito en `specs/cajon-empaquetable.md` §4.6.

## #633 · 2026-09-18 · `[DECIDIDO owner]` F2 se cierra, y el cajón de F4 lo implementa el carril de plataforma
El owner: *«El F2 lo vamos a cerrar, ya lo hemos revisado. El F4 vamos a proceder con él aquí; el otro agente en
el otro ordenador está en el carril diseñando la invitación digital.»*
**F2 cerrada**: se retiran `.claude/skills/{arranque-sesion,cierre-sesion,dod}` y sus menciones (enrutador paso
0, `CONVENCIONES`, `CAPA-DE-AGENTE.md`, `CARRIL-SPA.md`) y el plugin deja de ofrecer `/arranque-sesion` como
respaldo: sin el plugin no hay skill de arranque, se instala. ⚠️ Dicho como es: el criterio de salida escrito
era «6 de 6 en sesión nueva de cada máquina» y **el agente no lo midió**; lo medido es el 5 de 6 del 17/18-09,
los cuatro arreglos del mapa con su mutación (`1377d58`) y el hook disparando `/carril` en la sesión del 18-09.
Lo cierra el owner, que es quien teclea las frases y las dio por revisadas.
**F4 · el cajón cambia de manos**: `specs/producto-e-instancias.md` §4.9 lo asignaba al carril del SPA; lo
implementa plataforma, en este ordenador, porque el SPA está con la invitación digital. **Consecuencia**:
plataforma entra en ficheros del SPA (`resources/js/sidebar/**`, `public/css/site.css`, el layout, `app.js`):
se avisa en el buzón ANTES de cada tanda, se trabaja por tandas pequeñas y empujadas, y no se toca nada de la
invitación. La lectura previa de la spec por el SPA deja de ser condición: se le pide en paralelo.

## #634 · 2026-09-18 · `[DECIDIDO owner]` El cajón que NACE abierto se lleva el foco dentro del panel
Preguntado con las dos variantes delante (F4 · T3a). Cuando el cajón nace abierto —`/entradas`, las puertas de
cuenta y la vuelta de la pasarela—, `cajon/shell.js` mete el foco en el primer control del panel, igual que ya
hacía al abrirlo con un clic.
**Por qué**: es la conducta de un diálogo modal. Quien llega a esas páginas se encuentra el panel delante y, con
teclado o lector de pantalla, tenía el foco DETRÁS del telón, en una página que no puede operar.
**No es conducta nueva**: el `a11yPanel` que la T3a sustituyó la llevaba escrita —«si el panel ya estaba abierto
al cargar»— y la condicionaba a `this.$data.$evaluate`, que no existe; nunca corrió. Medido al reescribir la
carcasa, y por eso se preguntó en vez de arrastrarlo.
**Consecuencia**: cambio VISIBLE y aceptado — el anillo de foco sobre la × al cargar esas páginas, que es el
único píxel que se mueve (comparado en navegador). Caso en `shell.test.js`, fila en la sonda
(`scripts/sonda-cajon-apertura.mjs`, sección D) y mutación en `scripts/mutar-cajon-apertura.sh`.
**Descartado**: dejarlo como estaba (la paridad que la T3a mantuvo a propósito hasta preguntar).

## #635 · 2026-09-18 · `[DECIDIDO]` La hoja del cajón se GENERA, y sus tokens viven en la raíz del cajón
F4 · T4. `public/css/cajon.css` sale de `scripts/hoja-del-cajon.py` desde las tres hojas del producto, en su
mismo orden de carga; `site.css` no se toca. **Se genera y no se parte** porque de los 102 bloques que emite el
cajón, 45 los pinta también la landing: moverlos la deja sin estilo y duplicarlos crea dos copias que divergen.
**Dónde se declaran los tokens**: en `:where(.sidecart)`, no en `:root`. Con `.sidecart` a secas el producto le
ganaba a `client.css`; con `:where(:root)` mandaba el ANFITRIÓN y el cajón salía azul marino en una landing
ajena. Así, la PROXIMIDAD le gana al `:root` de quien sea y la especificidad 0 deja mandar al tema.
Jerarquía: **instalación → paquete → anfitrión**. Las tres pasadas, medidas, en la spec §4.3.
**Consecuencia de contrato**: un tema de instalación declara sus tokens en `:root, .sidecart`
(`INSTALACION-CLIENTE.md` §4). Hace falta cuando la instalación sirva SU landing con el paquete (F5); en la
landing del producto no cambia nada. ⚠️ Lo que NO se hace es scopear el tema del PANEL: ver `#637`.
**Consecuencia**: `HojaDelCajonTest` (5, con el sello SHA-1 de las fuentes en la cabecera de la hoja),
`scripts/mutar-hoja-del-cajon.sh` 8/8, sonda 34/34 y el juez `huella-maquetacion.mjs --cajon` en 0 diferencias.
**Descartado**: partir `site.css`; y namespacar los 193 tokens (`--jw-bg`), que arrastra la landing entera.

## #636 · 2026-09-19 · `[DECIDIDO]` El cargador del paquete es entrada propia, y su ruta estable REDIRIGE
F4 · T5. `resources/js/cajon/paquete.js` es entrada de Vite y `/cajon/paquete.js` la ruta que escribe una
landing. Medido: una página ajena descargaba **25,3 KiB** (la entrada del producto, con la coreografía del nav,
el hero y los raíles) y ahora **6,1 KiB**.
**Por qué 302 y no servir los bytes**: el módulo construido trae sus `import()` en RELATIVO y los resuelve
contra su propia URL; servido desde `/cajon/paquete.js` pediría `/cajon/sidebar-<hash>.js` —404— y el cajón no
abriría, sin que fallara la carga del cargador. Con el 302 los chunks resuelven solos.
**La redirección no se cachea a propósito**: `RGPD-04` pone `no-store` en toda la web y **no se abre una
excepción a una invariante de privacidad para ahorrar un salto**; el fichero con hash sí se cachea largo.
**Consecuencia**: `PaqueteDelCajonTest` (4), presupuesto del cargador en `SidebarBundleBudgetTest` y sonda
42/42 con una COMPRA entera desde la página ajena (201 + salto firmado, con la pasarela interceptada).
⚠️ **Y destapó un agujero**: el presupuesto de la landing medía solo el fichero de la entrada, así que al salir
`cajon/**` a un chunk compartido pasó a ver 19,3 KiB de los 25,3 reales. Ahora suma los importes estáticos.
**Descartado**: nombre sin hash para esa entrada (rompe la caché inmutable y pide cabeceras por fichero en el
servidor de cada instancia); servirla bajo `/api/v1` para heredar su caché (mete un asset en el contrato).

## #637 · 2026-09-19 · `[DECIDIDO]` El tema del PANEL no se scopea al cajón: lo compensa el instrumento
Cazado al preparar la v1.2.0, con la pregunta del método delante («¿qué tendría que tocar PlayJump para
desplegar esto?»). La T4 había puesto el `<style id="jj-theme">` en `:root, .sidecart` para que el juez de la
hoja del paquete diera cero. **Medido con el `client.css` de producción**: dentro del cajón, `--on-brand`
pasaba de `#101418` (el del cliente) a `#14130F` (el del panel) — el tema del producto, más cerca de los nodos
que el `:root` donde tematiza la instalación, le ganaba. Un despliegue habría cambiado los colores del cajón
de una instalación sin que nadie tocara su tema, y ninguna guarda lo veía.
**Por qué no hacía falta**: en la landing del producto no se carga `cajon.css`, y en una página ajena ese
`<style>` ni existe. El único sitio donde las dos hojas coincidían era el montaje ARTIFICIAL del juez.
**Consecuencia**: el bloque vuelve a `:root`; `huella-maquetacion.mjs --cajon` re-escopa ese `<style>` EN LA
PÁGINA antes de comparar, y `TemaDeInstalacionMandaTest` (3) vigila las dos mitades del orden de cascada
—instalación → paquete → anfitrión—, con sus dos mutaciones en `scripts/mutar-hoja-del-cajon.sh` (10/10).
▶ **La regla que deja**: *un juez no arregla el producto para poder aprobarlo; compensa su propio montaje.*
**De paso**: la huella dejaba de mirar el `<head>`, porque sus hijos no se pintan y renumerarlos daba 1.780
«diferencias» de cinco etiquetas invisibles por página.

## #638 · 2026-09-19 · `[DECIDIDO]` Guarda 9 del despliegue: lo que git no ve, `rsync` sí lo sube
Cazado también al preparar la v1.2.0. `deploy.sh` sincroniza el ÁRBOL DE TRABAJO, y la guarda que exige árbol
limpio se apoya en `git status --porcelain`, que **calla los ficheros ignorados** —`.gitignore` y, peor,
`.git/info/exclude`, que solo existe en una máquina—. El banco de pruebas de la F4
(`public/landing-ajena.html`, una landing falsa) estaba justo ahí y habría viajado en el décimo despliegue:
**publicado en el dominio del cliente, indexable y sin aparecer en ningún diff ni en ninguna revisión**.
**Qué hace**: lista lo que git no conoce bajo `public/` y aborta con `--go`; en seco, avisa. La lista blanca
—`build/`, `uploads/`, `storage`, `hot`, `css/client.css`, `img/`— es lo que vive ahí fuera de git a
propósito, y por eso se comprueba en las DOS direcciones: corta, el despliegue abortaría siempre y alguien la
borraría de noche; larga, deja de ver aquello para lo que existe.
**Consecuencia**: `DeployScriptGateTest` gana su fila y un caso que lee el patrón REAL del script (mutación
vista: con `.*\.html` en la lista blanca, rojo).
▶ La lección general, que vale más que la guarda: **ignorar un fichero en git no lo esconde de las
herramientas que copian el directorio**.

## #639 · 2026-09-19 · `[DECIDIDO owner]` Las tres de F5: redes en el panel, los campos muertos fuera, y «cero marca» = código vivo
Las tres se preguntaron con el CENSO delante (`specs/instancia-y-landing-fuera.md` §1) y las tres salieron por
la recomendada.
**Las redes** (Instagram, TikTok, feed) **se quedan en el panel** y viajan como hecho en `site.social`: medido
que ningún correo las usa, pero cambiar un Instagram tiene que ser editar un campo, no desplegar el repo de la
instancia. **Los cuatro campos muertos de `zones`** —`subtitle`, `age_label`, `area_sqm`, `rides_count`, que
no lee nadie fuera del formulario del panel— **se retiran con su columna**: un campo que se pide y no sale a
ningún sitio se vuelve a rellenar creyendo que sirve. **«Cero marca del cliente en el código» se lee como
CÓDIGO VIVO** —lo que viaja al navegador, a la BD o a un correo—: de 167 apariciones, 166 son citas de
artboards dentro de comentarios y borrarlas quitaría el porqué de cada pieza sin quitar una sola marca; la
única viva (`pjp-salta-record`, una clave de `localStorage`) cae en la T4.
**Consecuencia**: la spec de F5 pasa a ✅ y su §4 queda cerrado; la retirada de columnas es migración, así que
confirma el **MAYOR** de la v2.0.0. ⚠️ **Con este número se AGOTA la banda 610–639**: el carril de plataforma
sigue en **640–669** (fila añadida a `docs/DECISIONES.md`, mismo trato que el SPA al pasar a 700–729).
**Descartado**: bajar las redes al repo de la instancia; dejar los campos muertos; y aplicar el criterio de
marca al pie de la letra.

## #640 · 2026-09-19 · `[DECIDIDO]` El menú de hechos se sirve por lista blanca, y un secreto no sale ni declarándolo
F5 · T1. Nace `Platform\Services\PublicFacts` y con él `GET /api/v1/site` (identidad, dirección, contacto,
redes, legal y SEO; contrato **1.3.0**, cacheado en público con `ETag` y sin idioma: nada de eso se traduce).
**Por qué no se lee `Setting::value()` en un recurso público**: la tabla son 71 filas con `redsys_secret_key`
a dos de `contact.email`. Un recurso declara su lista y lee por ahí; pedir una clave no declarada **revienta**
en vez de devolver `null`, que se confundiría con «la instalación no lo ha rellenado».
**La segunda red**: un secreto no sale **ni declarándolo**, negado por FAMILIA (`*secret*`, `*key*`,
`*token*`, `*password*`, `redsys_*`). Una lista blanca protege del olvido, no del copiar y pegar.
**Dos cosas que decidió la primera respuesta real**: lo no rellenado NO viaja (ni como `""`), y cada bloque
se emite con `(object)` porque un array vacío de PHP sale `[]` —el tipo no puede depender de si rellenaron el
panel—. Y `business.address` es el domicilio FISCAL: servirlo dentro de `address` habría mandado a las
clientas a la gestoría (en la instalación real son municipios distintos).
**Consecuencia**: `SiteFactsTest` (6), `PublicFactsBoundaryTest` (7), `scripts/mutar-menu-de-hechos.sh` 8/8.
**Descartado**: un `/settings` filtrado en el controlador (la lista deja de ser revisable) y publicar cadenas
vacías (obliga a cada landing a filtrarlas otra vez).

## #641 · 2026-09-19 · `[DECIDIDO]` El horario va en DOS rutas, y el «abierto ahora» se calcula una sola vez
F5 · T2 del menú. `GET /api/v1/schedule` (semana, temporadas, fechas especiales; caché de 5 min) y
`GET /api/v1/schedule/now` (el estado en vivo; **1 min**). Contrato **1.4.0**.
**Por qué dos**: el calendario cambia cuando alguien toca el panel y el estado cambia dos veces al día —y en
ese minuto es cuando importa—. Juntos obligaban a elegir entre recalcular el calendario en cada visita o
decir «abierto» cinco minutos después de cerrar. Mismo criterio que separó `boot` de `session` en F4 · T1.
**Y nace `Content\Services\OpeningState`**: el hecho salía de `HeroStatus`, mezclado con los rótulos del chip.
Ahora lo calcula un sitio y lo visten dos; antes lo resolvían los dos por su cuenta y el día raro habrían
dicho cosas distintas sin que nada fallara. Hay un test que los compara.
**`weekday` es 0 = domingo** (Carbon y `Date.getDay()`), no ISO, y va escrito en el contrato: asumir lo otro
desplaza la semana entera de una landing sin romper nada. Se clava contra una fecha real.
**Consecuencia**: `ScheduleFactsTest` (8), `scripts/mutar-menu-de-hechos.sh` 13/13, y la guarda de frontera
pasa a barrer LOS 37 recursos de la API en vez de fiarse del nombre del fichero —`OpeningNowResource` se le
escapaba al patrón `*FactsResource` el mismo día que nació—.
**Descartado**: servir el estado en vivo dentro del calendario; y dejar que la landing lo calcule, que es
pedirle que reimplemente `special_dates` > `seasons` > `opening_hours` y la zona horaria del parque.

## #642 · 2026-09-19 · `[DECIDIDO]` Las normas salen traducidas por el servidor, y el ORDEN es parte del dato
F5 · T3 del menú. `GET /api/v1/rules?lang=` sirve las normas ACTIVAS ya ordenadas por el recorrido de una
visita (`before` → `gate` → `inside`, y dentro de cada uno la posición del panel), con su porqué y con la
fecha de la última tocada. Contrato **1.5.0**.
**El idioma va en la URL** —primer plato del menú que se traduce— por lo mismo que en `/sidebar/boot`: lo
cacheable tiene que ser función de su URL. **Y no se manda el mapa de idiomas**: la cadena «idioma → respaldo
→ la primera que haya» es del dominio, y repartirla entre landings da tres respaldos distintos.
**Una norma desactivada no vuelve por la API**: quien la retiró en el panel cree que ya no está.
⚠️ **El orden salía AL REVÉS** en la primera versión (`sortBy` con array de cierres): la visita contada de
atrás adelante, con el JSON válido y las diez normas dentro. Lo vio una llamada real. Ahora es una clave
compuesta `momento-posición` y el caso comprueba por nombre.
**Consecuencia**: `RulesFactsTest` (8) y `scripts/mutar-menu-de-hechos.sh` 17/17. Y se cerró un punto ciego
de `ApiContractTest`: **no bajaba a los `items` de una lista**, así que un campo de más en cada elemento
pasaba el contrato; los tres esquemas del menú son listas y se habrían colado. Lleva control propio, porque
una comprobación más permisiva no la caza una mutación.
**Descartado**: servir todas las traducciones y que elija el cliente.

## #643 · 2026-09-19 · `[DECIDIDO]` Los legales se publican INTERPOLADOS, y viaja la página, no lo firmado
F5 · T4 del menú. `GET /api/v1/legal/documents` y `/legal/documents/{clave}?lang=`. Contrato **1.6.0**.
**Interpolados**: el texto guardado lleva `:legal_name`, `:legal_nif`, `:legal_address` y `:legal_email`, que
el producto sustituye al renderizar. En crudo, una landing publicaría «El responsable del tratamiento de tus
datos es :legal_name» en su política de privacidad.
**Y viaja la PÁGINA**: medido, para `condiciones` y `waiver` hay además una versión publicada que es la
página interpolada y CONGELADA, y que es a lo que alguien se obliga. Son dos oficios distintos —en la
instalación de referencia el justificante tiene once secciones como página y una como versión—: servir la
versión quita diez secciones de explicación, servir la página como lo firmado publica texto que nadie firmó.
La versión va al lado como dato (`signed_version`); lo firmable sigue en `/legal/waiver`.
**No cuelgan de `/legal/{clave}`** porque `/legal/waiver` ya existe y dice otra cosa (el régimen del
justificante): la ruta genérica lo habría ensombrecido y dos peticiones iguales darían formas distintas.
**Consecuencia**: `LegalDocumentsTest` (8), `scripts/mutar-menu-de-hechos.sh` 21/21. ▶ Larastan corrigió el
contrato: `published_at` es `NOT NULL`, así que se EXIGE — un «por si acaso» contra el esquema es una mentira
que cada cliente tendría que programar.
**Descartado**: publicar el snapshot firmado como si fuera la página.

## #644 · 2026-09-19 · `[DECIDIDO]` Los precios por tarifa, y la promo NO entra en el contrato
F5 · T5 del menú. `GET /api/v1/prices?lang=` da el precio de cada producto en cada tarifa, con el rótulo que
escribe el panel. Contrato **1.7.0**. Importes en céntimos enteros: el símbolo y el formato los pone quien
pinta.
**No duplica `/catalog/products`**, que publica el «desde» para el embudo: aquí está el desglose por tarifa,
que no vivía en ningún sitio público. Un hecho, un sitio — y un caso vigila que el «desde» no aparezca aquí,
porque el catálogo lo calcula con los tramos de volumen (`#324`) y esto no: divergirían.
**La promo `#628` NO entra**: es una chapuza declarada con fecha de caducidad, y un contrato público no se
rompe después para desmontar un apaño. Lo traerá el mecanismo de ofertas de `#631`.
**Una tarifa sin precio se calla**, no viaja con `0`: un cero es un precio, y uno muy llamativo.
**Consecuencia**: `PricesFactsTest` (6), `scripts/mutar-menu-de-hechos.sh` 24/24. ▶ Dos cosas que enseñó la
tabla: `prices` es polimórfica y su `priceable_type` usa el ALIAS del morphMap (`ticket_type`) —con el FQCN
la fila se escribe y el producto sale sin precios—, y tiene columna `currency`, así que la moneda sale del
dato y no de una constante.
**Descartado**: servir el «desde» otra vez; y escribir `EUR` a mano teniendo la columna al lado.

## #645 · 2026-09-19 · `[DECIDIDO owner]` La ficha de producto y de zona: la foto se SUBE al hueco de la instalación
F5 · T6 del menú (`#632` P1). `GET /catalog/zones` pasa a servir la FICHA —descripción y foto— y el producto
estrena foto; contrato **1.8.0**. Los cuatro campos son opcionales: lo que la instalación no rellenó no
viaja, **ni como `""`**, y «escrito y borrado» cuenta como no rellenado.
**El owner elige que la foto del PRODUCTO se suba desde el panel** al disco `uploads` (`public/uploads`,
gitignorado y excluido del `rsync --delete`), no que se escriba una ruta como hace la zona: así la clienta
cambia su foto sin commitear al repo del producto, que es lo que `#632` quería evitar. Medido: **35 imágenes
del cliente están versionadas en `main`** y las 3 zonas con foto apuntan ahí.
**La ZONA no se convierte hoy**: mover esos ficheros es tocar material del cliente y va con la tanda de la
landing. Las dos formas conviven sin que el contrato se entere: las dos salen como **URL absoluta**.
**Reparto asimétrico**: la FOTO viaja en la lista y la DESCRIPCIÓN solo en el detalle — un catálogo se
recorre mirando fotos, la prosa se lee al abrir. Y la ficha de zona es un DTO aparte
(`CatalogZoneDetail`), que no engorda la zona anidada en cada producto. Las cifras, en la spec §4.1.
**Consecuencia**: `CatalogTest` (+4), guarda nueva de divergencia en `ApiContractTest`,
`scripts/mutar-menu-de-hechos.sh` **32/32**.
**Descartado**: ruta escrita a mano también en el producto (la instalación tendría que commitear su foto);
convertir la zona hoy; y meter la ficha en la zona anidada.

## #646 · 2026-09-19 · La prueba social publica la CIFRA; las reseñas esperan a su fuente
F5 · T6 del menú, y **cumple `#616` a medias a propósito**. `GET /api/v1/social-proof` publica media,
recuento, fuente y enlace; contrato **1.9.0**. Cierra el menú con lo que no va a cambiar.
**Las reseñas NO se publican hoy**, aunque `#616` las pedía aquí. `specs/google-business-profile.md`
—aprobada y que SUSTITUYE a Places como fuente— dice que el contrato `SocialProof` cambia (§4.3·9): la
reseña gana respuesta del parque, fotos y marca de anónimo, y aparece **la línea del filtro**, que es
obligación legal (Ómnibus 2019/2161). Publicarlas hoy sería repartir contenido de Places a otro repo y
atarse a una forma que ya sabemos que cambia — y un contrato público no se rompe después (`#644`).
**Llegan en la T2 de esa spec**, como clave hermana DENTRO del mismo sobre: por eso la cifra viaja envuelta
y no suelta en la raíz, y por eso la ruta pide el contrato y nunca `GoogleSocialProof`.
**La cifra sí puede ir sola**: la trae nuestro servidor, no lleva autor ni foto y una media de un negocio no
es dato personal (`FallingBackSocialProof` ya lo argumentaba al separarla de las opiniones), así que es
pública y cacheable sin consentimiento. Sin fuente sostenible el sobre va vacío; nunca un `0`.
**Consecuencia**: `SocialProofFactsTest` (4), arnés **38/38**. ▶ Trampa pagada: el sobre vacío salía `[]` y
no `{}` — el `(object)` por bloque de los hermanos no alcanza a la raíz.
**Descartado**: publicar las reseñas hoy sobre Places; y dejar la cifra suelta en la raíz.

## #647 · 2026-09-19 · `[DECIDIDO owner]` El paquete de instancia: invariante propio, una página primero y repo ya
Las tres de `specs/paquete-de-instancia.md` §7, contestadas con la spec delante.
**D1 · La ruta de vistas es INVARIANTE** (`SEC-12`): sale de configuración, vive fuera del docroot y jamás se
deriva de una petición. **Por qué**: Blade compila a PHP, así que no es «de dónde se leen ficheros» sino quién
ejecuta en el servidor; un `?paquete=` sería ejecución remota. *Descartado*: dejarlo solo en un test.
**D2 · Se estrena con UNA página** (`/contacto`, la más simple) y después el resto. **Por qué**: el namespace,
el respaldo y la exclusión del despliegue se prueban con poco en juego; si el diseño falla se ve con una
página y no buscando entre 3.142 líneas cuál de los 29 componentes se rompió. *Descartado*: las nueve de una,
como decía la T2.
**D3 · `instancia-playjump` nace como REPO** y la rama huérfana `cliente/playjump` se retira. **Por qué**: es
el destino de `producto-e-instancias.md` §4.1 y hacerlo ahora evita mudar dos veces. *Descartado*: seguir una
tanda más en la rama.
**Consecuencia**: `INVARIANTES.md` pasa de 62 a 63; la T2 se parte en T2a (mecanismo + plantilla + una
página) y T2b (el resto de las vistas). La deuda de la vía B queda aceptada con su cifra: la instancia
dependerá de **29 componentes** del producto, y renombrar uno la rompe sin que falle un test de `main`.

## #648 · 2026-09-19 · `[DECIDIDO owner]` El mundo JumpWeb bajo una carpeta, y el producto es `producto/`
El repo pasa de `~/proyectos/JumpWeb` a **`~/proyectos/jumpweb/producto`** y las instancias al lado, en
`jumpweb/instancias/<slug>`. Ahí entrarán la app de F6 y el plugin; un proyecto sin relación (un TPV en
Windows) cuelga de `~/proyectos/` y no se mezcla.
**Por qué**: el owner no quiere el mundo JumpWeb desparramado entre proyectos ajenos — y así **`SEC-12` no
se toca**: las instancias quedan FUERA del árbol del producto sin esfuerzo, que es justo lo que pide. El
montaje `../instancias` de `compose.yaml` sigue valiendo tal cual.
▶ **Se descartó RELAJAR `SEC-12`** para meter las instancias dentro del árbol (cosa que este repo ya hace
con `public/uploads`): innecesario en cuanto la carpeta está bien puesta.
**Y el nombre del proyecto de Docker se FIJA** (`name: jumpweb`): salía del nombre del directorio, así que
la mudanza habría levantado contenedores nuevos dejando huérfano el volumen de MySQL con la base de
desarrollo dentro.
**Consecuencia**: verificado tras mover — mismos contenedores, BD viva (4 zonas), web y `/contacto` en 200,
paquete resolviendo en `/var/www/instancias/playjump`. ⚠️ La memoria del agente va indexada por la RUTA: se
copia a mano, porque el clasificador deniega escribir en `~/.claude` y eso no se rodea.
**Descartado**: dejarlo en `~/proyectos/instancias`; y mover hoy también el plugin y el clon del cliente.

## #649 · 2026-09-19 · `[DECIDIDO owner]` Las pruebas de la landing se parten por lo que AFIRMAN, y nace el contrato de vista
**El contrato entre el producto y una instancia son los DATOS que recibe la vista, no el HTML que produce.**
El producto se queda las pruebas de CONDUCTA (que el controlador pasa sus variables, que un tema inválido se
rechaza, que el tema llega al asunto del correo) y el mecanismo; la instancia se queda las de MARCADO.
**Por qué**: el experimento de la T2a trazó la línea solo — de los 14 casos de `ContactPageTest`, **cayeron
9 y aguantaron 5** sin el paquete: los que aguantan no miran el HTML. Hoy el producto prueba su conducta *a
través del marcado de un cliente*, y por eso mudar una vista le rompe la suite. Es la línea de siempre entre
una prueba de CONTRATO (la escribe quien produce) y una de RENDERIZADO (quien consume).
**La herramienta de la instancia es la HUELLA de maquetación, no `assertStringContainsString`**: una cadena
se rompe cuando el cliente rediseña —su derecho—, y eso es un fallo equivocado; la huella dice «cambió sin
querer», que es la pregunta buena.
**Nace el CONTRATO DE VISTA**: cada vista que una instancia puede vestir declara qué variables recibe, con
guarda. Hoy renombrar `answers` rompe TODAS las instancias a la vez sin que el producto se entere. Afirma
sobre datos, nunca sobre HTML. Es la T2b; el detalle, en la spec §4.5.bis y §4.6.
**Descartado**: las tres salidas de §4.5 (CI propio en la instancia · paquete de prueba en los fixtures ·
quedarse solo con la huella): las tres daban por hecho que los 14 casos son una unidad que colocar.

## #650 · 2026-09-19 · T2b · Una regla escrita en una vista es una regla que se va con ella: la dirección baja al producto
Primera aplicación de `#649`. La dirección de dos líneas se unía **con coma** en tres sitios potenciales y
dos reales: en línea dentro de `pages/contact.blade.php` y en `StructuredData::postalAddress()`, **con
filtros distintos** (`filled()` contra `array_filter()`). Nace `Platform\Services\VenueAddress::written()`,
único hogar, y `GET /api/v1/site` publica `address.written` además de las dos líneas (contrato **1.10.0**).
**Por qué la coma**: «Ctra. de Prueba, 1 30000 Ciudad» se lee como si el código postal fuera parte del
número de portal.
**Y por qué esto es T2b y no cosmética**: la regla vivía DENTRO de una vista que se va a ir a la instancia.
El día de la mudanza se habría ido con ella, y cada landing habría re-derivado la coma —una copia por
cliente, y la primera que la olvidara publica una dirección que se lee mal sin que nada falle—. La prueba
que la vigilaba leía el HTML de `/contacto`; ahora afirma sobre el DATO y sobrevive a la mudanza.
▶ La duplicación ya había cobrado una: el `streetAddress` del JSON-LD hacía pasar en verde un caso que
aseveraba sobre la página entera aunque el bloque visible dijera otra cosa (contado en `ContactPageTest`).
**Consecuencia**: `VenueAddressTest` (4) y un caso en `SiteFactsTest`; `scripts/mutar-menu-de-hechos.sh`
**41/41**. Las dos líneas SIGUEN viajando: quitarlas sería romper el contrato para arreglar otra cosa.
**Descartado**: dejar la regla en la vista y documentarla para las instancias.

## #651 · 2026-09-19 · T2b · Cómo se escribe un número lo decide el idioma, en un solo sitio
Segundo barrido de `#650`, y esta vez la regla escondida en la vista **era un defecto vivo**. Medido en el
navegador el 19-09: la portada **en inglés** enseñaba la nota de Google como `4,8` y el recuento como
`1.234 reviews` —que se lee «uno coma doscientos treinta y cuatro»—, porque `home.blade.php` escribía
`number_format(..., ',', '.')` a mano en tres sitios. No fallaba nada: un número mal escrito se pinta igual.
**Nace `Platform\Services\LocalNumber`** (separador decimal y de millares por idioma) y la portada lo usa.
`Money::showcase()` **delega** su separador decimal, que era la misma regla escrita otra vez — y su docblock
ya decía que «el sitio donde se decide cómo se escribe un importe es UNO».
⚠️⚠️ **El separador de MILLARES de `Money` sigue a mano a propósito**: cambiarlo movería importes en la web
inglesa (`1.234,00 €` → `1,234.00 €`) y eso es dinero a la vista de un cliente. **Pendiente del owner**; un
caso fija lo que imprime hoy, valor por valor, para que nadie lo cambie sin verlo.
**Consecuencia**: `LocalNumberTest` (3), `scripts/mutar-menu-de-hechos.sh` **44/44**. ⚠️ Toca
`home.blade.php`, que es del carril de la web: avisado en el buzón.
**Descartado**: arreglarlo solo en la vista (se habría ido con ella a la instancia, y cada landing lo
re-derivaría — mal en inglés, que es donde no se nota).

## #652 · 2026-09-20 · T2b · Una línea vacía no es un 422: el contrato del anfitrión va como la web
`InvitationHostController` validaba `honoree_name` y `host_line` con `['sometimes', 'string']`, y
`ConvertEmptyStringsToNull` convierte un `""` en `null` ANTES de validar: una línea vacía era un **422** y el
anfitrión no podía guardar con «Te invita» en blanco. Lo midió el cajón el 19-09 (su buzón) y no lo arregló
a ciegas porque es contrato de API. Ahora `nullable`, como `GuestFormController` en la web.
**Semántica**: vacío = «déjalo como estaba», y el resto se guarda. No es un añadido: el dominio ya lo leía
así (`PublicFreeText::clean('')` devuelve `null` y `personalize()` no toca el campo), igual que con un texto
que lleva un enlace. **Consecuencia**: un caso en `InvitationApiTest` y la nota en el contrato (el `PUT` y
`InvitationPersonalizeRequest`); no sube versión por sí solo —cambia una conducta que el esquema ya admitía,
porque `""` es una cadena— y sale con la 1.11.0 de `#653`.
**Descartado**: que vacío BORRE el campo. La web tampoco lo hace, y decidir que un formulario que se envía
entero borre lo que va en blanco es una decisión de producto del carril del SPA, no un arreglo de contrato.

## #653 · 2026-09-20 · T2b · Cómo se resume un contenido se decide en un sitio, y la API lo publica escrito
Tercer barrido de `#650`. La `<meta description>` de la landing estaba escrita **cuatro veces**, cada una a
su manera: normas (seis nombres, ` · `, 155), atracciones (ocho), legales (primer párrafo, sin etiquetas,
155) y cumpleaños (la descripción del primer pack, **sin tope**). Cuatro vistas que se van a la instancia, y
con ellas la regla. Nace `Platform\Services\MetaDescription` (`fromNames`, `fromText`): tope **155** —lo
que cabe en el fragmento de un buscador—, sin etiquetas, vacíos fuera, `null` sin contenido y **cortado en
palabra entera** (las cuatro copias cortaban a mitad: «de que e...»). Las vistas delegan y ponen solo su
respaldo, que es de la página.
**Y la API lo publica escrito**, por lo mismo que `/site` publica `address.written`: `/rules` y
`/legal/documents/{clave}` llevan `summary`; contrato **1.11.0**. No se publica donde el contenido no es del
producto (las atracciones, `#631`) ni donde la prosa solo viaja en el detalle (el pack, `#645`): ahí la
instancia llama al servicio (vía B). Lo medido —8 páginas × 3 idiomas— está en `paquete-de-instancia.md` §4.7.
**Consecuencia**: `MetaDescriptionTest` (4), un caso en `RulesFactsTest` y otro en `LegalDocumentsTest`,
arnés **50/50**. ⚠️ Toca cuatro vistas del carril de la web: avisado.
**Descartado**: mantener los `take(6)`/`take(8)` (cotas perezosas: el tope ya acota) · el resumen en la lista
de productos (contra la asimetría medida de `#645`) · cortar a mitad de palabra por no cambiar un byte.

## #654 · 2026-09-20 · T2b · `/contacto` se muda a la instancia, y la suite corre SIN paquete
La primera vista que sale (D2 de `#647`). `pages/contact.blade.php` va TAL CUAL a
`instancias/playjump/web/contacto.blade.php` —mismo DOM, huella 0 diferencias en 34 pantallas, sitemap
11=11— y el producto se queda un **anfitrión mínimo** (`anfitrion/contacto`): armazón, formulario entero y
datos, sin arte del cliente. Antes, dos MECANISMOS que vivían en línea en la vista pasan a componente
(`<x-site.honeypot>`, `<x-site.turnstile>`, `Platform\Services\Honeypot`): una landing que los re-escribiera
mal dejaría de filtrar bots, o de entregar, sin que nada fallara.
**La suite corre SIN paquete** (`phpunit.xml` fija `INSTANCIA_RUTA` vacía). Es la salida de §4.5: leía el
`.env` de cada máquina y el gate salía verde donde hay paquete y rojo donde no. El producto prueba su
respaldo —marcado PROPIO, así que `AnfitrionContactoTest` sí mira HTML— y la instancia se prueba con la
huella. **Los 12 casos de marcado se van con la vista**: dos eran reglas del componente de canales y se
re-alojan (`ContactChannelsTest`), la guarda de `lang/` se queda (`CopyHasNoBusinessDataTest`) y los nueve
de diseño están escritos en la doc de la instancia (`paginas/contacto.md`). `mutar-contacto.py` conserva
solo los mutantes del producto.
**Descartado**: un respaldo desnudo (las guardas del armazón, del pie y de la cabecera recorren `/contacto`
y el producto sin paquete tiene que seguir cumpliéndolas) · CI de PHP en la instancia (§4.5).

## #655 · 2026-09-20 · T2b · `/normas`, `/bar` y los cinco legales se mudan, y una regla muerta se retira
Segunda mudanza con el método de `#654`, tres vistas de una vez: `pages/{rules,bar,text}` van TAL CUAL a
`web/{normas,bar,legal}.blade.php` del paquete y el producto se queda tres anfitriones mínimos. Medido:
mismo DOM, huella 0/34, sitemap 11=11; el detalle en la spec §4.7.
**La partición** (`#649`): `RulesPageTest` y `BarPageTest` afirman sobre los DATOS de la vista y matan los
mutantes de servicio y modelo; el marcado es de la instancia (`paginas/*.md`, huella de juez) y cada
anfitrión lleva su guarda (`Anfitrion*Test`), sujeto de los mutantes de vista. Dos guardas transversales
cambian de sujeto (la cabecera se prueba en su componente; el corpus de la fachada mira la portada).
**El material se queda y su consumidor no**: el CSS y las ranuras del kit que solo pintaba `/normas`
(`grain--fade`, dos iconos) se declaran en `InstanceViews::MATERIAL_CONSUMIDO_POR_LA_INSTANCIA`, un solo
hogar que las dos guardas de huérfanos leen —excluyen, y exigen que el producto NO lo pinte— y que se vacía
cuando el material se mude (T3–T5).
**El barrido** cazó una regla MUERTA en `text` (el aviso de borrador: las cinco legales en
`REVIEWED_LEGAL_SLUGS` desde el 01-09; se retira con constante, clave y tres asertos en vacío) y una VIVA
en `normas`: la fecha (`#656`).
**Descartado**: conservar el aviso «por si acaso» · probar `RuleBoard` en unitario (por la ruta se vigila
también lo que compone el controlador) · que el corpus de consumidores lea el paquete de la instancia (la
suite corre sin paquete, `#654`).

## #656 · 2026-09-20 · T2b · Cómo se escribe una fecha lo decide el idioma, en un solo sitio
Hermano de `#651`. Medido el 20-09 en la web: el pie de `/normas` decía «Updated in **September de 2026**»
y «Mis à jour en **septembre de 2026**», porque la vista escribía `translatedFormat('F \d\e Y')` a mano:
el mes se traducía y el «de» no. Un defecto vivo en los dos idiomas en los que nadie mira, y una regla
dentro de una vista que se iba a la instancia (`#650`).
Nace `Platform\Services\LocalDate::monthYear()` (español con partícula; inglés y francés, mes y año). La
vista de la instancia y el anfitrión la llaman. **Consecuencia**: `LocalDateTest` (2), un caso del anfitrión
en inglés y un mutante en `mutar-normas.py`. Cambia el HTML inglés y francés de `/normas` en esa línea; el
español, ni un byte.
**Descartado**: arreglarlo solo en la vista de la instancia (cada landing lo re-derivaría, mal donde no se ve).

## #657 · 2026-09-20 · T2b · `/atracciones` se muda, la foto baja al modelo y el juez gana dos páginas
Tercera mudanza con el método de `#654`/`#655`: la vista va TAL CUAL al paquete y el producto se queda
`anfitrion/atracciones`. Medido: mismo DOM en es/en/fr, huella 0 en 38 pantallas, sitemap 11=11.
**El barrido** (§4.7) cazó UNA regla viva: `asset($ride->image)`, en la vista y dos veces en el mosaico de
la portada. Es ruta relativa a `public/` —misma herencia que la foto de zona (`#645`)— y el producto ofrece
al lado la respuesta equivocada (`TicketType::imageUrl()` antepone `uploads/`). Nace
`Attraction::imageUrl()`, letra por letra el de `Zone`; ni un byte de HTML cambia.
**❗ Y el JUEZ no miraba la página que se muda**: la huella recorría 14 vistas, sin `/atracciones` ni
`/waiver` — y `/waiver` es uno de los cinco legales de `#655`, que dio «huella idéntica» habiendo medido
cuatro de sus cinco. Pasa a 16 vistas (38 pantallas), que es la línea base nueva.
**La partición** (`#649`): `AttractionsPageTest` afirma sobre `zones`, `total` y `active`, y exige que la
PORTADA prometa el mismo recuento (dos controladores, un número que el cliente cuenta). El marcado es de la
instancia (`paginas/atracciones.md`); las tres cuentas de contraste de la cifra se van a
`Theme/ZoneInkIsLegibleTest`: son regla de TEMA, no dato de la página.
**Descartado**: retirar `ZoneIdentityIsUniqueTest` al quedarse sin sujeto por TERCERA vez — el anfitrión
conserva los tres sitios que dicen cuál es una zona.

## #658 · 2026-09-20 · T2b · `/precios` se muda, y tres variables que nadie leía no llegan a ser contrato
Cuarta mudanza con el método de `#654`→`#657`: la vista va TAL CUAL al paquete y el producto se queda
`anfitrion/precios`. Medido: mismo DOM en es/en/fr, huella 0 en 38 pantallas, sitemap 11=11.
**El barrido no halló ninguna regla escrita en la vista** —todo llega compuesto de `RateTable`,
`ScheduleDisplay` y `QrCode`— **pero sí tres variables MUERTAS**: `tickets`, `zones` y `registrationUrl`
viajaban y **ninguna se leía**. Un dato sin consumidor no se nota mientras es una variable; el día que se
declara en `CONTRATO_DE_VISTAS` pasa a ser una PROMESA a cada instancia, y retirarla después sube el MAYOR
sin motivo. Se retiran ANTES de prometerlas, con guarda que lo afirma.
**La partición** (`#649`): `Site\PricingPageTest` afirma sobre `rateTable`, `week`, `specialLabel` y
`holidays`; el marcado es de la instancia (`paginas/precios.md`) y el anfitrión hereda los dos casos que
quedaban (sin CTA propio · la salida acotada a su banda) y gana uno: el rótulo del panel entra en un
`{!! !!}` y **se escapa antes**, o una tarifa `Findes <b>y</b> festivos` mete marcado en la web.
**Cambian de sujeto**: la mitad de `PageHeadTest` que exigía el abanico en su ranura se prueba sobre el
COMPONENTE de fachada; y **seis mutantes de `mutar-precios.py` se retiran CON el suyo** (complementos y
hora extra, `#583`; el chip de la que lidera, `#585`; la línea a mano hacia cumpleaños, hoy banda).

## #659 · 2026-09-20 · T2b · `/cumpleanos` se muda, y el trío estrena la lista de material de la instancia
Quinta mudanza con el método de `#654`→`#658`: la vista va TAL CUAL al paquete y el producto se queda
`anfitrion/cumpleanos`. Medido: mismo DOM en es/en/fr, huella 0 en 38 pantallas, sitemap 11=11.
**El barrido** halló UNA regla viva: `asset($zone->image)`, que re-derivaba lo que el producto ya resuelve
en `Zone::imageUrl()` desde `#645` —la misma foto que publica la API—.
**Y el TRÍO estrena de verdad `MATERIAL_CONSUMIDO_POR_LA_INSTANCIA`**: `.trio` y `.trio-stand` siguen con
consumidor en el producto (la sección 04 de la portada), pero el MODIFICADOR `trio--page` solo lo pedía
esta vista. Se declara con su consumidor y las guardas de huérfanos lo excluyen.
**La partición** (`#649`): `Site\BirthdayPageTest` afirma sobre `compare`, `form` y `choices` —incluido el
caso que importa: *el total es el precio que cobra la cesta por el número de niños, tramos dentro*, que es
la línea de `CartPricer`—; el marcado se reparte entre la doc de la instancia y el anfitrión, que hereda el
contador con `x-cloak`, el botón fantasma, la foto y el censo de clases de la página vieja.
**Una guarda que lee marcado como TEXTO cambia de fichero**: `SidebarSeamTest` leía `pages/events`; hoy lee
el anfitrión, y su CENSO de superficies que declaran intención de compra lo dice. **Y dos mutantes de
`mutar-cumple.py` se retiran con su sujeto** (el reloj y el carril de complementos, `#583`).

## #660 · 2026-09-20 · T2b · `/servicios` se muda, y dos reglas de dinero bajan al producto
Sexta mudanza (`#654`→`#659`). **Con ella `resources/views/pages/` DESAPARECE**: solo queda `home`.
**El barrido halló TRES reglas vivas**, y dos son de dinero:
· El `$fmt` de la tabla tecleada, tercera variante del producto → `Money::showcase()`. Medido: los doce
  importes de esta instalación son euros exactos, así que no cambia un byte.
· El «desde»: la vista elegía el mínimo A MANO y lo escribía con el registro de TRANSACCIÓN. **Defecto
  vivo medido**: en inglés la misma pantalla decía «from 14.95 €» (del producto) y «12,00 €» (de la
  vista). Nace `GroupRateTables::lowestWritten()`, que decide las dos cosas. ⚠️ **Cambia lo que se ve**
  (`[DECIDIDO owner]`, 20-09, con la medida delante): «12,00 €» → «12 €»; la huella da 3 nodos y los
  tres son de ANCHURA en la misma línea.
· Y la tercera copia de la foto: `LandingService::imageUrl()`, gemela de `#645` y `#657`.
**La CINTA `C3` estrena la lista con la familia entera**: solo la pintaba esta página. ⚠️ Se declaran las
CINCO clases: la guarda mira clase a clase y con el bloque solo, sus hijos seguían huérfanos.
**La partición** (`#649`): el marcado va al anfitrión y a `paginas/servicios.md`. Una aserción de
`PublicPagesTest` se retira con su sujeto: el producto no puede exigirle a una instalación que pinte una
decoración. Nace `scripts/mutar-servicios.py` (11/11; lo que enseñaron sus tres pasadas, en la spec §4.7).

## #661 · 2026-09-20 · El barrido de `home`, y el espacio que partía los importes
Primer paso de la última mudanza. El barrido de la portada halló **cuatro reglas escritas en la
vista** y **tres variables MUERTAS** (`zones`, `tickets`, `packages`), retiradas antes de prometerlas
(`#658`); el detalle, en `paquete-de-instancia.md` §4.7.
**Aquí se cierra el dinero** (`[DECIDIDO owner]`, 20-09, con la medida delante). El producto escribía sus
importes de escaparate con espacio NORMAL y tres vistas con `&nbsp;`: la misma página los escribía de dos
maneras y **«9,60 €» se partía en dos renglones a 390 px**. Nace `Money::showcaseWithSymbol()` con U+00A0 y
recoge **SEIS escrituras** de la regla: `euros()` · `formatPriceLabel()`, copia letra por letra ·
`RateCards::was`, cuyo hermano `special_was` ya venía duro · `PartyCards::price` con sus tres vistas —una
vive en el paquete de la INSTANCIA— · y `reserve_terms` en los tres idiomas.
⚠️ **`Money::format()` no cambia**: es el registro de TRANSACCIÓN (panel, correos, PDF). Ficha en `DEUDA.md`.
❗❗ **Y una guarda que NO existía**: cambiar `PartyCards::price` dejó la suite ENTERA en verde. Se escribe
(`ServicesPageTest`, euro exacto para distinguir escaparate de transacción) y se ve MATAR al mutante. Dos
NEGATIVAS llevan el duro o serían ciertas sin ejercer nada, y `CatalogTest` se cegaba con un `str_replace`
que normalizaba justo lo que vigilaba.
Medido: huella **569 nodos, TODOS en `/precios`** de pura geometría; 0 en las otras 36.

## #662 · 2026-09-20 · Dos reglas más salen de `home`: el horario y la escala de estrellas
Sigue a `#661`. **La vista resolvía un SERVICIO del dominio por su nombre de clase** y se lo pasaba a
`<x-site.visit>`; una landing de instancia no instancia código del producto. Ahora el `lede` es DATO del
controlador y el componente resuelve lo suyo — como ya hacen otros seis de `site/`.
❗❗ **El primer diseño fue un `singleton` y lo tumbaron cuatro casos de `VisitSectionTest`.** Medido: ni el
Kernel HTTP ni el `TestCase` llaman a `forgetScopedInstances()`, así que `singleton` y `scoped` se portan
igual y **la instancia sobrevive entre las peticiones de una misma prueba**. La salida es la que la casa ya
usa para el payload del composer: **memoizar en `request()->attributes`**. Medido en la portada: **54
consultas con memoización, 58 sin ella, 54 antes de la tanda**.
▶ Y la **ESCALA de estrellas** estaba tecleada DOS veces en la misma página: baja a `Rating::MAX`. No es
configurable —la fija la fuente: Places da 1–5 y el panel también—. El «sobre 5» de `lang/` es COPIA.
❗❗ **Dos guardas que NO existían**, las dos vistas MATAR a su mutante. Y **dos errores míos, en la spec
§4.7**: el nombre de la directiva de bloque PHP escrito DENTRO de un comentario Blade rompió la portada, y
la primera guarda de la escala derivaba su expectativa de lo que medía y su mutante SOBREVIVIÓ.
Medido: huella **IDÉNTICA en las 38 pantallas**.

## #663 · 2026-09-20 · El material gráfico del cliente sale de `main`
`[DECIDIDO owner]`. **37 ficheros y 9,2 MB** —35 fotos del catálogo, el vídeo de la portada y su póster—
dejan de estar versionados en el repo que dice no llevar la marca de ningún cliente (`#610`). Van a
`publico/` del paquete y **se copian a mano al instalar**, como el tema. **Sustituye a `#529`**: el clon
completo de una INSTALACIÓN es su paquete, no este repo.
⚠️⚠️ **EL ORDEN NO ES PREFERENCIA: al revés borra producción.** El despliegue es `rsync -az --delete`, así
que las exclusiones y la lista blanca de la GUARDA 9 van en el MISMO commit que el `git rm --cached`.
Verificado con control negativo: **sin exclusión el `--delete` mata los tres; con ella sobreviven** y
`images/providers/` —de Google, del PRODUCTO— se actualiza igual. ▶ **Las rutas no se pueden mover**: las
guarda la BD, así que `publico/` refleja `public/`.
❗❗ **El paquete gana REMOTO** (`[DECIDIDO owner]`): sin él era la única copia de material irreemplazable.
❗❗❗ **`git rm --cached` conserva el fichero, pero el commit REGISTRA UN BORRADO**: al rebasar, git lo
restaura del remoto y reaplica tu commit, que lo **borra del disco**. Los 37 murieron en el `pull --rebase`
— repuestos del paquete, copiado ANTES. **Se copia fuera antes de retirar.**
❗❗ Eso destapó **TRES casos que dependían del disco**: verdes aquí, **rojos en el otro ordenador**.
Partidos por lo que afirman (spec §4.7); **suite verde con material y sin él**.

## #664 · 2026-09-20 · La mudanza de `home`, MEDIDA antes de tocarla
Dos medidas, y las dos cambian el plan.
▶ **La regla de caché del vídeo NO se baja.** `@filemtime` aparece **37 veces en 15 ficheros** y su sufijo
es uniforme (13 × `}}?v={{ @filemtime`). No hay variantes: es el IDIOMA de la casa, no una regla con dos
escrituras como el euro. *Extraerlo para un solo sitio de llamada crearía la inconsistencia en vez de
quitarla.* Las cuatro reglas del barrido de `home` quedan resueltas.
▶ **Y `home` se midió como `#649` midió `/contacto`**: controlador apuntado a un anfitrión de nueve líneas
y suite entera. **161 rojos de 682, en 31 ficheros** (`/contacto`: 9 de 14, en uno). **521 sobrevivían**:
solo necesitaban «una página» para el armazón, las cookies, el tema, el idioma o el montaje del cajón.
❗❗ **La consecuencia de diseño**: **~44 de los 161 tienen su sujeto en un COMPONENTE del producto**
(`rate-rail` 25, `visit` 16, el mapa 3). Esos **no se mudan ni se retiran: CAMBIAN DE SUJETO**, como la
mitad de `PageHeadTest` en `#658`. Solo **~117** viajan a `paginas/home.md` con la huella.
⚠️ **Pregunta abierta antes del anfitrión**: las secciones que la portada pinta EN LÍNEA desde servicios
del producto se quedan sin consumidor, y con el hero la lista de material declarado **deja de ser una
lista corta de excepciones**. *Si la deuda de la vía B se vuelve inventario, el problema no es la lista:
es que esas secciones deberían ser componentes.* El detalle, en la spec §4.7.bis.

## #665 · 2026-09-20 · La deuda del CSS de la portada: nace la T2c
`[DECIDIDO owner]`, preguntado con la medida delante. **Al mudar `home`, 208 clases de `landing.css` y
`site.css` se quedan sin consumidor en el producto** (20 familias: `rev` 28, `before` 26, `hero` 22,
`salta` 20, `zone-card` 18…). ⚠️ **Y la guarda de huérfanos solo ve UNA** —su sujeto es el material de
FACHADA—: **207 pasarían sin que nada avise.**
❗❗ **Yo propuse componentizar las secciones y la medida lo descartó.** La spec dice que la deuda de la vía
B **no** es material del cliente en el producto: es que *«la instancia depende de 29 nombres de componente
del producto»* (§1.5), y que **la vía A —estáticos— es el DESTINO** (§3). Convertir siete secciones en
componentes llevaría esos 29 a ~36: profundizar en la transición y alejarse del destino. *Medir la premisa
mató la propuesta.*
▶ **DECIDIDO: `home` se muda TAL CUAL ahora, y el CSS va en una T2c propia.** No se mezclan dos riesgos —
mover marcado se verifica con la huella; mover REGLAS toca el orden de CASCADA, que es otra medida—. La
deuda queda FECHADA y en el plan, no silenciosa. *Descartado*: mudarlo todo de golpe (dos riesgos a la vez)
y fichar sin fecha (208 clases que nadie podría distinguir de las muertas).
⚠️ **Y arregla un hueco del plan**: las T3–T5 son el panel, `zones` y la versión — **el CSS no estaba en
ninguna**, aunque la lista de material declarado prometía vaciarse «cuando el material se mude (T3–T5)».

## #666 · 2026-09-21 · `home` MUDADA, y la cifra de rojos medía el anfitrión, no la página
La portada (1.639 líneas) vive en `instancia-playjump/web/portada.blade.php`; con ella **la T2b se
cierra**. Huella **0 diferencias en 38 pantallas** · mismo DOM en es/en/fr · sitemap 11=11 · suite 5.474.
❗❗❗ **`#664` contó 161 rojos y salieron 16.** No se equivocó: midió con un anfitrión de **nueve líneas**.
*La cifra de rojos de una mudanza no mide la página que se va: mide el anfitrión con el que se mide.* Uno
que monte los componentes, las ocho anclas y el contrato entero **conserva el sujeto** de 145 casos que
parecían condenados. Los 16: 6 guardas re-apuntadas, 4 piezas de material declaradas, 4 que el anfitrión
no consumía y **2 mudados**.
▶ **Criterio nuevo para un anfitrión**: no es «lo mínimo que renderiza», es **consumir el contrato
ENTERO**. Faltaban el velo teñido, la frontera de la escala y los puntos del carril — DATOS que el
producto compone, y un dato compuesto que nadie pinta es el defecto de `#658`.
▶ **No se muda una OBLIGACIÓN aunque sea marcado**: la atribución de Places, el autor, el aviso de
traducción con su original y la política se quedan en el anfitrión. **El vídeo sí**: exigir `hero__video`
salía verde aquí y rojo en una instalación recién montada (§4.5), y desde `#663` ni vive en `main`.
⚠️ Tres variables muertas retiradas ANTES de prometerlas (`#658`). El detalle y la trampa de la medianoche,
en la spec §4.7.bis y en mis trampas.

## #667 · 2026-09-21 · T2c · las 208 huérfanas eran 17, y un censo que no sabe COMPONER miente
`[DECIDIDO owner]` con la medida delante: **trinquete y podar**. ❗ **La premisa de `#665` caducó por lo
mismo que la de `#664`**: midió contra un anfitrión de nueve líneas y el real (`#666`) **sostiene 174**
de aquellas 208. Quedan **17** que solo pinta la instancia —ya declaradas— y **128 que no pinta nadie**.
❗❗❗ **El censo mintió tres veces antes de acertar: 161 → 139 → 128.** Hay **TRES formas de componer una
clase** aquí y un escaneo literal da por huérfanas las vivas: concatenación JS, concatenación PHP e
**interpolación Blade en el atributo** (`visit__dot--{{ $face }}`). La tercera la destapó un control
contra el DOM servido: daba `visit__dot--open` viva y `--later` muerta **porque a esa hora el parque
estaba cerrado**. *Una medida que solo ve la rama de hoy no ve las otras.*
▶ **PODADAS 119** (−20,3 KB entre las tres hojas) con **huella 0 en 38 pantallas**. Las **9 del hero
vacío no se tocan**: aparcadas por decisión del owner (`#226`).
▶ **Nace el TRINQUETE** (`LandingCssHasNoOrphansTest`): mira TODO el CSS y no solo la fachada —lo que
`#665` señaló— con **deuda de 15 que solo ENCOGE**, como `#625` y `#629`. Tres mutantes, vistos morder.
❗❗ **Y la poda DESPERTÓ dos guardas que vigilaban reglas sin sujeto** (`TagSystemTest`,
`SemanticFillTextTest`; el detalle en la spec §4.6.bis). *No las rompió: las despertó.*

## #668 · 2026-09-21 · T3·1 · el widget de ofertas y el complemento por atracción, fuera
`[DECIDIDO owner]` con la medida delante: la T3 va **por grados** y retira solo lo que no deja hueco.
De los seis recursos salen los dos con cero uso: el **widget de ofertas** (`#631`, 0 filas — decidido
hace tres días y aún montado) y el **complemento por atracción** (`#632`·P3, **0 de 23**). Los otros
cuatro tienen contenido vivo y el menú no los sirve: sacarlos haría que editar la web fuese desplegar
el repo de la instancia.
❗ **La medida mató la tercera pieza del alcance**: censadas las 71 claves de `settings`, **ninguna**
está sin consumidor, así que «las secciones de texto» de §4.2 no existen.
▶ **`InstanceViews::CONTRATO` sube a 2, y es la PRIMERA vez**: `offers` era una de las siete claves
del composer, o sea del contrato de vista de las NUEVE páginas. Lo predijo esa lista al nacer.
❗❗❗ **La lección: PODAR POR CLASE NO PODA UNA FEATURE.** El guion de `#667` dejó vivas sus
`@keyframes`, sus tokens y los selectores que mezclaban una clase suya con otra genérica. *Una
feature se poda por su BLOQUE* — 118 líneas de una vez.
⚠️ **Una retirada arrastra su cadena**: `AdminSettingsHub` listaba el recurso (**112 rojos**, el panel
entero), `body.book-bar-visible` existía SOLO para apartar su lanzador y **seis guardas** lo usaban de
sonda o en sus listas. La de at-rules se cae por **tercera vez** por atarse a un dato real; ahora
prueba el INSTRUMENTO. Detalle en §4.6.ter.

## #669 · 2026-09-21 · T4 · `zones` pierde sus cuatro columnas muertas
`#639`·D2 lo decidió con el censo delante y aquí se ejecuta: de las **26 columnas** de `zones`, cuatro
no las leía **nadie fuera del formulario del panel** —`subtitle`, `age_label`, `area_sqm` y
`rides_count`—. Ni la landing, ni la API, ni el cajón, ni un correo. Re-medido el 21-09: siguen así.
▶ El criterio del owner: *un campo que el panel pide y que no sale a ningún sitio se vuelve a rellenar
creyendo que sirve*. El coste no es la columna: es el rato de escribir un subtítulo en tres idiomas
que no se publica.
❗❗ **`rides_count` merece su párrafo, y su medida sorprende para bien**: coincide con el recuento REAL
de atracciones activas (15=15, 8=8), o sea que **hoy no miente**. Pero era un contador a mano de algo
que el producto ya calcula donde lo publica. *Un contador copiado no está mal el día que se copia:
está mal el día que alguien añade una atracción y nadie se acuerda de subirlo.* ▶ Y lo confirma quién
lo cazó: `ZoneImageTest` **comparaba las dos fuentes**. Que hiciera falta compararlas era el síntoma.
⚠️⚠️ **Se PIERDEN los datos y es parte de la decisión**: subtítulos traducidos, etiquetas de edad y los
metros cuadrados (5.000 y 2.000). No se migran porque no hay sitio al que migrarlos — no se publican.
El `down()` recrea la forma, nunca el contenido.
⚠️ **Con `#669` se agota la banda 640–669**; el carril sigue en **670–699** (`DECISIONES.md`).

## #670 · 2026-09-23 · `[DECIDIDO owner]` No se despliega en piezas: una sola versión grande al final del programa
Con la medida delante (v1.1.0→v1.2.0: 35 commits y **cero** migraciones · v1.2.0→HEAD: 78 commits y **siete**,
una destructiva), el owner: *«prefiero concentrarme en el diseño —landing, SPA, justificante, invitación,
analíticas, todo— y después desplegar una versión grande, de noche, y estar pendiente»*. **Por qué**: un
despliegue cuesta ATENCIÓN aunque salga bien, y la atención es el recurso que quiere gastar en diseñar.
*Descartado*: desplegar v1.2.0 aparte, que es lo que el agente recomendó (cero migraciones, vuelta atrás
simétrica).
**Consecuencias**: (1) **producción se queda en v1.1.0**; un defecto allí obliga a `cherry-pick` sobre la
etiqueta o a desplegar igualmente, no hay tercera salida. (2) **La T5 de F5 deja de estar aparcada y pasa a
ser el destino**: v2.0.0 ES esa versión, con el programa entero dentro. (3) ⚠️ **Los dos interruptores de la
invitación no se tocan** (`ticket_types.guest_invitation`, `guardian_authorization`): medido, **v1.2.0 lleva
T1→T5 y NO T6 ni T7**, así que encenderlos daría media feature —página del padre sin aterrizaje del anfitrión,
sin correos y sin `#718`—; el `CHANGELOG.md` dice «entra entera» y **es falso**, se corrige. (4) **Las
migraciones se acumulan para una sola noche** (siete): ese despliegue pide ENSAYO en staging.

## #671 · 2026-09-23 · Las DUDAS, servidas — y el filtro de «vacío» que iba antes del respaldo
Primer plato de los cuatro de la vía A (el owner eligió los platos, no el kit de `#632`·P2; `#670` los deja
sin desplegar hasta la v2.0.0). `GET /api/v1/faqs?lang=`, contrato **1.12.0**.
**Lo que decide el plato no es código: qué duda es PUBLICABLE.** Una pregunta sin respuesta publica algo que
el negocio no contesta, así que **una duda a medias no viaja** — hoy las portadas pintan el acordeón entero
y solo `StructuredData::faqPage()` salta las inservibles: **ya divergen**. La regla baja al DATO, y por eso
los dos campos van `required` sin excepción, sin entrada en `OPTIONAL_BY_DESIGN`.
❗❗❗ **LA TRAMPA, Y CASI ENTRA: el filtro de «vacío» va DESPUÉS del respaldo de idioma.**
`Translated::pick()` encadena con `??`, no con `?:`, así que resuelve por clave **ausente**. Medido: las
doce dudas están **solo en español**, con `en`/`fr` **ausentes**. Preguntar «¿está relleno el idioma
pedido?» antes de `tr()` habría dejado el recurso **vacío entero en `en` y `fr`**, con el JSON válido y todo
en verde. *Se filtra por lo que `tr()` devuelve* (spec §4.1).
⚠️ Tres más, en §4.1: **desempate por `id`**, **`updated_at` de lo SERVIDO**, y el `FAQPage` de schema.org
**sin publicar** —es marcado, no un hecho—.
**Medido**: `FaqsFactsTest` 10/36 · arnés **58/58** (ocho nuevos, ninguno «NO SE APLICÓ») · en vivo los tres
idiomas, su 304 y los dos 422. Pint y Larastan sin tocar la base.

## #672 · 2026-09-23 · Los SERVICIOS, su mitad EDITORIAL — y una tabla de precios que no se publica
Segundo plato de la vía A. `GET /api/v1/services?lang=`, contrato **1.13.0**: secciones activas, orden del
panel con desempate por `id`, texto y foto, fichas, y los productos como **identificadores**.
**El alcance se parte en dos, y ése es el fondo.** `LandingService` separa lo EDITORIAL de lo COMERCIAL a
propósito («cero drift»). Esta tanda sirve lo editorial; los **tramos de grupo** —`GroupRateTables`, que
medido **no los sirve ninguna ruta**— van aparte porque son DINERO y traen la decisión de registro de
`#661`: el contrato de vista quiere el importe ESCRITO y el de API lo quiere en CÉNTIMOS (`§10.5`).
❗❗ **`price_table` no viaja, y tiene dueño**: JSON **tecleado a mano**, ya jubilado por el owner en favor
del catálogo (`#534`). Publicarlo sería servir como hecho un precio que el checkout podría no cobrar.
⚠️ `nav_subtitle` y `show_in_nav` tampoco: sin consumidor desde `#521`, y estar en la tabla no hace
contrato a un campo. ▶ Lo demás —la medida de local contra producción, los ids sin `purchasable` al lado,
el filtro de título y el de ficha— en la spec §4.1.
❗ **Lo que destapó publicarlo**: **3 fichas en es y 2 en en/fr**, y «Horario» dice cosas distintas por
idioma. Datos del panel, no el filtro. Avisado al carril de la WEB.
**Medido**: `ServicesFactsTest` 13/44 · arnés **68/68** (diez nuevos, ninguno «NO SE APLICÓ») · Pint y
Larastan sin tocar la base.

## #673 · 2026-09-23 · El BAR — y el primer plato que lee `settings` SIN lista blanca
Tercero de los cuatro. `GET /api/v1/bar?lang=`, contrato **1.14.0**.
❗❗ **Estrena un camino que la receta no había usado: delega en `BarPage` en vez de declarar lista
blanca.** No es un rodeo de `PublicFactsBoundaryTest`: es lo que su docblock prescribe —*«lo demás lo
sirve un servicio de dominio, que es quien sabe qué significa su ajuste»*—. `BarPage` sabe que el NOMBRE
gatea la página, que `free_entry` solo admite `yes`/`no` y cuál es el respaldo; declararlas en el recurso
habría duplicado esas tres reglas. ▶ **Regla para los que vengan**: si el ajuste tiene servicio de
dominio se delega; la lista blanca es para quien lee `settings` a pelo.
⚠️ Sin nombre, la clave `bar` **no viaja** (como `rating` en `/social-proof`). `free_entry` sale
**booleano** y su ausencia **no significa `false`**: significa que no se afirma nada.
⚠️⚠️ **Las DIMENSIONES viajan** o la página salta al cargar la carta. ❗ El caso que las comprobaba salió
ROJO con razón: el modelo las **re-mide contra el disco**, y un `width` tecleado se sobrescribe con `null`.
▶ **Una imagen sin `alt` SÍ viaja**: esconder la carta no arregla la accesibilidad, quita el menú.
❗❗ **`updated_at` mira las DOS fuentes o miente**, con un mutante por fuente. Detalle en §4.1.
**Medido**: `BarFactsTest` 12/52 · arnés **79/79** (once nuevos, ninguno «NO SE APLICÓ») · Pint y Larastan
sin tocar la base · en vivo.

## #674 · 2026-09-23 · Los JUEGOS — el menú COMPLETO, y dos interruptores de zona que se parecen
Último de la vía A. `GET /api/v1/attractions?lang=`, contrato **1.15.0**. `#632`·P3 ya lo midió:
las 23 son PRESENTACIÓN, sin aforo ni venta.
❗❗❗ **Lo decisivo es QUÉ interruptor gatea la zona.** `zones.is_active` dice que **la zona OPERA**;
`show_in_landing`, que **sale en la web**. Se desacoplaron en `2026_06_07_000002`, y la medida enseña por
qué: **la de cumpleaños opera —vende packs— y NO está en la landing**. Gatear por `is_active` publicaría
una zona que el negocio retiró de su web sin que fallara nada. Se usa `show_in_landing`, que es además
**el gate de la página**: API y web no discrepan. ⚠️ El `join` dejó `is_active` ambigua en SQL (rojo de
SQLite): van las dos cualificadas.
❗❗ **EL MOSAICO NO VIAJA.** `RideMosaic` elige tres con nombre y dos veladas: es MAQUETA, y publicarlo
obligaría a toda landing a heredar el diseño de ésta. *El límite entre hecho y presentación no está en el
tipo del dato: está en quién tiene derecho a decidirlo.*
⚠️ La lista va **plana** con el `slug` de su zona; `is_special` y `ticket_type_id` no viajan (§4.1).
▶ **Efecto lateral**: el genérico de `Attraction::zone()` retiró una entrada de la línea base en OTRO
fichero, y el trinquete bajó `FROZEN_ERRORS` de **458 a 457**.
**Medido**: `AttractionsFactsTest` 14/47 · diez mutantes nuevos · Larastan con la base MÁS CORTA · en
vivo, los 23 juegos sin rastro del mosaico.

## #675 · 2026-09-23 · El CENSO del menú: no faltan platos, faltan cuatro HECHOS
Se cruzaron **las 49 claves de `CONTRATO_DE_VISTAS`** contra **las 16 rutas públicas**, una a una con su
composer: censar solo lo que uno sospecha devuelve lo que ya sabía. ❗ Lo primero que cazó fue
un error del censo previo: `PackageComparison` **no existe** —es `BirthdayComparison`—, y un `grep` de un
nombre inventado siempre sale a cero.
**Resultado: cuatro huecos de HECHO, y tres son campos de rutas que ya existen.** (1) **tramos de grupo**,
ruta nueva y dinero · (2) `height_min_cm`, `height_max_cm`, `age_range` → **`/catalog/zones`** · (3) **qué
días son tarifa especial** → **`/schedule.weekly`** · (4) `guest_age_min/max`, `duration_min` y el
suplemento → **`/catalog/products`**.
⚠️⚠️ **El (3) no se ve leyendo**: `weekly` da horario **sin tarifa** y `/prices` importes **por tarifa sin
decir qué día es cuál**. Cada uno tiene una mitad y ninguno permite juntarlas.
▶ **La mitad de la sospecha se cayó al medirla**: `choices` ya es servible, y `compare`/`form`/`holidays`
son maqueta sobre lo servido.
▶▶ **Nace una categoría sin nombrar: MECANISMO DEL PRODUCTO** —ni hecho ni presentación—: el gate de
cookies, el régimen del justificante y `topics`, que es una **constante del controlador**.
❗ `[PENDIENTE: owner]`: con la landing fuera, ¿de quién son los asuntos del formulario de contacto?
**Orden**: los tres lotes de campos, después los tramos. Censo en §4.1.quater.

## #676 · 2026-09-23 · Los TRES LOTES del censo y los asuntos de contacto — contrato 1.16.0
Ejecuta los huecos de `#675`, todo ADITIVO (spec **§4.1.quinquies**).
❗❗ **El censo se corrigió al medirlo**: los días **no iban en `/schedule.weekly`** —`RateType` ya tiene
columna `weekdays`, o sea que el dato es de la TARIFA— y el «suplemento» **no era un hueco**: `/prices` ya
publica los packs con sus dos. *El sitio de un hecho lo decide de quién es.*
▶ **`/catalog/zones`**: `height {from_cm, up_to_cm, written}` y `age_range`. ⚠️⚠️ El sentido va EN EL
NOMBRE: la misma cifra es «a partir de» o «hasta» según la columna. `written` viaja ADEMÁS, y la escribe
`ZoneHeightRule`, extraída de `ZoneCards` (la regla ya estaba en dos sitios).
▶ **`/prices`**: `special`, `weekdays` y `plain_weekdays`. ❗❗ Que FALTE no es «ninguno», es **«no se puede
saber»**; se reusa `ReadsRateFacts`, que ya distinguía los dos.
▶ **`/catalog/products`**: edad y duración, en la LISTA (4.079 → 4.607 B, **+13 %**). ⚠️ El filtro mira
`null`: **un 0 es una edad**.
▶ **`/site.contact.topics`**: CLAVES, como `moments` en `/rules` (`#533`): el producto es dueño del
vocabulario; quien pinta, de las palabras. ❗ `birthday`/`groups` siguen siendo del SECTOR: deuda.
🪤 **`#503` volvió a morder**: la ruta de idiomas con comodín en un docblock cierra el comentario, y *el
aviso estaba en el fichero que se acababa de leer.*
**Medido**: +9 casos · once mutantes · Pint y Larastan sin tocar la base.

## #677 · 2026-09-23 · `[DECIDIDO owner]` Los TRAMOS DE GRUPO, en `/prices` y en céntimos — el menú, completo
El cuarto hueco de `#675`, el de dinero. Contrato **1.17.0**, aditivo (spec **§4.1.sexies**).
❗❗ **No era ruta nueva**: `/prices` ya servía los packs de excursión al precio de su MÍNIMO y le faltaban
70 y 100. Va en `products[].tiers`: *el sitio de un hecho lo decide de quién es* (tercera vez).
**El registro de `#661`, con la medida delante: céntimos, y escribe la landing.** Los 86 importes del
contrato ya lo eran; una sola mano por página evita `#660`; el producto no impone presentación (`#631`); y el
«1.234 €» en en/fr se habría congelado en contrato. El contrato de VISTA no cambia.
▶ Las filas las decide `GroupRateTables::quantities()` para la vista Y la API. La primera es el mínimo y
repite `prices` por construcción; sin escalera la clave falta; un complemento nunca la tiene.
❗ **Y la regla compartida aprendió una cosa: un tramo por encima del MÁXIMO del pack no se publica**
(`OrderCreator` rechaza la cantidad). Cambia las dos superficies; en local no mueve nada, y en producción
se mide antes de la v2.0.0.
⚠️ `/prices` no se validaba contra su esquema: `PricesFactsTest` pasa a `ApiTestCase`.
**Medido**: `PricesFactsTest` +8 · `ServicesPageTest` +1 · arneses 110/110 y 12/12 · Pint y Larastan sin
tocar la base · en vivo, las dos excursiones con su escalera.

## #678 · 2026-09-23 · `[DECIDIDO owner]` La analítica: libro propio exento como verdad, herramienta intercambiable
Objetivo del owner: *«medir todo… lo más importante la CONVERSIÓN en cada sitio; lo segundo conocer al
cliente»*. Diseño en `specs/analitica.md`.
**Se decide**: (1) la verdad de la conversión es un **libro de eventos propio**, en servidor: su régimen
AGREGADO es exento (guía AEPD 2024) y mide al 100 %; (2) el **origen se sella en el pedido** al nacer;
(3) **PostHog nube UE** para grabaciones, mapas de calor y experimentos, como **driver intercambiable**;
(4) atar la navegación a la cuenta, la 360 de navegación y el driver van bajo la categoría **`analytics`**;
comunicaciones solo con `marketing_opt_in`; (5) los anuncios (Google, Meta, TikTok) se atribuyen en el libro
sin banner y las conversiones van a las plataformas **desde el servidor**, solo con `marketing`; **sin
remarketing**.
**Por qué**: hoy no hay rastreo, UTM ni canal en `orders` (spec §1); y `PAY-16`: la verdad vive en casa.
**Descartado**: GA4/SaaS como verdad (gateado por el banner, una compra por plataforma); Matomo como todo
(149–249 €/año por complemento y otra aplicación en producción).
**Consecuencia**: contrato 1.18.0 (`POST /events`), `POLICY_VERSION` sube en T3, `RGPD-07` propuesto.
El mismo día, `[DECIDIDO owner]`: **todo con la v2.0.0** (sin excepción a `#670`), **sin** la pregunta «¿cómo
nos has conocido?», y **sí** a la revisión adversarial con enjambre: 16 agentes, 72 hallazgos confirmados
(spec §7.1), el mayor: el cruce cookie↔cuenta NO es exento y pasó a `analytics` (la v1 lo daba por LIA).

## #679 · 2026-09-23 · `[DECIDIDO owner]` La analítica sigue en el OTRO ordenador (T2→T5) y en éste arranca la LANDING NUEVA
Lo aparcado, resuelto en #681 y #682 (2026-09-24): Blade en la instancia, no Astro; la isla, segunda carcasa.
El owner, con la T1 cerrada: *«en el otro ordenador se seguirá el sistema de analíticas y aquí el diseño; el
nuevo diseño no tiene nada que ver con el antiguo, es totalmente diferente y tal vez debamos hacer lógica
nueva, no es solo diseño; lo subiré directamente a la instancia o tal vez con DesignSync, no lo sé; es un
sistema de diseño completo»*.
**Se decide**: (1) la T2 (el cuadro, `specs/analitica.md` §4.5) y las que siguen las hace el carril del SPA,
con su banda; el traspaso va en el buzón de plataforma (qué queda hecho, qué exige §4.5, las trampas); (2)
este carril abre la **landing nueva** de la vía A (`#611`, `#631`): un sistema de diseño **nuevo y distinto**
del canvas v1.32 que vistió la actual, que puede exigir lógica nueva — entonces entra primero por el menú de
hechos (`instancia-y-landing-fuera.md` §4.1.bis).
**Aparcado hasta ver el diseño** (el owner: «lo valoramos en la siguiente sesión… lo iteramos cuando la
veas»): cómo llega (a la instancia, o `/design-login` + DesignSync, hoy sin autorizar en esta máquina); con qué
se construye la vía A (recomendación: Astro); cómo entran los hechos (recomendación: horneados al construir +
refresco en vivo, a explicar como «la opción más profesional»); la primera tanda (probablemente la portada).
**Consecuencia**: `ESTADO.md` y el carril lo dicen; la fila T2 de §4.8 nombra al otro ordenador.

## #680 · 2026-09-23 · La T1 de la analítica cierra con tres correcciones medidas: la UTM se pega TRAS firmar, `trustProxies '*'` se retira (`SEC-13`), y nacen `RGPD-07` y `PAY-21`
Técnico, dentro de `#678`; cada pieza con su medida en la spec (§4.1 y §6) y en las invariantes.
**(1) La UTM de los correos**: la spec decía «antes de firmar Y `ignoreQuery`», y son excluyentes — el HMAC
cubre la query entera y `hasCorrectSignature()` retira lo ignorado antes de recalcularlo (leído en el
framework): firmada dentro e ignorada, 403 al padre. Se pega DESPUÉS de firmar y se ignora al validar
(`EmailUtm::IGNORED_QUERY`); `EmailUtmTest` lo prueba en las dos direcciones.
**(2) `trustProxies(at: '*')`** venía de la Fase 4 suponiendo un proxy. Medido en staging y en producción
(GETs autorizados): PHP habla con LiteSpeed en el mismo host, sin CDN, y una `X-Forwarded-For` falsa **se
honraba** (65 con XFF rotatoria → 0 × 429; sin cabecera → 429 en la #61): el limitador por IP se saltaba. Se
retira; un CDN futuro se acota a SUS rangos (`SEC-13`, `TrustedProxiesTest`, `ENTORNOS.md` §4).
**(3) Invariantes**: `RGPD-07` (el libro exento no lleva persona; ninguna `prop` es dato personal; [T3] el
tercero) y `PAY-21` (el fallo de la analítica nunca alcanza al pago). Y la regex de PII cuenta CIFRAS: tomaba
`2026-09-23` por un teléfono.
**Verificado**: `scripts/mutar-analitica.sh` 19/19 (destapó tres guardas débiles, ya escritas),
`redsys:verify-concurrency` ✓, sonda 9/9, contrato **1.19.0**. Queda el ojo del owner en la fuente del pedido manual.

## #681 · 2026-09-24 · `[DECIDIDO owner]` La landing nueva va en Blade dentro de la instancia y recibe solo hechos; ni Astro ni web estática
Las páginas del sistema nuevo (Claude Design `33397ca2…`, «Saltia») son vistas Blade en
`instancias/playjump/web/`, servidas por `instancia::` (`SEC-12`), que reciben **solo hechos del menú**: los
mismos datos que las rutas públicas de `/api/v1`. Resuelve lo aparcado en `#679`.
**Por qué** (medido el 24-09): producción no tiene Node (`ENTORNOS.md` §6), así que Astro solo cabría
estático; los datos del panel quedarían horneados, y con despliegues de noche (`#594`) un precio cambiado
tardaría horas. Un estático en `public/` se salta `SecurityHeaders`, `NoStoreWebResponses` y
`ResolveVisitor`; el `rsync --delete` y la guarda 9 pedirían una segunda subida.
**Descartado**: Astro estático (lo anterior); Nuxt/Next con servidor (no hay Node); la web en otro proveedor
(un proxy delante reabre `SEC-13`); un constructor en el panel (`#611`).
**Consecuencia**: `specs/isla-y-landing-nueva.md`; la instancia gana pruebas de sus vistas; la vía estática
sigue abierta para otros clientes (el cajón ya compra desde una página ajena, F4).

## #682 · 2026-09-24 · `[DECIDIDO owner]` La isla es una segunda CARCASA del producto, apagada por defecto y medida contra el cajón
La «isla» del sistema nuevo (menú, compra y Mi cuenta en una pieza flotante que cambia con el contexto) se
construye en el producto, en Vue, sobre el MISMO motor que el cajón (stores, máquina de pasos, cliente de
`/api/v1`). Cada instalación elige carcasa por ajuste; por defecto, el cajón lateral. PlayJump la enciende y
se mide isla contra cajón con los experimentos de la analítica (`analitica.md` §4.4). La hace este carril,
avisando antes al SPA (`CONVENCIONES §10`).
**Por qué**: el owner la ve innovadora y arriesgada, y no todo cliente la querría. En la instancia la compra
quedaría fuera del gate y habría dos compras. Medido: el SPA son ~10.600 líneas de lógica JS y ~12.900 de sus
tests frente a ~8.100 de pantallas Vue; cambiar la forma es diseño (`cajon-empaquetable.md` §4.9).
**Descartado**: la isla solo en la instancia con el SPA oculto; la isla para todos; reescribirla en React.
**Consecuencia**: la lógica nueva que pide (Apple, Bizum…) entra antes por la API; el A/B espera a la T5 (`#735`).

## #683 · 2026-09-24 · `[DECIDIDO owner]` Las cuatro de la isla: Kids y Jump primero, Lucide, pasos compartidos, y Bizum, Apple y el aviso en la v2.0.0
Respuestas a `isla-y-landing-nueva.md` §7, las cuatro con la recomendación delante:
**(1)** La primera página que estrena la cadena (página en la instancia, calculadora y compra por la isla) es
**Kids y Jump**: un molde da dos páginas y su compra no lleva señal. **(2)** Los iconos son **Lucide**, metidos
en el HTML al construir (sin CDN), en la isla y en las páginas; `IconSetAnatomyTest` deja de decir «nada de
otra librería» en la T1. **(3)** El cajón clásico **adopta los pasos nuevos**: un solo contenido de pasos
montado por las dos carcasas. **(4)** En la v2.0.0 entran **Bizum, entrar con Apple y el aviso de día
liberado**; el cálculo compartible, después. La fecha de fin de oferta la cubre `#684`.
**Por qué**: las razones de §7. **Descartado**: Cumpleaños o la portada primero; el set propio redibujado; dos
juegos de pantallas. **Consecuencia**: T1 y T4 de §4.7; Bizum pasa por `INVARIANTES` §1 y `VERIFY_CONC=1`.

## #684 · 2026-09-24 · `[DECIDIDO owner]` Un SISTEMA DE PROMOCIONES en el panel, en su propia página; el modelo, a iterar
Matiza `#631`. El owner: promociones manuales **para cada producto, pack o complemento**, hechas de **un texto
y un icono** que se sitúan en ese elemento, más un **aviso arriba** para ofertas y noticias; «página
independiente, porque añadiremos más sistemas de promociones y el operador las gestiona todas en un sitio».
Es lo que hoy hace `ticket_types.gifts` en los productos, generalizado a packs, complementos y más sitios que
dirá después. Cada promoción lleva sus fechas, y eso da la fecha de fin que pide la isla (situación 4).
**Por qué**: el operador necesita tener las promociones a mano. **Descartado**: una promoción dentro de cada
ficha (se dispersa). **Consecuencia**: spec propia antes de construir; entra al menú de hechos
(`instancia-y-landing-fuera.md` §4.1.bis). ⚠️ Una etiqueta no es dinero: un descuento sigue siendo hecho de
precio (`promo.percent`) y va por `CRITICAL_RE`. **Precisado el mismo día**: el aviso de arriba es para
noticias y avisos generales; cada oferta, en la página de su producto (regla 5 del brief base).

## #685 · 2026-09-24 · «Idéntico al diseño» se juzga con `scripts/pixel.mjs`, y las fuentes de la instancia se sirven desde el propio dominio en `public/instancia/`
Técnico, dentro de `#681`. El owner pide el sistema «píxel por píxel, idéntico». **(1)** El juez es
`scripts/pixel.mjs`: referencia y lo nuestro en el mismo Chromium, pieza asentada, y cuenta de píxeles
distintos; idéntico = 0, con control negativo (sin fuentes, 97.956 distintos y código 1). **(2)** Las fuentes
de Saltia son los nueve `woff2` que Google entrega al `@import` del diseño, servidos desde el dominio
(`font-src 'self'`), no desde `fonts.bunny.net` como `ThemeFonts`: Bunny sirve pesos sueltos y los héroes
usan el eje de anchura de Archivo. Medido: 0 píxeles distintos a 390, 1440 y ×3. **(3)** El material nuevo de
una instalación va en `public/instancia/`: una carpeta ignorada y excluida del `rsync`
(`DeployScriptGateTest`, mutado).
**Descartado**: juzgar a ojo o con la huella (A y B son marcados distintos); Bunny; una exclusión por fichero.
**Consecuencia**: `isla-y-landing-nueva.md` §4.8; la referencia exacta (T1a) espera a que el owner publique el
diseño como artifact, porque `DesignSync` corta a 256 KiB y pierde `ParkIsland`.

## #686 · 2026-09-24 · Lucide 0.544.0 vive versionado en `resources/icons/lucide/` y `<x-lucide>` pinta igual que el `Icon` del diseño
Técnico, dentro de `#683`. `scripts/traer-lucide.py` baja `lucide-static@0.544.0` del registro de npm, comprueba
su `sha512` y deja los 1.865 SVG con su `LICENSE` (ISC) y un manifiesto; no toca `package.json`, que es
compartido. `Lucide::svg()` y `<x-lucide>` repiten lo que hace `Icon.jsx`: las mismas sustituciones, solo en
su primera aparición, el mismo `<span>` y ni un espacio alrededor. **Medido**: los 84 iconos del diseño a 16,
20 y 24 px y sus variantes, contra el `Icon` del diseño, 0 píxeles distintos en 1280 y 390
(`scripts/banco-lucide.php`); un solo trazo alterado da 99 y código 1. `LucideIconTest`, 9 casos, mutados.
**Descartado**: pedir los iconos a jsDelivr en cada visita (tercero y red en el camino); instalar el paquete
con npm (toca `package.json`). **Consecuencia**: `IconSetAnatomyTest` sigue guardando el set antiguo
(`components/icons`), donde Lucide no entra; el nuevo tiene su propia carpeta y su propia guarda.

## #687 · 2026-09-24 · La isla se porta 1:1 a Vue, pinta con roles `--isla-*` y textos de `lang/`, y es idéntica al diseño en 52 pares
Técnico, dentro de `#682` (`isla-y-landing-nueva.md` §4.9). `ParkIsland.jsx` pasa a `resources/js/isla/` bloque a
bloque (la tabla de prioridades en `situacion.js`, con pruebas), para que un cambio del diseño se traslade
comparando fichero con fichero. Como es del producto, **no nombra la paleta de nadie**: sus seis primitivos de
PlayJump y siete colores de marca escritos a mano pasan a 13 roles `--isla-*` con valor neutro en `:where()`, y
PlayJump los asigna en su instancia; sus textos, a `lang/*/isla.php`, con las horas por `:hora`.
**Medido**: `scripts/banco-isla.php`, la isla del diseño contra la nuestra en 26 situaciones, abajo a 390 y
arriba a 1280: **52 de 52 con 0 píxeles distintos**; un rol cambiado da 60 y código 1.
**Descartado**: reescribirla «inspirada» (se pierde la comparación fichero a fichero); usar el React del
diseño (un segundo framework y código de prototipo en producción); los nombres de la paleta en el producto.
**Consecuencia**: la compra (situación 10), Mi QR y la conexión al consentimiento real llegan con la T3, la T5 y
la T3 del SPA; el primer consumidor real es la página de Kids y Jump (T4).
