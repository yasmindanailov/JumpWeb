# Decisiones #700 → #799

> Centena abierta el **2026-09-18** para el carril del **SPA** (banda **700–729**), porque la 550–579 se
> agotó con `#579` y **la 520–549 no estaba libre**: la tabla de reparto no se la asignaba a nadie, pero
> ya tenía entradas de los carriles de correos y de la web. *La tabla dice quién PUEDE, los números
> dicen quién YA lo hizo — y hay que mirar los dos.*
>
> Formato y techo, como el resto: `## #N · AAAA-MM-DD · título`, ≤ 1,5 KB por entrada, al final.

## #700 · 2026-09-18 · `[DECIDIDO owner]` La lista completa deja de rechazar: se cierra el oráculo
Spec `celebracion-e-invitacion.md` §4.5·3 y §10.4.7·A. **Sustituye a D2 de `#569`.** Un «sí» ya no se
rechaza nunca por lista completa: se acepta, toma plaza propia y sale en el aviso del anfitrión «hay N
respuestas que ya no caben» (§4.7). `REASON_FULL` desaparece del dominio y del enum del contrato.
**Por qué**: rechazar abría un **oráculo de pertenencia** —el nombre que emparejaba con una ficha
escrita se aceptaba y el nuevo recibía «full», así que cualquiera con el enlace podía reconstruir la
lista probando nombres—. No era un descuido sino una contradicción de la propia spec: §4.5·3 mandaba
rechazar y §7.2·R1 manda hoja en blanco **también en los errores**. Gana la hoja en blanco, que es de
donde cuelga la feature: el enlace se reparte a un grupo de clase entero.
**Descartado**: aceptar sin que ocupe plaza (el anfitrión no se enteraría) · un desenlace uniforme que
rechazara a todos (el padre de un niño invitado no podría confirmar) · anti-bot, que encarece el sondeo
pero no lo cierra.
**Consecuencia**: los cuatro motivos que quedan comparten la propiedad que los hace seguros —**ninguno
depende del nombre**—. Guarda nueva que compara los DOS campos del desenlace con el mismo fixture. ⚠️ Y
el verificador de concurrencia se reorienta: medía «16 por la última plaza → entra 1», que ya no
existe; ahora fuerza **16 «sí» del MISMO niño** y exige que ocupen una plaza. Visto fallar sin el lock:
**16 de 16** creen estrenar plaza.

## #701 · 2026-09-18 · `[DECIDIDO owner]` T5·1 · la página de la invitación, vestida con el sistema
Spec `celebracion-e-invitacion.md` §4.6 y §10.5.1. Nace `/invitacion/{token}` con su controlador, y con
ella **`Invitation.url` deja de ser `null` solo**: el mecanismo de la T4·6 se cierra al existir la ruta.
Los TRES temas de §3.4 —`confeti` (defecto), `fiesta`, `sereno`— con banda, confeti y chapa de edad
hechos con formas del sistema y tokens de la instalación. Y el **«Más info»** de cada plato.
**Por qué**: la T5 se parte en cuatro y ésta va primera porque **no recoge ningún dato**, así que puede
existir sin el aviso de privacidad de §7.2·R7 ni los textos pendientes del owner.
**Descartado**: inventar el confeti con gradientes propios —`F-06` lo tumbó y el sistema ya tiene
`.grain`, «la única textura que admite sobre tinta»— · un mapa embebido (un tercero dentro de la fiesta
de un niño) · el naranja de comprar, que aquí no vende nada.
**Consecuencia**: `InvitationPageTest` (10). ⚠️ El OJO del owner cazó lo que ninguna guarda podía:
«Dónde» y el menú salían en TEXTO PLANO —clase y regla existían, pero la regla era un párrafo sin caja—.
Ahora usan las piezas del sistema. ⚠️ Y la sonda cazó que retirar un complemento de la VENTA lo borraba
del menú de una fiesta ya pagada. ▶ Sigue T5·2 (contestar, con su aviso).

## #702 · 2026-09-18 · `[DECIDIDO owner]` T5·2 · contestar desde la página, y el aviso de privacidad
Spec `celebracion-e-invitacion.md` §4.6 y §10.5.2. La barra pegada con campo y **dos botones del mismo
peso**, los cuatro desenlaces en sus tonos, Turnstile, y **el aviso de privacidad** al pie: sin casilla
(`#350`), dice para qué son los datos, **que los verá quien organiza la fiesta** —un tercero— y que se
borran a los 14 días. Textos aprobados por el owner.
**Por qué**: los dos botones iguales porque no hay respuesta correcta —jerarquizar el «sí» empujaría a
decir que sí a quien no puede ir, y el anfitrión tendría una lista falsa—. Y el aviso va aunque la
pantalla solo pida un nombre: el nombre de un niño ya es dato de un menor, y quien lo escribe no tiene
cuenta ni ha aceptado nada.
**Descartado**: reutilizar `.gf-savebar`, que es de TRES páginas y resuelve un «guardar» en fila —
tocarla movería el post-form y el justificante, como en `#571` · `back()` para volver, que depende de
un `Referer` que las apps de mensajería quitan · un desenlace de «lista completa», que `#700` retiró.
**Consecuencia**: `InvitationPageTest` (14), con la que compara el aviso ENTERO entre un nombre
conocido y uno nuevo. ⚠️ Sin medir: `§7.2·R12` pide contar los toques de Turnstile y en local no hay
claves. ▶ Siguen T5·3 (el recibo y G2/G3) y T5·4 (`og:*` y el `.ics`).

## #703 · 2026-09-18 · `[DECIDIDO owner]` T5·3 · el recibo de dos horas, y la firma que no se puede pegar
Spec `celebracion-e-invitacion.md` §4.5·6 y §10.5.3. Nace `receiptUrl()` —aplazado desde la T4·2 porque
nombraba una ruta que no existía—: firmado, **2 horas** (D9) y atado a **UNA respuesta, no a la fiesta**.
Con él, G2 (las columnas del pack, con su aviso propio porque ahí hay alergias) y G3 con el salto al
justificante **con la respuesta atada**. «Voy con él» deja de enseñar el botón de firmar (D4), con
`:has()` y sin una línea de JS.
**Por qué el alcance de la firma**: el token de la invitación abre la fiesta entera y el recibo abre una
sola respuesta. Mezclarlos daría los datos de todos los niños a cualquiera con el enlace, que lo tiene
un grupo de clase entero.
**Descartado**: pegar los parámetros a la URL del justificante — **medido, eso invalida la firma** (el
HMAC cubre la query entera) y el padre que acaba de decir que su hijo viene habría recibido un 403.
Ahora viajan DENTRO, y el justificante los lee de su query firmada.
**Consecuencia**: `InvitationReceiptTest` (6). ⚠️ **El owner levantó que el flujo firmar ↔ invitación no
es coherente**: queda abierto en §10.6 con tres defectos (C y D) y **una contradicción que es decisión
suya** (A: `minor_surname` es `required` y §4.5·7 manda no partir el nombre). ▶ Siguen T5·4 y T5·5.

## #704 · 2026-09-18 · T5·5 · el flujo firmar ↔ invitación se cierra, y la vuelta deja de perder la atadura
Spec `celebracion-e-invitacion.md` §10.6·C y §10.6·D. **C**: el recibo ofrecía firmar a quien ya había
firmado; ahora lo pregunta por el contrato nuevo `Booking\Contracts\SignedInvitationReplies`, que
implementa `GuardianPlaces` —la misma frontera y el mismo implementador que `#444`— y responde **por la
ATADURA de `#576`, nunca por el nombre** (`#328`). **D**: quien firma desde su invitación vuelve a SU
recibo y allí lee que ya está firmado; antes se quedaba en la misma hoja que acababa de enviar.
**Un tercer defecto que no estaba en la spec**: la vuelta de un formulario RECHAZADO iba sin los extras
firmados, así que el segundo intento ya no iba atado, **cobraba plaza** y con la lista llena acababa en
«no quedan plazas» — lo que `#576` existe para impedir.
**Por qué la credencial se decide por la FIRMA y no por el cuerpo**: el recibo abre los datos de un menor
dos horas. El `invitation_reply_id` del cuerpo vale para ATAR —el dominio lo contrasta con el contrato—,
no para EMITIR: los extras viajan ahora también en la firma del POST, y el recibo sale solo de ella y
solo para una respuesta de esa reserva.
**Descartado**: preguntar por el NOMBRE (es `#328`) · repetir el control de acceso en los ayudantes.
**Consecuencia**: `InvitationSigningFlowTest` (5) · un caso en `ModuleContractsTest` · arnés
`mutar-flujo-invitacion.py` **7/7**. ⚠️ **§10.6·A sigue siendo del owner**.

## #705 · 2026-09-18 · T5·4 · compartir y calendario: el `.ics` dice la hora del PARQUE, no la del servidor
Spec `celebracion-e-invitacion.md` §4.6 y §10.5.4. Nace `Platform\Services\CalendarFile` —genérico— y la
ruta `/invitacion/{token}/calendario.ics`, con el mismo portero y el mismo 404 que la página. Y la vista
previa (`og:*`), para la que `focused-layout` gana un hueco de cabecera **vacío por defecto**: las otras
dos hojas del molde salen igual que antes.
**Por qué `TZID` y no UTC** (`§7.2·R13`): las franjas son hora de PARED. La cifra con una `Z` pegada
adelanta la fiesta una o dos horas, y el instante en UTC ata el evento a las reglas horarias de HOY: si
el país cambia sus cambios de hora antes, el evento se mueve solo. Con `VTIMEZONE`: `TZID` a pelo lo
descartan los lectores estrictos.
**Por qué la vista previa lleva tan poco**: la pinta el chat de la clase entera. Nombre, edad, día, hora
y negocio — ni dirección, ni menú, ni respuestas, ni el token en una meta.
**Descartado**: Google Calendar de primera opción (§4.6 lo deja de respaldo) · declarar medidas de una
imagen externa.
**Consecuencia**: `CalendarFileTest` (6, unitario) · `InvitationSharingTest` (6) · arnés **8/8**.
❗ **El fichero servido por HTTP destapó lo que el unitario no vio**: recortar la lista de transiciones
reindexaba y cada observancia entraba en el desfase del que venía. Hay guarda del PAR.
⚠️ Sin probar en un teléfono real (§4.6); `og:image` es el logotipo 1200×441, no una tarjeta 2:1.

## #706 · 2026-09-18 · `[DECIDIDO owner]` Nombre y apellidos siguen siendo DOS campos: se retira el prerrelleno
Spec `celebracion-e-invitacion.md` §10.6·A, que queda CERRADA. **`#236` se mantiene** y el justificante
no toca su esquema: nada de unificar, ninguna migración, ninguna firma reescrita. Y como la invitación
pide el nombre en UNA casilla, **el prerrelleno del menor se retira**: la hoja le ENSEÑA lo que escribió
(«En la invitación escribiste "…". Repártelo aquí») y lo reparte él.
**Por qué no las dos salidas que propuso el owner**, medidas antes de contestar:
· *«prerrellenar solo el nombre»* obliga a adivinar dónde acaba, y cualquier corte recorta un nombre
compuesto —«María del Carmen Ruiz Gil» → «María»— **dentro de un documento que se firma**;
· *«en la invitación pedir solo el nombre»* rompe la lista del anfitrión: `child_key` es la identidad del
niño, así que **dos «Martina» distintas de una clase colapsan en una** (medido: el anfitrión ve «vienen 1»
y una sola propuesta; la segunda niña desaparece sin que nada avise).
**La prueba de que el defecto era real la dio el owner**: firmó desde el enlace y su justificante quedó
como «Hugo Ruiz Pla» + «DANAILOV», que es exactamente lo que el prerrelleno provocaba.
**Consecuencia**: el caso de `InvitationReceiptTest` que afirmaba el volcado ahora exige lo contrario
—casillas vacías y el aviso—; `lang/*/guardian.php` gana `minor.from_invitation`. Sin migraciones.

## #707 · 2026-09-19 · El `addBtn` que faltaba en `DependentsZone`, y la secuencia de firmar baja al módulo
Defecto VIVO en producción desde v1.1.0, levantado por plataforma con ESLint (`#629`) y **reproducido en
navegador antes de tocarlo**: la plantilla tenía `ref="addBtn"` y el `<script setup>` no lo declaraba, así
que plegar el alta lanzaba `addBtn is not defined` y **el foco caía al `<body>`** — justo lo que el
comentario de `#217` promete que no puede pasar. Ningún test lo veía: una referencia no declarada solo
revienta al EJECUTARSE.
**Lo que costó de verdad**: declararla dejó el componente en 41 líneas con techo 40, y el gate `CE-6` paró
el commit. **No se sube el techo**: bajó `signDependent()` —la secuencia de firmar con su regla del texto
releído— a `account/dependents.js`, que es donde tiene `node --test`. Es el mismo movimiento que ya hizo
`reread`, y por la misma razón escrita en ese módulo.
**Por qué el arreglo no lleva test de servidor**: no existe en la casa arnés para montar un `.vue`. La
guarda es el TRINQUETE de ESLint —visto morder: sin la línea, `lint:js` sale 1— y la sonda nueva
`scripts/sonda-foco-cuenta.mjs`, versionada por la lección de `#475`.
**Consecuencia**: `FROZEN_JS_ERRORS` **12 → 10** (la referencia congelaba dos errores, no uno) · tres
casos nuevos en `dependents.test.js` para una secuencia que no tenía ninguno · sonda en verde.

## #708 · 2026-09-19 · T6·1: el bloque de la invitación entra por su PROPIO POST, no por el de guardar
Spec `celebracion-e-invitacion.md` §4.7 y §10.8. El anfitrión ya tenía API (`#578`) y no tenía pantalla:
entran compartir, personalizar, el resumen y el plazo **escrito como fecha**.
**Lo que decide la forma**: personalizar escribe **solo `party_invitations`**, y `order_items.updated_at`
es el testigo optimista con el que el post-form detecta que el parque movió la reserva. Dentro del POST
de siempre, **cambiar el color de una banda** dejaría obsoleta la página abierta y tumbaría la compra de
un extra. Por eso ruta aparte (`reservation.invitation.update`), con la MISMA puerta —el trait— y su URL
firmada compuesta por el dominio, para que lleve la versión del enlace (D14).
**Descartado**: meterlo en `gf-form` con `form=` como el número de invitados (un `form` dentro de otro no
es HTML válido, y seguiría moviendo el testigo) · devolver **422** ante un texto con enlace (convertiría
un descuido de redacción en un formulario que no guarda nada: se rechaza ESE campo y se DICE).
**Medido** (el detalle, en §10.8): un campo vacío llega como `null`, así que `['sometimes','string']` le
devolvía **422** a quien borraba «Te invita» —y la API tiene hoy la misma regla—.
**Consecuencia**: `PublicFreeText::rejects()` distingue «vacío» de «no se admite» ·
`OrderItem::invitationSignedUpdateUrl()` · 10 casos con el CONTROL del testigo · arnés 8/8 · sonda a 390
y 1280. Sin migraciones, sin contrato de API y sin tocar el `CRITICAL_RE`.

## #709 · 2026-09-19 · T6·2: la propuesta se pinta sobre la ficha y se adopta al guardar, no antes
Spec `celebracion-e-invitacion.md` §4.7 y §10.9. El dominio es de la T4·6; entra la pantalla: el «sí»
pendiente se pinta sobre su ficha rellenando **solo lo vacío**, con la chapa «Por la invitación» y su id
en `adopt[]` —**fuera** de la fila (§7.2·R3)—; al guardar, `adopt()` y luego `reconcileAdopted()`, en el
mismo orden y con las mismas condiciones que la API, porque adoptar marca con la clave del nombre que él
**acaba de escribir**.
**Lo que decidió el ARNÉS, y es lo que más vale**: el caso del INTERCALADO **estaba mal escrito** —la
respuesta tardía caía en una ficha vacía, y una ficha sin nombre no se adopta ni queriendo, así que
«adoptar solo lo pintado» y «adoptarlo todo» daban lo mismo—. Reescrito para que empareje con una ficha
escrita, la mutación muerde. *Un superviviente es una pregunta sobre el TEST* (`#578`).
**Retirado**: la comprobación `attending` de la pantalla. El contrato de `proposalsFor()` ya garantiza
que un «no» llega sin ficha, así que ninguna prueba podía ponerla en rojo: guarda que nada tumba es
ruido (`#704`). La mutación apunta ahora al dominio.
**Descartado**: adoptar «todo lo pendiente» al guardar (rompe el intercalado: se llevaría respuestas que
él no ha visto) · marcar campo a campo lo prerrellenado (el canvas no lo dibujó; la chapa es de la ficha).
**Consecuencia**: `GuestFormController::withProposals()` · 8 casos · arnés 6/6 · sonda de la ficha a 390
y 1280. Sin migraciones y sin tocar el contrato de la API.

## #710 · 2026-09-19 · T6·3: «No lo apuntes» es el par del suelo, no una comodidad
Spec `celebracion-e-invitacion.md` §4.7 y §10.10. Entran el grupo «No vienen» con la frase de D3 y su
chapa en la ficha que empareja, el aviso de «no caben» (la carrera de §7.1·3, dicha) y **«No lo
apuntes»** sobre un «sí» pendiente o sobre un «no».
**Por qué importa**: desde `#576` un «sí» pendiente es una plaza con dueño y **sube el suelo** por
debajo del cual el anfitrión no puede bajar el número de invitados. Sin este gesto, una respuesta que no
quiere le deja ATRAPADO. Por eso el caso mide el SUELO antes y después, no lo que se pinta.
**Forma**: su propio POST (`reservation.invitation.dismiss`), como personalizar y por lo mismo —escribe
solo `invitation_replies` y no puede mover el testigo de los extras—, con su control. El botón vive en
dos sitios y el formulario fuera de los dos: los une `form=`, el recurso de `#571`.
**Medido, no supuesto**: `GuestCountAdjuster` **sí** está en el `CRITICAL_RE`, pero **no hizo falta
tocarlo** —el suelo ya salía de `GuardianPlaces::takenIn()`—, así que este push no pidió `VERIFY_CONC`.
**Arreglado de paso, defecto de la T6·1**: personalizar mandaba al titular a «Mis pedidos», porque
reutilizaba el `backUrl()` de guardar. Guardar es terminar; esto son gestos DENTRO de la pantalla. Los
casos decían «redirige» sin mirar a dónde.
**Consecuencia**: `PartyInvitations::declinedPendingIn()` · `invitationBackUrl()` · 10 casos · arnés
8/8 · `proposalsFor()` se pide una vez por render, no dos. Sin migraciones.

## #711 · 2026-09-19 · T6·4: la puerta ve a los niños SIN firma, y por lotes porque no le cabe otra
Spec `celebracion-e-invitacion.md` §4.8 y §10.11. La lista de la puerta pasa a ser *las fichas con
nombre + los «sí» que el anfitrión no ha apuntado*, con su **estado de entrada** (firmado · viene con un
adulto · sin resolver, ninguno en rojo) y la cuenta «8 de 12», medida sobre **lo contratado**. Y la hoja
de sala imprime la respuesta pendiente **marcada**: sin eso, un anfitrión que no vuelve a guardar deja
niños fuera del papel.
**Lo que decide la forma**: el presupuesto de la puerta estaba en **27 de 28 consultas**, así que las
fichas viajan en el contrato que ya carga la línea (coste cero) y las respuestas se piden **en un viaje
y solo si hoy hay fiesta con invitación** — un escaneo normal no paga nada, y hay caso que lo mide.
**Frontera**: Booking publica `PartyGuests::partyGuestsIn()` e Identity compone el estado, porque las
firmas son suyas. Es la misma salida que el suelo de plazas de `#576`, no una nueva.
**Medido, y no estaba escrito**: el emparejado **no es la igualdad de claves** —«Mateo» y «Mateo Ruiz»
salían como dos niños—, así que la regla sube a `PersonNameKey::cardMatches()`, que ahora usan los dos
módulos. Y un fixture de HOY no puede contestar: el plazo ya cerró (D14).
**Declarado**: el nombre de un niño invitado puede traer apellidos y **no contradice `#236`** (§10.11).
**Consecuencia**: 10 casos nuevos + 2 del papel · arnés 9/9 · PDF real renderizado · la línea base de
Larastan baja a **458**. Sin migraciones.

## #712 · 2026-09-19 · T6·5: el botón que le faltaba a una acción escrita hace cuatro tandas
Spec `celebracion-e-invitacion.md` §4.8 y §10.12. En la línea del pedido entran el resumen «N vienen ·
M no · K por repasar», **«Copiar el enlace de la invitación»** —el público, el que se reparte al grupo
de clase— y el botón de **anular**.
**Lo que más vale de esta unidad**: la acción de anular **ya existía desde `#576`**, con su audit y sus
casos, y su docblock decía que el botón lo pintaba la T6. **Escribí otra igual sin buscarla**, y solo lo
cazó el ARNÉS —encontró su `rotateToken()` dos veces en el fichero—. Se retiró la duplicada. ▶ Antes de
escribir una acción del panel, búscala: este repo lleva 700 decisiones y muchas dejaron la mitad hecha
a propósito.
**Decidido**: el panel **no materializa** la invitación (`existingFor()`, no `forReservation()`): nace
en el GET del anfitrión (§4.5·1), y crearla al abrir una ficha daría un enlace que repartir antes de
que el cliente viera su formulario · el resumen va memorizado en una propiedad **privada**, porque una
pública viajaría en el payload de Livewire con cuentas de niños dentro.
**Retirado**: la comprobación de «pedido pagado» de esta página — `isShareable()` ya la hace y ninguna
prueba podía tumbar la copia (`#704`). Queda el IDOR, ahora con su caso.
**Consecuencia**: `invitationLinkForManageItem()` y `invitationSummaryFor()` en `ViewOrder`,
`PartyInvitations::existingFor()`, 9 casos y arnés 7/7. Sin migraciones.

## #713 · 2026-09-19 · T6·6: el recordatorio que no envía nada, y la cuenta que suma la BD
Spec `celebracion-e-invitacion.md` §4.7 y §10.13. Cierra la T6. El anfitrión pulsa, se compone el
texto —saludo, nombres **solo si marca la casilla**, plazo y enlace **dentro**— y se guarda que avisó.
**Decidido (owner)**: `reminded_count` cuenta **VECES**, no familias: es lo que evita mandarlo tres
veces. Descartadas «a cuántos iba el último» y «las dos con una columna nueva» (una migración más).
**Decidido**: una respuesta **descartada es una respuesta** —`awaitingNamesIn()` mira todas, no solo
las vivas—: «no lo apuntes» es quitarse algo de la lista, no un «no me han contestado» · la casilla
nace **sin marcar**: nombrar a quien falta señala a unas familias delante de las demás · el texto
vuelve por la **sesión**, nunca por la URL: lleva nombres de menores · las dos columnas de la T4·1
**se las queda esta unidad**; la T7 traerá su propia marca para el aviso de la víspera.
**Lo que enseñó**: `DisplayTime::dayLabel()` **no convierte de zona** y la columna sale en UTC — sin
`setTimezone` un aviso de las 00:30 de Madrid se fechaba **ayer** · y un texto con `rows="5"` traía su
propio scroll dentro del de la página: **lo vio la captura, no la medida**.
**Consecuencia**: `awaitingNamesIn()`, `reminderTextFor()`, `remind()` y `namedGuestCards()` en
`PartyInvitations`, ruta y POST propios, 14 casos y arnés **13/13**. Sin migraciones.

## #714 · 2026-09-19 · La T7 se parte en tres, y el aviso de la víspera sale a las 18:00
Spec `celebracion-e-invitacion.md` §4.9 y §10.14. Última tanda de la invitación.
**Decidido (owner)**: el aviso sale **a las 18:00 del día ANTES**, hora del parque y **fija** — es
cuando un padre mira el móvil y aún tiene la tarde para arreglarlo. Descartadas: la hora
**configurable** desde el panel (un ajuste nuevo y 24 pasadas al día en vez de una) y **48 h de
antelación** (más margen, pero deja de ser «la víspera»).
**Decidido**: se parte en **T7·1** (`GuestFormRequest` rehecho: sin migración ni comando),
**T7·2a** (el LECTOR de «qué queda por hacer», que es dominio y cruza a Identity por contrato) y
**T7·2b** (la notificación, el comando, el scheduler y la marca). El corte pone lo único que manda
correo y lo único que pide migración **al final**, sobre un lector ya verde.
**Decidido**: la marca idempotente **no reutiliza `reminded_at`/`reminded_count`** — se las quedó la
T6·6 (`#713`) y son otro gesto: aquél lo escribe el anfitrión, éste lo manda el parque.
**Declarado**: la tanda **cruza al carril de correos** (sus notificaciones, `vendor/mail/**`,
`lang/*/emails.php` y su spec). Es del SPA porque depende del dominio de la invitación, y va avisada
en el buzón **antes** de tocar nada (`CONVENCIONES §10`).
**Consecuencia**: ni una línea de código todavía; lo que entra hoy es el plan y el aviso.

## #715 · 2026-09-19 · T7·1: con invitación, lo primero no es teclear — y un comentario que rompió un censo
Spec `celebracion-e-invitacion.md` §10.15. `GuestFormRequest` —el ÚNICO correo que lleva al post-form—
cambia el cuerpo y la llamada cuando el producto ofrece invitación.
**Decidido**: **el asunto y la línea de adelanto NO tienen variante**. El censo averigua el grupo del
diccionario escaneando la cabecera y quedándose con su primer argumento, que ha de ser literal; uno
por variable sacaría el correo del censo y con él el tope de 85, los tres idiomas y el solape.
**Decidido**: **no hay segundo botón**. «Rellenarlo yo» es una FRASE porque el destino es **la misma
página**, y un fantasma exigiría un componente de correo nuevo, que aquí nace por partida doble o el
envío revienta. Si el owner lo quiere botón, es otra unidad.
**Decidido**: el botón lleva el **ancla `#gf-invite`**. Un `?param` pegado a una URL firmada la
invalida; un **fragmento no se envía al servidor**, así que la firma sobrevive — y el anfitrión
aterriza EN el bloque, que en un teléfono empieza por debajo de los 844 px (medido en la T6·1).
**Lo que enseñó**: **un COMENTARIO rompió el censo**. La nota que explicaba el escáner escribía su
llamada entre comillas y el `grep` se quedó con el ejemplo: 12 avisos en rojo. Es `#553` del lado del
inventario, y el arnés lo deja demostrado.
**Consecuencia**: 4 claves por idioma, 6 casos y arnés **8/8**. Sin migraciones.

## #716 · 2026-09-19 · T7·2a: el lector de «qué queda por hacer», y tres supervivientes que eran casos
Spec `celebracion-e-invitacion.md` §10.16. `PendingWork` (fichas, respuestas, plazas de menor y
céntimos a pagar en el parque) y `PendingBeforeVisit`. **No manda nada**: es la mitad del aviso de la
víspera que se puede cerrar sola.
**Decidido**: devuelve **hechos, no frases** · **cancelada o ya celebrada = nada pendiente**, cortado
en el lector porque quien avisa serán tres sitios · sin justificante en el producto **cero menores sin
resolver**, o reclamaría 20 papeles que nadie pide · y solo cuenta el saldo **`pay_at_park`**.
**Medido**: **no hizo falta contrato nuevo**. Las plazas con dueño ya las publica
`ReservationPlacesTaken` (`#444`) — se buscó antes de escribirlo.
**Lo que enseñó**: los **tres supervivientes del arnés eran casos que faltaban** (`#578` otra vez), y
uno señalaba un defecto real: la mutación del saldo era EQUIVALENTE contra una devolución —el
`max(0, …)` ya tapa lo negativo— y lo que la mata es **`pay_online`**, cuya cifra es POSITIVA sin ser
dinero del parque; sin esa guarda el aviso mandaría al parque lo que se paga por web. ▶ Y «a pagar en
el parque» se monta con la **señal**: un cobro parcial rompe las identidades y sale `under_review`,
que también da 0 — los casos afirman antes qué dice el libro.
**Consecuencia**: 13 casos y arnés **10/10**. Sin migraciones y sin correo todavía.

## #717 · 2026-09-20 · T7·2b: el aviso de la víspera, y el constructor que SÍ toca el testigo
Spec §10.17. Cierra la T7: `VisitEveNotice`, `reservations:eve-notice` y `order_items.eve_notice_at`.
**Decidido**: corre **CADA HORA** y la hora la decide el comando. La zona del parque es un AJUSTE y
`->timezone()` lo resolvería **al registrar** —en cada arranque de consola—; y con una sola pasada una
hora de cron caído se lleva el aviso. Desde las 18:00 cualquier pasada lo recupera y la marca impide
el duplicado.
**Decidido**: el correo **nombra solo lo que falta** y siempre dice que nada de eso impide la fiesta —
la cifra sola se lee como un reproche la noche antes del cumpleaños de tu hijo.
**❗ Lo que enseñó, un defecto que el caso cazó**: **el constructor de Eloquent SÍ escribe
`updated_at`** (`Builder::update()` llama a `addUpdatedAtColumn()`). La spec decía desde el diseño que
la marca «va por el constructor de consultas y no toca el testigo», y era **falso**: así escrito,
mandar el aviso tumbaba la página abierta del cliente y su compra de extras. Se arregla con
`toBase()`. Medido: ninguna otra escritura del repo depende de esa creencia.
**Declarado**: el `whereNull('cancelled_at')` **no muerde** — filtro que ahorra filas, no guarda: la
conducta es del lector (`#716`).
**Consecuencia**: 11 casos, arnés **12/12 (+1 declarado)**, una migración y el inventario a **26**.
▶ Con esto la invitación digital queda **completa en código**.





## #718 · 2026-09-20 · El borde §7.1·5: qué ficha se pierde al bajar invitados
Spec §7.1·5 y §10.18. Se cierra **antes de encender** la invitación.
**El defecto, reproducido**: el recorte se lleva las filas del FINAL y el suelo cuenta **plazas, no
posiciones** — dice cuántos confirmaron, no CUÁLES. Con ellos en las últimas fichas, una bajada
PERMITIDA borraba a quien el suelo protegía y dejaba vacías en pie; y `reconcileAdopted()` veía la
ficha ausente y **descartaba la respuesta**: la protección se deshacía sola y en silencio.
**Decidido** (de las dos salidas de la spec, midiendo): **compactar antes de recortar** —
confirmados, escritas, vacías—, **estable** dentro de cada grupo. Se descarta reordenar al ADOPTAR:
movería fichas cuando el anfitrión no lo pide, y de la posición cuelgan su régimen y la hoja de sala.
**❗ Lo que enseñó: la costura tiene DOS puntas y leyéndola parecía tener una.** Con el dominio ya
arreglado, por HTTP seguía vivo — el controlador ajusta **y después** guarda las fichas del navegador
en el orden viejo (§4.7·2). Lo cazó andarlo, no releerlo. ▶ La regla vive en **`GuestCardOrder`**,
una clase para las dos puntas: escribirla dos veces produjo los defectos de §10.4.7·B. Y a quién
proteger lo dice **`PartyGuests`**, la fuente del suelo.
**Decidido**: se compacta **solo si se va a recortar** (lo cazó `InvitationApiTest`).
**Consecuencia**: 5 casos, arnés **10/10**, tres verificadores verdes sobre InnoDB. Sin migraciones.




## #719 · 2026-09-20 · JumpSystem: la identidad del producto ante Google
`google-business-profile.md` §7·A. `[DECIDIDO owner]`.
**Decidido**: el proyecto CENTRAL, la pantalla de consentimiento y el dominio van a nombre de
**JumpSystem** —la casa de JumpWeb, JumpApp y lo demás—, con cuenta y web propias (`/`,
`/privacidad`, `/aviso-legal`).
**Por qué**: lo que ve el dueño de un parque al autorizar es el **nombre de la app y el dominio**, y un
white-label no puede llevar la marca de otro producto ni de un cliente. La aprobación de la API queda
atada a la CUENTA: se elige una vez.
**Descartado**: `playjump.es` (marca de un cliente ante parques que compiten con él) y la cuenta
personal del owner (migrarla obliga a re-solicitar el acceso y esperar otra vez).
**❗ Medido hoy contra la doc oficial, y CORRIGE el §7·A**: (a) la ficha que habilita la solicitud puede
ser **de un cliente que gestionas** —«*…or it could belong to one of the clients they manage*»—, así que
JumpSystem **no necesita ficha**: PlayJump añade la cuenta como **administrador** de la suya (la spec la
daba por gestionada); (b) inicio y privacidad en **el mismo dominio**, públicos, y el inicio **describe
la app** o hay rechazo; (c) el vídeo de verificación exige el flujo **funcionando y desplegado** → el
ámbito se verifica **tras la T1**.
**Consecuencia**: la privacidad de JumpSystem (datos de Google del admin que conecta) es **otro
documento** que la del §10 (autores de reseñas, en la web del parque). Sin código.




## #720 · 2026-09-20 · T1·1: dónde vive el token de la ficha, y cómo se lee
`google-business-profile.md` §4.2. Primera tanda con código de `#524`.
**Decidido**: tabla propia, **fila única por índice UNIQUE** (no por convención), token `encrypted`, y
**quién conectó como FK SIN relación de Eloquent**, para que `ModuleBoundariesTest` siga diciendo
`'Platform' => []`. **Descartado** seguir a `AuditLog`, que sí tiene su entrada en `SEAM`: el §4.0 puso
la conexión en Platform **porque** no depende de nadie, y la excepción habría borrado ese motivo.
**Decidido**: el token se descifra **a mano** (`Crypt::decryptString` sobre el crudo) y no por la
propiedad, aunque el cast haga lo mismo (medido). Por la propiedad **el fallo es invisible**: Larastan
llegó a declarar MUERTO el `catch (DecryptException)` y medirlo demostró que no lo está.
**❗ Lo que enseñó**: `getRawOriginal()` devuelve lo **leído de la base**, así que entre poner un token
nuevo y guardarlo entregaba el ANTERIOR — media reconexión con la llave vieja, sin romper nada visible.
Se lee de `getAttributes()`. **Lo destapó el arnés al no hallar caso que lo cazara**: el superviviente
era una pregunta sobre el TEST y salió un caso nuevo.
**Decidido**: `app:set-setting` admite **entrada oculta** (valor omitido → `secret()`); un secreto como
argumento queda en `~/.bash_history` y en `ps`.
**Consecuencia**: 15 casos, arnés **12/12**, Larastan 0. Una migración: **empujada ≠ aplicada**.




## #721 · 2026-09-20 · T1·2: la ida y la vuelta de OAuth, con PKCE
`google-business-profile.md` §4.2·2. Segunda tanda de `#524`. **PKCE no existía en el repo.**
**Decidido**: la ida va por **POST con CSRF**, más estricto que el §4.2·2 (solo lo pedía al
desconectar): abrir el reto escribe en la sesión del admin.
**Decidido**: la pantalla es una página de Filament que **no habla con Google**. Una que hiciera el
viaje guardaría el `code_verifier` en su estado, **que viaja al navegador en el snapshot** — justo lo
que PKCE existe para impedir.
**Decidido**: aquí el MOTIVO del fallo **sí se enseña**, al revés que en `GoogleAuthException`: al
otro lado hay un admin, y «no concediste el permiso» y «Google no devolvió token» se arreglan
distinto. El material no sale.
**❗ Lo que enseñó, y son dos**: (a) **Pint destroza los nombres de método con palabras en MAYÚSCULAS**
(`_UN_` → `_u_n_`) y rompe los `--filter` de un arnés **en silencio**: un filtro que no ejecuta
nada también sale ≠ 0. Van sin mayúsculas. (b) **Dos supervivientes eran tests que
afirmaban una AUSENCIA en un mundo donde la presencia era imposible**: el endpoint de revocar no
estaba fingido y el `catch` se tragaba el cortafuegos, así que «no se llamó» era cierto por el motivo
equivocado. ▶ **Lo que se afirma que NO pasa hay que hacerlo posible primero.**
**Consecuencia**: 24 casos, arnés **15/15**. La URI que va en el cliente OAuth central es
`/admin/ficha-google/callback`, ruta completa y por instalación.




## #722 · 2026-09-20 · T1·3a: el cliente de la API, y qué clase de «no» es cada no
`google-business-profile.md` §4.2·5–§4.2·7. Tercera tanda de `#524`.
**Decidido**: UN solo envoltorio HTTP (§4.2·6), y ser único es la mitad del valor: mientras toda
llamada pase por ahí hay **un solo sitio** donde comprobar que un token no acaba en un log. El de
acceso vive **solo en memoria**; el de refresco, cifrado en su tabla.
**Decidido**: un fallo de Google lleva **un estado o ninguno**. Con estado, la conexión deja de
llamar; sin él —429, 5xx— es pasajero y **no se toca** lo guardado: apagar la conexión de un parque
por un mal minuto de Google sería confundir un fallo con una avería.
**❗ El 403 son DOS cosas que solo separa la RAZÓN**, no el código: `PERMISSION_DENIED` lo arregla el
parque en su ficha; `SERVICE_DISABLED`/`accessNotConfigured` es la solicitud de JumpSystem que Google
no ha aprobado (§7·A·2) y **no lo arregla el parque**. Confundirlos le cuesta el día al admin.
**Descartado** reintentar cualquier 4xx: solo el 401, **una vez y con token nuevo**. Reintentar un 403
no arregla nada y gasta cuota que comparten todos los parques.
**❗ Lo que enseñó**: el canario se probó con la mutación que de verdad ocurre —**la línea de
depuración que cualquiera escribe un martes**— y muerde. Y Pint destrozó tres nombres de test con
mayúsculas **en la tanda siguiente a anotarlo** (`#721`): la trampa es real.
**Consecuencia**: 20 casos, arnés **10/10**, Larastan 0. Sin migraciones.




## #723 · 2026-09-20 · T1·3b: elegir la ficha, y lo que se comprueba antes
`google-business-profile.md` §4.2·4 y §5·SEC-07. 4ª tanda de `#524`.
**Decidido**: la comprobación de la **web de la ficha** es **dura en producción y aviso fuera**. En
desarrollo el sitio es `localhost` y la ficha apunta al dominio real: dura en todas partes obligaría a
saltársela para trabajar, y una guarda que se salta a diario acaba desactivada donde sí importa.
**Decidido**: `mapsUri`/`newReviewUri` se sanean **donde nacen**, más estricto que `safeExternalUrl`:
`https` **y** host de una lista blanca **exacta**. Aquél protege un campo de formulario; éstas llegan
de la red y van a un enlace público. **Los acortadores quedan fuera** aunque sean de Google: ahí «es
de Google» no implica «lleva a Google».
**Decidido**: la pantalla **sí** llama a Google al pintar —`PERF-02` protege la PORTADA, no el panel—
pero **no escribe el estado** aunque falle: apagar una conexión por abrir una pantalla se saltaría el
comparar-y-escribir del §4.2·7. **Descartado** listarlas por un botón y llevarlas en la sesión:
estado que caduca solo para ahorrar una llamada que se hace una vez.
**❗ Lo que enseñó el arnés**: la revalidación sobrevivía a `str_starts_with` porque faltaba el caso
del **PREFIJO**: `locations/9` traería `locations/99`, **otro negocio**.
**Consecuencia**: 32 casos, arnés **11/11**. Falta el **correo a los admins** (§4.2·4): cambia el
censo de correos, avisado en mi buzón, entra en la T1·4.




## #724 · 2026-09-20 · El techo de un fichero de carril sube a 32 KB
`CONVENCIONES §2` y §9.4, `docs-check` check 10. `[DECIDIDO owner]`. Sustituye el 24 KB de `#620`.
**Medido antes de pedirlo**: el 20-09 el carril del SPA tocó el techo en **cuatro tandas seguidas** y
cada una se pagó recortando —apretar trampas, fundir avisos del buzón, bajar detalle a las specs—; la
última cerró con **1 byte** de margen, o sea bloqueando al siguiente en su primera línea. Y no era
solo mío: `plataforma.md` iba a **22,6 KB**.
**Decidido**: 32 KB. **Sigue siendo un techo** y sigue obligando a que el detalle de una feature baje
a su spec, que es lo que el techo protege de verdad; lo que cambia es dónde empieza a doler.
**Descartado quitarlo**: un carril sin techo vuelve a ser la narrativa que `#620` desterró. Y el
recorte SÍ mejoró el fichero —lo que sobrevivió está mejor escrito—, así que la herramienta sirve.
**Descartado dejarlo en 24 y vaciar el buzón**: libera ~1 KB y se llena en dos tandas.
**Consecuencia**: cambian el gate y la doc compartida (`CONVENCIONES`, `ESTADO`, `README`).
⚠️ Los encabezados de `web.md`, `correos.md`, `pasarela.md` y `plataforma.md` siguen diciendo «24 KB»
y **no los toco** (`#621`): quedan más estrictos que el gate, que es inofensivo. Avisado en mi buzón.




## #725 · 2026-09-20 · T1·4: desconectar de verdad, y el aviso de que cambió la ficha
`google-business-profile.md` §4.2·1, §4.2·4 y §4.2·8. 5ª tanda de `#524`.
**Decidido**: desconectar **borra la fila entera**, no solo el token. Media conexión —sin llave pero
con la ficha y el `placeId` dentro— es un estado que nadie sabe leer y que la pasada intentaría usar;
sin fila, el estado efectivo vuelve a «lista para conectar», que es la verdad.
**Decidido**: primero se borra y **después** se revoca, y **si Google no confirma se dice**, con dónde
retirarlo a mano. Al revés quedaría una llave viva en una instalación que se cree desconectada.
**Decidido**: el aviso del §4.2·4 lo manda la capa de ENTREGA y el dominio solo **cuenta lo que pasó**
(`GoogleBusinessChoice`): `User` vive en Identity y Platform no puede mirar a nadie — la misma frontera
que hizo la FK sin relación de `#720`. Va a todo el que tenga `settings.manage`, **no solo al rol
admin**, y **no** a una cuenta anonimizada, cuyo correo rebota. Y **no tumba la elección** si falla:
el cambio ya está guardado y auditado, y perderlo por un SMTP sería el peor desenlace.
**❗ Lo que enseñó el arnés**: el caso de «quién conectó» pasaba **por la barra superior de Filament**,
que pinta el nombre de quien mira. Se mide desde OTRA cuenta.
**Consecuencia**: 16 casos, arnés **11/11**. Correo nuevo `GoogleBusinessLocationChanged`: el censo de
correos pasa a **27**, avisado. ▶ Queda `business-profile:verify` (§4.2·10).




## #726 · 2026-09-20 · T1·5: `verify`, y la cuenta sin la que la T2 no traería una reseña
`google-business-profile.md` §4.2·10. 6ª tanda de `#524`; cierra la T1.
**❗ Medido contra la doc oficial, y era un muro para la T2**: **DOS APIs nombran la misma ficha de
dos formas**. `locations.list` (v1) da *«…in the form: `locations/{locationId}`»*; `reviews.list` (v4,
**otro host**) exige `{parent=accounts/*/locations/*}`. Lo guardado en la T1·3b **no sirve para pedir
sus reseñas**, y leyendo el código no se ve.
**Decidido**: se guarda la cuenta (`account_name`, migración) y el recorrido **se muda a
`GoogleBusinessLocations`**: listar es el único momento en que se sabe de qué cuenta cuelga cada ficha,
y un `allLocations()` de fichas sueltas la tiraba por el camino.
**Decidido**: `verify` **no escribe nada**: un diagnóstico que cambia lo que mide deja de servir;
marcar estados es de la pasada (§4.2·7). Y **no imprime secretos a NINGÚN nivel de detalle**, medido
con `-vvv`.
**Retirado** el aviso propio de `APP_KEY`: **inalcanzable** —el resolvedor ya da «caducada» para un
token ilegible— y una guarda que nada puede poner en rojo es ruido (`#704`). El consejo pasa al texto
de «caducada», que sí se lee.
**❗ Lo que enseñó el arnés**: una mutación apuntaba a la CAPA equivocada —el cuerpo nunca entra en la
excepción— y otra destapó que faltaba el caso de que elegir ficha **guarde** la cuenta.
**Consecuencia**: 14 casos, arnés **10/10**. **Migración: empujada ≠ aplicada.**




## #727 · 2026-09-20 · T2·1: dos plazos para una reseña, y la imagen como ruta nuestra
`google-business-profile.md` §4.3·2→§4.3·11. 1ª tanda de la T2.
**Decidido**: el tope de 30 días **se aplica AL LEER** (29 días) y la purga corta a 30. Dos números
distintos a propósito: una purga programada es un comando que puede no haber corrido, y ese día la
portada pintaría la cara de un tercero pasado el plazo. **La garantía está en la consulta; la purga
es limpieza.**
**Decidido**: **un segundo plazo, de 3 días, sobre el dato PERSONAL**. Sin una pasada que confirme la
reseña, nombre y foto dejan de pintarse y sale anónima; el texto sigue. Caduca la certeza de que sigue
publicada, no la opinión. Se mide contra el `fetched_at` de **LA FILA** y no contra el de la última
pasada: así la que borró su autor **envejece sola**.
**Decidido**: una imagen es **ruta nuestra o `null`**, con guarda en `saving()` que **lanza** en vez de
sanear — una URL de Google ahí deshace la tanda entera **sin romper nada visible**: la foto se vería
igual y el visitante volvería a pedírsela (`RGPD-05`). Caza `//host/…` y `data:`.
**Descartado `MassPrunable`**: borra por consulta sin instanciar, y la T2·4 cuelga de `pruning()` el
borrado de los ficheros.
**Decidido**: **dos tablas**. Con la media en cada fila, el día que el filtro vaciara la tabla **la
cifra se iría con ella**, y §4.3·10 dice que no se filtra nunca.
**Consecuencia**: 25 casos, arnés **12/12**. **Migración: empujada ≠ aplicada.**




## #728 · 2026-09-20 · T2·2: traer las reseñas, y contar lo que se VE y no lo que se queda
`google-business-profile.md` §4.3·1→§4.3·10. 2ª tanda de la T2.
**Decidido**: el recorrido cuenta **FILAS vistas**, no candidatas. Contando solo las que pasan el
filtro, un mínimo estricto se leería como «la ficha no tiene reseñas» y §4.3·3 **borraría la tabla**.
**Decidido**: `coherent()` son **TRES** preguntas y vive en la pasada, no en quien persiste: ¿se
terminó?, ¿la ficha se estuvo quieta entre la primera página y la última?, ¿una lista vacía cuadra
con el total? La T2·3 obedece.
**Decidido**: **tope de 40 páginas**. Un `nextPageToken` que no termina nunca es un bucle infinito en
un worker; al cortar, la pasada sale **incompleta** y ya no puede borrar. Y un testigo **vacío** es
«no hay más»: si no, se pide la misma página para siempre.
**Decidido**: el analizador **falla cerrado en cuatro formas** —media pareja, pareja repetida, sin
original detrás, y cualquier paréntesis que nombre a Google y no se haya separado—. Publicar la
traducción sería firmar con la cara de una persona un texto de una máquina.
**Decidido**: el `null` de una anónima entra **al mapear**, antes de que exista fila.
**❗ Lo que enseñó el arnés**: la guarda del host de la foto **sobrevivió porque faltaba un caso**:
`lh3…com.malo.net` se cuela con `str_contains` y `evil.lh3…com` con `str_ends_with`.
**Consecuencia**: 36 casos, arnés **22/22**. Ajuste nuevo `reviews.min_stars` (4, acotado 1–5).




## #729 · 2026-09-20 · T2·3: escribir lo que se vio y borrar solo lo que se puede creer
`google-business-profile.md` §4.3·1→§4.3·3. 3ª tanda de la T2; cierra la banda.
**Decidido**: **escribir y borrar son dos permisos distintos**. Lo que la pasada vio se guarda
siempre —también si quedó a medias—; se BORRA solo con una pasada coherente. Lo que no ha visto se
parece a lo que ya no existe, y esa portada la ven los clientes del parque.
**Decidido**: el candado **en `cache_locks` de la base**, nombrado explícito y no heredado del
`default`: el de Redis va con `allkeys-lru` y puede **desalojar la llave**. Caducidad = presupuesto
(120 s): menos, y entra una segunda mientras la primera escribe; más, y un worker muerto la deja
bloqueada.
**Decidido**: el presupuesto **para el recorrido de verdad** —antes de pedir otra página— y deja una
pasada incompleta, que ya no puede borrar.
**Decidido**: lo retirado se borra **fila a fila por el modelo**: la T2·4 cuelga del evento el
borrado del fichero, y un borrado en masa no instancia nada.
**Decidido**: el comando sale con **0 también cuando no llama**. Un cron que se pone rojo cada noche
en una instalación sin conectar no lo mira nadie.
**❗ Dos que enseñó escribirlo**: el `finally` soltaba el candado **antes** de persistir —ningún test
de un hilo lo ve; hay caso—; y `Http::fake()` **FUSIONA** los dobles, así que llamarlo dos veces deja
ganando al primero.
**Consecuencia**: 25 casos, arnés **24/24**. Primer uso de `tokenStillIs()`.




## #730 · 2026-09-21 · T2·4: las imágenes, traídas y servidas desde casa
`google-business-profile.md` §4.3·6 y §4.3·9. 4ª tanda de la T2; abre la banda.
**Por qué**: si la cara del autor se pide a Google, la petición la hace el NAVEGADOR del visitante:
sin la categoría `maps` no se pinta (`RGPD-05`) y con ella `img-src` nombra a Google (`SEC-01`).
Descargándola, la portada no le pide nada a nadie.
**Decidido**: **disco privado y ruta propia**, no `public/`: ahí el servidor web las serviría aunque
la fila ya no exista, y no llevarían su propia CSP.
**Decidido**: ruta **pública y sin firma** —la portada se cachea y una firma caduca—; lo que protege
es la **forma del nombre**, misma regla que el borrado.
**Decidido**: nombre = **hash del CONTENIDO** (ni autor ni URL, y deduplica). Sin redirecciones, tope
de bytes a trozos, tipo por **bytes mágicos** (lista BLANCA: el SVG cae) y escritura atómica.
**Decidido**: **desconectar se lleva reseñas y ficheros**; sin ficha no hay base para datos de
terceros. En la capa de ENTREGA: Platform no mira a Content.
**Renunciado, con recibo**: sin `Cache-Control`; `NoStoreWebResponses` es global (`RGPD-04`) y lo
pisaría. **Cada visita repide cada foto**; eximirla se mide en la T2·6.
**❗ Lo que enseñó el arnés**: un `catch (Throwable)` se tragaba el aviso de «petición sin doble» y
las descargas **no se medían**; y `nosniff` aquí era código muerto (lo pone `SecurityHeaders`).
**Consecuencia**: 60 casos, arnés **26/26**. Disco `google-reviews`.




## #731 · 2026-09-21 · T2·5: «Ocultar» son dos cosas y la segunda es la que dura
`google-business-profile.md` §4.3·7. 5ª tanda de la T2. Lo exigió la revisión de privacidad: se
publican reseñas de terceros **sin pedir permiso** (riesgo aceptado, §8·R1), y esto lo mitiga.
**Decidido**: ocultar **borra la fila** —con sus ficheros— **y apunta un hash**. La reseña sigue en
Google: sin el apunte, ocultar duraría hasta las 04:40; sin el borrado, sigue a la vista hasta
mañana. Las dos o ninguna.
**Decidido**: la lista guarda **un hash y nada más**. Es la **única** tabla de la feature que no
caduca, así que cada columna de más es un dato ajeno para siempre. Por eso el panel **no puede decir
qué reseña era**: saberlo obligaría a conservar su texto.
**Decidido**: el motivo es **tasado**. Un campo libre en una tabla que no caduca acaba con el nombre
de alguien dentro, escrito por quien no pensaba que eso fuera un dato personal.
**Decidido**: el rastro lleva **el hash y el motivo**, nunca el texto ni el autor (`RGPD-02`): ese
registro **sobrevive a la reseña que lo causó**. Y **no toca la media ni el total**; que ocultar
bajara el recuento sería el parque cambiando la nota de su ficha.
**▶ AÑADIDO a la spec, a sabiendas**: **dejar de ocultar**. Sin él un clic es irreversible para
siempre. No restaura nada —texto e imágenes se borraron— sino que deja de tapar: la reseña vuelve
**solo si sigue en Google**.
**Consecuencia**: 38 casos, arnés **18/18**. `RGPD-01` ya tiene procedimiento.




## #732 · 2026-09-21 · T2·6: la cascada de tres y la línea que obliga la ley
`google-business-profile.md` §4.3·9 y §4.3·10. 6ª tanda de la T2.
**Decidido**: **Places NO se retira** (`[owner]`, 21-09) y la cascada pasa a **tres**: ficha → Places
→ propias. Sin conexión la 1ª responde vacío y la portada sigue enseñando lo de hoy: **evita una
regresión**.
**Decidido**: **la fuente declara si necesita consentimiento**; la cascada solo recorre. «Google
necesita permiso» estaba escrito como verdad del sistema y dejó de serlo: la ficha se sirve entera
desde casa. ▶ **Consecuencia forzada**: con Places enlazado, **`img-src` no deja de nombrar a
Google** (§4.3·12); ese cambio va con su retirada.
**Decidido**: `ReviewSelection` entra en el contrato y en `CONTRATO_DE_VISTAS`, porque **la Ómnibus
obliga a decir que se filtra** y sin ella la landing no podría. `null` si la fuente no filtra: avisar
de un filtro que no se aplica es peor que callar.
**Decidido**: **la cifra no se filtra, las tarjetas sí**, y hay cifra aunque no quede tarjeta. La
cuenta Google sobre TODAS, y sale **con su fecha**: la trajo una pasada.
**Decidido**: binding **`scoped`** con memo: con `bind`, cuatro recorridos (`PERF-02`).
**❗ Lo que enseñó el arnés**: dos guardas sobrevivieron por falta de CASO — la foto que caduca
necesitaba una reseña **con** foto, y «la selección sale de quien responde» solo se alcanza con un
doble.
**Consecuencia**: 26 casos, **25/25**. ⚠️ **La portada real vive en la instancia.**

## #733 · 2026-09-21 · T2·7: lo que vio el ojo del owner en el panel, y el corte de red
`google-business-profile.md` §4.1. Primera vez que el owner vio el sistema con datos (fixture local).
**Decidido**: **un corte de red es un fallo PASAJERO, traducido en el envoltorio único**
(`GoogleBusinessApi::send()` y el canje de OAuth). La pantalla daba un 500 y, medido después, la
pasada diaria, `verify` y la vuelta de OAuth tampoco lo atrapaban. ❌ Descartado atraparlo en la
pantalla: arreglaba uno de cuatro sitios. ❌ `catch (Throwable)`: se traga el cortafuegos (`#730`).
**Decidido**: **toda hora del panel va por `DisplayTime`**: `app.timezone` es UTC y el panel decía
16:30 por 18:30. Un caso viejo calculaba su expectativa en UTC y **bendecía el defecto**.
**Decidido**: el panel dice lo que queda («enlazada»), la última pasada completa y su aviso a los
3 días, «:count **en Google**», **en la web / de reserva** con `shownIds()` —la MISMA consulta que las
tarjetas— y **la respuesta, las fotos y la cara**: sin verlas no se decide «ocultar por un menor».
**Hallazgo**: el panel habla `zh_CN`, el respaldo es `en` sin `admin.php` y **las 14 claves de
«Ocultar» salían en crudo**. Traducidas, con guarda de paridad; los motivos, al catálogo.
**Consecuencia**: 22 casos nuevos, arnés **29/29**. Queda la tarjeta de la portada (T2·8).

## #734 · 2026-09-22 · T2·8: la tarjeta pinta lo que el contrato ya traía
`google-business-profile.md` §4.3·5, §4.3·8 y §4.3·10. Los ocho hallazgos del ojo (`#733`).
**Decidido**: **la atribución se DECLARA en el contrato** (`Testimonial::ATTRIBUTION_GOOGLE_WORD`, y
en `Rating`) y no se deduce de `source`: Places y la ficha dicen las dos `google` y sus licencias
piden marcas distintas —el logotipo de Google **Maps** sin mapa (`#494`) contra la
**palabra** (§4.3·10)—. ❌ Descartado un `source` nuevo: una vista que no conozca el campo —la de una
instancia sin actualizar— se quedaría **sin atribución**; con `null` sigue atribuyendo, con la marca
vieja. ⚠️ Y las estrellas dejan de ir pegadas a la marca.
**Decidido**: la **anónima** se pinta «Usuario de Google» con su inicial (§4.3·5); la **respuesta**
va debajo y las **fotos** en miniatura, cuatro y «+N»; el texto conserva sus **saltos de línea** y su
dirección; la cifra sale con **«a fecha de»** en la zona del parque, y **sin fecha no se escribe**.
**Decidido**: la **entradilla** cambia con la fuente —«no las elegimos nosotros» solo vale para
Places— y los enlaces de la línea del filtro van **aparte y subrayados**: pegados a la frase no
parecían enlaces. La señal es la selección, no el `source`.
**Consecuencia**: 14 casos, arnés **26/26**. ✅ owner en vivo. La portada de la INSTANCIA necesita lo
mismo (avisado). Queda el aviso de cookies, con el texto de privacidad.

## #735 · 2026-09-24 · La analítica pasa ENTERA al SPA, y la T2 se amplía: el dinero al detalle, los registros y la puerta
`analitica.md` §4.5 y §4.8. El owner: *«todo tipo de cálculos sobre el dinero —el valor medio de los
clientes, el total por mes y semana, el depósito, lo pagado online—; cuántos se registran cada día y mes; y a
cuántos usuarios se busca en la pantalla del QR, para saber cuántos clientes ha habido en el día»*; y «el
otro agente cerró para delegarte toda la analítica».
**Decidido**: T2→T5 desde este carril y con su banda; la T2 en CINCO (T2a dinero · T2b registros y puerta ·
T2c embudo y fuentes · T2d CSV · T2e `analytics_daily` + `ad_spend`), en el orden de sus prioridades. **El
dinero sale de las filas del libro** (`payments`, `payment_refunds`, `deposit_split`, `orders.total`), nunca
del catálogo, y el cuadro no compone ningún libro. **La puerta se mide desde su rastro** (`audit_logs`,
`customer_visits`), que existe desde agosto: historia desde el primer día. ❌ Un evento nuevo como única
fuente nacería vacío. **El permiso es `reports.view`**, sembrado en F7.11 y sin consumidor; ❌ `analytics.view`
sería un sinónimo más en la matriz; el CSV, `reports.export`. «Analítica» es el 5.º sitio del menú del
admin. Los cortes por día: hora UTC agrupada en SQL y asignada al día del parque en PHP (sin `CONVERT_TZ`).
**Consecuencia**: `AdminNavigationTest` pasa a cinco sitios; cuatro índices aditivos; el tracker cambia de mano.

## #736 · 2026-09-24 · T2f: el cuadro por pestañas, la comparación elegible y el año en directo
`analitica.md` §4.5 y §4.8. El owner, tras ver T2a–T2d en escritorio: *«ordenar y organizar mejor todos esos
números, mejorar las gráficas, hay demasiadas tablas; lo de exportar está perfecto; ¿el filtro podrá comparar
este año con el anterior, por mes, año, trimestres?»*.
**Decidido**: (1) **Tres pestañas** (Dinero · Clientes · Conversión) y en cada una el mismo orden: tarjetas →
gráficos → tablas **plegadas** al pie; ❌ una página más larga con más gráficos: el problema era el orden, no
la cantidad. (2) **Seis gráficos de categorías** (barras horizontales y anillos, `CategoryChart`) donde había
tablas; la tabla no se borra: queda plegada como vista de tabla y como lo que lleva el CSV. (3) **La comparación
se elige**: el periodo anterior o el mismo periodo del año pasado (`Comparison`); el 29 de febrero cae al 28.
(4) **Trimestre, año y dos fechas a medida en DIRECTO** contra las tablas de siempre (por mes más allá de 92
días, techo un año); ❌ esperar al roll-up `analytics_daily` de la T2e: con el volumen de hoy son las mismas
consultas con otra ventana, cacheadas 5 min; la T2e queda para cuando el `EXPLAIN` con volumen lo pida. (5) La
pestaña va en la URL (`?pestana=money`).
**Consecuencia**: `ReportPeriod` +5 casos, `Window::yearAgo()`, `BucketLabel`, la caché por ventana Y
comparación; el CSV lleva «Comparado con»; 21 widgets; la sonda mide por pestaña.

## #737 · 2026-09-24 · T5a: el sujeto de un experimento es el VISITANTE, y la cookie se acuña antes de componer la página
`analitica.md` §4.4. La spec (23-09) decía «`hash(visitor_id)`, o `hash(user_id)` en el régimen identificado». Se
midió lo que haría en la compra: identificarse es el paso 5, así que a quien entra con su cuenta le cambiaría la
variante A MITAD de compra, y toda compra con sesión sería una exposición contaminada.
**Decidido**: (1) **El sujeto es `v:<visitante>` siempre que hay cookie, y `u:<titular>` solo sin ella** (la app con
Bearer sin `X-Visitor`); la unidad de la exposición y la de la conversión es la misma (`analytics_events.visitor_id`);
❌ el titular con sesión: rebaraja en el paso 5. (2) **`ResolveVisitor` acuña la cookie ANTES de `$next` y deja el id
en la petición** (`Visitor::ATTRIBUTE`): hasta hoy la primera vista se resolvía sin visitante y todo el mundo
estrenaría la web con la variante de control; ❌ asignar desde la segunda página (sesga el test). (3) Sin
asignaciones guardadas: `hash(clave | sujeto)` en cada petición; cambiar pesos u orden rebaraja → un experimento vivo
se cierra y se abre otro.
**Consecuencia**: `experiments` en `/sidebar/session` (contrato 1.23.0) y en el `data-boot` solo con vivos; el sello
de un pedido y la PRUEBA de consentimiento de la primera petición de un visitante (o con una cookie que no es un
ULID) llevan ya el id que esa misma respuesta acuña.

## #738 · 2026-09-24 · `[DECIDIDO owner]` Los experimentos se quedan codificados uno a uno; sin mecanismo de textos desde el panel
La T5c de `analitica.md` §4.4 NO construye un mecanismo genérico de «experimentos de texto desde el panel» (rótulos
en juego y textos por variante en tres idiomas). Cada experimento es una tanda de código: el owner nombra la
hipótesis, el componente lee `variant()` y avisa con `expose()` (`sidebar/experiments.js`), y el panel solo enciende
y mide (`ExperimentResource`, `ExperimentsWidget`). La T5c queda abierta hasta que haya datos reales (v2.0.0) y una
hipótesis fina del owner.
**Por qué**: el owner (24-09, al ver el panel en vivo) quiere «medir cosas finas que de verdad ayuden a mejorar
conversión», y un rótulo desde un formulario no es una de ellas; el mecanismo costaba dos tandas más el contrato de
sesión con los textos, y no acortaba ninguna hipótesis real.
**Descartado**: (1) los experimentos de texto desde el panel (`t()` consultando la variante, los textos en
`/sidebar/session`); (2) la carcasa isla contra cajón como PRIMERA prueba: no descartada, aplazada a que la isla
compre entera (`#693`→`#695`). **Consecuencia**: `analitica.md` §4.4 y §4.8 lo dicen; el tracker deja T5c ⬜ «a la
espera de datos y de la hipótesis»; ningún fichero de código cambia.

## #739 · 2026-09-24 · `[DECIDIDO owner]` La analítica de la fiesta: el invitado no es un visitante, y las tres respuestas
Se aprueba `specs/analitica-fiesta.md` (la T6 de la analítica): en el post-form, el justificante y la invitación no se
le pone al invitado ni cookie de medición ni script; lo que hace queda como hecho de la RESERVA (`order_id`, sin
`visitor_id` ni `user_id`) emitido desde el controlador; el dinero de después de reservar sale del libro
(`order_adjustments`) y el cobro en el parque de `payments`; una pestaña «Fiestas» y el segmento de los invitados
que vuelven. Las tres respuestas del owner: (1) la unidad de tiempo del embudo es el DÍA DE LA FIESTA; (2) pestaña
propia «Fiestas» con su CSV; (3) los invitados que vuelven se cuentan y se exportan con opt-in como cualquier cliente.
**Por qué**: medido el 24-09, un invitado recibe hoy `visitor_id` (13 meses) en una página sin tracker ni pie legal,
y el owner no quiere «spamearles con cookies»; los tres hechos de la fiesta del `Contract` no se emitían; lo vendido
después de reservar no salía en ningún cuadro.
**Descartado**: la cookie exenta también al invitado (daría vistas únicas y enlace por cookie, pero es una cookie sin
transparencia en el borde de la exención de la AEPD y mezclaba invitados con visitantes); solo tablas de negocio (sin
arriba del embudo ni tiempos). **Consecuencia**: T1→T3 de esa spec; ningún fichero del `CRITICAL_RE`; el tracker T6 🟦.

## #760 · 2026-09-24 · El sistema de diseño entra SOLO por el zip; sin contraste con el proyecto vivo
Procedimiento, a pregunta del owner (plataforma; `isla-y-landing-nueva.md` §4.11, `diseno/README.md` de la
instancia). Su cuenta de Claude Design no es la de desarrollo, así que `DesignSync` no puede leer el proyecto y `/design-login` no
se hace. El zip ya era la fuente (T1a, `#685`); desde hoy es la ÚNICA: se baja después del último cambio y lo
garantizan su CRC, el sha256 de cada fichero y la hora de sus entradas en `MANIFIESTO.json`, que delata un zip más
viejo que el anterior. **Descartado**: contrastar con el vivo (necesita la otra cuenta) o compartir el proyecto con la
de desarrollo (una cuenta más con acceso a su diseño, para una comprobación que el zip ya cubre).
**Consecuencia**: nadie vuelve a pedir `/design-login`; el paso sale del README de `diseno/` y del carril.

## #761 · 2026-09-24 · La T4 (Kids y Jump): el plan, los idiomas, el material y la regla de Jump
`[DECIDIDO owner]` (plataforma; `isla-y-landing-nueva.md` §4.12). (1) **El plan**, en su orden: T4a los datos → T4b
la ruta y el layout → T4c las piezas en Blade → T4d la calculadora → T4e la isla viva en la página → T4f la sonda.
(2) **Inglés y francés**: los redacta el agente desde el brief, marcados «a revisar», y no salen sin el visto bueno
del owner. (3) **Sin el material** (los vídeos de las destacadas, la cabecera, la foto de la pieza 5): cada
atracción con su foto del panel (23/23), la cabecera con la foto de la zona y la pieza 5 sin foto; **nunca un hueco
de «pendiente» en la web**. (4) **Jump: con menos de 1,30 m se entra con un adulto, sin altura mínima** (el
diseño); `/normas` decía «entre 1,10 y 1,30» y Kids «de 4 a 8»: el owner los corrige en el panel.
Técnico, consecuencia de (4) y de `#699`: la zona gana dos reglas de «con un adulto» explícitas —por debajo de la
edad, desde una altura (Kids, 90 cm); por debajo de una altura (Jump, 130)— y el 130 de Jump deja de ser un mínimo.
La edad en números NO va en la zona (corregido al medir: `#676` la dejó en el PRODUCTO, `guest_age_min`/`max`); el
panel solo la dejaba editar en los packs, y se abre a las entradas. Los valores, del panel: las migraciones solo
crean campos.

## #740 · 2026-09-24 · `[DECIDIDO owner]` Las encuestas: atadas al cliente, una por cliente, todos los formatos, correo de servicio, una viva por clase
Se aprueba `specs/encuestas.md` (la T7 de la analítica) con las cinco respuestas del owner: (1) las respuestas van
**atadas al cliente y a su visita** (recomendación del agente ante su duda: una mala puntuación con persona se puede
llamar y arreglar —«Por atender» en el cuadro—, se cruza con lo que el negocio ya sabe de ella y hace posible no
preguntar dos veces; el cuadro enseña agregados y la persona solo aparece con `customers.insights` y se borra con su
cuenta); (2) **una respuesta por cliente y encuesta**; (3) **todos los tipos**: elección única y múltiple, escala 1–5,
sí/no y texto libre; (4) el correo del día siguiente es **de SERVICIO a todo el que visitó**, sin contenido comercial y
con baja de un toque (`users.surveys_opt_out` `(futuro)`); (5) **una interna y una externa vivas como máximo**.
**Por qué**: el owner quiere «medir cosas finas que de verdad ayuden» y «priorizar tener esa información y dar valor
al cliente»; lo anónimo solo da medias. **Descartado**: respuestas anónimas (sin ficha ni atención); solo con opt-in
comercial (perdería a la mayoría; la encuesta de servicio no es publicidad, `[PENDIENTE: asesoría]` lo confirma);
varias vivas por clase (spam y ambigüedad en la puerta). **Consecuencia**: T1→T4 de esa spec; +1 tarea del
planificador (avisado a plataforma por `deploy.sh`); `Contract` +3 hechos sin respuesta dentro (`RGPD-07`).

## #762 · 2026-09-24 · La T4c: las piezas en tres capas de la instancia, estilo por clases y un banco de la página
Técnico (plataforma; `isla-y-landing-nueva.md` §4.12, `#630`). (1) Los componentes de la instancia van en
`web/components/` y se nombran `<x-instancia::…>`: Laravel ya los resuelve con el espacio de vistas de
`InstanceViews` (medido). Se descarta registrar otro (`componentes/`, lo que proponía la spec): un mecanismo más y
otra ruta que validar (`SEC-12`). Entra la guarda, con la mitad de seguridad: un componente del paquete no suplanta
uno del producto (su mutante, `prependLocation`, muere). (2) **Tres capas**: componentes (los del diseño: sus props y
su DOM), piezas (componen desde `$z`, con la forma exacta de `contenido.js`) y el modelo (de `$hechos` y `lang` a
`$z`): lo que se ve se juzga con los datos del diseño, y las cifras se prueban aparte. (3) **Estilo por clases**
(`entradas.css`), no en línea: el `hover` del diseño es estado de React y un `:hover` no puede con un `style`. (4) Lo
que el diseño MIDE con JS (cuatro componentes), con `@container` sobre un solo DOM. (5) El A del banco es la pieza
React en el marco de la PÁGINA, no la ficha de `sections/`, que lleva la isla viva (T4e) y los interruptores.
**Descartado**: portar el estilo en línea (sin hover), dos DOM ocultando uno (dos `<h1>`, enlaces repetidos).

## #741 · 2026-09-25 · `[DECIDIDO]` El ESCANEO del carné acredita la visita: es lo que abre la encuesta interna y alimenta la externa
`searchByCard()` de `ValidarRegistro` llama a `GateVisits::register()` (idempotente por cliente y día, con el
permiso de la ficha) ANTES de componer la ficha; la búsqueda tecleada NO acredita, y `registerVisit()` queda para
ella (sin botón desde `#234`).
**Por qué**: la spec de encuestas (`#740`) dispara la interna «tras acreditar la visita» y la externa desde
`customer_visits`, y al empezar la T2 se midió que el botón «Registrar visita» no existe desde `#234`: sin esto
ni una ni otra ocurrirían nunca. Son las palabras del owner («al escanear el QR le sale un banner»; «al escanear
el QR, al día siguiente le mandamos un correo») y cero gestos entre dos clientes (`#234`). Se le presentó como la
recomendada de tres y contestó «Perfecto, continúa».
**Descartado**: reponer el botón (un gesto más por cliente; olvidarlo = ni encuesta ni correo) · ofrecer al
escanear sin acreditar y sacar la externa de las reservas de ayer (se aleja de sus palabras).
**Consecuencia**: `customer_visits` vuelve a crecer (`DEUDA.md`; `lealtad-jumppoints.md` §9: JumpPoints hereda
«visita = escaneo»); `GateVisits` deja de ser «un acto explícito, nunca un efecto de abrir la ficha»;
`specs/encuestas.md` §1 y §4.2; `ValidarRegistroProfileTest` y `GateSurveyTest` lo aseveran.

## #763 · 2026-09-25 · `[DECIDIDO owner]` Kids y Jump: tres cifras sin dueño claro
(plataforma; `isla-y-landing-nueva.md` §4.12, T4c·8.) (1) **El precio de los calcetines** («2 € el par») sale de un
HECHO. ⚠️ Medido tras preguntar: ya existía, en la ficha de cada producto (`/catalog/products/{id}`, `addons[].
price_cents`); `PageFacts` gana `product_details`, sin API nueva. (2) **«Desde los 14, solos» y «a los menores de 16
los registra un adulto»**: texto de PlayJump, como «Mínimo 3 monitores»; el sistema no aplica esos umbrales (medido).
(3) **Los días de cada tarifa**, con la etiqueta del panel (`/prices.rates[].label`), no la frase del diseño.
**Descartado**: el precio como texto, quitarlo de la frase, dos ajustes nuevos para las edades.

## #742 · 2026-09-25 · `[DECIDIDO owner]` Cuatro reglas de la revisión de las encuestas: el correo no repite a quien contestó en la puerta, «Por atender» sin permiso va sin texto, la baja no calla la puerta y el gráfico va sin selector
El owner tomó la recomendada de las cuatro (`specs/encuestas.md` §4.2–§4.4). (1) `surveys:send-external` no escribe
a quien CONTESTÓ la interna en esa visita; a quien dijo «no preguntar», sí. (2) `SurveysAttentionWidget` sin
`customers.insights` esconde la persona Y el texto libre. (3) `surveys_opt_out` es del correo: en la puerta se
pregunta igual y el cliente dice «no» en persona. (4) `SurveysAnswersChart` pinta la primera pregunta con opciones de
la primera encuesta; el resto va en las tablas y el CSV.
**Por qué**: (1) dos encuestas por la misma visita cansan y sesgan, y el correo es el canal tranquilo para quien no
quiso en el mostrador; (2) la regla de §7·1 es «la persona con permiso propio», y un texto libre es la persona
hablando (puede llevar su nombre); (3) el rótulo del interruptor dice «correo» y no se le miente; (4) un selector es
UI nueva para lo que ya está en las tablas.
**Descartado**: mandar siempre · no mandar a nadie con fila interna ese día · dejar el texto sin permiso · que la
baja apague la tarjeta · un selector de encuesta y pregunta.
**Consecuencia**: `SendExternalSurveys::eligible()` (+1 `whereNotExists`), `SurveysAttentionWidget`,
`SurveySendTest`, `SurveysAttentionWidgetTest`, el arnés +1; spec §4.2, §4.3 y §4.4.

## #764 · 2026-09-25 · `[DECIDIDO owner]` La pieza 5 de Kids y Jump, con una foto provisional del panel
(plataforma; `isla-y-landing-nueva.md` §4.12.) Corrige `#761`·3 SOLO para la pieza 5 («Para tu tranquilidad» /
«También es para ti»): hasta que llegue la del brief (un monitor a la vista), lleva la foto de una atracción del panel
que enseña cómo se cuida —Kids, el circuito de obstáculos con su suelo acolchado; Jump, los cascos junto al
rocódromo (Circuito High)—. La elige el modelo de la instancia (material de PlayJump, no un hecho), con su texto
alternativo en es/en/fr y el encuadre mirado a 1280 y a 390; la forma con foto ya estaba juzgada a 0 píxeles.
**Descartado**: sin foto hasta el material (lo de `#761`·3) · la de la zona (ya es la de la cabecera).

## #765 · 2026-09-25 · `[DECIDIDO owner]` Las tres páginas de la fiesta del sistema nuevo las viste el SPA
(plataforma; `isla-y-landing-nueva.md` §4.11, «El traspaso al SPA».) Corrige `#697` SOLO en quién: la lista de
invitados, la invitación con su recibo y la autorización (el «justificante digital») las viste el carril del SPA, en
el otro ordenador y EN PARALELO con la web pública, para acelerar la v2.0.0. Su lógica, sus controladores y sus vistas
ya eran suyos (`celebracion-e-invitacion.md`, `waiver-por-reserva.md`): no hay escrituras cruzadas. El método no
cambia (la referencia byte a byte de `instancias/playjump/diseno/`, banco de píxeles → 0, piezas portadas), y la
lógica que FALTA (§4.11) se le lleva al owner una a una antes de empezarla. El zip entra SOLO por este ordenador
(`diseno/actualizar.py`: un escritor) y viaja por el repo de la instancia. Este carril sigue con la T4e→T6.
**Descartado**: que las vista este carril al acabar la T4 (lo de `#697`: en serie, días más tarde).

## #743 · 2026-09-25 · `[DECIDIDO owner]` La fiesta del sistema nuevo: la spec aprobada con sus ocho respuestas
(SPA; `specs/fiesta-sistema-nuevo.md`.) **Qué**: las tres páginas viven en el PRODUCTO (modelo de página, piezas
portadas 1:1, `x-pagina-enfocada`) con roles `--fiesta-*` neutros; los valores de PlayJump, en una hoja de su instancia
por un contrato de hojas (pedido a plataforma). Primero lo que HAY (T1→T4); lo que FALTA, una pieza por tanda con estas
respuestas: **1** quien cumple cuenta como uno más · **2** el asunto de «Fiesta reservada» sin nombre · **3** la zona 3
sube el número desde la lista, en tanda propia con `VERIFY_CONC=1` · **4** el recibo dura 24 h (no edita) · **5** «¿Vas
tú con él?» se quita; la puerta, con dos estados · **6** tres campos por niño, como DATO del panel · **7** «Ver el parque»
ENCENDIDO con el vídeo de portada · **8** «Avísame de fechas» entra, sin marcar y con su texto legal. **Por qué**: el
diseño trae lógica que el producto no tiene y cada pieza mueve dato, aforo o RGPD: se decide antes de construirla.
**Descartado** (spec §3): vistas de la instancia, reestilar la vista de 1.279 líneas, la convención «si existe
`fiesta.css`»; y en la 7, la recomendada (apagado hasta el clip corto). **Consecuencia**: `celebracion-e-invitacion.md`
§4.5·6 y §4.5·10 cambian con la F que los construya; hasta entonces el código sigue como está.

## #766 · 2026-09-25 · `[DECIDIDO owner]` Tres «Por confirmar» de la fiesta: quien cumple, el asunto y el plazo
(plataforma, para el SPA; `isla-y-landing-nueva.md` §4.11.) Del `readme.md` del diseño, las tres recomendadas:
(1) **Quien cumple es uno del número**: «para 10 niños» es él y 9 más; ocupa aforo y se cobra como uno más. Si algún
día no paga, se hace con el precio, nunca sacándolo del aforo. Hoy la lista da `quantity` fichas y el nombre va
aparte (`celebrant_*`), así que nada lo impedía. (2) **El asunto del correo «Fiesta reservada» va SIN nombre**
(con el día y la hora): al pagar aún no se sabe, y no se añade un campo al camino del dinero. (3) **Un solo
plazo** para la lista y el número: el que ya existe, `packs.guest_count_cutoff_hours`, 24 h antes (el suelo de
`#444`); cada extra conserva el suyo (`postform_cutoff_hours`). Los dos días del diseño (jueves y viernes) eran
una incoherencia de sus datos de prueba.
**Descartado**: quien cumple aparte (11 en la zona con 10 plazas) · pedir el nombre en «Listo» o al reservar.

## #767 · 2026-09-25 · `[DECIDIDO owner]` El diseño se toma SOLO del mockup; los briefs son referencia
(plataforma y SPA; `isla-y-landing-nueva.md` §4.11; completa `#760`.) El owner: «el diseño te guías siempre solo del
mockup, el zip que te paso de Claude Design; los briefs son solo de referencia para algunos casos». Manda el zip
puesto en `instancias/playjump/diseno/`: sus fichas, su código y su `readme.md`. El Mapa y los briefs (guardados en
la instancia, `docs/estrategia/`) no deciden cómo se ve nada ni corrigen al mockup; si no dicen lo mismo, manda el
mockup, y lo que parezca un error suyo se le cuenta al owner. Se consultan para lo que el mockup no cubre y el owner
señale (las promociones, el sistema de la vuelta).
**Descartado**: el brief como fuente cuando el mockup calla, sin preguntar.

## #768 · 2026-09-25 · `[DECIDIDO owner]` La identidad con el mockup se verifica UNA vez, al final, y ligera
(plataforma y SPA; `isla-y-landing-nueva.md` §4.11; ajusta el método de `#685`.) El owner: «prepara todo y me dices
"está listo"; revisa si es idéntico al mockup como última verificación, pero no de esta manera: tardamos más en
verificar que en trabajar». Medido ese día: portar el zip nuevo, ~30 min; verificarlo con los cuatro bancos enteros
(~310 pares con reintentos) y repetirlos, ~2 h. Desde ahora: un zip se lee con el `git diff` de la instancia y se
porta; se construye sin banco por tanda; al acabar una página (o antes de cortar la v2.0.0), UNA pasada ligera por
página (piezas en reposo, 390 y 1280, sin estados); los bancos por pieza y estado, solo para diagnosticar lo que esa
pasada señale. Controles (mutantes), solo en dinero, aforo o seguridad. Más de ~10 min de verificación, se avisa antes.
**Descartado**: banco completo por tanda (la T1→T4) · no verificar la identidad.

## #769 · 2026-09-25 · `[DECIDIDO]` El contrato de hojas: la instancia declara las de cada superficie del producto
(plataforma, a petición del SPA; `paquete-de-instancia.md` §4.6.bis; `fiesta-sistema-nuevo.md` §3.3·b.) Una vista
del producto con roles neutros (la isla, la fiesta del sistema nuevo) carga las hojas de la instancia que ésta
declara en `instancia.json` (`hojas.<superficie>`, rutas bajo `public/instancia/`, en su orden), leídas con
`InstanceViews::hojas()` y listas para la prop `hojas` de `<x-pagina>`. Solo `.css` que existan dentro de
`public/instancia/` (`realpath`, sin `..`, sin enlaces que salgan); lo que no cuadra, fuera con aviso; sin paquete
o sin clave, vacío. **`CONTRATO` no sube** (el SPA proponía 3): es aditivo, como las páginas de la T4b, y el aviso
compara por igualdad. PlayJump declara `fiesta` (fuentes, saltia y `fiesta.css`, que empuja el SPA). Medido:
`InstanceSheetsTest` 3 casos y 3 mutantes muertos.
**Descartado**: la convención «si existe `fiesta.css`» · subir el contrato a 3.

## #744 · 2026-09-25 · `[DECIDIDO owner]` La invitación del sistema nuevo: el recibo directo, el nombre de pila, la descripción del pack
(SPA; `fiesta-sistema-nuevo.md` §4.1 y §7, T2; hermana `celebracion-e-invitacion.md` §4.5·6.) Tres preguntas con
opciones, contestadas con la recomendada: (1) **tras «Vamos» o «No podemos», el padre ve SU recibo, directo** (la
misma tarjeta con su titular, su ficha y la autorización como oferta), como el mockup; la credencial de 24 h viaja en la
redirección a quien acaba de contestar, nunca en el HTML de la página, y el desenlace es el mismo para un nombre que ya
estaba y para uno nuevo (`#700`). (2) **Los textos nombran a quien organiza por el nombre de pila del titular de la
cuenta** («habla con Lucía», «Lucía lo verá en su lista»): reparte él mismo el enlace; sin nombre, «quien organiza la
fiesta». (3) **«Qué es la fiesta» es la descripción pública del pack** (la del catálogo, tres idiomas, editable en el
panel); vacía, sin bloque. De paso, sin pregunta: la merienda del diseño se pinta con lo que HAY (cada complemento
marcado es un grupo, con icono neutro), «Los calcetines van incluidos» no entra (texto del parque, no del producto), y
el recibo caducado devuelve a la invitación con su aviso en vez de un 403 (la firma que no cuadra sigue siendo 403).
**Descartado**: volver a la invitación con el botón «Dejar sus datos» (hoy) · «quien organiza la fiesta» sin nombre ·
un campo nuevo del pack para el texto (queda como F si la descripción no sirve).

## #745 · 2026-09-25 · `[DECIDIDO owner]` La autorización del sistema nuevo: qué campos del brief y cuáles del producto
(SPA; `fiesta-sistema-nuevo.md` §1.4 y §7, T3; hermanas `waiver-por-reserva.md` §4.6 y `waiver-probatorio.md` §4.4.)
El brief pide cuatro cosas (nombre y apellidos del niño, tu nombre y apellidos, tu teléfono, la casilla, el correo
opcional); el producto pedía además la fecha de nacimiento del menor, la relación con el menor y el apellido del adulto
aparte, y dejaba el teléfono opcional. Medido: la fecha entra en la prueba firmada (`subject_born_on`, `WaiverSigner`,
`CRITICAL_RE`) y da a la puerta la edad exacta; la relación entra en la prueba (`signer_relationship`); el apellido solo
compone un nombre completo que la cuenta tampoco tiene partido. Con opciones: (1) **la fecha se mantiene** (el owner
preguntó si la edad del recibo servía: no, es opcional, vive en la respuesta y no entra en la prueba); (2) **la
relación se mantiene**; (3) **el adulto escribe nombre y apellidos en UNA casilla**. De paso: el teléfono es
obligatorio (el brief); quien responde del menor sale en la tarjeta con su teléfono (§12.4, `[DECIDIDO owner]` 1-09) por
la ranura que la tarjeta ya tiene; el descargo se PRESENTA en el flujo y «Leer el descargo» es un ancla, no un modal
(§4.4: lo que se prueba es que se le presentó).
**Descartado**: quitar la fecha o pedir la edad (cambian lo que se firma, `VERIFY_CONC`) · quitar la relación y
guardar «tutor» · dos casillas para el adulto · el descargo detrás de un modal.

## #770 · 2026-09-25 · `[DECIDIDO owner]` Promociones: por objetivo, tres objetivos, los regalos dentro y apiladas
(plataforma; `specs/promociones.md`; concreta `#684`.) El owner, con el censo del mockup: (1) sale SOLA en los sitios de
su objetivo; (2) objetivos: producto, zona o instalación; (3) los regalos (`gifts`) pasan a Promociones —oferta, con
fin obligatorio, y regalo, permanente—, y `gifts` de la API se calcula desde ellos; (4) varias a la vez, apiladas.
Es TEXTO: no toca precios (`promo.percent` sigue aparte) ni va en `SiteNotice`.
**Descartado**: marcar sitios; dejar los regalos en el producto; una sola.

## #771 · 2026-09-26 · `[DECIDIDO owner]` Las reseñas de SU ficha de Google, copiadas al panel y elegidas por página
(plataforma; `specs/google-reviews.md` §9; corrige `#616` en dos puntos.) El owner: «las reseñas son mías, yo administro
esa empresa»: mientras Google no aprueba el Perfil de Empresa (60 días), se COPIAN de su ficha —autor, su foto, estrellas,
fecha, texto, fotos y su respuesta— a «Opiniones» del panel (`origin = google`), y cada página enseña las que él etiqueta
con ella (`tags`), con la marca y el enlace a Google, como pide el diseño. (1) El CMS de opiniones se QUEDA (`#616` lo
retiraba). (2) Las imágenes se DESCARGAN y se sirven desde casa: nada se pide a Google, sin consentimiento, así que el
hecho nuevo `/reviews` (1.31.0) lleva caras y fotos (`#616`: «sin avatares»). Herramientas: `scripts/resenas-google.mjs`
o su versión de consola, y `php artisan reviews:import`. **Riesgos, dichos al owner**: las condiciones de Google y el
nombre y la cara de terceros (se firman «Nombre I.»; se retira la que alguien pida). **Descartado**: raspar a diario.
